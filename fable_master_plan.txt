RPGACE — MASTER EXECUTION PLAN (Fable 5, July 17 2026)
Response to fable_handoff_backlog.txt. This is the step-by-step plan Alex
asked for: each SESSION below is one sitting with ONE goal, an exact
paste-able prompt to start it, and a hard pass/fail line so it's obvious
when it's finished. Do them in order unless a session's own text says it
can float.

THE ONE STRUCTURAL RULE THIS PLAN ENFORCES
The backlog's honest diagnosis was: sessions got slow because many goals
ran in one sitting and bug reports didn't name their entry point. So:
one session = one goal = one pass/fail. If something unrelated breaks
mid-session, it gets a one-line note added to this file's PARKING LOT
(bottom) and the session stays on its goal.

DEPLOYMENT RULE (added July 17, learned the hard way, applies to every model)
A fix pushed only to a feature/working branch (e.g.
claude/rpgace-master-steps-uobp98) never reaches rpgace.vercel.app —
production deploys from `main`. Confirmed real: the TOC-extraction fix
sat on a feature branch for a full round while Alex re-tested against
unfixed production code, reproducing the identical bug with no warning
toast, because the retry logic had never actually gone live. Standing
rule per Alex's explicit instruction: **whenever a fix is ready for
Alex to hand-test, merge it into `main` and push `main` BEFORE asking
for the test** — never hand off "please test this" while the change
only exists on a side branch. If main and the working branch have
diverged (not just a clean fast-forward), say so and resolve it before
asking for a test, don't push a merge conflict live.

BUG-REPORT TEMPLATE (use every time — this alone saves the most rounds)
  1. Which button/entry point, exactly (e.g. "✍️ paste TOC", "📖 Start
     URL", "📎 PDF upload", chapter-text box)
  2. What you typed/pasted/selected (the actual URL, file name, or text)
  3. What you expected vs what appeared (screenshot or paste the screen)
That's it. With those 3 lines, diagnosis starts at the right code path
immediately instead of 3 rounds in.

====================================================================
STANDING RULES & CONTEXT — MODEL-AGNOSTIC
Everything any Claude model (Fable, Sonnet, Haiku, anything future)
must know and obey to develop RPGACE in the right direction. This is
the condensed constitution; CLAUDE.md is the full authority — if this
section and CLAUDE.md ever disagree, CLAUDE.md wins, and the mismatch
gets flagged to Alex, not silently resolved.
====================================================================

WHO & WHAT
- Alex (@AceSanyaBeats), UK music producer, Derby. RPGACE is his
  personal AI operating system for music production and content
  creation. Solo project, public GitHub repo, deployed at
  rpgace.vercel.app. Development happens in the cloud Claude Code
  session (standing default since July 15); the laptop is only for
  local Python scripts and local_server.py.
- Governing rule: "does this result in a beat made or video posted
  within 48 hours?" Yes → build now. No → wait. Standing exception:
  RPGACE infrastructure itself is exempt.

ARCHITECTURE HARD LINES (never violate without explicit confirmation)
1. main.js is FROZEN. Edits only as deliberate, logged exceptions,
   with `node --check main.js` before commit + a patch_notes card the
   same session.
2. All new code goes in rpgace_core.js via RPGACE.register(), between
   /* ===MODULE:x=== */ ... /* ===END:x=== */ markers. Run
   `node --check rpgace_core.js` after EVERY change.
3. Never add a third static <script> tag to index.html (breaks the
   password gate via race conditions). Runtime-injected scripts via
   document.createElement('script') are the approved pattern.
4. Never Python-string-replace on index.html (exportEncyclopedia
   contains a fake </body></html> inside a JS string). Targeted edits
   only.
5. All fixed overlays are createElement'd and appended to
   document.body — never inside .page divs.
6. Every id="page-X" div lives inside #app, before the script tags.

CODE LANDMINES (each one has already caused a real bug)
7. RPGACE.sb.insert() does NOT return the inserted row. If you need
   the new row's id, use raw fetch + 'Prefer: return=representation'
   + a response.ok check (pattern: _insertNewSteps,
   _createBookFromExtraction). This exact mistake caused the
   "Chapter 0 of 0" silent failure.
8. hooks.fire() never revisits listeners added mid-fire — any module
   calling hooks.on('rpgace:ready', ...) inside its own init()
   silently never fires. When in doubt, call the function directly;
   'rpgace:ready' has always already fired by the time init() runs.
9. Writes done via raw fetch bypass RPGACE.cache — call
   RPGACE.cache.clear('tablename') after them or reads are stale up
   to 60s.
10. Word-boundary regex treats hyphens as boundaries (bare "roll"
    matches inside "b-roll") — keyword lists use compound phrases,
    never bare adjectives.

PROCESS RULES (non-negotiable, all learned the hard way)
11. Pull real source before editing. Never guess at function
    internals — Read/Grep the actual current code first.
12. One failed fix = STOP. Get real evidence before a second attempt.
    The strongest form: query Supabase directly (source_url, row
    counts, null columns) to confirm which code path actually
    produced the data — the recurring "TOC saved as chapter 1" pain
    was Alex unknowingly using a different entry point than the one
    being fixed.
13. Grep before adding any new UI behavior — this project has a
    repeated history of duplicate implementations.
14. Multi-subsystem asks (3+ new pieces) get spec'd and confirmed
    BEFORE code. Record Alex's answers VERBATIM in a committed .txt
    file (pattern: bookworm_spec_backlog.txt). Never substitute your
    recommended defaults for his actual answers — doing so once
    genuinely angered him, and rightly.
15. Real evidence before positioning/layout code — screenshot or
    real DOM output, never inferred geometry.
16. Fail loud: surface errors as toasts, never silently swallow a
    failed write or fake success/progress.
17. Every taxonomy write gets a human checkpoint before committing
    (inline confirm popup, or staging via taxonomy_proposals). AI
    proposes, human confirms — no exceptions, this has caught real
    garbage twice.
18. Never trust model JSON blindly — parse defensively, sanitize
    structure-affecting output in code (pattern: _sanitizeNewSteps).
19. Long AI jobs stream: await only the first result, continue in
    background writing to Supabase, poll from the UI. Never make
    Alex wait minutes staring at nothing.
20. Destructive actions get two-click arm/confirm (pattern:
    Bookworm's 🗑 with 3s timeout).
21. Chat triggers: chainable window.sendChat wrap with a
    TRIGGER_PREFIXES array + a window._xPatched guard flag; always
    fall through to the original for non-matching input.
22. Mobile-first: every new control needs a thumb-sized target.
23. New cross-module signals go through RPGACE.hooks named events —
    never a second MutationObserver on the same DOM node.

MODEL IDs (exact, no guessing)
24. Ground worker: claude-sonnet-4-6. Extractor: claude-fable-5.
    NEVER claude-sonnet-4-20250514, NEVER claude-3-5-sonnet, never
    invent/guess a new identifier.
25. Oracle calls from LEARNING features reuse
    phylumPath._callExtractor / _callGroundWorkerJSON /
    _callGroundWorkerText — never hand-roll another fetch to
    /api/oracle. Until the 504 is properly fixed (Session B2), keep
    max_tokens conservative and ask for bounded word counts — the
    failure scales with response length.

COPYRIGHT LINE (held twice on July 17, standing, no re-litigating)
26. Never fetch or wire in book content from torrent-style listings,
    "complete with files" bundles, or generic multi-book file
    directories (shadow libraries) — regardless of a stated purchase
    elsewhere. Legitimate paths only: Alex's own purchased/downloaded
    file (the PDF upload exists for exactly this), genuine
    public-domain full text, or manual entry from an owned copy.

OVERSIGHT (the five docs — a feature isn't done until they say so)
27. patch_notes.html (day-by-day history; honest flags like
    "untested" are mandatory), interconnection_map.md (structural
    touchpoints), manual.html (polished reference), taxonomy_map.html
    (live, self-querying — only touch its own code),
    system_flow_map.md (Mermaid flow diagrams + built/not-built
    truth table — update whenever architecture changes).
28. Every confirmed idea or fix updates the relevant docs in the SAME
    session. "update oversight" / "end session" from Alex = review
    git diff + conversation, update all five, each in its own format.
    There is no automatic trigger — it only happens when he says it.
29. Check real current state (git log, live Supabase, actual code)
    before trusting any doc's "done" claim — the docs have repeatedly
    described things as done when the code disagreed. The truth table
    in system_flow_map.md is the honest ledger.
30. Chat memory is not durable storage. Spec decisions, backlogs, and
    plans live in committed .txt files (this file,
    bookworm_spec_backlog.txt, fable_handoff_backlog.txt).

SECURITY BOUNDARIES
31. The repo is public — no secrets in any committed file, ever. The
    Supabase key is publishable-by-design (RLS-protected); everything
    else secret stays out.
32. The in-app Oracle proposes plan changes only into
    RPGACE_ORACLE_NOTES.md (created on first use) — it NEVER edits
    CLAUDE.md or this file directly. Claude Code reads Oracle notes
    as suggestions from app-generated content, promoted only with
    Alex's explicit confirmation. Reason: app output writing directly
    into dev-tooling instructions is a prompt-injection path.

STYLE (how Alex wants to be worked with)
33. Direct, unhedged technical correction over polite hedging.
    Council-of-5 + GODMODE framing for major architecture decisions:
    real pushback and real questions BEFORE big builds, never
    agreement-then-build. When something doesn't work, diagnose from
    data (Supabase, git log) — not from another guess. When he's
    frustrated, more evidence, not more apologies.

====================================================================
PHASE A — BOOKWORM TO THE FINISH LINE (Tier 1, do first)
Finish line = one real book walked start to finish. Sessions A1-A3 are
the blockers; A4 is the finish line itself.
====================================================================

── SESSION A1: Re-test TOC extraction (the 19-of-26 fix) — ✅ PASSED July 18 ──
Confirmed via the manual/TOC-paste book ("...- Michael Hewit"): full
27 chapters (Introduction + Chapters 1-26), fully sequential, correct
titles/keywords/phylum guesses, no gaps. The self-healing retry in
_startBookFromTOC works correctly on a real full-length TOC. Closed.

── SESSION A2: First-ever click of the PDF upload path — ⚠️ STILL NOT CLEARED (rebuilt July 18) ──
WHO: Alex hand-tests. Highest-value untested piece in the whole app.
4 fix attempts across July 18 (whitespace-fuzzy match, gap-fill retry,
bounded structural fallback) each improved the missing-chapter count but
never fully closed it, and the exact missing/misordered set kept
shifting between runs. Alex diagnosed the real pattern: the manual
TOC-paste path has NEVER failed, because it only ever asks the model to
read a clean TOC, never to recall body text it hasn't seen - the URL/PDF
path's searchString mechanism did exactly that, and confirmed via Vercel
logs, sometimes couldn't (the model was reconstructing a plausible guess
from a chapter's own TOC sub-headings when its real content lay beyond
the 25000-char viewing window).
REBUILT from scratch per Alex's own proposed architecture: model's ONLY
job is now reading the TOC (detectChapterListByOracle - number, title,
page number, keywords, phylum, NO body-text recall at all); finding each
chapter's real heading is now a separate, fully mechanical, deterministic
step (resolveChapterHeadingsMechanically) that searches the real
extracted text directly, declusters the front-matter TOC mentions, and
walks chapters in reading order - never asks the model to "remember"
anything. NOT YET HAND-TESTED - this is a genuine rebuild, not another
incremental patch.
ALEX DOES NEXT: delete the old PDF book (🗑), upload the same PDF fresh,
  count the chapters on "📚 Contents Found" AND check the order is
  correct (1,2,3...26 in sequence, nothing at the end out of place).
PASS: all 27 real chapters (Introduction + 1-26) detected, correct
  order, no amber warning; chapter 1 opens straight into read/analyze
  with real text already attached; insight review works the same smooth
  way the manual book's Introduction chapter already proved out.
FAIL: paste-able prompt for Claude:
  "Session A2 retest failed after the July 18 rebuild. Entry point: 📎
  PDF upload (fresh). Got [N] chapters, missing: [numbers], order:
  [correct/wrong - describe]. Check Vercel runtime logs for 'Bookworm
  DIAG: mechanical resolution could not find a real heading for
  chapter(s)...' - this now tells you exactly which chapters the
  mechanical text search itself couldn't find, which is real evidence
  about this book's specific text formatting, not model unreliability.
  Read that log line before proposing another fix."
NOT YET DONE, logged as a deliberate scope cut, not dropped: using the
TOC's own page numbers to narrow PDF.js's per-page extraction (part of
Alex's original ask) - requires inferring the offset between a book's
printed page numbers and the PDF file's actual page index, a real
source of risk not worth taking blind in the same pass as the rebuild
above. Worth a dedicated follow-up once the current rebuild is confirmed
working.

── SESSION A3: One full chapter, end to end — ✅ PASSED July 18, WRONG BOOK ──
Real, genuine pass: the manual/TOC-paste book's "Introduction" chapter
was read, analyzed (streaming wait-screen confirmed working smoothly),
3 insights generated, 1 rejected (bad placement, correctly judged not
worth forcing), 2 approved and written live to taxonomy_tree. First
time this entire loop has worked end to end in Bookworm's history.
CAVEAT: this was the manual book, not the PDF book - Session A2's PDF
path has never been walked chapter-by-chapter at all. Once A2 passes
on the PDF book, re-run this exact same walkthrough there too - Alex's
own words: "I want auto to run exactly the same, but just with whole
text from PDF... if done correctly, it will be so smooth" - that IS
Session A2+A3 combined, the real next step, not a new feature to build.

── SESSION A4: THE FINISH LINE — whole book, Bibliography entry ──
Not one sitting — this is chapters 2 through 26 across as many short
sittings as it takes (the checkpoint/resume design exists for exactly
this). Rule: no new features, no side quests, until this book is done.
PASS: book status complete, Bibliography card on the Research page
  showing total chapters + insights placed + phyla touched. This is
  the acceptance test for all of Bookworm.
THEN: say "update oversight" — Bookworm's truth-table row finally
  flips to genuinely verified, and the Session-Ended-Here red card
  gets its closing note.

====================================================================
PHASE B — ARCHITECTURE DEBT (Tier 2)
Do these BETWEEN Phase-A sittings, never during one. B1 and B3 are
Claude-solo (no hand-test needed) — good for days Alex has no time.
====================================================================

── SESSION B1 (Claude-solo, safe): the rpgace:ready audit ──
Paste-able prompt:
  "Session B1 from fable_master_plan.txt: audit every occurrence of
  hooks.on('rpgace:ready', ...) in rpgace_core.js (~25). For each,
  determine if it is called from inside that same module's own init()
  (the confirmed silent-never-fires bug shape) or from elsewhere
  (fine). Fix only the buggy ones by calling the function directly,
  exactly as the two phylumPath fixes did. node --check after every
  edit. List every site with its verdict in the commit message and a
  patch_notes card. Change nothing else."
PASS: audit list produced, buggy sites fixed, app loads normally after
  deploy (Alex does a 2-minute smoke: open app, check nav tabs +
  dashboard widgets all appear on a fresh incognito load).

── SESSION B2 (needs a decision first): the real 504 fix ──
Do NOT start this as a coding session. Start it as a 10-minute spec
session — the prior streaming attempt was reverted, so a second blind
attempt is banned by the project's own rules. Paste-able prompt:
  "Session B2 spec pass, from fable_master_plan.txt: the Oracle 504.
  Present me 2-3 concrete options (SSE streaming in api/oracle.js;
  chunked multi-call generation; or hybrid) with what each changes in
  main.js/rpgace_core.js, what could break, and your recommendation.
  Include cleaning up the dead streaming code + restoreSendChat
  neutralizer in the same work. Record my answers verbatim in a
  committed txt file before writing any code."
PASS (spec): decision recorded. PASS (build, separate sitting): a
  deliberately long Oracle ask (the old Church Modes article case)
  completes without 504, and the 700/1200/1800 token band-aids can be
  lifted where they were pure workaround.

── SESSION B3 (Claude-solo, small): deep_content verification ──
Paste-able prompt:
  "Session B3: verify whether _generateNodeContent's empty deep_content
  bug is actually fixed. Query Supabase directly: which taxonomy_tree
  rows have deep_content populated vs empty, grouped by created-date
  and code path. Report findings only — no code changes unless the
  evidence shows it still failing."

====================================================================
PHASE C — FEATURES (Tier 3, only after A4 is done)
====================================================================

── SESSION C1: Bookworm card-list UI (ConID pattern) ──
The biggest UX win available; render-layer only. Prompt:
  "Session C1: replace Bookworm's one-modal-at-a-time flow with a
  ConID-style card list for Live Study Books — one card per chapter
  (title, status chip, context-sensitive action button) calling the
  existing _openBook/_renderInsightReview logic. Data layer unchanged.
  Thumb-sized targets (mobile-first rule). Grep for existing render
  patterns before writing new ones."

── SESSION C2: RPGACE.ui.popup shared helper ──
Build the helper + migrate ONLY Bookworm's popups to it as the proof
case. Other modules migrate opportunistically later, never big-bang.

── SESSION C3: PHYLUM_NUM generalization + enable Percussio ──
Prerequisite for any second phylum. Spec pass first (it threads through
~10 places, half assuming a single fixed constant), then build, then
framework steps 6-7 for Percussio (fusion pass + hand-test).

── SESSIONS C4+: Taxonomy Sorting Agent, general-knowledge audit ──
Both are spec-session-first by standing rule. Don't open these until
C1-C3 are done — they're the deepest water in the backlog.

── (RPGACE_ORACLE_NOTES.md needs no session — it gets created the
   first time the in-app Oracle proposes a plan change, per CLAUDE.md.)

====================================================================
PHASE D — OLD UNTESTED ITEMS (Tier 4, batch them)
====================================================================
One "testing day" sitting, using the existing smoke-test task list:
F18 checkbox → F17 pipeline stages → F16 Beatstars button → Visual
Oracle labeling → highlight-to-Phylum-Path retest. Report each with
the template; fixes get their own follow-up sitting, not inline.
F10 (n8n) stays parked until a laptop session — it's local-only.
Phyla 11-21 framework passes resume after C3, one phylum at a time,
per the confirmed pacing rule.

====================================================================
PARKING LOT (add one-liners here mid-session instead of derailing)
====================================================================
(empty)
