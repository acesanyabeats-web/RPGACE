RPGACE TOTAL SYSTEMS GALAXY MAP — /CEO Loop 1 PLANNING PASS
(/paranoia-shaped, /Engineer + /aintergration + /restructure as advisors,
/drift-checked against Alex's own compiled ask) — 2026-08-13

Loop 1 produces an approved plan and builds nothing (per .claude/skills/
CEO/SKILL.md). This file is Step 1 (compile) + Step 2 (interrogation,
folded in as named open forks) + Step 3 (paranoia-shaped scrutiny) +
Step 4 (drift-check against Alex's own words) + Step 5 (the report).
Step 6 (Alex reads/pushes back/approves) happens in chat, not here.
No code for the galaxy map itself was built this pass. Two small,
real, separately-scoped fixes WERE built this pass (see "Also done
this session" at the end) — neither is part of the plan below.

================================================================
STEP 1 — COMPILE, close to verbatim
================================================================

Alex's real ask, compiled from his own words: build a new visual
output for graphify + Obsidian styled closely on the Assassin's
Creed Valhalla "All Skills" reference image — an organic, radial,
category-clustered skill-web (Melee/Ranged/Stealth branches fanning
out from a central figure, glowing connecting lines, named nodes with
icons). Applied to RPGACE Total Systems, using minotaur_map.html's
own river logic as the guide for "what currently exists," and
future_integrations.html for "where future update files will live"
within this same landscape — framed as an "astrological-esque"
starmap.

Structure requested, in his own order:
1. Top level = "galaxies": Orchestrator CC (this session), OpenMontage
   CC (renamed this session from "Engineer CC" — done, see below),
   and any external AI that plays a real role in Total-systems
   operations, each its own galaxy.
2. External galaxies show their real repo in graphify "to make it
   look bigger" and help a viewer deduce what they do, visually —
   explicit constraint: "these repos cant be touched, just shown in
   my own and your combined interpretation."
3. The RPGACE architecture galaxy breaks down, 2 steps down within a
   named parent sub-category, into the real Rivers — depicting where
   each river flows from and to.
4. Rivers break down via "minotaur logic," using the real truth docs,
   into every function/button/etc.
5. These depictions connect to Oversight as a river-flow, showing how
   Oversight docs get updated/store information.
6. Edges may need a different color scheme to show what KIND of
   interaction is occurring.
7. Recurse: keep breaking down every node until each one has its own
   logical path — this full per-node blueprint becomes what graphify
   + Obsidian work from for every future explain/document/visualize
   output.

Ends with: "/paranoia and /ceo this with /drift and /engineer as
advisors."

**Real cross-reference against what already exists — this is NOT a
from-scratch ask, a large amount of the real underlying DATA this
needs is already built and live**, confirmed by direct read this
session, not assumed:
- `scripts/graphify_river_group.py` already implements a real 16-river
  taxonomy (I-XVI) with a curated `RIVER_MODULES` table (module →
  river, from real `/* ===MODULE:x=== */` markers), a real
  `RIVER_FLOWS` table (which river flows into which, with the real
  condition/trigger note for each edge — this is already a real,
  evidence-grepped "what kind of interaction" tag, just not yet
  color-coded separately from river membership), and a real
  `EXTERNAL_CONNECTORS` table (OpenMontage/Composio/Kimi/Luna/
  librosa/FFmpeg/OpenArt/Graphify CC, each with real status/via/note).
- `total_system_members` (Supabase) already has the real galaxy-level
  member list Alex is asking for at the top: RPGACE app, RPGACE CC/
  Orchestrator CC, OpenMontage CC (renamed this session), Graphify CC.
- Graphify CC already ran a real `graphify clone` against
  `calesthio/OpenMontage` (public, no credentials) and built a real
  cross-repo graph (`~/.graphify/global-graph.json`: RPGACE 955 nodes
  + OpenMontage 11,280 nodes) — this is a real, already-gathered
  "show the size without touching it live" data point that directly
  answers point 2 above without a new repo access this session.
- `minotaur_map.html` already has the real river → river
  `.river-flow-next` connectors and the real "Oversight Rivers"
  section (built Aug 11) describing exactly how information flows
  into a)-(f) oversight docs — this is the real source for point 5.
- The current pipeline (`graphify_recolor.py` → `graphify_river_group.py`
  → `graphify_to_obsidian.py` → `obsidian_vault_to_html.py`) already
  produces `graphify-out/graph.html` (a real, working, physics-based
  vis.js force-directed graph with river-zone clustering) and
  `graphify-out/obsidian_vault.html` (a real, working, linked
  human-browsable vault) — both wired into the live Oversight popup.

**Real, honest gap — the actual new work this ask is asking for**:
none of the above renders anything like the reference image's real
visual language — a hand-illustrated, organic, radial branching web
with glowing curved connectors and no visible force-directed "blob"
physics. `graph.html`'s vis.js renderer is a real, correct, working
TRUTH graph — it was never designed or intended to look like a game
skill tree, and doesn't. Matching the reference image is a genuinely
new, separate FRONTEND VISUALIZATION build, not a config change to
the existing pipeline — this is the actual scope of what's being
asked for, named plainly rather than undersold.

================================================================
STEP 2 — Real open forks (/interrogation) — Alex's answers needed
================================================================

**Fork 1 — rendering mechanism.** The reference image is a hand-laid-
out, curated illustration (~80 skills, positioned by a human artist
for legibility and visual balance). RPGACE Total Systems, at the
"break down until each node has its own logical path" depth Alex
asked for, is a different order of magnitude — 955 river-tagged nodes
in `graph.json` alone, before counting main.js's ~240 keyword-matched
nodes, before counting the external galaxies. A single flat image at
that node count cannot read the way the reference image reads, no
matter the styling — it would be an unreadable wall of nodes.
Two real, honest options:
  (a) **Drill-down, matching "as many times as it takes" literally.**
      Top level shows only the galaxies (Orchestrator CC/OpenMontage
      CC/Graphify CC/external providers/RPGACE architecture) as a
      small number of large stylized nodes, styled close to the
      reference image. Click a galaxy → a fresh radial view scoped to
      JUST that galaxy's own children (e.g. RPGACE architecture →
      its 16 rivers, styled the same way). Click a river → its real
      functions/buttons. Each level is its own small, legible,
      reference-image-styled diagram; the FULL depth Alex asked for
      exists, just spread across levels instead of one flat image.
  (b) **One giant flat image, real depth, accept illegibility at the
      full node count**, and cap the literal per-node breakdown at a
      shallower real tier (e.g. river-level, not function-level) to
      keep it as one screen. This does not honor "break down... until
      each node has its own logical path" literally.
  Recommendation: (a) — it's the only option that actually delivers
  what he described ("as many times as it takes"), and it reuses the
  EXACT same real river/module data already gathered rather than
  inventing a new shallower taxonomy.

**Fork 2 — is this a new artifact or a replacement.** The current
`graph.html`/`obsidian_vault.html` pipeline is real, working, wired
into Oversight, and IS layer (f)'s real present-tense truth source
per CLAUDE.md's own tier system — it answers "what does the AST
actually say," a genuinely different job than a curated, illustrated
overview. Recommendation: build the galaxy map as a NEW, additional
artifact (a real new HTML page, generated from the SAME underlying
JSON the existing pipeline already produces — not a second, divergent
data source, per rule 8) rather than replacing graph.html — but this
doubles the real maintenance surface (two pipeline outputs instead of
one), which is a real cost Alex should weigh, not one I should decide
for him.

**Fork 3 — external galaxy sizing, given "cant be touched."** Read
literally, "shown in my own and your combined interpretation" reads
as: don't re-clone or re-analyze the external repo THIS pass — use
what's already known/cached (the real OpenMontage 11,280-node count
already sitting in `~/.graphify/global-graph.json` from Graphify CC's
Aug 6 run) plus a real, human-written description (what CLAUDE.md
already documents about what OpenMontage does), rather than a purely
invented illustrative size. Recommendation: use the real cached count
as a visual-size input (bigger galaxy = more real nodes, an honest
signal) alongside hand-written descriptive text — confirm this
reading is what "your combined interpretation" meant, since the
alternative (purely artistic sizing, zero real number behind it)
is also a legitimate reading of "just shown."

**Fork 4 — edge color taxonomy.** `RIVER_FLOWS` already has a real,
evidence-grepped condition/trigger note per edge (e.g. "Oracle page
selected," "a real error is captured," "the OpenMontage handoff...
routes out through here") — these ARE already real interaction-type
data, just not yet color-differentiated as their own legend axis
separate from river-membership color. Real candidate categories,
derived from what's actually in the data already (not invented):
page-navigation trigger, Oracle/API call-out, Supabase write,
human-confirm checkpoint, external Total-system dispatch. Confirm
this is the right axis, or if a different interaction taxonomy was
intended.

**Fork 5 — Oversight-as-river-flow.** `minotaur_map.html`'s real
"Oversight Rivers" section + `RIVER_FLOWS`' existing entries into
River XIV already model this. Confirm whether this pass should just
CARRY this existing real connection into the new galaxy map's visual
language (same underlying fact, new presentation) or whether Alex
wants a genuinely new/different oversight-connection mechanic drawn.

================================================================
STEP 3 — /paranoia-shaped scrutiny, advisors folded in
================================================================

**/Engineer's shape-sketch (a concrete implementation, not vague)**:
the realistic build is a NEW Python script (pattern-matching the
existing `graphify_river_group.py`/`graphify_to_obsidian.py`
convention — same repo, same "generate from real JSON, never
hand-author the diagram" discipline) that reads the SAME
`graph.json`/`RIVER_MODULES`/`RIVER_FLOWS`/`EXTERNAL_CONNECTORS`/
`total_system_members` data already gathered, and emits a hand-rolled
SVG (not vis.js — vis.js is a force-directed physics engine, wrong
tool for a curated radial layout) per drill-down level: a small,
deterministic radial/organic layout (golden-angle spiral positioning,
same technique `taxonomy_map.html`'s own real circle-pack diagram
already uses — real, working precedent in this exact codebase,
confirmed by direct read Aug 6), styled with CSS glow/blur filters
and curved (quadratic-bezier) connector paths to approximate the
reference image's look, one static self-contained HTML file per
level OR one file with JS-driven level-switching (closer to
`taxonomy_map.html`'s own click-to-drill pattern — real, proven,
reusable precedent, satisfies rule 8 directly).

**/aintergration verdict on tooling**: no third-party JS charting
library is needed or recommended — `taxonomy_map.html`'s own hand-
rolled radial layout is a real, working, zero-dependency precedent
already proven in this exact codebase for exactly this shape of
problem (circle-pack, click-to-drill, golden-angle positioning).
Reaching for D3/a game-engine/a canvas library would be Reference-
only at best (same verdict class as every prior Aintergration pass
on a framework RPGACE doesn't structurally need) — the zero-npm-
runtime rule doesn't even technically block it here (this is a
static, dev-generated oversight artifact, not the live RPGACE app),
but there's no real capability gap the hand-rolled approach doesn't
already cover, so pulling one in would be unjustified added surface.

**/restructure verdict**: this does NOT change RPGACE-Claude-Code's
own skeleton — it's a new presentational artifact over already-
existing data, same class of addition as `taxonomy_map.html`'s circle
-pack view was to the taxonomy system in Aug 6. No existing
infrastructure needs replacing.

**Real risk, stated plainly, not softened**: recursive per-node depth
at RPGACE's real scale (955+ nodes) is a genuinely large generation
job even with real data already gathered — likely several real build-
verify passes across multiple future sessions, not one sitting. This
is explicitly why Alex named `/CEO` for it himself; sizing it
honestly here rather than promising a one-pass build protects output
quality, per this session's own standing instruction not to let scope
compress quality.

**Real "why NOT to build this" case, stated honestly**: the existing
`graph.html`/`obsidian_vault.html` pipeline already serves layer (f)'s
real job (present-tense structural truth) and is already wired into
Oversight; a second, purely-presentational "pretty" artifact is real,
ongoing double maintenance (any future river/module change needs BOTH
pipelines re-run) for a genuine but non-functional benefit (easier
visual digestion, not new information). This is a legitimate case for
scoping this as "nice, not urgent" relative to Total-systems items
that close real functional gaps (e.g. server-side error_log capture,
the fusion-link gap on phyla 11/14) — worth Alex weighing explicitly,
not decided for him here.

================================================================
STEP 4 — /drift-check against Alex's own compiled ask (Step 1)
================================================================

Baseline: PROVISIONAL — this is a fresh ask this session, no prior
ratified plan exists for it yet (this file, once Alex reads/approves
it, becomes the real ratified baseline per Loop 1 Step 7).
Verdict: on the draft above (Steps 1-3), covers all 7 of Alex's own
numbered structure points from Step 1 — none silently dropped or
reworded into a smaller ask; 5 real, load-bearing open forks named
rather than guessed defaults substituted in their place.
Grade: no MATERIAL/CAPTURED findings — the draft has not silently
redefined the goal. The 5 forks are correctly NOT drift findings
themselves (an honest fork ≠ a broken promise) — they're the real
Step 2 interrogation this ask needs before Step 5's report can become
a buildable plan.

================================================================
STEP 5 — the report (delivered in chat, this file is its record)
================================================================

See chat response for the actual Council-of-5-shaped delivery to
Alex. Nothing in this plan is built. Per Loop 1 Step 6, it becomes
buildable only once Alex reads this, resolves the 5 forks above (or
tells me to use my own judgment on any of them), and approves.

================================================================
ALSO DONE THIS SESSION (separate from the plan above, real, small,
already built+verified+pushed — not part of what needs approval)
================================================================

1. **"Engineer CC" retired, "OpenMontage CC" is now the real name
   everywhere current-state docs use it** — Alex's own direct call:
   the old name collided with `/Engineer`, a skill living inside THIS
   session, wrongly implying a connection that never existed. Fixed
   in `total_system_members` (Supabase), `ai_tooling_and_rules_map.md`,
   `manual.html`, `interconnection_map.md`, `minotaur_map.html`,
   `.claude/skills/misunderstanding/SKILL.md`, and
   `scripts/graphify_river_group.py`'s `EXTERNAL_CONNECTORS`/river-
   role text — then the real pipeline (`graphify_river_group.py` →
   `graphify_to_obsidian.py` → `obsidian_vault_to_html.py`) re-run in
   place against the EXISTING graph.json (no fresh `graphify export`,
   deliberately avoiding the Aug 12 node-count-mismatch risk that
   stopped an earlier trickle-down attempt) and verified: zero
   "Engineer CC" references remain in any generated output. CLAUDE.md
   dated bullets that predate the rename keep the old name, per this
   file's own doc-discipline (same precedent as the Aug 11 phylum
   renumber) — one correction bullet added stating the rename and
   pointing here.

2. **Real /misunderstanding-shaped check on the mockOracle wrapper
   architecture, resolved with direct evidence, not just reassurance.**
   Alex's real worry: that RPGACE infrastructure only ever exercises
   `mockOracle` for execution, rather than all 3 real Oracle modes
   (Real API / Dummy / Fallback Scout) via a proper wrapper. Checked
   directly: `main.js`'s `callOracle()` is the SOLE real dispatch
   point (confirmed by grep — exactly one `async function callOracle`
   in the whole codebase), and it checks BOTH
   `mockOracle.isEnabled()` (dummy) AND `mockOracle.isFallbackMode()`
   (fallback) before ever falling through to the real `/api/oracle`
   fetch — the module named `mockOracle` (a legacy name from before
   the Aug 6 3-mode extension) IS the real wrapper deciding among all
   3 modes, not a mock-only shortcut. All 3 grounding wrappers
   (`oracleTreeGrounding`/`oracleAppGrounding`/`oracleFetchGuard`)
   chain to it via `orig.apply(this, arguments)` (confirmed by grep —
   every `orig.apply` call site forwards the full real `arguments`
   object, never a hand-listed subset), so none of them can silently
   bypass the mode check. Nothing in the codebase calls
   `mockOracle._fakeReply` directly outside this one real dispatch
   point. The architecture is already correct — errorLog (built the
   prior session) doesn't call Oracle at all, so it was never part of
   this concern either way.

3. **Session dating corrected going forward** — today is 2026-08-13,
   not the 11th; new work this session is dated accordingly.

================================================================
STEP 6/7 — Alex's real answers + RATIFICATION (same day, 2nd pass)
================================================================

Alex's own verbatim answers to the 5 forks, real and decisive — this
IS Loop 1 Step 6 (read, push back where needed, approve). Quoted in
full, not paraphrased, because Fork 2's answer materially expands the
plan's own scope beyond the original draft.

**Fork 1**: "drill down" — confirmed as drafted.

**Fork 2 — real, substantial elaboration, not a simple pick.**
Verbatim: "no, make this output do that so f) has truth source
alongside b) docs truth source. this mind map will also have this
purpose. if anything upstream updates, this output is then read by me
along with a) docs and others (not b)), and you read it too with
truth docs (b)) so minimal /misunderstanding and /drift doesnt even
have a chance to occur in the first place. these docs then get used
to suggest changes to downstream or further upstream connectors to
make the update integrate better with the system. this will be the
basis for the trickle down/up system that will then hand reports of
suggestions and errors that will be compiled into a report for me to
analyse and make decisions off of. This whole framework would be
crucial for CEO to not only make updates better, but also minimise
friction with already existing infrastructure and keep correct
documentation of what current, the all passes go through relevant
colour for plans, then go through /drift and everything when needed
to verify how well it integrated and how well my plan was executed to
a tee."

Real reading, checked plainly rather than assumed: the galaxy map is
NOT a decorative overview layered on top of the real truth docs — it
becomes a real SECOND truth source, sitting alongside Tier (b) rather
than under it. Two distinct real readers, two distinct real purposes:
Alex reads the galaxy map alongside Tier (a) (never Tier (b) directly
— matches the existing a)/(b) split, where (a) is "plain language for
Alex"); this session reads the galaxy map alongside Tier (b) as a
cross-check, specifically so a real drift/misunderstanding has less
surface to occur on BEFORE it happens, not just a faster way to catch
one after the fact. On top of that, a new mechanism is asked for: a
real "trickle down/up" system — when a real upstream change happens,
cross-reference the galaxy map + Tier (b) to surface real downstream
integration friction (and the reverse: a downstream change checked
against upstream expectations), producing real suggestions + errors
compiled into one report for Alex's own review/decision. This
explicitly becomes a standing part of `/CEO`'s own Loop 2 discipline
going forward — every real build passes through colourgradient, then
`/drift` (and other verification as needed) to confirm how well it
actually integrated and matched the plan.

**Fork 3**: "yes, the openmontage node will be the connector to
openmontage repo and rpgace repo, so both can exist in the galaxy
output." Confirmed — a single real OpenMontage node acts as the
bridge between the RPGACE-architecture galaxy and a real OpenMontage
galaxy, both shown in the same output. Matches Graphify CC's already-
cached real cross-repo count (RPGACE 955 + OpenMontage 11,280 nodes)
— no new repo access needed to represent this.

**Fork 4**: "right axis, but extract call etc. all these edges dont
have a strong distiction between what they do, only what river or
node etc that it came from, not useful information if you get me."
Confirmed the axis, corrected the categories: needed to describe the
real MECHANICAL ACTION at each edge (an extract call, a write, etc.),
not be redundant with river/node membership (already conveyed by
color/position). **Built this same pass** (see below) — a real,
evidence-grounded 10-type taxonomy, not the original 5 generic
categories.

**Fork 5**: "represented connection of course." Confirmed — carry the
existing real Oversight-river-flow connection into the new visual
language as-is.

**Ending directive**: "/paranoia and /ceo, with /restructure,
/engineer and /drift advising along every step of the way." Read as
real approval-with-refinement (Loop 1 Step 6), not a request to
re-plan from scratch — the draft's own structure held up, Fork 2's
answer is treated as a real scope amendment, not a rejection.

**RATIFICATION (Loop 1 Step 7)**: this plan is now the ratified
`/drift` baseline. `ceo_plans` row created: name "RPGACE Total Systems
Galaxy Map", `basis='ratified'`, `status='active'`, `source_file`
pointing to this record. Real item breakdown for Loop 2 (`/Engineer`
picks up the next unstarted item each session, per the skill's own
discipline — not all built in one sitting):

- **G1 — Edge interaction-type taxonomy.** SHIPPED this same pass (see
  below) — a real, additive, evidence-grounded fix to the EXISTING
  `graph.html`/Obsidian pipeline, and the real foundation G2+'s own
  edge coloring will reuse.
- **G2 — Level-0 Galaxy View.** The top-level galaxies only
  (Orchestrator CC / OpenMontage CC as the real RPGACE↔OpenMontage
  connector / Graphify CC / external providers / RPGACE Architecture),
  styled per the reference image, reusing `taxonomy_map.html`'s real
  circle-pack/click-to-drill precedent. Real next Loop 2 item, NOT
  built this pass — a genuine visual-design build deserving its own
  full `/Engineer` pass rather than a rushed same-turn add-on, per
  this session's own standing "don't diminish output quality" rule.
- **G3 — Level-1 drill-down.** RPGACE Architecture galaxy → the 16
  real rivers, radial view, `RIVER_MODULES`/`RIVER_FLOWS` data reused
  directly, click-through from G2.
- **G4 — Level-2 drill-down.** A river → its real tagged functions/
  nodes (`graph.json`'s river-tagged 955 nodes) — the literal
  "minotaur logic" breakdown Alex asked for.
- **G5 — Oversight-connection representation.** A visually distinct
  connector class showing each level's real flow into Oversight docs,
  reusing `minotaur_map.html`'s existing river-flow-next data (Fork 5).
- **G6 — Truth-source promotion.** Real doc updates (CLAUDE.md's
  Oversight section, `interconnection_map.md`) formally declaring the
  galaxy map's new role as a second real truth source read by Alex
  (with Tier a) and this session (with Tier b) — not merely derived
  from them.
- **G7 — Trickle-down/up suggestion-and-error report system.** The
  real, largest, most open item: detect a real upstream/downstream
  change (likely via `git diff` mapped through `RIVER_MODULES`), cross
  -reference against the galaxy map + Tier (b) for real integration
  friction, generate real suggestions (a genuine judgment call each
  time, same honesty discipline as `error_log.html`'s "rationalize"
  step — NOT an automated text generator), and compile into a report
  Alex reviews and decides on. Needs its own real `/interrogation`
  pass before it's buildable — how "upstream change" is detected
  concretely, and where the report lives (a new doc, or folded into
  `error_log.html`'s existing active/resolved shape) are both real
  open questions, not decided here.
- **G8 — `/CEO` Loop 2 wiring.** A process change to
  `.claude/skills/CEO/SKILL.md` itself: every future plan item's real
  build passes through colourgradient status, then this new galaxy-
  map/Tier(b) cross-check for integration friction, then `/drift`
  verification against the plan — formalizing what Alex's own ending
  directive already asked for.

================================================================
G1 — SHIPPED THIS SAME PASS (real, verified)
================================================================

`scripts/graphify_river_group.py` gained a real, evidence-grounded
`INTERACTION_TYPE_LABEL`/`INTERACTION_TYPE_COLOR` taxonomy (10 types:
nav_route, ai_judgment_call, external_extract_call, write_commit,
human_confirm_gate, dispatch_trigger, oversight_deposit,
session_start_pull, doc_staleness_flag, terminal_sink) — each of the
16 `RIVER_FLOWS` entries' real 2-tuple `(label, note)` extended to a
3-tuple `(label, note, itype)`, classified by re-reading what the
existing note text actually cites (not invented), e.g. "morningBrief's
real Composio Gmail-fetch call" → `external_extract_call`, "any
confirmable fusion-link bridge" → `human_confirm_gate`. `FLOWS_IN`'s
build loop and `build_river_notes()`'s `flows_into`/`fed_by` output
updated to carry `itype`/`itype_label`/`itype_color` through;
`riverNoteHtml()`'s JS template gained a small, real colored badge
per flow entry. `graphify_to_obsidian.py`'s matching `## Flows into`/
`## Fed by` sections updated to the same 3-tuple unpacking, badge
text rendered as real bold Markdown instead of a CSS badge (matches
each surface's own real format). Colors are real RPGACE style.css
tokens, deliberately a different swatch set from `RIVER_COLOR` so the
two legends never visually collide.

Verified: `python3 -c "import ast; ast.parse(...)"` clean on both
files; full pipeline re-run (`graphify_river_group.py` →
`graphify_to_obsidian.py` → `obsidian_vault_to_html.py`), all 3 exit
clean; direct grep confirms real `itype_label` values appear in
`graph.html`'s embedded JSON and the Obsidian vault's real Markdown
output; the embedded `<script>` blocks in `graph.html` extracted and
run through `node --check` — clean, no syntax corruption from the
Python string-template edit.

================================================================
SAME DAY, 3RD PASS — a paste-and-annotate manual.html audit + connector
completeness fixes + doc-cleanup automation (Alex's real ask, run
through the same /paranoia+/CEO framework, /restructure+/engineer+
/drift+/colourgradient as advisors)
================================================================

Alex pasted a large real chunk of manual.html's own content with 4
inline annotations, plus a mid-turn correction generalizing the
OpenMontage connector-node pattern to every external. Real work done:

**1. manual.html real /deduplication pass, 4 sections trimmed to
pointers, verified against real duplication before touching anything
(never deleted a fact, only ever pointed to its rightful home):**
Session History (redundant with patch_notes.html/patch_notes_archive.
html, confirmed by grep — 16 real hits for the same dates already
there), the 4-level Diagram Chain (redundant with interconnection_map.
md, confirmed 5 real hits for the same function names), "Every Bug
Found + Fixed" (patch_notes.html's own explicitly-stated role, 33 real
F-series hits alone confirming overlap), F0-F18 Roadmap + the old
lettered "Future Integrations a-f" (patch_notes.html's live F-series
table + interconnection_map.md's already-renamed "Items 1-6" — this
manual.html copy had been MISSED by the Aug 11 collision fix and was
still using the stale a-f lettering that collides with the NEW
oversight-tier lettering). **Real, separate staleness bug also fixed,
not just dedup**: "Currently Open Bugs" listed 2 bugs that are BOTH
actually resolved per CLAUDE.md's own current state (Oracle 504 fixed
July 28 via real streaming; the ~25-places re-subscription bug closed
July 29/31) — this section was actively wrong, not just redundant.
Sidebar nav updated to match every real anchor change; div-balance
verified (63/63); all nav hrefs verified resolving to a real id.
**Kept exactly as Alex asked, untouched**: the Supabase Tables section
("keep this text in full manual, love it").

**2. Total-systems connector completeness — real gaps found and
fixed, not assumed.** Alex: "why isnt Supabase or whisper here?"
Real check: Whisper WAS already in `ai_tooling_and_rules_map.md`'s
canonical table (added Aug 11) but missing from BOTH `manual.html`'s
own mirror AND `scripts/graphify_river_group.py`'s `EXTERNAL_
CONNECTORS` mirror — a real, silent drift between 3 copies of the same
fact, not a fresh discovery. Supabase was missing from all 3 — a
genuinely new gap, and correctly NOT folded into the "external
provider" table (RPGACE owns this data, it isn't handing off to an
external agent) — given its own real "1e — Core infrastructure"
section in the canonical doc, `SUPABASE_CORE` in the Python mirror,
and its own row in manual.html's table. Full parity restored across
all 3 copies: Anthropic (a real, previously undocumented row — the
actual default Oracle provider), Jina AI, Last.fm, n8n added
everywhere they were missing.

**3. The OpenMontage connector-node pattern generalized to every real
connector** (Alex's mid-turn correction: "this is only one worked
example for an external ai... do this for all of them"). Every
`EXTERNAL_CONNECTORS` entry gained a real `bridges_to` field — the
actual external system/repo/account that connector is the real spring/
mouth into, the concrete design basis G2's future galaxy-map build
reads from. A `tested` boolean added to every entry too, per Alex's
separate real ask ("ones that arent tests should be present, just
logged to smoke test as not tested, yet visible in rivers and galaxies
since they are actually present") — an untested connector (Whisper,
n8n) is now flagged ⚠ in both the graph.html node-info panel and the
Obsidian vault, never hidden. Full pipeline (river-group → to-obsidian
→ vault-html) re-run clean, `node --check` on extracted embedded JS
clean, real grep confirms Supabase/Whisper present in generated
output. **Real design notes logged to G2's own `ceo_plan_items.
evidence`** (not built yet, since G2 itself isn't built) for whoever
picks up that item: Oracle API becomes its own distinct galaxy node
(RPGACE is the harness, not the provider), self-awareness
(`oracleAppGrounding.SELF_KNOWLEDGE`) becomes its own node+edges.

**4. `future_integrations.html` restructured** per Alex's real ask
("mark your output... with all the steps in a group, with each
subgroup explained... other changes... combined by what they are
changing as in the river... or other extranals connections"). The
Galaxy Map plan's G2-G8 now render as ONE labeled plan-group (a real
new `.plan-group` CSS treatment, visually distinct from loose cards),
each step its own explained subgroup card — G1 correctly does NOT
appear, since it already graduated to green. Every other existing item
(A2-A10, the 3 blue ideas) regrouped under its real affected river/
dashboard-card/external-connector (River III/Oracle Current, River V/
Two Independent Streams, River XI/Content Production Live + External:
OpenMontage, River VI-VII/Taxonomy, River IV/Bookworm, River X/
Chronicles, the /CEO framework itself) instead of a flat list per
color. Div-balance verified (102/102).

**5. The "make cleanup auto" ask — a real standing rule, not just a
one-off fix.** New artifact type 16 in `update-logging-system` SKILL.
md: before adding narrative/history/table content to a Tier (a)
explaining doc, check whether it's already told in full elsewhere
first (Tier (a) docs get edited more often than Tier (b), so new
content naturally lands there and never gets moved to its rightful
home — this closes that specific obstacle, named honestly rather than
just "audit occasionally"). Artifact 15 also gained the real grouping
rule (plan-group-first, then river/card/connector grouping) as a
standing discipline for future `future_integrations.html` updates,
not just this pass's own manual regrouping.

Real, honest scope note: this pass fixed 3 copies of the connector
list (canonical/Python-mirror/manual.html-display) and trimmed
manual.html's own redundant sections — it did NOT attempt G2 itself
(the actual visual galaxy build), which stays real, ratified, future
Loop 2 work per the earlier same-day ratification.

================================================================
SAME DAY, 4TH PASS — "update obsidian and graphify where needed,"
real /deduplication + /paranoia + /restructure on the structural-
freshness question, resolving (not just re-hitting) the Aug 12 stop
================================================================

Alex pasted graph.html's own live Communities/legend panel as real
evidence of current state and asked for a real graphify/Obsidian
update, run through /deduplication+/paranoia+/restructure.

**Confirmed first, before touching anything**: `graphify-out/graph.
html`/`obsidian_vault.html` already fully reflect this session's real
content work (Supabase/bridges_to/interaction-type badges present,
zero "Engineer CC" leftover, `git diff --stat` clean against the last
commit) — the 3rd pass's pipeline re-run already covered this. What
this pass actually investigated: whether the underlying `graph.json`
STRUCTURE itself (967 nodes) is stale relative to this session's real
code changes — confirmed yes, `errorLog` (a real module added a
session ago) has zero graph.json representation.

**Real re-attempt of the Aug 12 structural refresh, this time with an
actual explanation, not just a repeat of the same stop.** Fresh
`graphifyy` 0.10.1 install (npm global, cleaned up after) surfaced a
real new tool feature since Aug 12: `graphify migrate-state` — an
official, safe migration path from the old `graphify-out/` state
convention to the new `.graphify/` one (`--dry-run` confirmed 13
clean copies, 0 conflicts). Did NOT run the full migration — see
below for why. Ran `graphify update . --scope tracked --no-description`
(the real, zero-LLM-cost code-only rebuild) instead: **744 nodes vs
the committed 967 — the exact same discrepancy as Aug 12.** Per rule
9 (a defect surviving a second attempt stops the loop), did NOT force
a third blind pass — but this time did the real diagnostic work
Aug 12 didn't have the chance to: diffed the two `graph.json` file
lists directly.

**Real, now-fully-explained finding, not a mystery anymore**: the gap
is not a tool bug. `--scope tracked --no-description` is explicitly
code-only (confirmed by its own console output, "no LLM needed") — it
never touches markdown/doc files at all. The "missing" 223 nodes are
almost entirely real doc-file nodes the ORIGINAL richer export
included (CLAUDE.md 36, interconnection_map.md 27, multiple skill
`.md` files, system_flow_map.md 22, and more — genuine semantic
extraction that needs a real LLM pass, which `--no-description`
explicitly skips to stay free). The fresh run DID correctly find real,
genuinely new code nodes the old graph never had: `main.min.js`/
`rpgace_core.min.js` (269 nodes — this session's own minified build
output) and 3 real hand-authored scripts (`obsidian_vault_to_html.py`,
`supabase_dedup_scan.py`, `minify_client.js`).

**Real decision, not deferred, matching this project's own established
precedent (the "56 real components is an honest picture, leave it"
call) for exactly this shape of tradeoff**: did NOT overwrite the
committed `graph.json` with the code-only rebuild. Real reasoning,
each axis checked directly: **/deduplication** — the migrate-state
dry-run would have created a second, verbatim-duplicate copy of every
file already in `graphify-out/` (13 files) under `.graphify/`, a real
rule-8 violation if kept; cleaned up rather than committed. **/paranoia**
— blindly accepting the code-only rebuild would have silently deleted
~300 real doc-cross-reference nodes just found this same session to be
exactly what Alex asked the galaxy map system to protect ("f) has
truth source alongside b) docs") — the wrong direction on his own
stated goal. **/restructure** — checked directly against its own real
axes (shorter/quicker/more detailed output): the new `.graphify`
code-only convention is NOT more detailed for RPGACE's actual use case
(it's measurably less detailed, losing doc semantics) — verdict: stick
with the current `graphify-out/` convention and its richer graph, not
a genuine improvement to switch.

**Real, honest, small residual gap, named rather than hidden**:
`errorLog` (3 real functions) and the 3 new real scripts above have
zero graph.json node representation — the same class of gap
`main.js`'s own 12 honestly-unclassified nodes already carry, not a
functional break (river-group's module-marker parsing reads directly
from `rpgace_core.js`'s own source for river/zone assignment, not
from graph.json's node list). **Real, deliberately NOT decided here**:
closing this gap for real needs either (a) accepting the doc-coverage
loss of a code-only rebuild, or (b) a real semantic re-export (LLM
cost, genuinely small but real spend) to refresh BOTH code and docs
together without the tradeoff — a real open fork for Alex, not
guessed. `.graphify/` directory + the global npm install both cleaned
up, nothing untracked left behind.

================================================================
SAME DAY, 5TH PASS — "run a small semantic re-export to close the
errorLog gap": investigated first, found the real request wouldn't
have worked, did the fix that actually closes it instead
================================================================

Real evidence check BEFORE running anything (rule 1) found the
literal ask wouldn't have worked. Fresh `graphify update --scope
tracked --no-description` (free, zero-cost, code-only — the "no LLM
needed" mode) was re-run and directly checked for `errorLog` nodes:
**still zero**, even in a pure AST pass with no semantic/description
step involved at all. This proves the gap is NOT a missing-description
problem (which a semantic re-export would fix) — it's a missing-
EXTRACTION problem (nothing for a description to attach to in the
first place). Running the literal semantic re-export Alex asked for
would have burned real effort and still left the gap open, so it
wasn't run blind.

**Real root cause, found by direct measurement, not guessed**:
`rpgace_core.js` is 19,812 lines; graphify's own extractor's last real
captured node in BOTH the original 967-node graph AND the fresh 744-
node rebuild sits at line ~18,844 — the same cutoff point in both,
confirming this is a structural/tool limitation, not something a
fresher rebuild fixes. The blind ~1,000-line tail contains 6 whole
real modules with zero graph.json representation: `jargonEncyclopedia`,
`pathRouter`, `perfWatch`, `voiceInput`, `mockOracle`, `errorLog` — 34
real methods total, not just errorLog's 3. This is a real, confirmed
EXTENSION of the already-documented `RPGACE.register()`-invisible-to-
graphify finding (known since Aug 6), now localized to a specific line
range rather than a vague "somewhere in the file."

**Real fix applied — hand-entering accurate ground-truth nodes, not a
semantic pass, since I already have complete real knowledge of this
code (built errorLog + 2 of the 3 recently-added scripts myself this
session)**: parsed the 6 modules' real method names + real line
numbers directly from source (regex against the actual 2-space-indent
`name: function(` pattern these modules use), built 34 real node
entries matching graphify's own schema exactly (verified against a
real existing node's shape first), and merged them into BOTH
`graphify-out/graph.json` (967→1001 nodes, 0 id collisions) and
`graph.html`'s own separately-embedded `RAW_NODES` array (extracted/
re-injected via the script's own real `extract_array` bracket-counting
helper, not a fragile regex — a first regex attempt genuinely failed
on the 900K+ char single-line file, confirming why the script's own
helper exists). Re-ran `graphify_river_group.py`: 955→976 river-tagged
nodes, the 3 river-mapped modules (`jargonEncyclopedia`->River VII,
`pathRouter`->River II, `mockOracle`->River III) correctly recolored
from a neutral gray placeholder into their real river colors, verified
directly (`#cc7a3a`/`#4a8ccc`/`#c9a84c` respectively); `perfWatch`/
`voiceInput`/`errorLog` correctly stayed the neutral placeholder,
matching the same cross-cutting-infra treatment `leftNav`/popup
scaffolding already get. `graphify_to_obsidian.py`/
`obsidian_vault_to_html.py` re-run — genuinely produced byte-identical
output (the vault's own module notes list real module NAME+line-range,
not per-function node counts, so this was expected, not a bug).
Verified: div-balance clean (29/29 on graph.html), embedded `<script>`
blocks re-extracted and run through `node --check` — clean.

**Real, honest caveat documented in the script itself, not just this
record**: a future FULL `graphify export`-family rebuild will very
likely drop these 34 hand-entered nodes again, hitting the same tail-
cutoff — a comment was added directly above `EXTERNAL_CONNECTORS` in
`scripts/graphify_river_group.py` so a future session re-applies this
same fix rather than being surprised by it. `.graphify/` scratch dir
+ global npm install cleaned up again, nothing untracked left behind.

================================================================
SAME DAY, 6TH PASS — a real mapping-methodology doc + the /cartographer
skill, real recommendations for new tables, Consumer/Dev tiering,
CEO wiring. /paranoia+/drift with /restructure+/Engineer advising.
================================================================

Alex's real ask, compiled close to verbatim: a document graphify/
Obsidian read to know how to map RPGACE Total Systems out; real
recommendations for new docs/tables that let externals (and RPGACE's
own self-awareness) map and execute what he wants, invoking /drift,
/paranoia, /misunderstanding where needed; make this a real skill in
the RPGACE skillbase; real cross-talk between all Total-system members
via these tables, using trickle-down/up to also flag needed updates
back to HIM directly (an explicit expansion of G7's original scope,
not a new idea); a Consumer/Developer visibility split so a future
market-facing product can hide dev-only detail; wire all of it into
`/CEO` so it runs whenever CEO mode is on.

**Real cross-reference against what already exists, before building
anything new (rule 8 discipline)**: the ratified Galaxy Map plan
already had G6 (truth-source promotion), G7 (trickle-down/up), and G8
(CEO wiring) sitting exactly where this ask lands — this message is
real, substantive content for those items (especially G7's own
explicit "needs its own /interrogation pass before buildable," which
this message directly supplies), not a new parallel initiative. Only
the Consumer/Developer tiering is genuinely NEW — no existing G-item
covered it — so it became a real new G9, not folded artificially into
an unrelated one.

**Built, real, low-risk (process/prose artifacts, no schema commitment
baked in)**:
- `system_map_spec.md` — the real methodology doc, 8 sections (the
  Galaxy->River->Module->Connector hierarchy; the connector-bridge-node
  rule, generalizing the OpenMontage pattern per the earlier mid-turn
  correction; when an external earns a full galaxy vs. a plain bridge
  node; the 10-type interaction taxonomy; the confirmed extraction
  blind-spot precedent from this same day's 5th pass, written down so
  it doesn't get re-discovered from scratch; the Oversight-connection
  rule; the Consumer/Developer convention (rule stated, NOT yet
  applied — see G9); the trickle-down/up procedure). Real /deduplication
  check applied to its own drafting: it states RULES, never re-states
  FACTS `interconnection_map.md`/`ai_tooling_and_rules_map.md` already
  own — verified by re-reading each section against those docs before
  finalizing.
- `.claude/skills/cartographer/SKILL.md` — the real skill, 6 real
  steps (ground against the spec doc -> identify the change's real
  level -> trickle-down/up cross-reference against Tier (b) -> surface
  real findings to Alex directly, his own explicit expansion -> tag
  Consumer/Developer on NEW artifacts only -> invoke /drift/paranoia/
  misunderstanding where the finding's own shape calls for them, never
  reflexively). Added to `ai_tooling_and_rules_map.md`'s skill catalog
  same pass (rule 6/artifact-9 obligation).
- `.claude/skills/CEO/SKILL.md` gained a real new Grounded Mode Step 5:
  when a touched plan item sits in the mapping hierarchy, `/cartographer`'s
  own trickle-down/up check runs alongside the existing `/drift` check
  — this is the literal "update into ceo so it happens when ceo mode is
  on" ask, done. `ceo_plan_items` G6/G7/G8 evidence updated to reflect
  all of this; G8 moved red->yellow (real code shipped, zero real hand-
  test yet — honestly not green).

**Real recommendation, NOT built this pass — a genuine proposal for
Alex, per his own "recommendations for creating new documents or
tables" framing, not silently executed**: a new `system_map_flags`
Supabase table, the real persistent-tracking half of the trickle-down/
up procedure (the skill surfaces a finding to Alex live in chat; this
table is what keeps it from being lost between sessions too — both
real, not a choice between them). Proposed schema:

```sql
CREATE TABLE system_map_flags (
  id uuid primary key default gen_random_uuid(),
  detected_by text not null,        -- which real session/skill found it
                                     -- (e.g. 'orchestrator_cc_cartographer')
  map_level text not null,          -- galaxy | river | module | connector
  change_summary text not null,     -- the real change that triggered this
  friction_note text not null,      -- what's now stale upstream/downstream
  severity text not null,           -- minor | material | captured (drift vocab)
  visibility text not null default 'developer', -- developer | consumer
  status text not null default 'flagged', -- flagged | alex_reviewed | resolved
  linked_plan_item_id uuid references ceo_plan_items(id),
  created_at timestamptz default now(),
  reviewed_at timestamptz,
  resolution_note text
);
```
`anon_all`/`authenticated_all` RLS, matching the established internal-
dev-tooling posture (`session_memory`/`graphify_jobs`/`ceo_plans`) —
this is RPGACE-Claude-Code's own working data, additive per the CEO-
framework's own reduced-friction carve-out, so this table CAN be built
without a separate stop-and-ask once Alex confirms the shape above is
what he wants (a real, small, genuine confirm — not guessed, since the
exact column set is a real design choice, not obviously the only
option).

**Real open fork, explicitly NOT decided — Consumer/Developer tagging
granularity (G9)**: `system_map_spec.md` §7 states the RULE (a real
`visibility` tag, `developer`/`consumer`) but deliberately does NOT
commit to WHERE it attaches. Three real options, not guessed at:
(a) per-DOC (a whole oversight artifact is one or the other — coarse,
cheap, but `manual.html` genuinely has both consumer-relevant and
dev-only content mixed in one file today); (b) per-SECTION (finer,
matches how `manual.html`'s own `<details class="domain-group">`
sections are already structured — real, reusable precedent); (c) per-
Supabase-ROW (finest, needed for tables like `smoke_test_items`/
`future_integrations` where individual items genuinely differ in
consumer-relevance). Real recommendation, not forced: (b)+(c) together
— doc-level tagging is too coarse to be honest given `manual.html`'s
own real mixed content confirmed this same session — but this is a
recommendation, not executed; Alex's own confirm decides it.

**/paranoia-shaped scrutiny, real risks named**: the biggest real risk
in this whole ask is scope creep into a permanent, heavy ceremony
layered onto ordinary work — mitigated directly in the skill's own
"real, honest scope" section (explicit: doesn't replace `update-
logging-system` for ordinary content edits, only fires on a real
mapping-hierarchy change) and in CEO's own new Step 5 (only fires when
a touched plan item is actually in the hierarchy, not every prompt).
A second real risk: `system_map_flags` as proposed could become a
second, competing home for findings `error_log.html`/`future_
integrations.html` already own — checked directly: `error_log` is real
RUNTIME errors (thrown exceptions), `future_integrations.html` is real
NOT-YET-BUILT ideas: `system_map_flags` is neither — it's a real,
distinct third thing (a real, ALREADY-BUILT thing that's gone stale
relative to something else that changed), genuinely not covered by
either existing table. Kept separate rather than force-fit.

**/restructure verdict**: this doesn't change RPGACE-Claude-Code's own
skeleton — it's a new methodology doc + a new skill layered on top of
already-existing real infra (`ceo_plans`/`session_memory`/`graphify_jobs`/
the river/connector data already built this session), same class of
addition as `/cartographer`'s own sibling skills.

**/Engineer's shape-sketch, for `system_map_flags` once confirmed**: a
real, small addition to the existing session-start-check pattern
(`oracle_dev_suggestions`/`graphify_jobs`/`error_log` already query
Supabase at session start) — query `status='flagged'` rows the same
way, no new mechanism invented.

**/drift-check against Alex's own compiled ask (Step 1 of this pass)**:
Baseline PROVISIONAL (fresh ask, becomes ratified once Alex reads this).
Verdict: ON TRACK for the built pieces (spec doc + skill + CEO wiring
cover his real words directly); the new-table/tiering pieces correctly
stay PARTIAL/open-fork rather than guessed defaults substituted in
their place — no MATERIAL/CAPTURED drift, an honest fork isn't a
broken promise.

================================================================
SAME DAY, 7TH PASS — Alex's real answers: G9 reclassified blue,
system_map_flags confirmed and built, G2 (the actual galaxy visual)
built and shipped
================================================================

Real, decisive answers, not another round of questions: "flag as blue
for this plan, it it way further down the line when rpgace is scoped
for consumers and its is a ready product, flag the recommendation
too." — G9 (Consumer/Developer visibility tiering) reclassified
red->blue in `ceo_plan_items` (confirmed genuinely far-future, not a
near-term plan) and added to `future_integrations.html`'s Blue section
in its own real group ("Product readiness — Consumer/Developer
split"), correctly not present in the Red plan-group (it had never
been added there — checked directly before assuming a removal was
needed). "1. yes" — confirmed the proposed `system_map_flags` schema;
built for real via `apply_migration` (additive, matches the CEO-
framework's own reduced-friction carve-out), `anon_all`/
`authenticated_all` RLS matching `session_memory`/`graphify_jobs`/
`ceo_plans`. `.claude/skills/cartographer/SKILL.md` + `system_map_spec.
md` §8 updated from "recommended, not built" to real/live. New
session-start check added to CLAUDE.md (same passive-pull pattern as
`oracle_dev_suggestions`/`graphify_jobs`/`error_log`).

"then build g2" — built for real, not deferred again. New
`scripts/galaxy_map.py` generates `graphify-out/galaxy_map.html`: a
real hand-rolled radial SVG (NOT vis.js — a genuinely different tool
for a genuinely different job, per the earlier /aintergration
verdict), reusing `EXTERNAL_CONNECTORS`/`SUPABASE_CORE` directly from
`graphify_river_group.py` (imported, never re-derived — rule 8). 4
real galaxies per `system_map_spec.md` §1/§3 (RPGACE Architecture
central, Orchestrator CC/OpenMontage CC/Graphify CC as satellites —
external API-only providers correctly do NOT get their own galaxies,
per §3's own rule), Oracle and self-awareness as their own harness
nodes (the design constraint logged on G2 itself), all 14 real
connector/core nodes styled tested/untested (dashed ring + reduced
opacity for unconfirmed ones, never hidden), and — a real content fix
caught during the FIRST screenshot review, not assumed correct — a
second bridge line drawn directly between the OpenMontage connector
node and the OpenMontage CC galaxy node, since Fork 3's confirmed
answer ("the openmontage node will be the connector to openmontage
repo and rpgace repo, so both can exist in the galaxy output") wasn't
actually visually expressed in the first render.

**Real verification, not just code inspection**: rendered via an
actual headless-Chromium screenshot (`/opt/pw-browsers`, no Playwright
package needed — direct CLI screenshot flag) and visually reviewed
twice — once before the OpenMontage-bridge fix, once after, confirming
the fix rendered correctly (a visible dashed red line from the
connector cluster up to the OpenMontage CC galaxy). Node/edge counts
verified against the real expected math (20 circles = 4 galaxies + 2
harness + 14 connectors; 19 paths = 3 satellite + 2 harness + 14
connector edges, before the extra bridge line). Div-balance clean.

Wired into the live app: a new "Galaxy Map (Level 0)" entry added to
`dashDeck._openOversight`'s real Auto-generated group (`rpgace_core.js`)
— `node --check` clean, re-minified (`rpgace_core.min.js` 663,301
bytes), onclick-name safety re-verified (all 47 real handlers present;
the same 2 known pre-existing dead buttons still missing, plus a real,
harmless false-positive from the verification script itself —
`quickPrompt` is a runtime-rewired onclick string, not a plain global
function, confirmed pre-existing and untouched by this diff, not a
regression). `oracleAppGrounding.SELF_KNOWLEDGE` updated same pass
(rule 6) with the new Galaxy Map's real scope, explicitly stating it's
Level 0 only so Oracle never claims the drill-down levels exist yet.
`index.html`'s cache-bust bumped to `20260813a`.

G2 marked green in `ceo_plan_items` with full real evidence. G3
(river-level drill-down) and G4 (function-level drill-down) remain
real, ratified, NOT built — each is its own future `/Engineer` pass,
reusing this same script's structure once picked up, not started
blind here.

