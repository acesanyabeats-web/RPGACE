GALAXY MAP — L0/DIMENSION/RIVER/CURRENT REDEFINITION — /CEO LOOP 1 PLAN
Steps 2 (/interrogation, complete), 3 (/paranoia-advised draft), 5 (report)
Companion file: galaxy_map_l0_dimension_river_redefinition_spec_2026-08-18.txt
                 (Step 1 — the verbatim compiled spec)
Date: 2026-08-18
Advisors: /restructure, /Engineer (code-change scrutiny per Alex's own ask)
Basis for /drift: NONE YET — this plan IS the candidate ratified baseline;
                   no prior ratified baseline exists for this specific
                   redefinition, so /drift here checks the DRAFT against
                   the compiled spec (Step 1), not against old shipped work.

=====================================================================
STEP 2 — /interrogation ANSWERS (all 9 forks resolved with Alex directly)
=====================================================================
F1 (L0 nesting): PEER UNIT, drill into it. The 7 new L0 units sit at the
   top; clicking "RPGACE Architecture" is the door into the EXISTING
   4-galaxy / Dimension->River->Module->Current chain. Nothing existing
   is thrown away.
F2 (External AI unit): EXTEND existing G27 (galaxy_map_externals.html).
F3 (Supabase artifact): ONE new page, all real tables, which
   Level/River/Module reads or writes each.
F4 (Skills artifact): EXTEND existing G28 (galaxy_map_skills.html) +
   G36 (galaxy_map_skill_network.html) with the same Level/River usage
   mapping, not a new file.
F5 (Current shape): NEW TAB on the EXISTING Level 3 module page, reusing
   its already-built function-call-chain data plus Level 6 + Logic
   Dimension content inline — not a 20th standalone file.
F6/F7 (River rebuild aggressiveness): Alex chose FULL REBUILD — recompute
   all 16 rivers' membership from real module-interaction/bundling
   evidence, not the conservative "add a view, merge only with clear
   evidence" option I had recommended. **See the real /paranoia finding
   in Part 2 below — this choice needs one more real decision now that
   real data exists to look at.**
F8 (Matrix relationship): TWO SEPARATE matrices. G30 (module-grain, 44
   modules x 5 tags) stays exactly as-is. A NEW L0-unit-grain matrix (7
   units x their real edges) gets built fresh for the new top map.
F9 (code-change gate): REPORT FIRST, build only after explicit approval.
   Nothing in this pass touches rpgace_core.js/main.js or ships a new
   Galaxy Map HTML file — this whole document is Step 5 of Loop 1, not
   Loop 2 execution.

=====================================================================
PART 1 — WHERE EACH L0 UNIT ALREADY HAS REAL, EXISTING GROUND TO STAND ON
=====================================================================
Real /restructure-flavored evidence check (does existing infrastructure
already cover part of this, before proposing anything new — rule 8):

1.1 Rivers XII-XVI ALREADY EXIST as zero-module "zones," built Aug 6/11
    from real file-path evidence, and line up remarkably closely with
    Alex's new L0 unit list:
        River XII  — "The API / Auth Layer"      (RPGACE_MODULES: none)
        River XIII — "Skills"                     (RIVER_MODULES: none)
        River XIV  — "Oversight Docs"              (RIVER_MODULES: none)
        River XV   — "Session Records / Backlog"   (RIVER_MODULES: none)
        River XVI  — "Dev Tooling"                 (RIVER_MODULES: none)
    This is a REAL, PRE-EXISTING, evidence-backed precedent for exactly
    two of Alex's asked-for L0 units:
        - "Skills" (L0 unit) <-> River XIII, already named "Skills"
        - "Oversight Docs" (L0 unit, explicit "deserves a L0 promotion")
          <-> River XIV, already named "Oversight Docs"
    Real, honest implication: Alex is not asking for these to be invented
    from nothing — he's asking for something that ALREADY EXISTS as a
    river-shaped zone to be promoted to a first-class L0 unit with its
    own dimension-edges to the other 6 units. This narrows real build
    risk considerably and is a strong point in favor of the "peer unit"
    F1 answer already chosen: the new L0 layer is a real re-framing of
    already-computed zone data, not a green-field build.
    River XII ("API/Auth Layer") is also a strong real candidate for
    where "RPGACE Architecture (backend)" boundary work already lives —
    worth citing explicitly inside that unit's own drill-down rather than
    re-deriving it.

1.2 Real module census (fresh run, this pass): 57 total top-level modules
    exist in rpgace_core.js. Only 45 are assigned to a river (Rivers 1-11
    hold real code; 12-16 hold zero, by design, per 1.1). **12 modules are
    in NEITHER any river NOR any zone**: `config`, `dashDeck`, `docsLinks`,
    `errorLog`, `leftNav`, `perfWatch`, `pwaInstall`, `quickActions`,
    `suppressQuestPopup`, `uiBatchList`, `uiSlidePanel`, `voiceInput`. This
    is a real, fresh, previously-uncaught gap (distinct from the Aug 14
    "main.js 12 unclassified nodes" finding — this is rpgace_core.js
    modules, a different file/layer). Most of these are real, legitimate
    cross-cutting/global infrastructure (dashDeck IS the dashboard shell
    itself; leftNav/perfWatch/pwaInstall/voiceInput are app-wide chrome,
    not river-scoped features) — flagged as a real, honest finding for
    /restructure to weigh, NOT fixed blind here. A real candidate
    resolution once this plan is approved: these 12 modules may belong
    under the new "RPGACE Architecture" L0 unit's own top-level
    Dimension/River drill-down as a real "cross-cutting / app shell"
    grouping rather than being forced into one of the 16 goal-oriented
    rivers where they don't actually fit thematically.

=====================================================================
PART 2 — REAL /paranoia FINDING: literal full-graph river rebuild, TESTED
=====================================================================
Per rule 4 ("get real evidence, don't patch blind a second time") and
GODMODE's "honest why-NOT-to-build-this" requirement, before drafting a
16-river rebuild I actually computed it, using every real module-
interaction signal already built and proven in this codebase (direct
RPGACE.modules.X() calls, window.X wrap-chains, shared RPGACE.utils.
sendToOracle/fillGaps convergence, RPGACE.hooks.fire()/on() pairing) —
the same 4 signal classes compute_intra_river_flow() already uses, just
computed GLOBALLY across all 57 modules instead of scoped inside each
river. Real script, real run, not simulated:

    Total modules: 57 | Total real edges: 148 (81 direct, 9 wrap,
    45 utility, 13 hook) | mainjs-bridge hook edges (excluded from
    module-to-module clustering, a different real signal kind): 32

    Real result — 19 connected components:
      - ONE giant component: 39 of 57 modules (68%), spanning 9 of the
        16 current rivers (III, IV, V, VI, VII, VIII, IX, X, XI) —
        everything that ever calls RPGACE.utils.sendToOracle/fillGaps
        (the Oracle-send utility) collapses into a single undifferentiated
        blob, because that ONE shared convergence point alone connects
        modules across 9 different real goal-domains.
      - 18 further components, every one a SINGLETON (1 module each) —
        agentsIntoOracle, authGate, docsLinks, errorLog, jargonEncyclopedia,
        journalQoL, oracleProviderMode, pathRouter, perfWatch, pwaInstall,
        quickActions, scheduleFixes, shiftSync, suppressQuestPopup,
        uiBatchList, uiSlidePanel, voiceInput, youtubeOracle.

    Real per-river breakdown (which of the 16 current rivers a pure
    interaction rebuild would actually SPLIT internally):
      River III (Oracle Current, 13 mods)  -> 4 real components [SPLIT]
      River V   (Two Independent Streams, 10 mods) -> 4 real components [SPLIT]
      River VII (Library Current, 5 mods)  -> 2 real components [SPLIT]
      Rivers I, II, IV, VI, VIII, IX, X, XI -> unified (1 component each,
        several trivially so since they're already single-module rivers)

HONEST VERDICT, stated plainly rather than built silently: a LITERAL
full rebuild — river membership recomputed from module-interaction
evidence ALONE, discarding the current goal-based grouping — actively
WORKS AGAINST the stated success criterion ("digestible... explain to
even the biggest idiot"). It would replace 16 real, named, goal-oriented
rivers with 1 giant undifferentiated 39-module blob plus 18 disconnected
singletons — objectively LESS legible, not more, because one shared
utility function (`RPGACE.utils.sendToOracle`) is real but far too
generic a signal to carry real thematic meaning on its own (the same
"a scanner artifact, not a bug" class of over-broad signal this project
has hit before — c.f. the deliberately-excluded RPGACE.utils.toast()
exclusion already noted in compute_intra_river_flow()'s own docstring).

This is NOT a reason to abandon Alex's real, stated intent — "rivers
should be redefined into how modules interact and bundle... so both
interactive and non-interactive processes can be explained" is a
genuinely good, real widening of river membership rules (several
current rivers are single-module precisely BECAUSE they're goal-
categories with no interaction to show, which the new definition
explicitly wants to keep legible). The finding is narrower and sharper:
CONNECTED-COMPONENTS ON RAW INTERACTION DATA ALONE is the wrong
algorithm for what Alex actually wants, because it has no concept of
"goal" or "purpose" at all — it only knows "did code literally touch
code." Alex's own explicit words already say the goal-framing must
survive: "rivers put those units into context of how it is used in
SEQUENCE TO FULFILL A GOAL... each river has a use and goal to get to
as we can see in current structure" [emphasis added] — this is a
real, explicit instruction that the interaction rebuild must be
RECONCILED against, not replace.

RECOMMENDED RECONCILIATION (real, evidenced, not a guess): treat the
interaction-component data above as a real DIAGNOSTIC OVERLAY on the
existing 16 goal-based rivers, not as the sole new definition:
  - The 13 unified/trivially-unified rivers are CONFIRMED as-is — real
    evidence supports keeping their current membership.
  - The 3 SPLIT rivers (III, V, VII) get a real, evidenced closer look —
    genuine candidates for the "some rivers will become dimensions and
    merge where needed" language in Alex's original trigger prompt. Note
    River V splitting into 4 components matches the ALREADY-KNOWN G22/G25
    finding (7 of 10 River V modules still show no detected connection,
    logged blue as `ceo_plan_items` G22) — this is not a new problem, it's
    the same one, now with sharper data.
  - River membership going forward is decided by BOTH signals together:
    real interaction evidence (this pass's new data) AND real shared-goal
    coherence (the existing per-river "use and goal" framing, unchanged)
    — never interaction evidence alone. This is the literal meaning of
    "so both interactive and non-interactive processes can be explained":
    a river can legitimately hold non-interacting modules if they share
    one real goal (as most already do), and the new work is widening the
    EVIDENCE a river's description can cite, not discarding purpose as
    the organizing principle.

**This is the one point in this whole plan that genuinely conflicts with
your literal "Full rebuild" answer, backed by real numbers rather than my
earlier hedge — flagged directly rather than quietly downgraded to my
original recommendation.** See the question at the end of this report.

=====================================================================
PART 3 — CONCRETE SHAPE OF EVERY OTHER PIECE (ready to execute on approval)
=====================================================================

3.1 L0 map (peer unit, F1) — 7 units, real edges — CORRECTED per Part 7B:
    External AI | RPGACE Architecture (UI+Backend) | Skills |
    Orchestrator CC | Alex | Supabase | Oversight Docs
    Click a unit -> list of real dimension-edges from that unit (one per
    connected unit) -> click an edge -> real drop panel: bubbles
    ("structures") + yes/no forks (reusing Decisions/G26's already-built
    fork pattern) explaining real communication/work between the two.
    **Correction (was wrong in the original draft, see Part 7B): RPGACE
    Architecture is NOT a privileged single gateway into the L0-4
    structure.** EVERY one of the 6 other units' own dimension-edge WITH
    RPGACE Architecture is its own real, independent doorway into that
    same underlying L0-4 hierarchy, entered from that unit's own real
    vantage point (Alex's decisions, Supabase's tables, Skills'
    procedures, External AI's calls, Orchestrator CC's commits,
    Oversight Docs' coverage) — never through one unit alone. See Part 7
    for the concrete per-unit shape.

3.2 Dimension Matrix, L0-grain (F8, new, separate from G30):
    7 units x their real edges, table form — externals show which AI is
    used (reuses G27/oracleProviderMode data); Supabase/Skills columns
    point into the new pages from 3.3/3.4; UI/Alex column reuses the
    already-built accessibility-badge evidence (G2x work).

3.3 New Supabase page (F3): every real table (grep-derivable from
    `RPGACE.sb.*`/`secureWrite`/`ALLOWED_TABLES` call sites, same
    detection-script discipline as every other Galaxy Map page), which
    Level/River/Module reads or writes each. A real `/perspective`-style
    write-up frames the page's own intro (reusing that skill's report
    shape per F3's answer), not a full per-table report set.

3.4 Skills page extension (F4): add a Level/River usage column to the
    existing G28 axis table and a corresponding cross-reference on G36's
    skill-network page — no new file.

3.5 "Current" tab (F5): a new tab on the existing Level 3 module page.
    Per-function, chained: (a) real input sources (can be plural,
    already-detected via Level 3's own call-chain data), (b) real
    handling logic (pulls the matching Level 6 branch listing + any
    Logic Dimension passage inline, reuse not re-derivation), (c) real
    output formation, (d) real next-function hop. This is a genuine
    reuse of 3 already-shipped data sources recomposed into one new lens
    — zero new detection logic required.

3.6 River redefinition (F6/F7): per Part 2's reconciliation — pending
    your answer to the one open question below before any river
    membership actually changes.

3.7 Oversight Docs / Chronicles role redefinition: real, cheap doc-text
    change once approved — Oversight Docs' own description becomes
    "external change documentation in respect to RPGACE Architecture";
    Chronicles' own description becomes "mostly internal, reflecting
    externally via system_updates." No code change, no schema change —
    a real CLAUDE.md / interconnection_map.md / minotaur_map.html prose
    update, Tier 1 once approved.

3.8 Taxonomy-as-partner: explicitly NOT touched. Per your own words,
    "ill think that through later" — nothing here proposes a shape for
    it, and nothing should until you raise it again.

=====================================================================
PART 4 — /restructure + /Engineer SCRUTINY (real code-change candidates)
=====================================================================
Per F9's answer (report only, build after approval), these are FLAGGED,
NOT BUILT:
  R1. The 12 rpgace_core.js modules with no river/zone home at all
      (Part 1.2) are a real, standing gap independent of this whole
      redefinition — worth a real decision (fold into "RPGACE Architecture"
      L0 unit's own cross-cutting grouping, or give them a real 17th
      River/Zone) regardless of which river-rebuild path gets chosen.
  R2. No rpgace_core.js/main.js runtime code needs to change to build
      ANY of Part 3's pieces — every one of them is read-only detection-
      script + new/extended static HTML, matching the standing "Galaxy
      Map is auto-generated, never hand-authored" discipline. /Engineer's
      shape-sketch requirement is satisfied trivially: no build risk to
      live Oracle/taxonomy/beat-pipeline behavior exists in this plan at
      all — the entire redefinition is a documentation/visualization
      layer change, not an app-behavior change. This is a real, positive
      finding worth stating plainly: this whole plan is low Tier-2 risk,
      not Tier-3, once the Part 2 open question is settled.

=====================================================================
PART 5 — /drift CHECK
=====================================================================
BASIS: provisional (this plan IS the not-yet-ratified draft; Step 1's
compiled spec is the real source it's checked against).
VERDICT: on track, with one MATERIAL gap surfaced and returned to you
rather than silently resolved — Part 2's literal-vs-reconciled river
rebuild question. Every other fork (F1-F5, F8, F9) has a concrete,
evidence-grounded shape ready to execute. No CAPTURED-grade finding
(nothing was built without your say-so; no destructive/Tier-3 action
proposed).

=====================================================================
PART 6 — RESOLVED: river rebuild path (Alex's answer, same session)
=====================================================================
Given the real numbers in Part 2 (1 giant 39-module blob + 18 singletons
under pure connected-components), Alex chose the RECONCILED path:
"Reconciled: interaction data overlays goal-framing (Recommended)."

Real, final scope this locks in:
  - The 13 already-unified/trivially-unified rivers (I, II, IV, VI, VIII,
    IX, X, XI, plus the always-unified single-module ones) keep their
    current membership — real evidence confirms them, no change.
  - Rivers III (Oracle Current), V (Two Independent Streams), VII
    (Library Current) each get a real, evidenced closer look in Loop 2 —
    a possible internal split, a merge, or a conversion of part of a
    river into a dimension-edge instead, decided per-river from BOTH the
    real interaction-component data (Part 2) AND real shared-goal
    coherence, never interaction data alone.
  - River V's 4-way split is the SAME already-known gap as `ceo_plan_items`
    G22 (7 of 10 modules still undetected) — Loop 2 work on River V
    should resolve G22 and this redefinition in the same pass, not
    twice (rule 8).

=====================================================================
PART 7 — REAL ADDENDUM: Oversight Docs bubble system (Part 4B of the spec)
=====================================================================
Real /interrogation answers on the 2 new forks this addendum raised:
  - Capabilities-list HOME: inside the Oversight-Docs<->Orchestrator-CC
    dimension edge's own drop panel — not a new standalone page, not a
    new ai_tooling_and_rules_map.md section. Lives exactly where you
    click for it.
  - Capabilities-list METHOD: hand-compiled from real demonstrated
    project history, not mechanically scanned — same judgment-driven
    discipline as session_lessons.html's own retroactive seed.

7.1 Oversight Docs bubble system — concrete shape:
    A real, evidence-gated bubble trio (R12 discipline: never forced-
    permanent) on the Oversight Docs L0 unit, showing its real
    interaction with exactly 3 other units — Skills, Orchestrator CC,
    Alex — during development:
      - Oversight Docs <-> Skills: evidenced by real skill .md files
        that cite oversight-doc maintenance as part of their own
        procedure (CEO SKILL.md's Loop 2 "populate smoke_test.html",
        update-logging-system SKILL.md's whole dependency map, Bedtime
        SKILL.md's Step 1, colourgradient's error_log cross-check) —
        real, grep-checkable citations, not asserted.
      - Oversight Docs <-> Orchestrator CC: evidenced by the standing
        rule-6 obligation itself (every confirmed idea/fix updates
        oversight docs same-session) plus the real commit history
        touching these files — and carries the new capabilities catalog
        (7.2 below) as its actual drop-panel content.
      - Oversight Docs <-> Alex: evidenced by the explicit human-checkpoint
        pattern already named throughout this file (Alex reading/
        confirming Current State entries, the "update oversight"/
        "end session" trigger phrases, his own direct corrections like
        this session's schematic re-read) — real, cited, not invented.

7.2 The real capabilities catalog (Oversight-Docs<->Orchestrator-CC
    drop-panel content) — hand-compiled from actually-demonstrated
    project history, not aspirational:

    WHAT ALEX AND ORCHESTRATOR CC CAN DO TOGETHER (real, evidenced):
    1. Direct code implementation — rpgace_core.js/main.js/api/*.js
       edits following the RPGACE.register()/module-marker convention,
       node --check discipline, real merge-to-main-before-hand-test
       gating.
    2. Supabase schema + data work — migrations, RLS policy changes
       (the real Approach-B flip, independently verified via pg_policy,
       not just applied), dedup scans, direct evidence queries.
    3. Multi-angle deliberation frameworks — Omnitrix/Council-of-5/
       GODMODE/Aintergration, /CEO, /paranoia, /interrogation, /drift,
       /restructure, /Engineer, /debate, /5thDimension, /scope,
       /colourgradient, /perspective, /cartographer — real structured
       scrutiny before a big build, not agreement-then-build (this
       whole document is a live instance of it).
    4. Oversight documentation upkeep — keeping all 11 real oversight
       artifacts (patch_notes/manual/minotaur_map/interconnection_map/
       system_flow_map/ai_tooling_and_rules_map/smoke_test/error_log/
       session_lessons/future_integrations/CLAUDE.md) truthful against
       live code/Supabase, same session as any real change (rule 6).
    5. The Galaxy Map / graphify pipeline — building and regenerating a
       whole multi-level, evidence-derived visualization system (7
       levels + a dozen dimension pages) from real detection scripts,
       with idempotency and anchor-verification checked, not assumed.
    6. Genuine collaborative decision-making — surfacing real forks via
       AskUserQuestion with real evidence attached (like Part 2's river-
       rebuild math) instead of guessing or agreeing reflexively; Alex
       overriding a recommendation is a normal, expected outcome, not a
       failure state.
    7. Subagent orchestration — Sonnet/Opus/Fable role-splitting
       (Omnitrix), background dispatch, and real Total-systems
       coordination with OpenMontage CC / Graphify CC via Supabase
       dispatch tables (openmontage_jobs/graphify_jobs), honestly
       scoped around the real constraint that no live session-to-
       session channel exists.
    8. Session-continuity infrastructure — session_memory,
       /Bedtime/Routine/Summary — durable state across ephemeral
       containers, catching real gaps like a compaction silently
       dropping an attached file.
    9. Error/lesson tracking with real promotion to standing rules —
       error_log.html's dedup-by-signature + resolution history,
       session_lessons.html's obstacle/reasoning/solution cards, and
       CLAUDE.md's own numbered rules 12-15 as real examples of a
       one-off mistake becoming a permanent, generalized guardrail.
    10. Security/architecture hardening done carefully — the API-auth
        fix, the RLS flip, the password-gate redesign — real Tier-3
        work, independently verified against production (a live 401,
        a direct pg_policy query), never taken on trust alone.
    11. Scheduling / automation — Routines/triggers (Morning Brief,
        Fallback Drain), push notifications, cron dispatch that survives
        container restarts.
    12. Real third-party tool judgment — Aintergration verdicts
        (Adopt/Adopt-with-modification/Reference-only/Not-applicable/
        Reject) grounded in the tool's actual docs and RPGACE's actual
        architecture, never a pitch taken at face value.
    13. Live mid-build course correction — Alex catching a genuine
        misreading (this exact session's schematic reinterpretation is
        itself a live instance) and Orchestrator CC re-planning from his
        real correction rather than defending the wrong reading.

    Honest framing note for the eventual drop-panel copy: this list is
    itself evidence-of-itself — every single item above is something
    that has actually happened in this project's real history, not a
    marketing description of hypothetical capability.

=====================================================================
PART 7B — REAL CORRECTION: symmetric unit-gateway model + level-tagged
decision/current bubbles (Part 4C of the spec)
=====================================================================
Real bug in the original Part 3.1 draft, Alex's own words: "RPGACE
architecure should not be the one gate to see further structures, that
doesnt make sense." Confirmed on re-read: his ORIGINAL spec already said
this correctly ("click a unit... shows what choice dimension i can go
for") — the error was introduced in this plan's own summarizing prose,
not in his ask. Fixed at 3.1 above.

7B.1 The corrected model, concretely: the L0-4 Galaxy Map hierarchy isn't
     behind one privileged door. It's ONE real underlying dataset that
     every other unit's own dimension-edge with RPGACE Architecture
     provides a genuinely different, real, evidence-grounded LENS over —
     filtered to what that specific unit actually touches. Clicking into
     Alex's dimension shows Alex's real touchpoints; clicking into
     Supabase's shows Supabase's; both are looking at the same real L0-4
     structure, from different, equally real vantage points. No lens is
     the "main" one.

7B.2 The level-tagged bubble/current mechanic, per unit, using ONLY
     already-built real evidence (rule 8 — no new detection logic):

     ALEX <-> RPGACE Architecture: reuses Decisions/G26's already-built
       DECISION_POINTS (10 real human-gate decisions, already anchor-
       verified against live code, already cross-linked into Level 3/5).
       Each decision point becomes a bubble tagged by the real level(s)
       it touches (typically L2 module / L3 function, some also L5
       logic). INPUT = the real UI element that surfaces the choice to
       Alex (a confirm popup, an arm/confirm button — real DOM evidence
       already cited in G26). HANDLING = Alex's actual yes/no. OUTPUT =
       the write/action taken, which G26 already documents. This is
       ~90% already-built data being re-presented through Alex's own
       dimension-lens, not new work.

     SUPABASE <-> RPGACE Architecture: reuses the new Supabase page
       (3.3). Each real table gets a bubble per level/module that reads
       or writes it. INPUT = the function issuing the query. HANDLING =
       the query itself. OUTPUT = the row(s) returned/written. NEXT =
       whichever function consumes the result (Current-chain data,
       3.5).

     EXTERNAL AI <-> RPGACE Architecture: reuses the already-built
       Oracle-bubble call-count data (`compute_module_oracle_call_count`/
       `compute_card_oracle_call_count`, G16) and the G27 Externals page.
       A bubble per module/level with a real call_count > 0. INPUT = the
       prompt/context assembled there. OUTPUT = the model's reply. NEXT
       = whichever function consumes it.

     SKILLS <-> RPGACE Architecture: reuses the extended G28/G36 data
       (F4/3.4). A bubble per real skill x level pairing where that
       skill's own procedure names a real touch. INPUT = what triggers
       the skill (Alex naming it, or another skill invoking it per G36's
       skill-network edges). OUTPUT = what the skill actually changes.

     ORCHESTRATOR CC <-> RPGACE Architecture: real git commit history
       touching rpgace_core.js/main.js/api/*, tagged by which level/
       river/module the touched code lives in. A bubble per level
       summarizing real, dated commit activity there.

     OVERSIGHT DOCS <-> RPGACE Architecture: a bubble per real doc x
       level pairing — which doc documents which level (minotaur_map.html
       ~ River-level, system_flow_map.md ~ pipeline/flow-level,
       smoke_test.html ~ per-feature/module-level). INPUT = the real
       shipped change that prompted the doc update. OUTPUT = the doc's
       own new content. Matches 4B.3(b) exactly (rule 8, same axis, not
       redefined twice).

     Every one of these 6 lenses reuses data this multi-day effort has
     already built — the "new" work is a real cross-cutting PRESENTATION
     layer (level-tagging + input/handling/output framing per bubble),
     not 6 new detection subsystems.

7B.3 River-to-dimension promotion (4C.2): folded directly into the
     already-locked-in Part 6 River III/V/VII closer look — dimension-
     promotion is now an explicit, real candidate outcome for that work
     (not just split/merge), decided per-river from the same real
     evidence already gathered.

7B.4 Standing process rule (4C.4): added as R16 in
     `.claude/skills/CEO/SKILL.md`'s Rules and Workflows section (same
     file/format as R1-R15) — a structural design that special-cases one
     peer among structurally-equal units without a stated real reason is
     probably an oversight, check every peer before finalizing. Done,
     not deferred — this is a process-documentation fix, not a Galaxy
     Map code/HTML change, so it isn't gated by F9's "report first, build
     after approval" (which scopes the Galaxy Map deliverable itself).

=====================================================================
PART 8 — REAL EXTENSION: Level 4/5/6 /deduplication + Current/Module/
River lowest-to-highest model (Part 4D of the spec)
=====================================================================

8.1 REAL EVIDENCE GATHERED FIRST (rule 4 -- read the actual files, don't
    assume duplication): direct read of the 3 named scripts today.
      - Level 4 (`galaxy_map_level4.py`): per-DASHBOARD-CARD real
        frontend flow -- click a card -> real go: trigger ->
        popup/page/module, with real DOM output/input evidence and a
        real Oracle-call badge. Entry point = a real UI click.
      - Level 5 (`galaxy_map_level5.py`): 7 hand-curated "core logic"
        decision points, function-scoped, verbatim code excerpts, a real
        `decider` field (WHICH actor decides -- Alex/Oracle/code-logic),
        plus decides/changes/result prose.
      - Level 6 (`galaxy_map_level6.py`): the exhaustive, mechanical
        counterpart -- every real if/else-if/else/switch branch
        (1088 real branch points, `compute_function_branches()`),
        module->function grouped, condition text only, unnarrated.

8.2 REAL MAPPING onto the new model (this is what makes it a genuine
    /deduplication case, not just Alex's hunch taken on faith):
      - Level 6's exhaustive per-function branch data IS what 4D.2 (
        "current... function by function... logic per step") is asking
        for, once the unit-bubble-tying mechanic (7B.2) is laid over it.
      - Level 5's curated `decider`/decides/changes/result framing is the
        real PROTOTYPE of the unit-tying idea, applied so far to only 7
        hand-picked functions instead of every one.
      - Level 4's dashboard-card-click evidence is real, concrete proof
        of the Alex/UI unit's own involvement in a specific module --
        exactly the kind of evidence 4D.3 ("modules... tying all units
        together") needs, just currently organized by card instead of by
        module.
      - Level 3 (function-call chains) is NOT named by Alex for change
        and stays as the real navigational container page -- Current
        becomes its new tab (F5, already agreed), not a replacement.

    CONFIRMED, not assumed: Levels 4/5/6 as SEPARATE STANDALONE PAGES do
    become genuinely redundant once Current (absorbing 5+6's content)
    and Module (absorbing 4's content) are built as specified. Alex's
    "i really think" was right.

8.3 REAL /paranoia (right-sized -- GODMODE evidence + genuine multi-angle
    pushback, not the full 6-phase ceremony, matching how this whole
    session has already scaled the protocol to a Loop-1 planning
    refinement rather than a from-scratch adoption question):

    Con 1 -- Level 5's curated 7-item "what actually matters" view is a
    real, useful FAST browsing path; an undifferentiated function-by-
    function firehose (all ~1088 branches presented with equal weight)
    could be LESS legible, not more -- the same "raw exhaustive data
    isn't automatically more useful than curated data" lesson Part 2's
    river-rebuild finding already taught this exact plan once today.
      MITIGATION: fold, don't discard -- Level 5's curation becomes a
      real "⭐ notable" tag on the matching Current entries (not a
      separate page, a real filter/sort inside Current), so the fast
      7-item browsing path survives inside the richer view instead of
      being lost.

    Con 2 -- Level 4's dashboard-card entry point is a genuinely
    different STARTING navigational path ("start from what I actually
    click in the live app") than starting from a module and drilling in.
    Folding it flat into Module risks losing that "start from the UI"
    path.
      MITIGATION: Level 4's real click-evidence becomes the concrete
      data feeding the Alex<->RPGACE-Architecture dimension lens (7B.2,
      already designed) -- the "start from what I click" path still
      exists, just correctly homed under Alex's own unit-lens instead of
      as an unrelated flat numbered level.

    Con 3 -- real rebuild surface across multiple existing scripts
    (level3/4/5/6, module, river) carries the same anchor-drift/
    idempotency risk this whole build has hit repeatedly (R5).
      MITIGATION: no new risk class -- apply the exact same standing
      verification discipline (idempotency re-run + diff, anchor
      verification, div/svg-balance check) already proven across every
      prior Galaxy Map shipment.

    Verdict: the /deduplication call holds. Nothing of real value gets
    deleted -- Level 5's curation and Level 4's UI-entry evidence both
    get FOLDED into the richer new model (their real content lives on,
    just no longer duplicated across a separate standalone page), which
    is the correct reading of rule 8 ("eliminate deduplication gaps")
    established throughout this whole project -- rule 8 has never meant
    "delete real content," only "route it through one shared place."

8.4 REAL /drift CHECK: BASIS = provisional (this whole document, still
    being refined same-session). VERDICT = on track -- every claim in
    4D is additive/consistent with what's already agreed (F5's Current-
    as-new-tab, Part 6's reconciled river approach, Part 7B's level-
    tagged bubble mechanic), not a contradiction of any prior decision.
    No MATERIAL gap found.

8.5 New/changed real deliverables, per Loop-2 G-step (item codes G42-G50
    minted in `ceo_plan_items`, all status=red -- "a real, specified
    plan, not built," per Alex's own explicit ask to move this from an
    untracked idea to a real red row, then a real breakdown):

      G42 (umbrella) — L0/Dimension/River/Module/Current redefinition,
          Galaxy Development Framework restructure. Evidence: this file
          + its companion spec file.
      G43 — L0 map, 7 peer units, symmetric per-unit dimension gateways
          (no privileged unit — the corrected 3.1/7B model).
      G44 — Dimension Matrix, L0-unit grain (F8/3.2), separate from G30.
      G45 — New Supabase page: every real table x Level/River/Module
          (F3/3.3).
      G46 — Skills page extension: Level/River usage column on the
          existing G28/G36 pages (F4/3.4).
      G47 — Current tab on the existing Level 3 page: absorbs Level 5
          (curated decisions, kept as a "notable" tag) + Level 6
          (exhaustive branches) content, real unit-bubble-tying per
          function/step (F5/3.5/4D.2). Retires galaxy_map_level5.html/
          galaxy_map_level6.html as standalone pages once built.
      G48 — Module-level real unit-tying (4D.3); retires
          galaxy_map_level4.html as a standalone page, its real
          dashboard-card-click evidence folded into the Module page +
          the Alex<->RPGACE-Architecture dimension lens (7B.2).
      G49 — River redefinition v2 (4D.4): all of a river's Module-level
          unit-bubbles interacting with each other, extending the
          already-locked Part 6 reconciled approach (III/V/VII closer
          look) with real unit-visibility, not just module-interaction.
      G50 — Oversight Docs bubble system + the hand-compiled
          Orchestrator-CC capabilities catalog (Part 7, already drafted
          in full at 7.2).

    All 9 rows inserted into Supabase `ceo_plan_items`
    (plan_id 6f8536fd-7d98-4565-9369-c75a149fce8f) at status=red per
    Alex's own explicit ask -- nothing moves to yellow/green until Loop 2
    execution actually starts on that specific item.

=====================================================================
PART 9 — REAL EXTENSION: Level 3 replaced, Level 4 repurposed (Part 4E)
=====================================================================

9.1 /interrogation, run per Alex's explicit ask — both real forks
    resolved as the recommended option:
      - River cardinality: EMERGENT (G49's existing evidence-driven
        III/V/VII closer look decides real per-river module count going
        forward) — NOT a forced blanket "every river becomes exactly 1
        module" rule. Real grounding number: forcing it now would take
        the project from 16 to 50 total top-level groupings and discard
        Part 6's already-locked reconciled approach for no real gain —
        confirmed the right call not to force it.
      - Current-walk scope: PER-MODULE, not per-river. "Reaching a new
        module" (the already-built cross-module-call detector) is the
        real stop, independent of river boundaries.

9.2 /paranoia payoff: this DECOUPLES the good Level 3/4 UX idea from the
    much riskier river-cardinality-forcing idea Alex floated alongside
    it — the good idea ships in full, the risky one doesn't need
    forcing (G49's own real evidence-driven work may still arrive at
    something close to 1:1 for individual rivers over time, exactly as
    already designed).

9.3 CONCRETE, REVISED SHAPE — G47 and G48 updated in place (not two new
    items; this refines their already-tracked scope, rule 8):

      G47 (revised) — Level 3's OLD role (a per-module function-call-
        chain graph) is RETIRED, not extended with a bolted-on tab.
        Level 3's new role: a compact, ordered, per-module list of that
        module's own real Currents (absorbing Level 6's exhaustive
        branch data + Level 5's curated `decider` framing, kept as a
        real "⭐ notable" tag — same fold-not-delete verdict as Part 8,
        unchanged by this revision).

      G48 (revised) — Level 4's OLD role (dashboard-card click flow) is
        RETIRED, folded into the Module page + the Alex<->RPGACE-
        Architecture dimension lens (unchanged from Part 8). Level 4's
        FREED slot gets a genuinely NEW role: the zoomed, per-Current
        detail walkthrough — click a Current from Level 3's list, see
        its full input/handling/output/next breakdown (4D.2/7B.2's
        unit-bubble-tying), and keep walking forward, Current after
        Current, until the chain crosses into a different module (the
        real, already-built compute_cross_module_function_calls()
        signal) — the natural, evidence-backed hand-off point to that
        module's own Level 3 list.

    This is a genuinely cleaner design than the original G47/G48 split
    (a bolted-on tab plus a separately-retired page) — Level 3 and
    Level 4's OWN numbered slots get real, coherent new jobs instead of
    one page growing an extra tab and the other simply disappearing.

    G49 (river v2, unchanged) — confirmed still correct as scoped: the
    emergent/evidence-driven answer on river cardinality means G49's
    real interaction+goal closer look on III/V/VII proceeds exactly as
    already planned, with no forcing.

    No new G-item numbers needed — G47/G48's Supabase rows updated in
    place with this refined description (evidence field appended, per
    Alex's own already-established append convention for these rows).

=====================================================================
STEP 6 — ALL FORKS NOW RESOLVED. AWAITING ALEX'S FULL-PLAN APPROVAL.
=====================================================================
Every real fork in this document (F1-F9, plus the Part 2/6 river-rebuild
method) now has a concrete, evidence-grounded answer. Nothing has been
built yet — no rpgace_core.js/main.js edit, no new/changed Galaxy Map
HTML file, no Supabase write beyond this plan's own dated record files.
Per the CEO skill's own Loop 1 Step 6, this compiled plan is handed back
to Alex in full (a chat-level summary accompanies this file) for him to
read and explicitly approve before Loop 2 execution begins.
