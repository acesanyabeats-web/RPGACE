JOURNAL / CHRONICLES / MINOTAUR CROSS-FEED — DEFERRED IDEA, NOT YET SCOPED
Recorded July 23, 2026. Verbatim, per Alex's own explicit instruction this
session ("paste my answers as txt to not lose them") and CLAUDE.md rule 5
(record real answers verbatim, never substitute a recommended default).

STATUS: NOT BUILT. NOT YET GIVEN ITS OWN GODMODE/interrogation pass. This
file exists purely so the idea survives to a future session instead of
being lost inside an answer to an unrelated AskUserQuestion round (it was
raised as a tangent inside the answer to Q2 of the Research Lab lazy-load
spec - see research_lab_lazyload_spec_backlog.txt - and deliberately NOT
folded into that build, since this is a genuinely separate, multi-
subsystem idea and CLAUDE.md rule 5 requires 3+ new pieces to be spec'd
and confirmed first, not built inline as a footnote to a different task.

VERBATIM (Alex, July 23, answering a question about Research Lab tab
caching, but raising this instead/in addition):
"i say 1, then journal can save what done so i can go back to certain
frameworks or ideas that were in convo, needs work from brainstorming to
tie these. I would also like chronicles to feed off minotaur map and other
oversight docs in measured doses (minotaur because chronicles needs to be
readable, and minotaur is best candidate to feed that info through
/Scope) (and oversight to feed from chronicles to keep timeline straight
between Alex - RPGACE self awareness - Oracle self awareness - and claude
code (you now)"

WHAT THIS SEEMS TO DESCRIBE (a first read, NOT a locked interpretation -
real interrogation is still owed before building anything):
1. Journal should save "what's done" in a way that lets Alex go back and
   find specific frameworks or ideas that came up in a past conversation
   (Oracle chat, presumably) - today's journal is a running log, not
   obviously structured around "frameworks/ideas from a specific
   conversation" as a retrievable unit. Alex's own words: "needs work from
   brainstorming to tie these" - he is flagging this as unresolved/half-
   formed, not a locked spec.
2. Chronicles should pull a readable digest FROM minotaur_map.html and the
   other oversight docs, "in measured doses" - i.e. not a raw dump, some
   kind of summarization/selection step - explicitly suggesting /scope (the
   evidence-gathering skill already built this session) as the mechanism
   that does that selection, since minotaur_map is named as "best candidate"
   specifically because Chronicles needs to stay readable and minotaur is
   already the most narrative/readable of the six oversight docs.
3. Oversight docs should ALSO feed FROM Chronicles - a bidirectional
   relationship, not one-way - so that four separate "narrators" of
   RPGACE's own history (Alex himself, RPGACE's own in-app self-awareness,
   Oracle's self-awareness grounding, and Claude Code / this assistant)
   stay on one consistent timeline of what actually happened, rather than
   four separately-drifting accounts.

REAL ANSWERS (Alex, July 24, real interrogation via AskUserQuestion):

Q1 - Is "journal" the existing #page-journal feature, or a new concept
tied to Oracle conversations?
A1 (verbatim): "Not sure yet — needs more brainstorming." PARKED. No
build decision on the journal piece this round.

Q2 - What does "measured doses" mean operationally?
A2 (verbatim): "1 when mid session, 3 when /Bedtime for rpgace on all
oversight, not just minotaur." Read as: TWO tiers, not one -
(a) mid-session, Chronicles does a lightweight ON-OPEN read (option 1),
pulling a short real digest, but from ALL SIX oversight docs, not just
minotaur_map.html as originally floated; (b) at /Bedtime time (session
close), a heavier full pull (option 3's /scope-style thoroughness)
across the same six docs, not scoped narrowly to minotaur.

Q3 - What should "oversight feeds from Chronicles" concretely change?
A3 (verbatim): "1 and vice versa to get on same page both rpgace and
Claude code." Read as: system_updates already satisfies Chronicles
showing Claude Code's own work (option 1, confirmed sufficient) - but
Alex ALSO wants the reverse that was in his original July 23 framing:
real RPGACE-side activity that Claude Code did NOT originate (a beat
logged, a taxonomy proposal Alex approved himself in the review queue, a
journal entry, a finance ledger sale) should feed back INTO Claude Code's
own oversight docs, so a fresh session (or Alex reading CLAUDE.md) knows
what happened in the live app, not just what Claude Code itself did.

REAL TECHNICAL EVIDENCE GATHERED BEFORE PROPOSING A DESIGN (GODMODE, not
assumed):
- `/Routine`'s own Step 1 evidence pass (`.claude/skills/Routine/SKILL.md`)
  currently only names "Chronicles/system_updates" as a real evidence
  source - NOT the other real RPGACE-activity tables (journal,
  chronicles_finance, content_productions, taxonomy_proposals Alex
  approved himself). Confirmed via reading the skill file directly: the
  reverse-feed idea (A3) is genuinely not yet covered by any standing
  process, only by ad hoc querying when a session happens to think to
  check (this session's own item-7 taxonomy-backlog work being the one
  real example so far).
- A REAL BLOCKER for the Chronicles-reads-the-docs half (A2): checked
  `vercel.json`'s rewrites directly - the catch-all
  `"source": "/(.*)", "destination": "/index.html"` means a client-side
  fetch for `/CLAUDE.md`, `/interconnection_map.md`, or
  `/system_flow_map.md` today resolves to `index.html`, not the real
  file. Chronicles (client-side JS) CANNOT read these three docs' live
  content as things stand - this has to be solved first, and is a real
  fork with two real options:
  (a) sync doc content into a new Supabase table whenever Claude Code
      edits a doc (new, standing discipline - every doc-edit session
      would need to also write/update a Supabase row);
  (b) add explicit `vercel.json` rewrite exceptions for just these three
      filenames so they're served as real static files, bypassing the
      SPA catch-all (cheap, but exposes raw doc content at a public URL -
      no NEW real exposure since the repo is already public on GitHub,
      just a second access path).
  `patch_notes.html`/`manual.html`/`minotaur_map.html`/`taxonomy_map.html`
  are NOT affected - they're already real deployed pages Chronicles
  could read via a normal fetch today, no rewrite issue.

STATUS: still not built - this now has a real, concrete confirmation
question to bring back to Alex (the doc-access fork above) before any
Chronicles UI work. The reverse-feed piece (A3) is comparatively cheap -
a skill-file text edit to `/Routine`'s Step 1, no app code, no
architecture fork - and could proceed with less ceremony if Alex confirms
he wants it now.
