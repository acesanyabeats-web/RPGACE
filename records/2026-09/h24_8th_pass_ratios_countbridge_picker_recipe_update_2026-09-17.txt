H24 8TH PASS: REAL SUBSTITUTION-POTENCY RATIOS, A REAL GARLIC CLOVE/BULB
COUNT-EQUIVALENT BRIDGE, A REAL "PICK FROM STOCK" DECLARE PICKER, AND A
REAL "UPDATE RECIPE TO MATCH CURRENT STOCK" BUTTON
==========================================================================

ALEX'S VERBATIM REPORTS (in order, across this pass)
------------------------------------------------------------
"how about when all subsitutions and merge terms happen, instead of
asking for something as same term, i have an option of agreeing or
selecting from stock to determine select: [doubanjiang/white pepper
example] ... yet i have no way in system to tell you this"

"[garlic clove/garlic bulbs example, agreed as substitute] ... a bulb
consists of cloves - so make 8 cloves a bulb and vice versa. cumin
powder for some odd reason wants sub with turmeric instead of trying to
sub ground cumin, very weird"

[pasted a real, live Schedule Preview showing the exact ingredient list
this pass fixes/builds against]

"Start this session - also the recipe needs an update button, so it can
take in all colour schematics of present ingredients and subs to make
the recipe as precise as possible to current conditions"

"Tamarind concentrate is about three times stronger than standard
tamarind paste, meaning you need one-third the amount of concentrate to
match the sourness of the paste."

"i need these conversion ratios to always be looked up to make educated
subsitutions"

REAL EVIDENCE FIRST (rule 4) — 2 more duplicate-ingredient merges
------------------------------------------------------------
Direct Supabase query confirmed 2 more real duplicate-ingredient-row
splits, same class as every earlier merge this session:
- "cumin powder" (recipe-canonical) vs "ground cumin" (Alex's real 70g
  stock) — 0 real references anywhere else for "ground cumin", merged
  directly, ingredient_aliases row added for 'ground cumin'.
- Doubanjiang was re-checked live and confirmed ALREADY fixed by the
  7th pass's own merge (368g real stock now correctly under doubanjiang's
  id) — the pasted schedule screenshot showing it still blue was simply
  captured before that merge landed, not a new bug.

FIX 1 — REAL "powder" STOPWORD (5th observed _GENERIC_HEAD_STOPWORDS instance)
------------------------------------------------------------
"cumin powder" was heuristically matching "turmeric powder" purely via
the shared bare word "powder" — the same class of bug onion/sauce/milk/
paste already closed, now a 5th real observed culprit. Added to
logic._GENERIC_HEAD_STOPWORDS.

FIX 2 — REAL COUNT-EQUIVALENT BRIDGE (garlic clove <-> garlic bulb, 8:1)
------------------------------------------------------------
A real, 3rd relationship type, distinct from both an alias (same real
product, different wording) and a substitute (a different product
standing in): a bulb genuinely CONTAINS a real countable number of
cloves. Alex's own explicit correction that the existing curated
_SUBSTITUTION_GROUPS entry (garlic clove/cloves/bulb/bulbs, a flat 1:1
swap) was wrong for this pair — removed from that table, replaced with
a new, real, curated logic._COUNT_EQUIVALENTS table (perBig=8, Alex's
own stated real number, never a generic "10-20 cloves per bulb"
average) + logic._findCountEquivalent + logic._resolveCountEquivalentHave,
wired into logic._computeIngredientUsage's own `have` resolution
(additive, real direct stock + real bridged stock both genuinely count).
Bidirectional per his own explicit "and vice versa."

A REAL BUG FOUND AND FIXED DURING BUILD, NOT SHIPPED BLIND
------------------------------------------------------------
The first version gated the bridge on `_normalizeUnit(neededUnit) ===
'unit'` — but logic._normalizeUnit has always kept "clove" as its OWN
canonical bucket, deliberately separate from the generic unit/whole/
Count bucket (pre-existing, not touched this pass). A recipe asking for
"12 clove" (the exact real example this feature was built for) would
have silently never bridged at all. Found by a real headless test, not
inspection. Fixed with a new logic._COUNTABLE_UNITS set (unit/clove/
piece/slice) checked on BOTH sides of the bridge — still never weight/
volume, same honest discipline as WEIGHT_TO_G/VOLUME_TO_ML.

FIX 3 — REAL, CURATED SUBSTITUTION-POTENCY RATIOS
------------------------------------------------------------
Alex's own real culinary fact (tamarind concentrate ~3x stronger than
paste, use ~1/3 the amount) exposed a real gap: every curated
_SUBSTITUTION_GROUPS pair was treated as a flat 1:1 swap regardless of
real strength differences — a genuinely wrong quantity for at least this
one pair, silently over-dosing the dish. New logic._SUBSTITUTION_RATIOS
(rule 7 — seeded ONLY with this one real, checkable fact; every other
curated pair stays unrated rather than inventing a precise ratio nobody
has confirmed) + logic._findSubstitutionRatio (directional, real honest
reverse-ratio math, never a guess). Wired into logic._findSubstitute's
curated return (ratio/ratioNote resolved once, rule 8) and through
logic._computeIngredientUsage's own row (subRatio/subRatioNote) into
ui._renderSubstitutableStatusRow's real confirm button — now shows a
real adjusted quantity ("~0.33 tbsp tamarind concentrate instead") plus
the real reason, both when confirming and after confirming. The real
DB write (_confirmSubstitution) still saves the plain substitute name —
only the display text carries the adjusted quantity.

FIX 4 — REAL "PICK FROM STOCK" DECLARE PICKER
------------------------------------------------------------
The existing single-guess "🔗 Same item as X?" button (H24 7th pass)
only ever offers the ONE candidate the system found — Alex's own real
doubanjiang/white-pepper example showed this guess can be wrong with no
way to correct it. A new, real, independent "🔍 Pick from stock instead"
toggle on every orange/blue row with real stock available — reveals a
real text input + datalist of every genuinely distinct in-stock name
(logic._computeIngredientUsage's own real stockNames, already built the
prior session), a real 2-click arm/confirm "Declare same item" button
that validates the typed name is a REAL stock name before ever calling
the merge (fails loud on an unlisted name, no write). Same underlying
logic._declareSameItem merge as the single-guess button — a different
way to SUPPLY the alt name, never a 2nd merge mechanism (rule 8).

FEATURE — REAL "🔄 UPDATE RECIPE TO MATCH CURRENT STOCK" BUTTON
------------------------------------------------------------
Alex's own real ask: "the recipe needs an update button, so it can take
in all colour schematics of present ingredients and subs to make the
recipe as precise as possible to current conditions." Reuses the exact
same critique -> RECIPE_UPDATE_JSON -> code-computed diff -> Accept/Deny
pipeline the pre-existing "Update Method for this" banner already
proves (rule 8, never a 2nd update mechanism). Gated on a real saved
recipe id (existingRecipeId) since it needs a real recipe_ingredients
set to classify against live stock. On click: calls
logic._computeIngredientUsage for THIS recipe's own real id, filters to
ONLY real, already-CONFIRMED orange substitutions (subConfirmed===true)
— a still-unconfirmed orange suggestion is just the system's own guess,
never something Alex has actually committed to (rule 7 — never present
a guess to Oracle as a real decision) — builds a real prompt line per
confirmed row (with the real ratio-adjusted quantity + note when one
exists), and sends it via oracleControl._sendFromOverlay (the one real
path that prepends live recipe context). Zero confirmed substitutions ->
an honest info toast, zero Oracle call.

VERIFICATION
------------------------------------------------------------
30 new real headless-Chromium assertions (the powder-stopword fix incl.
a real identity-word regression guard; the count-equivalent bridge both
directions plus a real full _computeIngredientUsage integration proving
a real 12-clove need against zero direct stock resolves green via 5.5
real bulbs; the substitution-ratio table both directions and its "no
real ratio -> null, never guessed" honesty; the real confirm button's
adjusted-quantity display, with and without a ratio; the real stock
picker's toggle/datalist/2-click-arm-confirm flow including a real
fail-loud reject on an unlisted name and a real successful merge against
a DIFFERENT name than the system's own guess; the real "Update recipe"
button rendered via the ACTUAL _showRecipeCard function — not a
re-transcribed copy of its logic — confirming it only reaches Oracle for
confirmed substitutions, with the real adjusted prompt text, gated
correctly on a real saved id, and correctly silent on zero confirmed
rows). Plus a full re-run of every pre-existing suite (H18 through H24h,
261 prior assertions) — one real, deliberately superseded assertion
found and corrected (test_h24_ingredient_matching.js's own garlic-clove/
garlic-bulb curated-match check, now downgraded to the honest
'heuristic' tier since the curated 1:1 entry was correctly removed this
pass in favor of the real count bridge — not a regression, a documented,
reasoned correction, same discipline every earlier stale-assertion fix
this session already used). 291 total assertions, zero failures.

node --check clean, re-minified, cache-bust bumped 20260917k -> 20260917l.
==========================================================================
