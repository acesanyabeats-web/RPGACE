TAXONOMY CURRICULUM RESTRUCTURE + JARGON ENCYCLOPEDIA MERGE — spec backlog
Sep 25 2026. Real /interrogation, verbatim record per rule 5.

=====================================================================
1. ALEX'S OWN VERBATIM REQUEST (first message, unedited)
=====================================================================
"i want it so that the 21 phylum get broken down into the next biggest
separable chunks of groups, like a curriculum for a subject, with each
group breaking down until on jargon word is left with an insight tied
to it, the collection of insights tied to the jargon is the smallest
group of insights and a penultimate step of the phylum path for the
database. did you understand what i meant?"

Followed, same session, by 3 real additions (verbatim, in order):

"jargon encylopedia should come into play here too as the holder of
jargon end point and where it belongs down phylum path"

"a lot of these will have topics like by genre or time period, so
having these would be planned out"

"i did also want to see if galaxy map development concepts can be
used into taxonomy development with jargon encyclopedia merge"

=====================================================================
2. REAL /interrogation — 3 QUESTIONS ASKED, ALEX'S ACTUAL ANSWERS
=====================================================================
Q1. Should each jargon leaf's insights become real, separately-stored
    rows, or stay as the existing single deep_content blob?
    ANSWER: Separate rows.

Q2. Should the curriculum breakdown be designed top-down for all 21
    phyla up front, or should this just be a rule change shaping the
    existing bottom-up/organic placement engine?
    ANSWER: Top-down, all 21 phyla.

Q3. The tree is hard-capped at depth 6 today. Should that flex past 6
    where genuinely needed, or should curriculum design stay within
    the existing 6-rank limit?
    ANSWER: Flex past 6 where genuinely needed.

=====================================================================
3. REAL EVIDENCE GATHERED THIS SESSION (grepped, not assumed)
=====================================================================
- taxonomy_tree: depth 0-6 (Phylum/Order/Class/Family/Genus/Species/
  Variant), enforced by phylumPath.logic.sanitizePlacement's
  `maxNew = 6 - attachDepth` hard cap. RANK_NAMES only names 7 ranks;
  taxonomyTree.rankNameForDepth already falls back gracefully to
  'Rank ' + depth beyond that — the display side is not a blocker for
  flexing the cap.
- decidePlacementScored (phylumPath.logic): the one real placement
  engine (unified July 19), reacts to ONE insight at a time — attach
  point + new steps + explainers + justification + confidence. It is
  NOT currently shaped to design a whole subtree/curriculum in one
  call; a top-down design pass needs a different prompt shape (see
  Phase 4 below), not a reuse of this function as-is.
- _insertNewSteps (phylumPath.logic, the one write choke-point):
  already supports an optional sourceMeta {source, title,
  bibliographyId} that creates ONE new `encyclopedia` row per insight,
  linked via encyclopedia.taxonomy_node_id back-reference to the leaf.
  REAL GAP FOUND: the dedup-extend branch (an insight landing on an
  ALREADY-EXISTING leaf, newSteps.length===0) does NOT pass sourceMeta
  and does NOT create an encyclopedia row — it only regenerates
  deep_content via _generateInsightContent. This means today, a
  second/third insight landing on the same jargon leaf does not
  accumulate as a separate real row; it just overwrites deep_content.
  This is the concrete mechanical blocker for "insights tied to the
  jargon are a real collection."
- jargon_encyclopedia: a real, existing, read-only Postgres VIEW
  (security_invoker) over taxonomy_tree's leaf rows, built July 22
  specifically because "Jargon Encyclopedia" was blocked on nothing
  but a read-only view existing at all — zero new writes at the time.
  Exposes name/path/explainer/phylum_number only, via
  jargonEncyclopedia.ui._openGlossary (a searchable popup, injected
  into Phylum Path's existing page shell). It does NOT currently
  expose a leaf's linked encyclopedia rows — only the single
  `explainer` string set at insert time. This is real, direct
  confirmation that Alex's "jargon encyclopedia... holder of jargon
  end point" maps onto something that already exists and half-does
  the job, not something new.
- taxonomy_links (fusion links, July 16): cross-branch, symmetric,
  real evidence-bearing connections between any two nodes anywhere in
  the tree (any rank, any phylum), each with a one-sentence
  link_insight explaining HOW they connect, staged pending/confirmed
  through the same review queue as taxonomy_proposals. Structurally
  identical in shape to what Galaxy Map's P1 (shipped this session,
  Sep 25) just built for cross-reference edges — real, evidence-
  bearing, click-to-inspect connections with a stored explanation.
  Currently rendered as a plain list (_renderFusionLinks), not as an
  evidence-inspector element the way P1's edges now are.
- Galaxy Map concepts confirmed as real, transferable patterns (not
  vague inspiration — specific, named mechanisms):
    (a) P1's edge-as-gateway pattern (_curved_edge's evidence_text/
        evidence_source/zoom_href + the shared _gmInitEdgeInspector()
        floating panel) — directly reusable CONCEPT for fusion links.
    (b) verify_edge_evidence_coverage.py — a cheap, regex/query-only,
        zero-AI-cost coverage checker reporting real percentages, not
        claims. Directly reusable CONCEPT for a taxonomy coverage
        checker (leaves with 0/1/2+ linked insights, depth
        distribution, which branches are still coarse).
    (c) R22 ("bubble systems always follow and showcase what's on
        table") and the reverse-link pattern (pure rendering over
        already-known data, never a second detector, rule 8) —
        applicable to a future "Jargon Web" bubble view.
  Confirmed NOT transferable: the Galaxy Map's static-HTML generation
  pipeline itself (R5 idempotency, verify_anchors.py) is built for a
  regenerated, read-only artifact from code — taxonomy is live
  Supabase data edited through the running app, a different medium.
  Only the UI/consistency-checking PATTERNS transfer, not the
  generator architecture.

=====================================================================
4. THE REAL, CONFIRMED SHAPE (synthesis, not yet built)
=====================================================================
- A jargon leaf = a taxonomy_tree row, node_type:'leaf'. Stays the
  PENULTIMATE step, exactly as Alex said.
- Its insight collection = real `encyclopedia` rows linked via
  taxonomy_node_id — the TRUE final/bottom layer. Already real,
  already writable; the dedup-extend gap (Section 3) is what's
  currently stopping it from actually accumulating as a collection.
- jargon_encyclopedia (the view) becomes the real, authoritative
  "holder of jargon end point and where it belongs down phylum path"
  Alex asked for — extended to surface each term's real linked insight
  rows, not just one explainer line.
- The breakdown ABOVE the leaf (Order->Class->Family->Genus->Species->
  jargon) gets deliberately, top-down curriculum-designed, phylum by
  phylum — reusing genre/time-period (and other real, foreseeable
  axes) as actual grouping criteria where a phylum's subject calls for
  them, not invented arbitrarily.
- Depth flexes past 6 wherever a phylum genuinely needs more splits to
  reach one irreducible jargon term.
- Fusion links get a real P1-style evidence-inspector treatment;
  eventually a "Jargon Web" bubble view (lower priority, bigger lift,
  only worth it once real curriculum data + real fusion links exist
  in volume across phyla).

=====================================================================
5. PHASED PLAN (proposed — see chat message for the version presented
   to Alex; this file is the durable record, not a duplicate)
=====================================================================
Phase 1 — mechanics (bounded, single session, Tier 2 funnel):
  depth-cap flex; fix the dedup-extend sourceMeta gap so every insight
  landing on a jargon leaf (new or existing) creates a real linked
  encyclopedia row; extend jargon_encyclopedia's glossary UI to show
  the real insight collection per term instead of one explainer line.

Phase 2 — fusion-link evidence inspector (bounded, UI-only, no schema
  risk): reuse P1's evidence_text/evidence_source/zoom_href +
  floating-panel pattern for taxonomy_links rendering.

Phase 3 — coverage checker (bounded, cheap, zero AI cost): a real
  script reporting per-phylum leaf count, insight-row coverage, depth
  distribution, which branches still look coarse — a real evidence
  baseline BEFORE curriculum design starts, so later progress is
  gradeable against real numbers, not a claim.

Phase 4 — pilot the top-down curriculum-design methodology on ONE
  currently-flat phylum (not 12, already done) — validate the "split
  until one jargon term is left" rule, genre/era-axis usage, depth-
  flex, and real encyclopedia-row wiring on one real subject before
  scaling. Real Oracle-generation cost — proportionate to rule 11.

Phase 5 — scale to the remaining ~19-20 phyla as a tracked, real
  multi-session /CEO plan (matches CLAUDE.md's own existing stated
  philosophy: "real, tracked multi-session work... not crammed into
  one pass at the cost of quality").

Phase 6 (optional, later, bigger lift, not scoped in detail yet) — the
  "Jargon Web" bubble/graph view, once real curriculum data + fusion
  links exist in enough volume to make it worth building.

Nothing in this file has been built yet. Written per rule 5 before any
code changes, per Alex's own "write it and come back with the phased
plan" instruction.

=====================================================================
6. REAL SCRUTINY PASS (Alex's own ask: "use /paranoia /CEO
   /interrogation /drift /commit-archaeologist to pack it out"),
   Sep 25 2026, same session
=====================================================================
6a. /commit-archaeologist findings, real git history, not inferred:
  - The depth-6 cap is NOT arbitrary. Commit 42ca91e ("Tree audit:
    unify all 3 placement pipelines into one scored engine + guards")
    fixed a REAL depth-14 corruption chain (a branch re-nested inside
    itself, entered via an unsanitized manual Edit box that bypassed
    every model-side prompt rule). taxonomy_placement_rules.txt R2
    ("DEPTH NEVER EXCEEDS 6") names this explicitly, cites the same
    incident as evidence, and the cleanup pass verified "0 rows above
    depth 6" as a hard invariant afterward. Depth-flex is real risk,
    not a free change — it needs a deliberate NEW ceiling, still
    mechanically enforced, not the guard's removal.
  - RANK_NAMES (Phylum/Order/Class/Family/Genus/Species/Variant) is a
    COSMETIC display convention, confirmed via commit d992888 (the
    original Phylum Path ship, July 14) — borrowed from
    taxonomy_map.html's pre-existing display-only array, never chosen
    for a structural/content-depth reason. rankNameForDepth already
    falls back to 'Rank ' + depth past index 6 — naming was never the
    real constraint, the numeric cap always was.

6b. /drift finding: this whole plan has BASIS=none — it is a brand
  new idea from this session, not yet ratified as a real /CEO plan.
  VERDICT is therefore "not yet startable," which is exactly what CEO
  Loop 1's own sequence expects at this stage (compile -> interrogate
  -> paranoia-advised draft -> drift-check -> report -> Alex approves).
  Nothing here should be treated as authorized until Alex ratifies it.

6c. Real adversarial case (paranoia's "why NOT" requirement), weighed
  honestly against Alex's own confirmed answers, not overridden by
  them:
  - Real cost risk (rule 11): a genuine top-down curriculum design for
    21 phyla, SCAFFOLDED AHEAD OF CONTENT (Alex's own confirmed
    choice, Section 6d below), is real, substantial Oracle-generation
    spend — likely the largest single content-generation cost this
    project has taken on. This needs an explicit go/no-go checkpoint
    before Phase 5 actually runs, not a standing blank-cheque
    authorization from this spec alone.
  - Real philosophical tension: Phylum Path's founding scope was
    explicitly "new insights only, no retroactive redesign," bottom-up
    by design. Scaffolding-first is a genuine, deliberate reversal of
    that for the STRUCTURE layer (insights still arrive bottom-up
    under it) — named here so it's a conscious choice, not a silent
    drift away from a standing principle.
  - Real minor risk: extending jargon_encyclopedia's glossary to show
    each term's linked encyclopedia rows needs care against an N+1
    query pattern once real insight volume grows — batch it, don't
    fire one query per term.
  - decidePlacementScored is NOT currently shaped to design a whole
    subtree — it reacts to one insight at a time. A real top-down
    curriculum-design prompt is genuinely NEW build scope (Phase 4),
    not a reuse of the existing engine as first implied.

6d. Real /interrogation round 2 — 2 more questions this scrutiny
  surfaced, both answered:
  Q4. Should depth-flex mean a new FIXED, still-enforced ceiling, or
      removing the numeric cap entirely?
      ANSWER: Raise to a new fixed cap. (Proposed concrete value: 10 —
      real reasoning, not arbitrary: doubles the old R2 ceiling,
      giving real curriculum-depth headroom while keeping a hard
      mechanical backstop against the exact corruption-chain failure
      mode R2 was built to catch. Open to Alex adjusting the number;
      the PRINCIPLE — a real fixed ceiling, not an open cap — is what's
      confirmed, not this specific number yet.)
  Q5. Should curriculum design scaffold a phylum's full empty branch
      structure ahead of real content, or should structure only follow
      content as it arrives (cheaper, closer to the existing bottom-up
      philosophy)?
      ANSWER: Scaffold ahead of content, matching Phylum 12's own
      precedent. Real, explicit cost consequence: this is the more
      expensive path (Section 6c above) — Phase 5's real go/no-go
      checkpoint is not optional, it directly follows from this
      answer.

=====================================================================
7. REVISED PHASED PLAN (supersedes Section 5's numbers where they
   differ; Section 5 kept for the real historical record, not deleted)
=====================================================================
Phase 1 — mechanics: raise the depth cap from 6 to a new fixed ceiling
  (proposed 10, Alex to confirm/adjust) rather than removing it; fix
  the _insertNewSteps dedup-extend sourceMeta gap so every insight on
  a jargon leaf creates a real linked encyclopedia row; extend
  jargon_encyclopedia's glossary UI to show each term's real linked
  insight collection (batched query, not N+1).

Phase 2 — fusion-link evidence inspector (P1-pattern reuse, UI-only,
  unchanged from Section 5).

Phase 3 — coverage checker (unchanged from Section 5) — now also a
  real, honest pre-Phase-5 evidence baseline given the scaffold-first
  cost decision in 6d.

Phase 4 — pilot the top-down, scaffold-ahead-of-content curriculum
  methodology on ONE currently-flat phylum. Real deliverable: does the
  "split until one jargon term" rule actually produce sensible
  breadth/depth within the new cap, using genre/era axes where real,
  with every resulting jargon leaf wired to a real encyclopedia
  insight row. Alex reviews the real pilot output before Phase 5.

Phase 5 — scale to the remaining ~19-20 phyla. REAL, EXPLICIT
  CHECKPOINT: given scaffold-ahead-of-content is the confirmed choice
  (a genuine, substantial spend across 21 phyla, rule 11/Tier-3
  spirit), this phase does not start on Phase 4's approval alone — it
  needs its own explicit go-ahead from Alex once the real pilot cost
  is known, tracked as a real, ratified /CEO plan (ceo_plans/
  ceo_plan_items), not assumed pre-authorized by this spec.

Phase 6 — unchanged from Section 5 (Jargon Web bubble view, later,
  optional).

Nothing beyond this spec file has been built or ratified. This section
is the real CEO Loop 1 "report" — ready for Alex's ratification, not
yet an approved plan.

=====================================================================
8. PHASE 1-3 EXECUTED (Alex's own ask: "Start Phase 1 to 3"),
   Sep 25 2026, same session
=====================================================================
8a. Phase 1a — DONE. phylumPath.MAX_TREE_DEPTH: 10 (new module
  constant, replacing the bare "6" in sanitizePlacement's maxNew calc).
  A deliberate new fixed ceiling, not a removed guard — matches the
  confirmed Section 6d answer exactly.

8b. Phase 1b — DONE. _insertNewSteps' dedup-extend branch (an insight
  landing on an already-existing leaf) now passes sourceMeta through
  to create a real linked encyclopedia row, same shape as the existing
  new-leaf path. This is the real mechanical fix that makes "insights
  as a collection tied to a jargon leaf" actually accumulate over
  repeat insights, not just the first one.

8c. Phase 1c — DONE. jargonEncyclopedia's glossary now fetches each
  visible term's real linked encyclopedia rows in ONE batched query
  (taxonomy_node_id=in.(...)), not N+1, and _renderTerms shows the
  real insight collection under each term (with an honest "No linked
  insights yet" state) instead of only the one explainer line. Real
  live view definition confirmed via direct Supabase query first
  (jargon_encyclopedia already selects `id AS node_id` — nothing
  needed changing on the Postgres side).

8d. Phase 2 — REAL FINDING: NOT NEEDED, nothing built. Direct read of
  phylumPath.ui._renderFusionLinks/_showLinkArticle found fusion links
  ALREADY have a full evidence-inspector mechanism — inline evidence
  text on every row, a real click-through popup with exit buttons to
  either connected node, and an Oracle-generated "Interlink Article"
  on demand. This already exceeds P1's own Galaxy Map pattern (which
  has no generated-article step). Building a second mechanism here
  would have been a real rule-8 duplication — caught before writing
  any code, not after.

8e. Phase 3 — DONE. scripts/taxonomy_coverage_check.py built (same
  family as verify_edge_evidence_coverage.py/supabase_dedup_scan.py —
  cheap, zero-AI-cost, read-only, reports real numbers, never writes).
  Run for real via mcp__Supabase__execute_sql (this session's own
  sandboxed network blocks raw supabase.co access, same known
  limitation supabase_dedup_scan.py already documents) — real current
  baseline, Sep 25 2026:
    - 246 real accepted leaves tree-wide; only 1 (0.4%) has any linked
      Encyclopedia insight row. This is the honest starting point
      Phase 1b's fix now builds forward from — it does not retroactively
      backfill old insights that only ever wrote to deep_content.
    - Phyla 1-10 are genuinely the deepest/most developed (max depth
      5-6, avg depth 4.5-5.15).
    - Phyla 12,13,14,15,17,18,19,20 sit at max_depth=1 — every leaf
      directly under the phylum root, genuinely flat/not yet
      curriculum-designed. Phylum 11 at max_depth=3, phylum 16 at
      max_depth=4 (avg only 1.78 — most leaves still shallow), phylum
      9 at max_depth=5 (avg 3.24).
    - Phylum 21 has zero real leaves — matches the documented
      deliberate "nothing here yet" design, not a bug.

8f. REAL, SIGNIFICANT CROSS-DOC DRIFT FOUND WHILE BUILDING 8e (rule 16,
  flagged to Alex directly, not silently corrected or ignored): CLAUDE.md's
  Taxonomy section claims Phylum 12 was rebuilt Aug 11 into "a real
  3-level hierarchy (6 Orders -> 16 Classes -> 16 leaves, 38 nodes
  total, proof-of-concept for the rest)." Direct live query
  (taxonomy_tree where phylum_number=12) found only 9 REAL rows total:
  3 branches (depths 1/2/3) and 6 leaves, ALL 6 leaves sitting at
  depth 1 (directly under the phylum root) — genuinely flat, not a
  3-level hierarchy, and nowhere near 38 nodes. Not investigated
  further this pass (real, separate scope from Phase 1-3 — whether
  this is data loss, an inflated original claim, or a later
  deliberate revert CLAUDE.md was never updated to reflect). This
  materially affects Phase 4's own "pilot on ONE currently-flat
  phylum, not 12, already done" framing — Phylum 12 may in fact be a
  live, real candidate too, not "already done." Reported to Alex in
  chat; CLAUDE.md not yet corrected (needs his steer on which of the
  3 real explanations is true before writing a correction).
