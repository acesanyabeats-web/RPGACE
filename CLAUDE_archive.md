# CLAUDE.md — Archive

**Created July 31, 2026.** CLAUDE.md's own doc-discipline rule (written July 28) said an archive file "should exist once this file's Current state section is next pruned in full." That prune happened July 31 during a full oversight-doc audit, so this file now exists.

**What this is:** the dated "Current state (July 24)" and "Prior state (July 18 / 20 / 22 / 23)" sections, moved here verbatim. Everything below was true when written and much of it has since been superseded. **Do not treat anything in this file as current** — CLAUDE.md's own Current State section, the Security note, and the Known landmines section are the live truth, and `patch_notes.html` holds the full incident narrative for any of it.

**Why keep it at all:** these sections record real reasoning, real incidents, and real decisions with their original justifications. Deleting them would lose the "why" behind rules that are still enforced. Reading them is for archaeology, not for orientation.

---

## Current state (July 24, 2026, session end) — don't re-discover this
- **API-auth fix: confirmed genuinely live after a real incident earlier the same day.** A bare "yes" was wrongly treated as confirmation the Vercel env vars were set; merged, then reverted same-session once Alex clarified they weren't. Re-applied once he set both AND gave specific confirmation, then independently verified against production (`/api/oracle` returns a real `401` unauthenticated — proof `RPGACE_API_SECRET` has a genuine server-side value). Standing lesson written into CLAUDE.md's resolved-incident note: a bare "yes" is not sufficient evidence for a Tier-3 merge with this blast radius.
- **RLS parity fix — done.** `bookworm_books`/`bookworm_chapters`/`bibliography` now have RLS enabled with the same permissive `anon_all` policy every other table already uses, closing the "not even nominally covered" gap. Confirmed via `get_advisors`.
- **Approach B phase 1 — built, deployed, and gating-verified.** A real restriction on top of the parity fix: `api/data-write.js` + `RPGACE.sb.secureWrite()` route writes through a server-side endpoint using a new `SUPABASE_SERVICE_ROLE_KEY`, bypassing RLS entirely. `node --check` clean, merged and deployed, `401` confirmed on an unauthenticated request. **✅ RESOLVED July 24, later same session — a real authenticated write succeeding end-to-end, independently confirmed, not assumed.** Alex used the new Review Queue dashboard card for real; a direct Supabase query (not a doc claim) found 8 genuine writes between 23:09-23:16 UTC — 90 minutes after the RLS flip on these exact tables — 3 `taxonomy_proposals` updates, 5 `taxonomy_links` confirms, plus one full accept chain that also INSERTed a new `taxonomy_tree` leaf and a `taxonomy_decision_log` audit row with a matching `node_id`. Since `anon` is SELECT-only on all of these post-flip and the client never authenticates as anything but `anon`, these writes could only have gone through `/api/data-write`'s service-role path. This is real proof for UPDATE **and** INSERT through the proxy, on 4 tables — DELETE through the proxy, the 6 deliberately-excluded tables, and the encyclopedia's `onConflict` upsert path remain genuinely unconfirmed, named honestly rather than folded into a blanket "it works" claim. (Real discriminator worth keeping as a technique: Postgres-side bulk writes carry 6-decimal `now()` timestamps; this app's own writes carry 3-decimal `new Date().toISOString()` timestamps — this is what separated 8 real clicks from 30 unrelated rows in the same table that shared one bulk-update microsecond, see the taxonomy backlog note below.)
- **Approach B phase 2, real gap found and fixed.** Alex asked to expand the write-proxy to the remaining ~17 tables, 3 at a time. Real evidence-gathering before each batch found the codebase actually has **three distinct raw-fetch idioms** for writing to Supabase (`RPGACE.sb.insert/update/del`, `fetch(RPGACE.sb.url(table))`, and inline `RPGACE.CONFIG.supabase.url + '/rest/v1/...'`) — phase 1's own verification only ever searched the first two, so **`taxonomy_tree`, `taxonomy_nodes`, `bookworm_books`, and `bookworm_chapters` were never actually fully migrated** despite being logged as complete. Found and fixed the same session it was discovered, via an exhaustive re-check across all three idioms for every table touched — now genuinely zero remaining direct-anon writes, confirmed by grep, not assumed. Also caught `intel_watchlist` reachable through an already-migrated wrapper (`intelDelete._sbDel`) but missing from the allowlist — a real regression that would have 403'd on the next delete, fixed before it could bite. 15 tables now covered total (7 from phase 1 + `intel_reports`/`intel_bibliography`/`intel_watchlist`/`encyclopedia`/`reference_tracks`/`conid_pot`/`content_productions`/`video_jobs` from phase 2 batches 1-2), merged and deployed. Remaining out of scope, split honestly by real evidence: `chronicles_finance` (1 real call site), `oracle_dev_suggestions` (1, via a dynamic-table-name fetch), `taxonomy_decision_log` (1, fire-and-forget audit log) are real candidates for a future batch 3. ~~`journal`, `rpgace_agendas`, `intel_jobs`, `encyclopedia_insights` have **zero real client write call sites at all**~~ — **corrected July 24 (real /Godmode audit, /Routine interconnection pass): this was false.** The exhaustive grep that produced this claim only ever searched `rpgace_core.js` — it never checked `main.js`, which makes **27 raw REST calls with the anon key**, including 8 real writes across exactly these 4 tables (`encyclopedia` POST/PATCH/DELETE, `journal` POST/DELETE, `encyclopedia_insights` POST/DELETE, `rpgace_agendas` POST, `intel_jobs` POST). Not currently a live break (RLS on these tables is still `anon_all`, unflipped), but it means Approach B's real coverage claim needs a FIFTH known write idiom added to the standing verification list: `main.js`'s own hardcoded-credential raw fetches, a separate file entirely from every idiom checked so far. `rpgace_shifts` is written by an external Python browser-use script outside the app entirely (already known); `system_updates` is written only by Claude Code sessions directly via Supabase MCP, never from client JS. Standing lesson, now sharper: "zero remaining writes" claims need to search every known write pattern **in every file**, not just the most obvious pattern in the most obvious file — a single grep pattern, or a single file's worth of grepping, is not exhaustive verification.
- **Taxonomy backlog triage — done, with a real process correction along the way.** Real Supabase evidence (not the prior audit's "107, largely garbage" framing) showed the 60 pending `taxonomy_proposals` were two genuinely different batches: 30 unscored pre-engine rows (rejected outright) and 30 real-scored July 19 rows plus 47 fusion links (genuinely good on inspection, left pending for Alex's own full review). A real process gap surfaced and closed: this decision didn't get its own dedicated verbatim backlog file the way the project's convention requires — `item7_process_debate_2026-07-24.txt` closes that, plus two new CLAUDE.md rule clarifications (rule 4 now names review-queue backlogs explicitly, rule 5 now covers backlog-clearing decisions, not just build decisions).
- **Two new skills: `/Summary` and `/Bedtime`, both Alex-named and defined.** `/Summary` (`.claude/skills/Summary/SKILL.md`) is a real evidence-checked context-recovery recap (GODMODE+scope → `/5thDimension`-style reconciliation → `/debate` on presentation → two Council of 5 passes), invokable standalone whenever Alex needs to be caught up, and scaled to the real size of what's being summarized. `/Bedtime` (`.claude/skills/Bedtime/SKILL.md`) is the session-END mirror of `/Routine` — logs everything real to all six oversight docs + Chronicles (by invoking `/Summary`), verifies before writing, states the real close-out status plainly, and hands off to `/Routine` for the next session's start. Replaces the old ad hoc "update oversight"/"end session" convention with a named, repeatable one — see the updated Oversight logging section below.
- **Claude Code fallback lane — built for real, not just diagrammed.** Following the diagram below, Alex confirmed the Tier-3 table creation and ran a real `/debate` to decide scope ("what does auto-resume actually mean") before any code was written — full record in `claude_fallback_build_plan_2026-07-24.txt`. Chose full auto-resume for Bookworm's chapter cascade plus "maturing" Content Intelligence/Taxonomy's silent proposal path the same way; real evidence showed CI/Taxonomy's silent path is a single atomic call (not a multi-step chain like Bookworm's) and already inherited credit-exhaustion detection for free via the 3 shared ground-worker functions (rule 3) — "maturing" it correctly meant real toast visibility (previously 100% silent) plus a simpler atomic resume, never copying Bookworm's heavier chain machinery onto a pipeline without Bookworm's problem. **Two real bugs found and fixed as prerequisites, not scope creep**: (1) none of the 3 shared ground-worker functions ever checked for an API error before this — `_callGroundWorkerText` used to silently return an empty string on ANY Anthropic failure; (2) Bookworm's `_placeInsightCascade`'s `tryPhylum` swallowed every error (including a real credit-exhaustion failure) into "insight doesn't fit," meaning a real outage could previously have silently corrupted a chapter with false-negative placements. Shipped: `oracle_fallback_queue` (real table + `context`/`resumed_at` columns), `bookworm_chapters.fallback_pending`, the shared detection/enqueue/sweep choke point, `bookworm._resumeFromFallback`, `taxonomyTree._resumeSilentProposeFromFallback`, and a new hourly "RPGACE Fallback Drain" Claude Code Remote Routine mirroring the Morning Brief's exact proven pattern. `node --check` clean. Minotaur Map's dashed placeholder promoted to a real (but honestly unexercised) flow inside River IX, `system_flow_map.md` §8 moved in sync. Not yet hand-tested against a genuine credit-exhaustion event — forcing one on purpose would mean actually draining Alex's real API credits.
- **Minotaur Map diagram for the Claude-fallback plan, drawn before the code, per Alex's own explicit request.** Real design question resolved first: extend River IX ("The Mirror and the Far Shore," which already owns both "water leaves RPGACE for a future Claude Code session" and a Routine-reads-a-reservoir-on-its-own-clock mechanism via the real Morning Brief Routine) rather than mint a speculative new River XI — added as a new dashed flow inside the existing "Dry Riverbeds" section instead, a deliberate, self-documented one-time exception to the map's own "no river until built" rule. `system_flow_map.md` §8's Mermaid kept in sync in the same session per the map's own drift-prevention rule. OpenMontage gets no diagram entry anywhere (its verdict was adopt-as-a-separate-tool, never RPGACE-embedded, so it has no attach point on a map of RPGACE's own internal flows). `oracle_fallback_queue` still does not exist — creating it remains a real Tier-3 migration still needing Alex's own explicit go-ahead, separate from his general "sounds good" on the idea.
- **Two new ideas logged and judged, deliberately NOT built.** Alex pasted OpenMontage (an open-source agent-operated video production system) and asked whether it could serve RPGACE's Beat Log/Visual Oracle, plus a second idea — could RPGACE fall back to a Claude Code Remote session's own separate Claude access via a Supabase queue when the app's own API credits run dry. Both judged via a real `/debate`+Council-of-5+`/5thDimension` pass, recorded verbatim in `openmontage_and_claude_fallback_spec_backlog_2026-07-24.txt`. **OpenMontage verdict**: adopt as a separate operated tool (RPGACE's own F16-F18 video pipeline is real but untested and text-only — a genuine capability gap), never as an embedded integration (toolchain mismatch, same class as Aintergration's Framer-Motion/OmniRoute precedents); beat-sync is a real future enhancement, not automatic. **Claude-fallback verdict**: adopt with modification — a real async fallback lane for background AI jobs only (reusing the existing Morning Brief Routine pattern), explicitly NOT a live-chat replacement (Claude Code Remote's `create_trigger` has an hourly-minimum constraint). Neither gets a new Minotaur River yet — that convention waits until something is actually built and used.
- **`/Routine` ran a real second round the same day**, post-`/Bedtime` — produced a fresh, tiered Top 10 (`daily_priorities_debate_2026-07-24.txt`, second file of that name pattern this date) after a genuine Team 2 counter-case won three real concessions: Approach B phase 2 (remaining ~17 tables) dropped for today as risk-stacking on a day that already had one incident; the OpenMontage/Claude-fallback logging reframed as a near-free "step 0" rather than a competing Top-10 slot; and older pending smoke tests (highlight-to-Phylum-Path button, 10-phylum browser click-through) folded into the F16-18 sweep rather than left out via recency bias.
- **Still open, stated plainly:** Oracle 504 fix and the XSS/`innerHTML` audit remain deferred (no API credits, unless confirmed otherwise). Item 6 (journal/Chronicles/Minotaur cross-feed) hasn't had its GODMODE+interrogation pass yet — real pre-existing backlog file (`journal_chronicles_oversight_crossfeed_spec_backlog.txt`) is ready to consult. Item 8 (Composio key rotation) needs Alex's action. The 77-item taxonomy backlog remainder is intentionally left for Alex's own manual review. The swipe-gesture freeze is still unsolved. Approach B's real authenticated write is still not functionally confirmed end-to-end.

### Prior state (July 23, 2026, second session) — still true unless superseded above
- **The API-auth merge-and-revert incident, corrected same session.** A "yes" to "have you set the two Vercel env vars yet?" was misread as real confirmation; the fix got merged to `main` (auto-deploys) before the vars actually existed, then Alex clarified they weren't set. Reverted immediately (`git revert -m 1`, main back to a known-working state, `node --check` clean) — the tested code itself is untouched, still held safely on `claude/rpgace-master-steps-uobp98`. See the corrected urgent flag near the top of this file for the full incident writeup and the standing lesson about what counts as real confirmation for a Tier-3 merge.
- **Bookworm's chapter-loop proof is closed out.** Real evidence: Chapter 13 ("Chordal Inversions, Octave Doubling, and Spacing") of the actual PDF-uploaded "Music Theory for Computer Musicians" book — 12 insights approved, 1 edited, via the real review-queue UI. Chapter detection and the insight-review loop have now genuinely run together on a real PDF book — open since the July 20 audit, now proven.
- **Oracle 504 streaming fix and the XSS/`innerHTML` audit deliberately deferred, named honestly.** Both need real Claude API calls to build/verify against; Alex is out of API credits until reset. Logged in `patch_notes.html`'s Still Open section, not silently dropped — pick up first next session with credits.
- **A new `/Routine` skill, named and defined by Alex.** Packages a two-team debate (GODMODE+Council-of-5 evidence pass produces Team 1's real Top 10; a genuine Team 2 counter-case argues for left-out items) through `/Debate` (`/Omnitrix` without Fable by default) and `/5thDimension` reconciliation, ending in a final Council of 5 lock-in — a standing session-opener now (`.claude/skills/Routine/SKILL.md`, referenced in the new "Session-start ritual" section below), so this exercise doesn't need to be re-typed from scratch each time. First real run produced today's reconciled Top 10, recorded verbatim in `daily_priorities_debate_2026-07-23.txt`: API-auth confirm (done), RLS policy design, Oracle 504 fix (deferred, credits), Bookworm proof (done), XSS audit (deferred, credits), journal/Chronicles/Minotaur cross-feed interrogation, taxonomy backlog fork resolution, Composio key rotation, a Lighthouse/PageSpeed audit, and a hygiene bundle.
- **Real debate discipline held**: Team 2 won a real concession (Composio key rotation + the Lighthouse audit were wrongly left off Team 1's first draft for being "cheap and old" rather than urgent) — logged as a genuine debate outcome, not agreement-then-merge.

### Prior state (July 23, 2026, first session) — still true unless superseded above
- **Real freeze root-caused (partially) + a diagnostic built for what's still open.** Alex's report of a 10-20s post-login lag traced to `careerStatCard._fetchAll()` firing in up to 4 overlapping waves (boot, 1500ms, every `page:show`, plus the new boot-loader prefetch) — fixed with real promise deduplication. Alex then reported the freeze persisted specifically on the leftNav swipe gesture (23,000ms, confirmed via a new `perfWatch` module — a passive `PerformanceObserver('longtask')` watcher that reports real main-thread blocks as a visible toast, built specifically so the NEXT repro self-reports real evidence instead of another guess). Root cause not found despite a real investigation (grepped the whole codebase for any `touchmove`/scroll listener that could make a drag expensive — none exist) — genuinely still open, not patched blind a third time.
- **Real pushState URLs, swipeable nav, and Chronicles folded into a dashboard card — then corrected mid-session.** New `pathRouter` module (zero server changes, `vercel.json`'s catch-all already handles it) and `leftNav._initSwipeGesture` shipped clean. Chronicles was NOT done right the first time: the old standalone "Recent Wins" feed kept rendering above the new card, un-styled — caught by Alex's own screenshot, not self-caught. Fixed for real: `careerStatCard._renderWins`/`_ensureWinsContainer` deleted entirely; Chronicles now exists only as a real dashDeck card.
- **Research Lab: real single-tab lazy loading restored, reversing a July 22 decision.** GODMODE evidence found ~6 sub-modules (refCorpus, beatLog, Idea Bank, Bibliography, Workshop button) fetching data regardless of active tab or even active page — a real, substantial freeze contributor. Gated behind a new `research:tab-active` hook (fires once per tab per session). A genuine dead-code bug found in the same pass: `researchTabs._inject()` (builds the in-page tab bar) was never called anywhere — the tab bar had never actually existed live. New `dashDeck._openResearch()` popup lists all 7 sub-navs as cards. "Intelligence" renamed to "File Analyzer" (display label only, confirmed with Alex).
- **`/5thDimension` — a new 6-phase meta-protocol, built and run once.** Combines GODMODE, Council of 5, Omnitrix, Aintergration, `/scope`, and `/debate` in a fixed sequence: what's actually built (real code/git/Supabase evidence) vs. what's reported as done (six oversight docs + Chronicles), reconciled via `/debate`, then a SECOND debate turning the findings into a prioritized rewiring plan vs. a genuine honest counter-case. Shipped as a skill (`.claude/skills/5thDimension/SKILL.md`) and RPGACE's 16th Prod. Oracle command. **Fable failed on usage credits dispatching Phase 1-2 — the SECOND time this exact failure has happened this project** (first was July 20) — reported honestly, Alex authorized substituting Sonnet, now a named recurring pattern worth budgeting for. The audit itself found **near-zero real doc-vs-code drift** (a genuinely positive result, not a sign of a shallow pass — it still caught 7 real smaller findings) plus a real prioritized rewiring debate, never a one-sided recommendation.
- **Alex's three real answers to the rewiring debate, all built or logged:** (1) **"do now"** — the API-auth + password security fix (below). (2) A real standing rule: "reduplication is always a priority... same processes must go through one pipeline or function if steps are identical" — promoted to CLAUDE.md rule 8. **His own wording had a genuine word mixup** (he meant "deduplication," not "reduplication" — an opposite-meaning real word) — caught and corrected the SAME session when he flagged it, verbatim quotes preserved, only the surrounding prose fixed, and the rule extended to explicitly cover taxonomy's own existing dedup precedent (`_continueAnalyzingInBackground`'s per-chapter leaf-name dedup) alongside `intelDedup`. (3) **"yes make live, automate where it improves things"** — Oracle's self-awareness partially made live (below).
- **API-auth + password moved server-side — built, tested, deliberately NOT deployed.** `api/auth.js` (server-side password check against `process.env.CORRECT_PW`, never shipped to the client) + `requireAuth()` (`api/_context.js`, called by every endpoint) + `authGate` (rpgace_core.js, one global `fetch()` wrap attaching the shared secret to all ~26 real `/api/*` call sites — one choke point, not 26 edits). `main.js`'s `checkPassword()` is a deliberate, logged FROZEN-file exception. **Applying rule 8 immediately found a real bug**: `api/composio.js` had its own divergent Composio `ACCOUNTS` map, drifted from `_context.js`'s copy since a June 28 update — `executor.js`/`orchestrate.js` had silently been using the WRONG Gmail/Instagram connected-account ids the whole time. Fixed; `_context.js` is now the single source of truth. Tested via a mock Vercel-shaped server + a real headless Playwright run through the actual login UI end-to-end. **Real, previously-uncaught finding: the OLD password is permanently baked into this public repo's git history** (confirmed via `git log -p -- main.js`) — reusing it as the new server-side value would be security theater, not a fix; a genuinely NEW password is required. Cannot deploy until Alex sets two Vercel env vars (`CORRECT_PW`, `RPGACE_API_SECRET`) himself — confirmed Claude Code has no tool access to do this directly (checked every available Vercel MCP tool). Flagged as an urgent action item at the very top of this file.
- **Oracle's self-awareness partially made live**, not wholesale-rewritten. `oracleAppGrounding`'s hand-maintained `SELF_KNOWLEDGE` digest now also gets a `_liveFactsLine()`: the real live `RPGACE.modules` count (free, always exact) and a real live pending-taxonomy-review count (Supabase, 10-min cache) appended ALONGSIDE the existing qualitative text — never replacing it, since the audit's own honest counter-case stood: a live check that silently fails and looks current is worse than an honestly-stale note. Both fail open (omitted, never guessed, if unavailable).
- **A real process failure, caught and fixed the same session, worth naming as a lesson.** Nearly this entire session's work sat unmerged on the feature branch, entangled with the still-pending Tier-3 auth fix — meaning the Minotaur Map restructure Alex explicitly asked for was genuinely invisible on the live site. Alex caught this himself ("not done properly... redo it") when the real problem was a merge/visibility gap, not a content defect (the file was correct all along). Fixed by splitting the doc-only commits from the application code and pushing them straight to `main` directly — zero code risk, since `main.js`/`rpgace_core.js`/every `api/*.js` file were untouched by that push. **Standing lesson restated**: even mid-session, holding real doc/feature work hostage to an unrelated pending Tier-3 change is itself the "fix on a feature branch is not a fix Alex can test" failure mode this file already warns about — should have been split out sooner, not just at session end.
- **Minotaur Map restructured into real chronological order + a new bird's-eye-view diagram — now live on `main`.** Ten rivers reordered into the actual step-by-step journey (Gatekeeper's Checkpoint → Great Confluence orientation → three entry rivers [Oracle/Bookworm/Content-Intel-Schedule] → Judgment Chamber → Library Current → Confluence Pool → Mirror/Far Shore → Confluence of Chronicles), every cross-river reference checked and renumbered. New bird's-eye-view section shows the same ten rivers as single blocks with zero internal detail, a genuinely new diagram (not a translation of an existing `system_flow_map.md` section, noted honestly as such).
- **A genuinely new idea raised mid-session, deliberately NOT built, recorded verbatim.** Alex raised (inside an answer to an unrelated question): journal tied to specific Oracle conversation frameworks, Chronicles digesting a readable summary from the Minotaur Map/other oversight docs, and oversight feeding back from Chronicles to keep Alex/RPGACE/Oracle/Claude Code on one shared timeline. Genuinely multi-subsystem (rule 5 territory) — recorded verbatim in `journal_chronicles_oversight_crossfeed_spec_backlog.txt` rather than guessed at inline, real open questions listed honestly, waiting for its own dedicated GODMODE/interrogation pass.
- **Still open, stated plainly:** `system_flow_map.md` §0's own module inventory is still stale (real live count 51+, table shows ~30) — flagged, not silently re-dated without doing the real re-verification work. The broader RLS finding (every "enabled" table actually runs `USING(true)`, not just the 3 fully-disabled ones) needs a real policy design pass. The swipe-gesture freeze is unsolved. Oracle's 504 streaming fix, the XSS/`innerHTML` audit, and RLS-status live-querying (needs a server endpoint) are all still not built. The journal/Chronicles/Minotaur cross-feed idea above needs its own pass.
- **All six oversight docs + Chronicles kept current incrementally throughout, not batched at the end** — CLAUDE.md, `patch_notes.html` (9 dated rounds), `interconnection_map.md`, `system_flow_map.md`, `manual.html`, `minotaur_map.html`, plus new verbatim spec-backlog files (`chronicles_spec_backlog.txt` rounds 2-6, `research_lab_lazyload_spec_backlog.txt`, `journal_chronicles_oversight_crossfeed_spec_backlog.txt`) and the two `/5thDimension` evidence/debate reports.

### Prior state (July 22, 2026, session end) — still true unless superseded above
- **Council of 5 shipped as a real Oracle command (15th Prod. Oracle command)**, directly answering whether RPGACE's runtime API could "get" the Claude Code Skills built July 20 — it can't (two genuinely separate mechanisms, checked against the claude-api skill's real docs, not assumed) — so the real answer was porting the protocol into the app's own existing command pattern instead. Reuses 100% existing infrastructure (`prodOraclePanel.CMDS` + `fillGaps`/`sendToOracle`), no main.js edit.
- **Aintergration applied to a ~150-chapter system-design book, all 3 batches processed.** Real, load-bearing scope discipline established and held across all three passes: most chapters are generic CS fundamentals with no adoption decision attached, and forcing a verdict on each would be empty ceremony — triaged for real signal instead of grinding through every chapter uniformly. Full findings logged as Aintergration Worked Precedents 4-6 in this file's Invokable Frameworks section. Two real, previously-undiscovered security findings surfaced this way: **every `/api/*.js` endpoint has zero server-side auth** (bigger exposure than the known RLS gap — billing/account-action risk, not just data risk), and **`CORRECT_PW` is readable via view-source regardless of where it's stored**, since the check runs entirely in client JS — both are one underlying architecture decision, not two separate quick fixes, deliberately held back for a dedicated sequenced pass rather than rushed. Also confirmed directly by Alex: **no Supabase backup/PITR exists at all** (was an open question, now settled fact) — Tier 3, his call on when/whether to fix.
- **Oracle gained real self-awareness + a two-way Claude Code bridge, 6 pieces, all shipped, none hand-tested yet:** `oracleAppGrounding` (reads `dashDeck`'s live cards + an honest status digest, keyword-gated, costs nothing when it doesn't trigger), `oracleFetchGuard` (hardens Oracle against prompt injection from fetched web/YouTube/Instagram content — a direct answer to a risk this session itself had flagged earlier), `oracleDevBridge` (a "🧪 Flag for Claude Code" button on any substantial Oracle reply, writing to a new `oracle_dev_suggestions` table), `taxonomy_decision_log` (an append-only audit trail written at `_insertNewSteps`'s single real choke point — the first event-sourcing-style pattern in the app, turns "which code path produced this" from a forensic Supabase query into a plain read), Council of 5's own conversation-capture button (`fillGaps`'s new opt-in `opts.allowConversationCapture` — captures the real chat instead of Alex re-typing a summary, backward compatible with the other 40+ command entries that already use `fillGaps`), and a real Claude Code Remote Routine (`trig_015uRjGVrdmiwc5zxq47mMA6`, daily 6am UTC) delivering an actually-pushed Morning Brief instead of pull-based — caught and fixed a real problem before it could fail silently (the trigger has no MCP connector access, so it was rewritten to curl Supabase's REST API directly, exactly like `main.js`'s own client code already does).
- **Session-start check added**: any session working on RPGACE now checks `oracle_dev_suggestions` for `status='new'` rows before other work — still pull, not push (a true push needs a Supabase webhook wired to a Claude Code Remote trigger, flagged as a future idea only).
- **All six oversight docs updated in the same session** — CLAUDE.md, patch_notes.html, manual.html, system_flow_map.md, interconnection_map.md, and minotaur_map.html (a genuinely new "River VIII," since this is the first wing in the whole map where water leaves RPGACE entirely and reaches a different world — a future Claude Code session — rather than ending at Alex's own screen).
- **Nothing from today is hand-tested.** All of it is `node --check`-clean / div-balance-clean / independently re-verified against real source before being logged, live on `main`, awaiting Alex's click-through.
- **QoL cleanup (2 passes) + a real nav-lag incident, root-caused twice.** Pass 1: sidebar load, schedule XP-per-view bug, Research Lab tab-wrapper removal, tighter list collapsing. Pass 2 (real answers, not guessed defaults): shift-sync cache fix (an external Python browser-use script writes `rpgace_shifts` outside the app — added to the no-cache table list), Agents folded into Oracle AI (moved live DOM, not rebuilt), Encyclopedia/Journal get real search + the new audit-trail tables surfaced as read-only entries. **Incident, same day:** the sidebar fix briefly made `leftNav._buildDrawer()` run outside the `rpgace:ready` gate, aborting ~25 of ~40 modules' registration mid-parse — caught live from a pasted console log, reverted immediately. The underlying bug re-surfaced contained (not catastrophic) and was root-caused for real: `leftNav` registers before `config` in file order, so `leftNav.init()` always ran before `RPGACE.CONFIG` existed — fixed with a defensive guard + retry, not just the hotfix revert. **A second, deeper, systemic root cause found afterward**, from the separate complaint "nav doesn't respond for ~10s after login": `onReady()` gated the entire module-registration cascade behind the `window.load` event (waiting on every image/font), when the real dependency — login — is completely decoupled from page-load timing. Switched to `DOMContentLoaded`-equivalent + a 50ms delay, a systemic fix improving every module's startup, not just leftNav's. Cache-busting version string bumped multiple times same day after finding it hadn't moved in days despite dozens of edits — standing lesson: bump it every time `rpgace_core.js` changes.
- **RPGACE is now a real installable PWA** (Android/desktop "Add to Home Screen"/"Install app") — `manifest.json` + `sw.js` (deliberately network-first, not cache-first, to avoid making the stale-cache bug class above permanent) + a generated gold-sword icon set in `icons/`, registered via a new `pwaInstall` module (no new `<script>` tag). `vercel.json`'s existing SPA catch-all rewrite would have silently served `index.html` for the manifest/SW/icon requests — fixed with explicit passthrough rewrites before shipping, not after. Also added: a full-screen boot-loader overlay, visible from first paint, hidden only once `rpgace:ready` confirms every module has registered — the login gate itself never appears mid-race now. Pure-CSS 6-second fallback, zero JS dependency, so it can never trap a user if script execution stalls.
- **The profile stat card rebuilt onto real, persisted data (GODMODE/Council of 5/Aintergration, Alex's own request).** Real finding: HP/MP bars and "Streak" were 100% cosmetic — confirmed via grep, zero code anywhere ever touched them — and XP/Level lived only in in-memory `STATE`, resetting to zero every page load. Real signal already existed unused in Supabase (journal, encyclopedia_insights, taxonomy_proposals, bookworm_chapters, reference_tracks, content_productions). Rebuilt as a real weighted career score with two deliberately SEPARATE lanes — **Output** (real shipped content only) and **Growth** (learning/knowledge-tree activity) — specifically so taxonomy busywork can never visually substitute for actually shipping something (a real Goodhart's-law risk raised and addressed before building, not after). Considered and explicitly rejected: variable-ratio/randomised reward schedules (the mobile-game "loot box" pattern) — effective at compulsive engagement, wrong shape for a tool whose own governing rule is real output.
- **Recent Wins → renamed "The Chronicles" (Alex's own name), interrogated before building further.** Round 1: every feed row became a real clickable detail popup (What was done / Outcome / Where stored / Why significant), reusing `dashDeck._popup` rather than a new one-off overlay. Round 2 (after real interrogation, verbatim Q&A in `chronicles_spec_backlog.txt`, since this touches real financial record-keeping): a full dedicated `#page-chronicles` log page (search, type filters, full history) plus a new `chronicles_finance` Supabase table for real sale/expense entries — explicitly a **personal-visibility tracker, not bookkeeping-grade** (real receipts still needed at actual tax time). Phylum 17 "Negotium" (already reserved in the phyla config for conceptual business/tax knowledge, never built out) was deliberately left untouched — a transaction ledger and a conceptual knowledge phylum are different data shapes, kept separate on purpose.
- **This oversight sweep itself: a new `/scope` skill, and Chronicles now logs Claude Code's own updates.** `/scope` (`.claude/skills/scope/SKILL.md`) is a reusable evidence-gathering skill — git log + live Supabase state + doc line-counts, organized into digestible grouped categories (bugs/features/infra/security/planned-not-built) — built specifically so an "update oversight" pass doesn't miss something or drown in blow-by-blow detail; it's the GODMODE evidence pass for oversight work specifically, not a separate ceremony on top of it. A new `system_updates` table (RLS + anon_all) + a new Chronicles filter/detail-formatter means every real Claude Code change to RPGACE now shows up in Chronicles alongside Alex's own real activity — one shared timeline, not two separate records. **Standing rule going forward, same discipline as the `patch_notes.html` rule**: any session that ships a real, user-visible change to RPGACE inserts a short row into `system_updates` (title/summary/category) in the same session, so Chronicles stays a live, honest record — not a retrofit project.
- **Real inconsistency caught and fixed mid-build, worth naming as a lesson**: the finance detail popup's "Why significant" text claimed a sale "feeds the Output-lane picture" — the code explicitly does NOT fold finance into that score (confirmed via interrogation, a deliberate separate-lane decision). Caught by re-reading the code against its own stated design before this sweep, not by Alex reporting it — the exact kind of doc-vs-code drift GODMODE evidence-gathering exists to catch, including inside a single session's own just-written work, not only across older docs.
- **The Taxonomy Sorting Agent and the Jargon Encyclopedia/Book Knowledge items it was blocking — real doc-drift found and fixed.** Before building the two "dry riverbed" items Alex asked for, traced the actual call chain (not the docs): `bookworm._decidePlacementScored` is a one-line pass-through to `phylumPath.decidePlacementScored` — the exact same function `encSync`/`ciAutoPropose` already call for non-book inputs. Book and non-book insights have shared ONE placement engine since the July 19 unification; the "Taxonomy Sorting Agent" as this file previously described it (see the struck-through line above) was already effectively built and the docs simply never caught up. What it was actually described as blocking — a queryable book-knowledge source and a read-only Jargon Encyclopedia — genuinely didn't exist. Both shipped as pure Postgres views over data already being written (`book_knowledge` unnests `bookworm_chapters.insights`, `jargon_encyclopedia` selects `taxonomy_tree WHERE node_type='leaf'`) — zero new tables, zero new writes, per Alex's explicit "build in to the existing rather than build on top." A real `security_definer_view` lint was found via `get_advisors` and fixed (`security_invoker=true`) before shipping. Jargon Encyclopedia surfaced as a button inside Phylum Path's existing page, not a new nav destination.
- **General-knowledge audit redesigned around a new `/debate` skill, not built as originally scoped.** Alex confirmed general/generic Claude knowledge should never be injected directly into the tree. New `/debate` skill (`.claude/skills/debate/SKILL.md`, alongside `omnitrix` and `scope`): structured adversarial rounds (both sides attack, then both defend, then Council of 5 cleans up both cases) specifically so comparing Claude's general training against RPGACE's own gathered content never has the side effect of writing to the tree — a debate "won" still requires an explicit separate build decision afterward, never automatic. **Not yet run** — Alex asked to hold off rather than pick a topic in this pass.

### Prior state (July 20, 2026, session end) — still true unless superseded above
- **Dashboard rebuilt (July 20, 5 commits):** `dashDeck` module — 11 command cards in three thematic rows + "Needs you now" panel. Big widgets (Bookworm, Knowledge Gaps, Content Pipeline) live inside card popups as MOVED live DOM nodes (pattern: `_ensureStash`/`_stashWidget(id,force)`); quest board relocated to the Agenda page. Landmine: a stash-guard cannot distinguish "popup open" from "popup closing" — every onClose must pass `force=true` (bug hit twice, July 20). Popup buttons that open lower-z-index surfaces must `closeWidgetPopup(id)` first (popups are 99999; drawers/panels are 9998-10110).
- **Research Lab fixed (July 20):** Idea Bank/Corpus/Beat Log were nested inside `#video-workshop-panel` via interior-anchored insertBefore; Bibliography rendered at `page.firstChild`. All Research-page injectors now append as direct children of `#page-learning` and fire `research:panel-injected`. Four of the ~25 dead `rpgace:ready` listeners fixed (~21 remain unaudited).
- **Navigation replaced (July 20):** top `.nav-tabs` bar removed from index.html; `leftNav` module — hamburger + left slide-out drawer (backdrop 10100/panel 10110), 9 pages, Research (7 tabs via new public `researchTabs.show(key)`) and Schedule sub-navs. main.js untouched, byte-identical.
- **NOTHING from July 20 is hand-tested.** All three items above are code-reviewed/reasoning-verified only (independent line-by-line re-checks against real diffs, not just agent self-reports), live on rpgace.vercel.app, awaiting Alex's click-through (checklists in patch_notes.html).
- **AI Advisor page restructured (July 20, 2nd session):** hardcoded Prod/Insta-Oracle panel colors → real `--blue`/`--purple` tokens, 8-button stack → 2-column `.prod-cmd-grid`. Done SOLO (Fable ran out of usage credits mid-audit-dispatch, so Alex said skip Omnitrix — no second reviewer this pass, hence smaller scope). Full page-by-page Council-of-5 verdict table (what shipped, what's deferred and why, what's flagged don't-build) lives in `DESIGN.md` §10 — Research Lab/Agenda/Schedule/Agents/Encyclopedia/Journal deliberately NOT touched this pass (real reasons, not a punt), Phylum Path explicitly flagged don't-build (too load-bearing, no clear case). One pre-existing (not this session's) unclosed `<div>` found in index.html, confirmed harmless and NOT introduced by this edit, flagged for a future dedicated pass.
- **Real limitation confirmed, worth remembering:** Fable 5 can run out of usage credits mid-task — a background dispatch will fail with a plain task-notification, not silently hang. When it does, do not substitute a different model into the Fable role without Alex's explicit say-so; report the failure and ask. **Confirmed a second time, July 23** (the `/5thDimension` Phase 1+2 evidence-audit dispatch) — this is now a real, recurring pattern on Fable specifically, not a one-off. Both times Alex's answer was to substitute Sonnet rather than wait/run `/usage-credits`. Standing lesson: Fable's fresh-context edge is real for genuinely large audits, but weigh its real failure-rate against just running the task Sonnet-direct from the start, per Omnitrix's own "most tasks don't need a dedicated fresh-context agent" reasoning.
- **Token economics (July 19):** ~8-10x per-chapter cost cut; see building-guide rules 10-11 and `token_cost_audit_2026-07-19.txt`.
- **Omnitrix formalized (July 20):** the Fable→Opus 4.8→Sonnet 5 workflow used across all three July 19-20 builds is now a named, invokable protocol, AND the default judgment funnel — see "Invokable frameworks" below.
- **Full project audit (July 20, GODMODE, Fable 5) — 3 real doc-drift corrections, independently re-verified against live Supabase:**
  1. **The PDF-book insight loop already ran, July 19 — the "never proven together" framing was stale.** `bookworm_chapters` for the real "Music Theory for Computer Musicians" book (book_id `70fd8faa-…`) shows chapters 8, 10, 13 (1-indexed; `chapter_index` 7/9/12) `status=complete`, 39 real insights total (17+10+12) reviewed through the OLD pre-audit/pre-retune engine — these are the exact runs that produced the garbage (ch-13 fragmentation, ch-1 shoehorning) which triggered the July 19 tree audit and the token retune. Chapter 1 (`chapter_index` 0) sits `in_progress`, 6 insights, 3 approved pre-queue / 3 still pending, no active heartbeat. **The real open test is narrower than previously stated: one clean chapter through the POST-retune unified engine + July 20 UI** — resume chapter 1 or open the next unstudied one — not "first-ever combined run." Also found, not previously logged: a **duplicate `bookworm_books` row** for this same book (`87268196-…`, all chapters pending, zero real progress) sitting alongside the real one — harmless but worth knowing before picking "the" book to resume.
  2. **Fusion links: 21 confirmed / 47 pending (68 total)**, not "6 confirmed" — badly stale, likely last updated right after the first few links shipped.
  3. **107-item review-queue backlog** nobody had logged: 60 pending `taxonomy_proposals` + 47 pending fusion links, largely generated by the pre-July-19 engines the placement-rules audit itself found produced bad output. `video_jobs` = 0 rows confirmed (Beat Log has never once persisted — F16/F17/F18 have no data to test against, the feature closest to the governing rule has never actually been used).
  Full done/missing/planned/discussed breakdown and the Council-of-5-scrutinized forward plan (highest priority: hand-test July 20's 3 unverified builds, THEN one clean PDF chapter through the new engine) delivered to Alex in chat, July 20 — not duplicated into a new file (Council-of-5 pushback on Fable's own suggestion: this repo already has too many stale point-in-time snapshot docs at root — see Missing/Housekeeping — adding another would repeat the exact problem).

### Prior state (July 18, 2026) — still true unless superseded above
- **Taxonomy: 10 of 21 phyla live in Phylum Path** (`ENABLED_PHYLA: [1..10]`), all through the 7-step Phylum Development Framework (spec → keywords → tree build → data repair → enable → fusion pass → hand-test). Placement logic hand-tested across 8 of 10 (data-layer). Zero orphan nodes tree-wide. Rank chain: Phylum→Order→Class→Family→Genus→Species→Variant (depth 0-6).
- **Fusion links** (leaf-to-leaf, `taxonomy_links`, 6 confirmed live) and **Concept Fusion** (branch-level cross-phylum merges creating a NEW synthesized leaf via `taxonomy_proposals` `engine:'concept_fusion'`) both shipped and data-layer tested.
- **Bookworm — both chapter-detection paths now genuinely proven, July 18.** Manual/TOC-paste path (`_startBookFromTOC`): confirmed correct on a real 27-chapter book. URL/PDF-upload path: rebuilt from scratch after an 8-round real-evidence debugging arc (deployment-branch mismatch → a second untouched code path → exact-match anchoring failing against PDF.js's word-joining AND word-splitting corruption → ordering overshoot into cross-references → TOC-read omissions → two remaining stragglers needing a title-only fallback) — final result: 26 of 26 real chapters, correct titles, correct order, zero warnings, on a real book. Current architecture: `detectChapterListByOracle()` (model reads ONLY the table of contents, self-verifies against 5 explicit checks, recircles on any numbering gap) + `resolveChapterHeadingsMechanically()` (100% deterministic text search, character-fuzzy title matching, strict reading-order-bounded, title-only fallback). The old `searchString`/body-text-recall mechanism is removed entirely, not superseded. **Still open: the chapter-by-chapter read→insight→approve loop has been proven (on the manual book) and chapter detection has been proven (on the PDF book) — but never proven TOGETHER on the same real PDF book. That's the concrete next step.** Spec record: `bookworm_spec_backlog.txt`. Session-by-session plan: `fable_master_plan.txt`.
- **Multi-model pipeline**: `MODEL_EXTRACTOR='claude-fable-5'` outlines, `MODEL_GROUND_WORKER='claude-sonnet-4-6'` executes; extractor failure always falls back gracefully to ground-worker-alone.
- **F0–F18 all shipped** except F12 (deliberately deferred). Still never hand-tested: F16 (Beatstars listing), F17 (video pipeline stages), F18 (auto visual treatment), highlight-to-Phylum-Path button, F10's n8n run.
- **Known open bugs**: Oracle 504 on long responses (root cause: single blocking non-streaming `callClaude`; needs streaming/chunking, not another token trim — a prior streaming attempt was reverted, dead code + `restoreSendChat` neutralizer still present); F11 silent "Content Unavailable" on failed Jina fetches; `_generateNodeContent` empty-deep_content mystery (partially resolved, unverified).
- **Biggest confirmed-not-built items**: Bookworm card-list UI (ConID-card pattern), F12, phyla 11-21 framework passes, page-number-assisted PDF extraction (deliberately scoped out of the July 18 rebuild). ~~Taxonomy Sorting Agent, Claude general-knowledge audit~~ — **superseded July 22, see Current State**: the Sorting Agent was found to already be effectively built, and the general-knowledge audit was redesigned around a new `/debate` skill rather than built as originally described here.
- **Standing deployment rule, confirmed the hard way twice this session:** a fix on a feature branch is not a fix Alex can test — always merge to `main` and push before asking for a hand-test.

