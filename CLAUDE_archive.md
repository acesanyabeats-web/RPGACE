# CLAUDE.md — Archive

**Created July 31, 2026.** CLAUDE.md's own doc-discipline rule (written July 28) said an archive file "should exist once this file's Current state section is next pruned in full." That prune happened July 31 during a full oversight-doc audit, so this file now exists.

**What this is:** the dated "Current state (July 24)" and "Prior state (July 18 / 20 / 22 / 23)" sections, moved here verbatim. Everything below was true when written and much of it has since been superseded. **Do not treat anything in this file as current** — CLAUDE.md's own Current State section, the Security note, and the Known landmines section are the live truth, and `patch_notes.html` holds the full incident narrative for any of it.

**Why keep it at all:** these sections record real reasoning, real incidents, and real decisions with their original justifications. Deleting them would lose the "why" behind rules that are still enforced. Reading them is for archaeology, not for orientation.

---

## Current state (July 24, 2026, session end) — don't re-discover this
- **API-auth fix: confirmed genuinely live after a real incident earlier the same day.** A bare "yes" was wrongly treated as confirmation the Vercel env vars were set; merged, then reverted same-session once Alex clarified they weren't. Re-applied once he set both AND gave specific confirmation, then independently verified against production (`/api/oracle` returns a real `401` unauthenticated — proof `RPGACE_API_SECRET` has a genuine server-side value). Standing lesson written into CLAUDE.md's resolved-incident note: a bare "yes" is not sufficient evidence for a Tier-3 merge with this blast radius.
- **RLS parity fix — done.** `bookworm_books`/`bookworm_chapters`/`bibliography` now have RLS enabled with the same permissive `anon_all` policy every other table already uses, closing the "not even nominally covered" gap. Confirmed via `get_advisors`.
- **Approach B phase 1 — built, deployed, and gating-verified.** A real restriction on top of the parity fix: `api/data-write.js` + `RPGACE.sb.secureWrite()` route writes through a server-side endpoint using a new `SUPABASE_SERVICE_ROLE_KEY`, bypassing RLS entirely. `node --check` clean, merged and deployed, `401` confirmed on an unauthenticated request. **✅ RESOLVED July 24, later same session — a real authenticated write succeeding end-to-end, independently confirmed, not assumed.** Alex used the new Review Queue dashboard card for real; a direct Supabase query (not a doc claim) found 8 genuine writes between 23:09-23:16 UTC — 90 minutes after the RLS flip on these exact tables — 3 `taxonomy_proposals` updates, 5 `taxonomy_links` confirms, plus one full accept chain that also INSERTed a new `taxonomy_tree` leaf and a `taxonomy_decision_log` audit row with a matching `node_id`. Since `anon` is SELECT-only on all of these post-flip and the client never authenticates as anything but `anon`, these writes could only have gone through `/api/data-write`'s service-role path. This is real proof for UPDATE **and** INSERT through the proxy, on 4 tables — DELETE through the proxy, the 6 deliberately-excluded tables, and the encyclopedia's `onConflict` upsert path remain genuinely unconfirmed, named honestly rather than folded into a blanket "it works" claim. (Real discriminator worth keeping as a technique: Postgres-side bulk writes carry 6-decimal `now()` timestamps; this app's own writes carry 3-decimal `new Date().toISOString()` timestamps — this is what separated 8 real clicks from 30 unrelated rows in the same table that shared one bulk-update microsecond, see the taxonomy backlog note below.)
- **Approach B phase 2, real gap found and fixed.** Alex asked to expand the write-proxy to the remaining ~17 tables, 3 at a time. Real evidence-gathering before each batch found the codebase actually has **three distinct raw-fetch idioms** for writing to Supabase (`RPGACE.sb.insert/update/del`, `fetch(RPGACE.sb.url(table))`, and inline `RPGACE.CONFIG.supabase.url + '/rest/v1/...'`) — phase 1's own verification only ever searched the first two, so **`taxonomy_tree`, `taxonomy_nodes`, `bookworm_books`, and `bookworm_chapters` were never actually fully migrated** despite being logged as complete. Found and fixed the same session it was discovered, via an exhaustive re-check across all three idioms for every table touched — now genuinely zero remaining direct-anon writes, confirmed by grep, not assumed. Also caught `intel_watchlist` reachable through an already-migrated wrapper (`intelDelete._sbDel`) but missing from the allowlist — a real regression that would have 403'd on the next delete, fixed before it could bite. 15 tables now covered total (7 from phase 1 + `intel_reports`/`intel_bibliography`/`intel_watchlist`/`encyclopedia`/`reference_tracks`/`conid_pot`/`content_productions`/`video_jobs` from phase 2 batches 1-2), merged and deployed. Remaining out of scope, split honestly by real evidence: `chronicles_finance` (1 real call site), `oracle_dev_suggestions` (1, via a dynamic-table-name fetch), `taxonomy_decision_log` (1, fire-and-forget audit log) are real candidates for a future batch 3. ~~`journal`, `rpgace_agendas`, `intel_jobs`, `encyclopedia_insights` have **zero real client write call sites at all**~~ — **corrected July 24 (real /Godmode audit, /Routine interconnection pass): this was false.** The exhaustive grep that produced this claim only ever searched `rpgace_core.js` — it never checked `main.js`, which makes **27 raw REST calls with the anon key**, including 8 real writes across exactly these 4 tables (`encyclopedia` POST/PATCH/DELETE, `journal` POST/DELETE, `encyclopedia_insights` POST/DELETE, `rpgace_agendas` POST, `intel_jobs` POST). Not currently a live break (RLS on these tables is still `anon_all`, unflipped), but it means Approach B's real coverage claim needs a FIFTH known write idiom added to the standing verification list: `main.js`'s own hardcoded-credential raw fetches, a separate file entirely from every idiom checked so far. `rpgace_shifts` is written by an external Python browser-use script outside the app entirely (already known); `system_updates` is written only by Claude Code sessions directly via Supabase MCP, never from client JS. Standing lesson, now sharper: "zero remaining writes" claims need to search every known write pattern **in every file**, not just the most obvious pattern in the most obvious file — a single grep pattern, or a single file's worth of grepping, is not exhaustive verification.
- **Taxonomy backlog triage — done, with a real process correction along the way.** Real Supabase evidence (not the prior audit's "107, largely garbage" framing) showed the 60 pending `taxonomy_proposals` were two genuinely different batches: 30 unscored pre-engine rows (rejected outright) and 30 real-scored July 19 rows plus 47 fusion links (genuinely good on inspection, left pending for Alex's own full review). A real process gap surfaced and closed: this decision didn't get its own dedicated verbatim backlog file the way the project's convention requires — `item7_process_debate_2026-07-24.txt` closes that, plus two new CLAUDE.md rule clarifications (rule 4 now names review-queue backlogs explicitly, rule 5 now covers backlog-clearing decisions, not just build decisions).
- **Two new skills: `/Summary` and `/Bedtime`, both Alex-named and defined.** `/Summary` (`.claude/skills/Summary/SKILL.md`) is a real evidence-checked context-recovery recap (GODMODE+scope → `/5thDimension`-style reconciliation → `/debate` on presentation → two Council of 5 passes), invokable standalone whenever Alex needs to be caught up, and scaled to the real size of what's being summarized. `/Bedtime` (`.claude/skills/Bedtime/SKILL.md`) is the session-END mirror of `/Routine` — logs everything real to all six oversight docs + Chronicles (by invoking `/Summary`), verifies before writing, states the real close-out status plainly, and hands off to `/Routine` for the next session's start. Replaces the old ad hoc "update oversight"/"end session" convention with a named, repeatable one — see the updated Oversight logging section below.
- **Claude Code fallback lane — built for real, not just diagrammed.** Following the diagram below, Alex confirmed the Tier-3 table creation and ran a real `/debate` to decide scope ("what does auto-resume actually mean") before any code was written — full record in `claude_fallback_build_plan_2026-07-24.txt`. Chose full auto-resume for Bookworm's chapter cascade plus "maturing" Content Intelligence/Taxonomy's silent proposal path the same way; real evidence showed CI/Taxonomy's silent path is a single atomic call (not a multi-step chain like Bookworm's) and already inherited credit-exhaustion detection for free via the 3 shared ground-worker functions (rule 3) — "maturing" it correctly meant real toast visibility (previously 100% silent) plus a simpler atomic resume, never copying Bookworm's heavier chain machinery onto a pipeline without Bookworm's problem. **Two real bugs found and fixed as prerequisites, not scope creep**: (1) none of the 3 shared ground-worker functions ever checked for an API error before this — `_callGroundWorkerText` used to silently return an empty string on ANY Anthropic failure; (2) Bookworm's `_placeInsightCascade`'s `tryPhylum` swallowed every error (including a real credit-exhaustion failure) into "insight doesn't fit," meaning a real outage could previously have silently corrupted a chapter with false-negative placements. Shipped: `oracle_fallback_queue` (real table + `context`/`resumed_at` columns), `bookworm_chapters.fallback_pending`, the shared detection/enqueue/sweep choke point, `bookworm._resumeFromFallback`, `taxonomyTree._resumeSilentProposeFromFallback`, and a new hourly "RPGACE Fallback Drain" Claude Code Remote Routine mirroring the Morning Brief's exact proven pattern. `node --check` clean. Minotaur Map's dashed placeholder promoted to a real (but honestly unexercised) flow inside River IX, `system_flow_map.md` §8 moved in sync. Not yet hand-tested against a genuine credit-exhaustion event — forcing one on purpose would mean actually draining Alex's real API credits.
- **Minotaur Map diagram for the Claude-fallback plan, drawn before the code, per Alex's own explicit request.** Real design question resolved first: extend River IX ("The Mirror and the Far Shore," which already owns both "water leaves RPGACE for a future Claude Code session" and a Routine-reads-a-reservoir-on-its-own-clock mechanism via the real Morning Brief Routine) rather than mint a speculative new River XI — added as a new dashed flow inside the existing "Dry Riverbeds" section instead, a deliberate, self-documented one-time exception to the map's own "no river until built" rule. `system_flow_map.md` §8's Mermaid kept in sync in the same session per the map's own drift-prevention rule. OpenMontage gets no diagram entry anywhere (its verdict was adopt-as-a-separate-tool, never RPGACE-embedded, so it has no attach point on a map of RPGACE's own internal flows). `oracle_fallback_queue` still does not exist — creating it remains a real Tier-3 migration still needing Alex's own explicit go-ahead, separate from his general "sounds good" on the idea.
- **Two new ideas logged and judged, deliberately NOT built.** Alex pasted OpenMontage (an open-source agent-operated video production system) and asked whether it could serve RPGACE's Beat Log/Visual Oracle, plus a second idea — could RPGACE fall back to a Claude Code Remote session's own separate Claude access via a Supabase queue when the app's own API credits run dry. Both judged via a real `/debate`+Council-of-5+`/5thDimension` pass, recorded verbatim in `openmontage_and_claude_fallback_spec_backlog_2026-07-24.txt`. **OpenMontage verdict**: adopt as a separate operated tool (RPGACE's own F16-F18 video pipeline is real but untested and text-only — a genuine capability gap), never as an embedded integration (toolchain mismatch, same class as Aintergration's Framer-Motion/OmniRoute precedents); beat-sync is a real future enhancement, not automatic. **Claude-fallback verdict**: adopt with modification — a real async fallback lane for background AI jobs only (reusing the existing Morning Brief Routine pattern), explicitly NOT a live-chat replacement (Claude Code Remote's `create_trigger` has an hourly-minimum constraint). Neither gets a new Minotaur River yet — that convention waits until something is actually built and used.
- **`/Routine` ran a real second round the same day**, post-`/Bedtime` — produced a fresh, tiered Top 10 (`daily_priorities_debate_2026-07-24.txt`, second file of that name pattern this date) after a genuine Team 2 counter-case won three real concessions: Approach B phase 2 (remaining ~17 tables) dropped for today as risk-stacking on a day that already had one incident; the OpenMontage/Claude-fallback logging reframed as a near-free "step 0" rather than a competing Top-10 slot; and older pending smoke tests (highlight-to-Phylum-Path button, 10-phylum browser click-through) folded into the F16-18 sweep rather than left out via recency bias.
- **Still open, stated plainly:** Oracle 504 fix and the XSS/`innerHTML` audit remain deferred (no API credits, unless confirmed otherwise). Item 6 (journal/Chronicles/Minotaur cross-feed) hasn't had its GODMODE+interrogation pass yet — real pre-existing backlog file (`journal_chronicles_oversight_crossfeed_spec_backlog.txt`) is ready to consult. Item 8 (Composio key rotation) needs Alex's action. The 77-item taxonomy backlog remainder is intentionally left for Alex's own manual review. The swipe-gesture freeze is still unsolved. Approach B's real authenticated write is still not functionally confirmed end-to-end.

### Prior state (July 23, 2026, second session) — still true unless superseded above
- **The API-auth merge-and-revert incident, corrected same session.** A "yes" to "have you set the two Vercel env vars yet?" was misread as real confirmation; the fix got merged to `main` (auto-deploys) before the vars actually existed, then Alex clarified they weren't set. Reverted immediately (`git revert -m 1`, main back to a known-working state, `node --check` clean) — the tested code itself is untouched, still held safely on `claude/rpgace-master-steps-uobp98`. See the corrected urgent flag near the top of this file for the full incident writeup and the standing lesson about what counts as real confirmation for a Tier-3 merge.
- **Bookworm's chapter-loop proof is closed out.** Real evidence: Chapter 13 ("Chordal Inversions, Octave Doubling, and Spacing") of the actual PDF-uploaded "Music Theory for Computer Musicians" book — 12 insights approved, 1 edited, via the real review-queue UI. Chapter detection and the insight-review loop have now genuinely run together on a real PDF book — open since the July 20 audit, now proven.
- **Oracle 504 streaming fix and the XSS/`innerHTML` audit deliberately deferred, named honestly.** Both need real Claude API calls to build/verify against; Alex is out of API credits until reset. Logged in `patch_notes.html`'s Still Open section, not silently dropped — pick up first next session with credits.
- **A new `/Routine` skill, named and defined by Alex.** Packages a two-team debate (GODMODE+Council-of-5 evidence pass produces Team 1's real Top 10; a genuine Team 2 counter-case argues for left-out items) through `/Debate` (`/Omnitrix` without Fable by default) and `/5thDimension` reconciliation, ending in a final Council of 5 lock-in — a standing session-opener now (`.claude/skills/Routine/SKILL.md`, referenced in the new "Session-start ritual" section below), so this exercise doesn't need to be re-typed from scratch each time. First real run produced today's reconciled Top 10, recorded verbatim in `daily_priorities_debate_2026-07-23.txt`: API-auth confirm (done), RLS policy design, Oracle 504 fix (deferred, credits), Bookworm proof (done), XSS audit (deferred, credits), journal/Chronicles/Minotaur cross-feed interrogation, taxonomy backlog fork resolution, Composio key rotation, a Lighthouse/PageSpeed audit, and a hygiene bundle.
- **Real debate discipline held**: Team 2 won a real concession (Composio key rotation + the Lighthouse audit were wrongly left off Team 1's first draft for being "cheap and old" rather than urgent) — logged as a genuine debate outcome, not agreement-then-merge.

### Prior state (July 23, 2026, first session) — still true unless superseded above
- **Real freeze root-caused (partially) + a diagnostic built for what's still open.** Alex's report of a 10-20s post-login lag traced to `careerStatCard._fetchAll()` firing in up to 4 overlapping waves (boot, 1500ms, every `page:show`, plus the new boot-loader prefetch) — fixed with real promise deduplication. Alex then reported the freeze persisted specifically on the leftNav swipe gesture (23,000ms, confirmed via a new `perfWatch` module — a passive `PerformanceObserver('longtask')` watcher that reports real main-thread blocks as a visible toast, built specifically so the NEXT repro self-reports real evidence instead of another guess). Root cause not found despite a real investigation (grepped the whole codebase for any `touchmove`/scroll listener that could make a drag expensive — none exist) — genuinely still open, not patched blind a third time.
- **Real pushState URLs, swipeable nav, and Chronicles folded into a dashboard card — then corrected mid-session.** New `pathRouter` module (zero server changes, `vercel.json`'s catch-all already handles it) and `leftNav._initSwipeGesture` shipped clean. Chronicles was NOT done right the first time: the old standalone "Recent Wins" feed kept rendering above the new card, un-styled — caught by Alex's own screenshot, not self-caught. Fixed for real: `careerStatCard._renderWins`/`_ensureWinsContainer` deleted entirely; Chronicles now exists only as a real dashDeck card.
- **Research Lab: real single-tab lazy loading restored, reversing a July 22 decision.** GODMODE evidence found ~6 sub-modules (refCorpus, beatLog, Idea Bank, Bibliography, Workshop button) fetching data regardless of active tab or even active page — a real, substantial freeze contributor. Gated behind a new `research:tab-active` hook (fires once per tab per session). A genuine dead-code bug found in the same pass: `researchTabs._inject()` (builds the in-page tab bar) was never called anywhere — the tab bar had never actually existed live. New `dashDeck._openResearch()` popup lists all 7 sub-navs as cards. "Intelligence" renamed to "File Analyzer" (display label only, confirmed with Alex).
- **`/5thDimension` — a new 6-phase meta-protocol, built and run once.** Combines GODMODE, Council of 5, Omnitrix, Aintergration, `/scope`, and `/debate` in a fixed sequence: what's actually built (real code/git/Supabase evidence) vs. what's reported as done (six oversight docs + Chronicles), reconciled via `/debate`, then a SECOND debate turning the findings into a prioritized rewiring plan vs. a genuine honest counter-case. Shipped as a skill (`.claude/skills/5thDimension/SKILL.md`) and RPGACE's 16th Prod. Oracle command. **Fable failed on usage credits dispatching Phase 1-2 — the SECOND time this exact failure has happened this project** (first was July 20) — reported honestly, Alex authorized substituting Sonnet, now a named recurring pattern worth budgeting for. The audit itself found **near-zero real doc-vs-code drift** (a genuinely positive result, not a sign of a shallow pass — it still caught 7 real smaller findings) plus a real prioritized rewiring debate, never a one-sided recommendation.
- **Alex's three real answers to the rewiring debate, all built or logged:** (1) **"do now"** — the API-auth + password security fix (below). (2) A real standing rule: "reduplication is always a priority... same processes must go through one pipeline or function if steps are identical" — promoted to CLAUDE.md rule 8. **His own wording had a genuine word mixup** (he meant "deduplication," not "reduplication" — an opposite-meaning real word) — caught and corrected the SAME session when he flagged it, verbatim quotes preserved, only the surrounding prose fixed, and the rule extended to explicitly cover taxonomy's own existing dedup precedent (`_continueAnalyzingInBackground`'s per-chapter leaf-name dedup) alongside `intelDedup`. (3) **"yes make live, automate where it improves things"** — Oracle's self-awareness partially made live (below).
- **API-auth + password moved server-side — built, tested, deliberately NOT deployed.** `api/auth.js` (server-side password check against `process.env.CORRECT_PW`, never shipped to the client) + `requireAuth()` (`api/_context.js`, called by every endpoint) + `authGate` (rpgace_core.js, one global `fetch()` wrap attaching the shared secret to all ~26 real `/api/*` call sites — one choke point, not 26 edits). `main.js`'s `checkPassword()` is a deliberate, logged FROZEN-file exception. **Applying rule 8 immediately found a real bug**: `api/composio.js` had its own divergent Composio `ACCOUNTS` map, drifted from `_context.js`'s copy since a June 28 update — `executor.js`/`orchestrate.js` had silently been using the WRONG Gmail/Instagram connected-account ids the whole time. Fixed; `_context.js` is now the single source of truth. Tested via a mock Vercel-shaped server + a real headless Playwright run through the actual login UI end-to-end. **Real, previously-uncaught finding: the OLD password is permanently baked into this public repo's git history** (confirmed via `git log -p -- main.js`) — reusing it as the new server-side value would be security theater, not a fix; a genuinely NEW password is required. Cannot deploy until Alex sets two Vercel env vars (`CORRECT_PW`, `RPGACE_API_SECRET`) himself — confirmed Claude Code has no tool access to do this directly (checked every available Vercel MCP tool). Flagged as an urgent action item at the very top of this file.
- **Oracle's self-awareness partially made live**, not wholesale-rewritten. `oracleAppGrounding`'s hand-maintained `SELF_KNOWLEDGE` digest now also gets a `_liveFactsLine()`: the real live `RPGACE.modules` count (free, always exact) and a real live pending-taxonomy-review count (Supabase, 10-min cache) appended ALONGSIDE the existing qualitative text — never replacing it, since the audit's own honest counter-case stood: a live check that silently fails and looks current is worse than an honestly-stale note. Both fail open (omitted, never guessed, if unavailable).
- **A real process failure, caught and fixed the same session, worth naming as a lesson.** Nearly this entire session's work sat unmerged on the feature branch, entangled with the still-pending Tier-3 auth fix — meaning the Minotaur Map restructure Alex explicitly asked for was genuinely invisible on the live site. Alex caught this himself ("not done properly... redo it") when the real problem was a merge/visibility gap, not a content defect (the file was correct all along). Fixed by splitting the doc-only commits from the application code and pushing them straight to `main` directly — zero code risk, since `main.js`/`rpgace_core.js`/every `api/*.js` file were untouched by that push. **Standing lesson restated**: even mid-session, holding real doc/feature work hostage to an unrelated pending Tier-3 change is itself the "fix on a feature branch is not a fix Alex can test" failure mode this file already warns about — should have been split out sooner, not just at session end.
- **Minotaur Map restructured into real chronological order + a new bird's-eye-view diagram — now live on `main`.** Ten rivers reordered into the actual step-by-step journey (Gatekeeper's Checkpoint → Great Confluence orientation → three entry rivers [Oracle/Bookworm/Content-Intel-Schedule] → Judgment Chamber → Library Current → Confluence Pool → Mirror/Far Shore → Confluence of Chronicles), every cross-river reference checked and renumbered. New bird's-eye-view section shows the same ten rivers as single blocks with zero internal detail, a genuinely new diagram (not a translation of an existing `system_flow_map.md` section, noted honestly as such).
- **A genuinely new idea raised mid-session, deliberately NOT built, recorded verbatim.** Alex raised (inside an answer to an unrelated question): journal tied to specific Oracle conversation frameworks, Chronicles digesting a readable summary from the Minotaur Map/other oversight docs, and oversight feeding back from Chronicles to keep Alex/RPGACE/Oracle/Claude Code on one shared timeline. Genuinely multi-subsystem (rule 5 territory) — recorded verbatim in `journal_chronicles_oversight_crossfeed_spec_backlog.txt` rather than guessed at inline, real open questions listed honestly, waiting for its own dedicated GODMODE/interrogation pass.
- **Still open, stated plainly:** `system_flow_map.md` §0's own module inventory is still stale (real live count 51+, table shows ~30) — flagged, not silently re-dated without doing the real re-verification work. The broader RLS finding (every "enabled" table actually runs `USING(true)`, not just the 3 fully-disabled ones) needs a real policy design pass. The swipe-gesture freeze is unsolved. Oracle's 504 streaming fix, the XSS/`innerHTML` audit, and RLS-status live-querying (needs a server endpoint) are all still not built. The journal/Chronicles/Minotaur cross-feed idea above needs its own pass.
- **All six oversight docs + Chronicles kept current incrementally throughout, not batched at the end** — CLAUDE.md, `patch_notes.html` (9 dated rounds), `interconnection_map.md`, `system_flow_map.md`, `manual.html`, `minotaur_map.html`, plus new verbatim spec-backlog files (`chronicles_spec_backlog.txt` rounds 2-6, `research_lab_lazyload_spec_backlog.txt`, `journal_chronicles_oversight_crossfeed_spec_backlog.txt`) and the two `/5thDimension` evidence/debate reports.

### Prior state (July 22, 2026, session end) — still true unless superseded above
- **Council of 5 shipped as a real Oracle command (15th Prod. Oracle command)**, directly answering whether RPGACE's runtime API could "get" the Claude Code Skills built July 20 — it can't (two genuinely separate mechanisms, checked against the claude-api skill's real docs, not assumed) — so the real answer was porting the protocol into the app's own existing command pattern instead. Reuses 100% existing infrastructure (`prodOraclePanel.CMDS` + `fillGaps`/`sendToOracle`), no main.js edit.
- **Aintergration applied to a ~150-chapter system-design book, all 3 batches processed.** Real, load-bearing scope discipline established and held across all three passes: most chapters are generic CS fundamentals with no adoption decision attached, and forcing a verdict on each would be empty ceremony — triaged for real signal instead of grinding through every chapter uniformly. Full findings logged as Aintergration Worked Precedents 4-6 in this file's Invokable Frameworks section. Two real, previously-undiscovered security findings surfaced this way: **every `/api/*.js` endpoint has zero server-side auth** (bigger exposure than the known RLS gap — billing/account-action risk, not just data risk), and **`CORRECT_PW` is readable via view-source regardless of where it's stored**, since the check runs entirely in client JS — both are one underlying architecture decision, not two separate quick fixes, deliberately held back for a dedicated sequenced pass rather than rushed. Also confirmed directly by Alex: **no Supabase backup/PITR exists at all** (was an open question, now settled fact) — Tier 3, his call on when/whether to fix.
- **Oracle gained real self-awareness + a two-way Claude Code bridge, 6 pieces, all shipped, none hand-tested yet:** `oracleAppGrounding` (reads `dashDeck`'s live cards + an honest status digest, keyword-gated, costs nothing when it doesn't trigger), `oracleFetchGuard` (hardens Oracle against prompt injection from fetched web/YouTube/Instagram content — a direct answer to a risk this session itself had flagged earlier), `oracleDevBridge` (a "🧪 Flag for Claude Code" button on any substantial Oracle reply, writing to a new `oracle_dev_suggestions` table), `taxonomy_decision_log` (an append-only audit trail written at `_insertNewSteps`'s single real choke point — the first event-sourcing-style pattern in the app, turns "which code path produced this" from a forensic Supabase query into a plain read), Council of 5's own conversation-capture button (`fillGaps`'s new opt-in `opts.allowConversationCapture` — captures the real chat instead of Alex re-typing a summary, backward compatible with the other 40+ command entries that already use `fillGaps`), and a real Claude Code Remote Routine (`trig_015uRjGVrdmiwc5zxq47mMA6`, daily 6am UTC) delivering an actually-pushed Morning Brief instead of pull-based — caught and fixed a real problem before it could fail silently (the trigger has no MCP connector access, so it was rewritten to curl Supabase's REST API directly, exactly like `main.js`'s own client code already does).
- **Session-start check added**: any session working on RPGACE now checks `oracle_dev_suggestions` for `status='new'` rows before other work — still pull, not push (a true push needs a Supabase webhook wired to a Claude Code Remote trigger, flagged as a future idea only).
- **All six oversight docs updated in the same session** — CLAUDE.md, patch_notes.html, manual.html, system_flow_map.md, interconnection_map.md, and minotaur_map.html (a genuinely new "River VIII," since this is the first wing in the whole map where water leaves RPGACE entirely and reaches a different world — a future Claude Code session — rather than ending at Alex's own screen).
- **Nothing from today is hand-tested.** All of it is `node --check`-clean / div-balance-clean / independently re-verified against real source before being logged, live on `main`, awaiting Alex's click-through.
- **QoL cleanup (2 passes) + a real nav-lag incident, root-caused twice.** Pass 1: sidebar load, schedule XP-per-view bug, Research Lab tab-wrapper removal, tighter list collapsing. Pass 2 (real answers, not guessed defaults): shift-sync cache fix (an external Python browser-use script writes `rpgace_shifts` outside the app — added to the no-cache table list), Agents folded into Oracle AI (moved live DOM, not rebuilt), Encyclopedia/Journal get real search + the new audit-trail tables surfaced as read-only entries. **Incident, same day:** the sidebar fix briefly made `leftNav._buildDrawer()` run outside the `rpgace:ready` gate, aborting ~25 of ~40 modules' registration mid-parse — caught live from a pasted console log, reverted immediately. The underlying bug re-surfaced contained (not catastrophic) and was root-caused for real: `leftNav` registers before `config` in file order, so `leftNav.init()` always ran before `RPGACE.CONFIG` existed — fixed with a defensive guard + retry, not just the hotfix revert. **A second, deeper, systemic root cause found afterward**, from the separate complaint "nav doesn't respond for ~10s after login": `onReady()` gated the entire module-registration cascade behind the `window.load` event (waiting on every image/font), when the real dependency — login — is completely decoupled from page-load timing. Switched to `DOMContentLoaded`-equivalent + a 50ms delay, a systemic fix improving every module's startup, not just leftNav's. Cache-busting version string bumped multiple times same day after finding it hadn't moved in days despite dozens of edits — standing lesson: bump it every time `rpgace_core.js` changes.
- **RPGACE is now a real installable PWA** (Android/desktop "Add to Home Screen"/"Install app") — `manifest.json` + `sw.js` (deliberately network-first, not cache-first, to avoid making the stale-cache bug class above permanent) + a generated gold-sword icon set in `icons/`, registered via a new `pwaInstall` module (no new `<script>` tag). `vercel.json`'s existing SPA catch-all rewrite would have silently served `index.html` for the manifest/SW/icon requests — fixed with explicit passthrough rewrites before shipping, not after. Also added: a full-screen boot-loader overlay, visible from first paint, hidden only once `rpgace:ready` confirms every module has registered — the login gate itself never appears mid-race now. Pure-CSS 6-second fallback, zero JS dependency, so it can never trap a user if script execution stalls.
- **The profile stat card rebuilt onto real, persisted data (GODMODE/Council of 5/Aintergration, Alex's own request).** Real finding: HP/MP bars and "Streak" were 100% cosmetic — confirmed via grep, zero code anywhere ever touched them — and XP/Level lived only in in-memory `STATE`, resetting to zero every page load. Real signal already existed unused in Supabase (journal, encyclopedia_insights, taxonomy_proposals, bookworm_chapters, reference_tracks, content_productions). Rebuilt as a real weighted career score with two deliberately SEPARATE lanes — **Output** (real shipped content only) and **Growth** (learning/knowledge-tree activity) — specifically so taxonomy busywork can never visually substitute for actually shipping something (a real Goodhart's-law risk raised and addressed before building, not after). Considered and explicitly rejected: variable-ratio/randomised reward schedules (the mobile-game "loot box" pattern) — effective at compulsive engagement, wrong shape for a tool whose own governing rule is real output.
- **Recent Wins → renamed "The Chronicles" (Alex's own name), interrogated before building further.** Round 1: every feed row became a real clickable detail popup (What was done / Outcome / Where stored / Why significant), reusing `dashDeck._popup` rather than a new one-off overlay. Round 2 (after real interrogation, verbatim Q&A in `chronicles_spec_backlog.txt`, since this touches real financial record-keeping): a full dedicated `#page-chronicles` log page (search, type filters, full history) plus a new `chronicles_finance` Supabase table for real sale/expense entries — explicitly a **personal-visibility tracker, not bookkeeping-grade** (real receipts still needed at actual tax time). Phylum 17 "Negotium" (already reserved in the phyla config for conceptual business/tax knowledge, never built out) was deliberately left untouched — a transaction ledger and a conceptual knowledge phylum are different data shapes, kept separate on purpose.
- **This oversight sweep itself: a new `/scope` skill, and Chronicles now logs Claude Code's own updates.** `/scope` (`.claude/skills/scope/SKILL.md`) is a reusable evidence-gathering skill — git log + live Supabase state + doc line-counts, organized into digestible grouped categories (bugs/features/infra/security/planned-not-built) — built specifically so an "update oversight" pass doesn't miss something or drown in blow-by-blow detail; it's the GODMODE evidence pass for oversight work specifically, not a separate ceremony on top of it. A new `system_updates` table (RLS + anon_all) + a new Chronicles filter/detail-formatter means every real Claude Code change to RPGACE now shows up in Chronicles alongside Alex's own real activity — one shared timeline, not two separate records. **Standing rule going forward, same discipline as the `patch_notes.html` rule**: any session that ships a real, user-visible change to RPGACE inserts a short row into `system_updates` (title/summary/category) in the same session, so Chronicles stays a live, honest record — not a retrofit project.
- **Real inconsistency caught and fixed mid-build, worth naming as a lesson**: the finance detail popup's "Why significant" text claimed a sale "feeds the Output-lane picture" — the code explicitly does NOT fold finance into that score (confirmed via interrogation, a deliberate separate-lane decision). Caught by re-reading the code against its own stated design before this sweep, not by Alex reporting it — the exact kind of doc-vs-code drift GODMODE evidence-gathering exists to catch, including inside a single session's own just-written work, not only across older docs.
- **The Taxonomy Sorting Agent and the Jargon Encyclopedia/Book Knowledge items it was blocking — real doc-drift found and fixed.** Before building the two "dry riverbed" items Alex asked for, traced the actual call chain (not the docs): `bookworm._decidePlacementScored` is a one-line pass-through to `phylumPath.decidePlacementScored` — the exact same function `encSync`/`ciAutoPropose` already call for non-book inputs. Book and non-book insights have shared ONE placement engine since the July 19 unification; the "Taxonomy Sorting Agent" as this file previously described it (see the struck-through line above) was already effectively built and the docs simply never caught up. What it was actually described as blocking — a queryable book-knowledge source and a read-only Jargon Encyclopedia — genuinely didn't exist. Both shipped as pure Postgres views over data already being written (`book_knowledge` unnests `bookworm_chapters.insights`, `jargon_encyclopedia` selects `taxonomy_tree WHERE node_type='leaf'`) — zero new tables, zero new writes, per Alex's explicit "build in to the existing rather than build on top." A real `security_definer_view` lint was found via `get_advisors` and fixed (`security_invoker=true`) before shipping. Jargon Encyclopedia surfaced as a button inside Phylum Path's existing page, not a new nav destination.
- **General-knowledge audit redesigned around a new `/debate` skill, not built as originally scoped.** Alex confirmed general/generic Claude knowledge should never be injected directly into the tree. New `/debate` skill (`.claude/skills/debate/SKILL.md`, alongside `omnitrix` and `scope`): structured adversarial rounds (both sides attack, then both defend, then Council of 5 cleans up both cases) specifically so comparing Claude's general training against RPGACE's own gathered content never has the side effect of writing to the tree — a debate "won" still requires an explicit separate build decision afterward, never automatic. **Not yet run** — Alex asked to hold off rather than pick a topic in this pass.

### Prior state (July 20, 2026, session end) — still true unless superseded above
- **Dashboard rebuilt (July 20, 5 commits):** `dashDeck` module — 11 command cards in three thematic rows + "Needs you now" panel. Big widgets (Bookworm, Knowledge Gaps, Content Pipeline) live inside card popups as MOVED live DOM nodes (pattern: `_ensureStash`/`_stashWidget(id,force)`); quest board relocated to the Agenda page. Landmine: a stash-guard cannot distinguish "popup open" from "popup closing" — every onClose must pass `force=true` (bug hit twice, July 20). Popup buttons that open lower-z-index surfaces must `closeWidgetPopup(id)` first (popups are 99999; drawers/panels are 9998-10110).
- **Research Lab fixed (July 20):** Idea Bank/Corpus/Beat Log were nested inside `#video-workshop-panel` via interior-anchored insertBefore; Bibliography rendered at `page.firstChild`. All Research-page injectors now append as direct children of `#page-learning` and fire `research:panel-injected`. Four of the ~25 dead `rpgace:ready` listeners fixed (~21 remain unaudited).
- **Navigation replaced (July 20):** top `.nav-tabs` bar removed from index.html; `leftNav` module — hamburger + left slide-out drawer (backdrop 10100/panel 10110), 9 pages, Research (7 tabs via new public `researchTabs.show(key)`) and Schedule sub-navs. main.js untouched, byte-identical.
- **NOTHING from July 20 is hand-tested.** All three items above are code-reviewed/reasoning-verified only (independent line-by-line re-checks against real diffs, not just agent self-reports), live on rpgace.vercel.app, awaiting Alex's click-through (checklists in patch_notes.html).
- **AI Advisor page restructured (July 20, 2nd session):** hardcoded Prod/Insta-Oracle panel colors → real `--blue`/`--purple` tokens, 8-button stack → 2-column `.prod-cmd-grid`. Done SOLO (Fable ran out of usage credits mid-audit-dispatch, so Alex said skip Omnitrix — no second reviewer this pass, hence smaller scope). Full page-by-page Council-of-5 verdict table (what shipped, what's deferred and why, what's flagged don't-build) lives in `DESIGN.md` §10 — Research Lab/Agenda/Schedule/Agents/Encyclopedia/Journal deliberately NOT touched this pass (real reasons, not a punt), Phylum Path explicitly flagged don't-build (too load-bearing, no clear case). One pre-existing (not this session's) unclosed `<div>` found in index.html, confirmed harmless and NOT introduced by this edit, flagged for a future dedicated pass.
- **Real limitation confirmed, worth remembering:** Fable 5 can run out of usage credits mid-task — a background dispatch will fail with a plain task-notification, not silently hang. When it does, do not substitute a different model into the Fable role without Alex's explicit say-so; report the failure and ask. **Confirmed a second time, July 23** (the `/5thDimension` Phase 1+2 evidence-audit dispatch) — this is now a real, recurring pattern on Fable specifically, not a one-off. Both times Alex's answer was to substitute Sonnet rather than wait/run `/usage-credits`. Standing lesson: Fable's fresh-context edge is real for genuinely large audits, but weigh its real failure-rate against just running the task Sonnet-direct from the start, per Omnitrix's own "most tasks don't need a dedicated fresh-context agent" reasoning.
- **Token economics (July 19):** ~8-10x per-chapter cost cut; see building-guide rules 10-11 and `token_cost_audit_2026-07-19.txt`.
- **Omnitrix formalized (July 20):** the Fable→Opus 4.8→Sonnet 5 workflow used across all three July 19-20 builds is now a named, invokable protocol, AND the default judgment funnel — see "Invokable frameworks" below.
- **Full project audit (July 20, GODMODE, Fable 5) — 3 real doc-drift corrections, independently re-verified against live Supabase:**
  1. **The PDF-book insight loop already ran, July 19 — the "never proven together" framing was stale.** `bookworm_chapters` for the real "Music Theory for Computer Musicians" book (book_id `70fd8faa-…`) shows chapters 8, 10, 13 (1-indexed; `chapter_index` 7/9/12) `status=complete`, 39 real insights total (17+10+12) reviewed through the OLD pre-audit/pre-retune engine — these are the exact runs that produced the garbage (ch-13 fragmentation, ch-1 shoehorning) which triggered the July 19 tree audit and the token retune. Chapter 1 (`chapter_index` 0) sits `in_progress`, 6 insights, 3 approved pre-queue / 3 still pending, no active heartbeat. **The real open test is narrower than previously stated: one clean chapter through the POST-retune unified engine + July 20 UI** — resume chapter 1 or open the next unstudied one — not "first-ever combined run." Also found, not previously logged: a **duplicate `bookworm_books` row** for this same book (`87268196-…`, all chapters pending, zero real progress) sitting alongside the real one — harmless but worth knowing before picking "the" book to resume.
  2. **Fusion links: 21 confirmed / 47 pending (68 total)**, not "6 confirmed" — badly stale, likely last updated right after the first few links shipped.
  3. **107-item review-queue backlog** nobody had logged: 60 pending `taxonomy_proposals` + 47 pending fusion links, largely generated by the pre-July-19 engines the placement-rules audit itself found produced bad output. `video_jobs` = 0 rows confirmed (Beat Log has never once persisted — F16/F17/F18 have no data to test against, the feature closest to the governing rule has never actually been used).
  Full done/missing/planned/discussed breakdown and the Council-of-5-scrutinized forward plan (highest priority: hand-test July 20's 3 unverified builds, THEN one clean PDF chapter through the new engine) delivered to Alex in chat, July 20 — not duplicated into a new file (Council-of-5 pushback on Fable's own suggestion: this repo already has too many stale point-in-time snapshot docs at root — see Missing/Housekeeping — adding another would repeat the exact problem).

### Prior state (July 18, 2026) — still true unless superseded above
- **Taxonomy: 10 of 21 phyla live in Phylum Path** (`ENABLED_PHYLA: [1..10]`), all through the 7-step Phylum Development Framework (spec → keywords → tree build → data repair → enable → fusion pass → hand-test). Placement logic hand-tested across 8 of 10 (data-layer). Zero orphan nodes tree-wide. Rank chain: Phylum→Order→Class→Family→Genus→Species→Variant (depth 0-6).
- **Fusion links** (leaf-to-leaf, `taxonomy_links`, 6 confirmed live) and **Concept Fusion** (branch-level cross-phylum merges creating a NEW synthesized leaf via `taxonomy_proposals` `engine:'concept_fusion'`) both shipped and data-layer tested.
- **Bookworm — both chapter-detection paths now genuinely proven, July 18.** Manual/TOC-paste path (`_startBookFromTOC`): confirmed correct on a real 27-chapter book. URL/PDF-upload path: rebuilt from scratch after an 8-round real-evidence debugging arc (deployment-branch mismatch → a second untouched code path → exact-match anchoring failing against PDF.js's word-joining AND word-splitting corruption → ordering overshoot into cross-references → TOC-read omissions → two remaining stragglers needing a title-only fallback) — final result: 26 of 26 real chapters, correct titles, correct order, zero warnings, on a real book. Current architecture: `detectChapterListByOracle()` (model reads ONLY the table of contents, self-verifies against 5 explicit checks, recircles on any numbering gap) + `resolveChapterHeadingsMechanically()` (100% deterministic text search, character-fuzzy title matching, strict reading-order-bounded, title-only fallback). The old `searchString`/body-text-recall mechanism is removed entirely, not superseded. **Still open: the chapter-by-chapter read→insight→approve loop has been proven (on the manual book) and chapter detection has been proven (on the PDF book) — but never proven TOGETHER on the same real PDF book. That's the concrete next step.** Spec record: `bookworm_spec_backlog.txt`. Session-by-session plan: `fable_master_plan.txt`.
- **Multi-model pipeline**: `MODEL_EXTRACTOR='claude-fable-5'` outlines, `MODEL_GROUND_WORKER='claude-sonnet-4-6'` executes; extractor failure always falls back gracefully to ground-worker-alone.
- **F0–F18 all shipped** except F12 (deliberately deferred). Still never hand-tested: F16 (Beatstars listing), F17 (video pipeline stages), F18 (auto visual treatment), highlight-to-Phylum-Path button, F10's n8n run.
- **Known open bugs**: Oracle 504 on long responses (root cause: single blocking non-streaming `callClaude`; needs streaming/chunking, not another token trim — a prior streaming attempt was reverted, dead code + `restoreSendChat` neutralizer still present); F11 silent "Content Unavailable" on failed Jina fetches; `_generateNodeContent` empty-deep_content mystery (partially resolved, unverified).
- **Biggest confirmed-not-built items**: Bookworm card-list UI (ConID-card pattern), F12, phyla 11-21 framework passes, page-number-assisted PDF extraction (deliberately scoped out of the July 18 rebuild). ~~Taxonomy Sorting Agent, Claude general-knowledge audit~~ — **superseded July 22, see Current State**: the Sorting Agent was found to already be effectively built, and the general-knowledge audit was redesigned around a new `/debate` skill rather than built as originally described here.
- **Standing deployment rule, confirmed the hard way twice this session:** a fix on a feature branch is not a fix Alex can test — always merge to `main` and push before asking for a hand-test.


---

## Archived Sep 15, 2026 — HABITS/Cooking full build narrative (Sep 10–14) and the Galaxy Development Framework's full G1–G109 build narrative (Aug 13–26)

**Why this is here:** a real `/paranoia`+`/free-for-all-debate` pass (Sep 15 2026) found CLAUDE.md's own July 31 doc-discipline rule — "when an entry's underlying event is done, verified, and no longer being actively re-litigated, collapse it to one durable-fact bullet" — had gone unenforced since. These two sections alone (HABITS/Cooking, and the Galaxy Map's own build history under "The 4 layers (c/d/e/f)") accounted for 227KB of CLAUDE.md's then-502KB total. Both are fully resolved as of this date; both are moved here verbatim and replaced in CLAUDE.md with durable one-line facts, per the file's own stated rule. **Do not treat anything below as current** — CLAUDE.md's own Current State section and Known Landmines section are the live truth.

### HABITS domain — full Sep 10–14 2026 build narrative (verbatim)

### HABITS (new domain, Sep 10 2026)
- **Cooking is the first real HABITS module, shipped Sep 10 via a real /CEO Loop 1 spec + Loop 2 build.** Full record: `records/2026-09/habits_cooking_domain_interrogation_spec_2026-09-10.txt` (the interrogation) and `patch_notes.html`'s own Sep 10 card (the build). Built entirely inside the existing app (`cookingOracle` module, `RPGACE.register()`/`dashDeck._popup()`, no new stack/repo/framework — Alex's own direct correction after an initial "new stack" misreading). Real Supabase schema: `recipes`/`ingredients`/`recipe_ingredients`/`ingredient_prices` + 4 views (cost totals, stale-price flags, low-value-buy flags, `ingredient_frequency`/`ingredient_frequency_by_month` — real long-term eating-trend tracking, gated on `journal.status='cooked'`, not merely saved) + a small `habits_config` table (habits toggle, unit system, default shop, real oven(2)/stovetop(4) capacity). Generate via the existing Oracle chat pipeline (a `RECIPE_JSON:` trailer, same convention as `DIRECTOR_CHOSEN:`/`EDL_JSON:`), review (real serving scaler, a step timeline tagged `prep_before_cooking`/`actual_cooking`/`prep_while_cooking`/`baking` with real per-step timers), Save writes `recipes`/`recipe_ingredients` + one `journal` row (`entry_type='recipe'`, `status='saved'`) — never regenerated per view. A saved recipe's Journal entry gets a real "Mark Cooked" button (`refreshJournalDisplay`'s own small conditional addition, main.js legacy block). Verified via 5 real headless-Chromium functional checks (card shape, module method surface, popup fields, `RECIPE_JSON` parsing, settings fields) — all passed. **H6 shipped Sep 13 2026** (this line's own "not yet built" claim about H6 is stale from that date — kept here as historical narrative per this file's own doc-discipline, corrected in place per rule 16): a real `planned_cooks` table (recipe_ids/schedule/scheduled_at/agenda_id, same `anon_read_only`/`authenticated_all` posture) backs a real in-progress multi-recipe cook session in `cookingOracle` — save a recipe, optionally add more (picked from existing saved recipes or freshly generated), then "📅 Schedule cook time" computes a real greedy longest-recipe-first interval-packing schedule (a genuine, verified joint hands(capacity 1)+equipment(oven/stovetop, CONFIG-set) feasibility check — a real bug where equipment capacity was tracked but never enforced was caught and fixed via a standalone Node test before it ever shipped) and shows a read-only proposed timeline; Accept writes `planned_cooks` and pushes one real agenda entry via the existing shared `scheduleToCalendar()` (rule 8). Each component recipe keeps its own `journal` row untouched — H3's `cooked_at`/FK contract and H1's `ingredient_frequency` view are unaffected. Verified via a standalone algorithm test (oven-capacity-1 serializes, capacity-2 parallelizes with a 3rd waiting) plus a real headless-Chromium run of the actual shipped module code end-to-end (save→session builder→add recipe→schedule preview→Accept→agenda push), zero JS errors. Full record: `records/2026-09/habits_cooking_domain_interrogation_spec_2026-09-10.txt` section 10 + `patch_notes.html`'s Sep 13 card. **Real, deliberate scope line held**: plain date/time only, no shift/calendar collision-checking — that's H5, still real, specified, and **not yet built** (deferred, no change this pass). **Untested by Alex's own hand** — same standing flag as the rest of this domain. **H7/H8/H9 shipped Sep 13 2026, same day, real CEO Loop 1 (`records/2026-09/habits_shopping_pantry_calories_spec_2026-09-13.txt`) → Loop 2, Alex's own explicit "log both questions as blue for later, do you to h9."** Real new tables: `pantry_stock` (a genuine persistent stock table, not a per-list tick), `shopping_lists`/`shopping_list_items` — all `anon_read_only`/`authenticated_all`. `cookingOracle` gained a real "📦 Pantry" view/edit, a "🛒 Generate shopping list" have/not-have split computed against live pantry stock from a planned-cook session, a price-entry popup on marking an item bought (real "↺ Redo" to correct before it's final) writing plain inserts into `ingredient_prices` (never updates, so price history accumulates naturally — brand/store columns added), and a recipe-card "🔎 Estimate Nutrition" section (kcal/protein/carbs/fat, totals + per-serving) using cached per-ingredient nutrition first, falling back to a live lookup. Oracle's `RECIPE_JSON:` trailer extended with a per-ingredient `grams_estimate`. **Real, same-day correction — Alex flagged the USDA choice directly ("Maybe not USDA, as I am in the uk"): USDA FoodData Central is a US database.** Real web evidence gathered before swapping (never guessed): CoFID (McCance & Widdowson, the UK government's own equivalent) has no live official API, only a static ~3,300-row downloadable dataset — a real future import job, not built this pass. Alex confirmed "Both, staged": `api/oracle.js`'s `lookupUsdaNutrition`/`action:'usda-lookup'` fully replaced (not left dormant alongside) with `lookupOpenFoodFactsNutrition`/`action:'nutrition-lookup'` — [Open Food Facts](https://uk.openfoodfacts.org), free, genuinely **no API key at all** (its own stated policy: read calls need only a descriptive `User-Agent`, no signup), live now, no "not configured" gate needed. Real UK preference: among up to 5 search matches, prefers one tagged `en:united-kingdom` in `countries_tags`, else first result. Real, honest tradeoff kept, not hidden: Open Food Facts is a crowdsourced BRANDED-PRODUCT database, so a generic ingredient name ("onion") can match a packaged product's nutrition rather than a true raw value. **Stage 2 (CoFID) built same session, same day, Alex: "Now do cofid"** — a new `cofid_foods` table (trigram-indexed `food_name`) + `cookingOracle.logic._lookupCofid` (client-side, zero AI call/server round-trip) now checked FIRST in `_ensureNutrition`, before Open Food Facts. Real word-overlap scoring picks the best `ilike`-matched candidate by counting overlapping words (never a bare substring accept); a real edge case (a CoFID row with every nutrient null, e.g. "Water, tap") is explicitly excluded from winning — caught and fixed via a synthetic-fixture test before shipping, not after. **Real, superseding update, same session, same day: the "genuinely empty" gap above is CLOSED.** `scripts/import_cofid.py` (header auto-detection, fuzzy column matching, correct `Tr`/`N`/footnote-marker handling) worked exactly as tested. Alex delivered the real files directly as chat attachments (the official 2021 XLSX + a genuinely separate "CoFID_oldFoods.xlsx" legacy dataset + the PDF docs) in response to the concrete gov.uk-download ask below — real inspection confirmed zero food-code overlap between the two files (2886 vs 393 unique codes), so both were imported, not just one: 3,280 real UK foods now live in `cofid_foods` (2887 `source='cofid'`, 393 `source='cofid_legacy'`). **A real bug was caught and fixed before import, not after**: `match_column()`'s multi-hint logic used `all()` instead of `any()`, silently discarding `food_group` for every row (the real header is bare "Group," never the literal phrase "food group") — fixed and re-verified before running the real batch. `_lookupCofid` now wins real matches ahead of Open Food Facts for generic ingredients, spot-checked against "chicken breast"/"onion"/"rice, white, basmati" with correct results. `ceo_plan_items` H9-COFID moved yellow→green. Untested by Alex's own hand in the live recipe UI. (The rest of this paragraph, including the gov.uk-download ask, describes the real state AT THE TIME it was written — kept per this file's own doc-discipline, superseded by the correction above.) `nutrition_source` now stores `'openfoodfacts'`; the old `noKey` concept is gone from `_ensureNutrition`'s return shape, replaced with an honest `lookupFailed` flag for a real transient miss (network/rate-limit), surfaced as an inline caveat rather than a separate blocking message. **Real, deliberate scope line held**: recipe-level nutrition only, no daily calorie log — deferred until Alex is back at the gym post-hand-injury. **Two real, pre-existing bugs found and fixed in the same pass, not shipped around**: (1) H6's own `_showSchedulePreview` (and this session's first draft of the new shopping-list generator) built a PostgREST `in.()` filter by wrapping each UUID in literal double quotes before joining — an invalid URL, throwing "Failed to fetch" before any real network attempt; fixed to the established unquoted-join convention already used elsewhere (`taxonomy_tree`/`bibliography`/etc). **H6's own Sep 13 "verified via headless Chromium, zero JS errors" claim never actually exercised this code path** — corrected here, not silently. (2) `RPGACE.sb.select`'s cache wrapper was missing `pantry_stock` from its `noCache` list, letting a fresh pantry write hide behind a stale ≤60s cached read and corrupt the have/not-have split — fixed, matching the existing `rpgace_shifts` precedent. Verified via a real headless-Chromium Playwright harness (19/19 assertions) after fixing a Service-Worker-interception issue in the TEST HARNESS itself (`serviceWorkers:'block'` needed on the browser context, since `rpgace_core.js`'s real `sw.js` registration otherwise intercepts every GET before `page.route()` sees it). `node --check` clean, re-minified, cache-bust bumped `20260912a`→`20260913a`. **One fork resolved same day (nutrition provider — Open Food Facts, see the correction above); one still deliberately deferred**: unit-to-gram normalization for ingredients with no cached weight — logged blue, not built. **Untested by Alex's own hand.** **Real fix, Sep 13 2026, same day, later pass — Alex's own direct complaint: "When i say in text field something Korean, I'm not given a list of 5 to choose from as discussed, I want a range or questions asking particular ingredients, or what type of dish (soup, noodle, rice, starter or dessert etc."** Real evidence checked first (rule 1): `cookingOracle.logic._generate` sent exactly one Oracle call for exactly one recipe with zero disambiguation, and no prior spec file (`records/2026-09/habits_cooking_domain_interrogation_spec_2026-09-10.txt` or elsewhere in `records/`) actually documents a "list of 5" agreement — treated as a genuine new gap, not a lost feature. Fixed: a description of 3 words or fewer now opens a real "Narrow it down" step (`ui._showNarrowDown`) instead of generating blind — 8 dish-type chips (Soup/Noodle/Rice/Main/Starter/Dessert/Side/Drink), an optional ingredients field, and an on-demand "🎲 Suggest 5 ideas" button (one real, cheap Oracle call, never a second premium-model triage call ahead of the real generation call, per rule 11); picking an idea or pressing "Generate with this info" runs the normal recipe generation. A description longer than 3 words is unaffected. A real rule-8 dedup landed alongside it: the original Generate button's disable/spin/call/show-card sequence was extracted into one shared `ui._runGenerateAndShow`, now used by all 3 real exit paths instead of each re-implementing its own copy. **Real, superseding correction, same day, immediate follow-up — a real live-production bug, found from Alex's own screenshot ("Could not parse dish ideas from the reply"), the moment he actually tried Korean → Rice → "Cabbage."** Real root cause, found by reading the actual capture pipeline, not guessed: the prompt originally asked for a numbered list, but `oracle:response-scanned`'s `text` is `lastMsg.textContent` read AFTER `renderMarkdown()` has already rendered the reply — `renderMarkdown()`'s own `^\d+\. ` regex strips a numbered list's digit prefix outright, and DOM `textContent` never reinserts a newline for the block-display markup that separated the original list items, so a numbered/bulleted reply is genuinely unrecoverable by the time ANY consumer sees it. Every prior consumer of this hook (`RECIPE_JSON:`/`DIRECTOR_CHOSEN:`/`EDL_JSON:`/`ORACLE_ACTION:`) only ever hunts one inline marker followed by a single blob, never multiple distinct list items — this feature is the first consumer needing real list extraction, exactly why it's the first to hit this. **A 2nd real bug found in the same pass**: `addMsg`'s own AI-message markup (`<div class="ai-name">✦ Oracle</div>${renderMarkdown(text)}`) has no separator, so `textContent` glues "✦ Oracle" directly onto the real reply — no prior consumer noticed since none of them split from the very start of the string. **Fixed at the prompt level, not patched around**: Oracle is now asked for one pipe-delimited line ("Dish One | Dish Two | ...") instead of a numbered list — survives both facts above; `_parseDishList` strips the known "✦ Oracle" label prefix, defensively strips a stray leading number/bullet per piece, and treats a single unbroken >80-char result as a genuine parse failure rather than silently presenting a garbled "idea." **Real, honest process finding**: the first ship's own "8/8 real assertions" headless test stubbed `visualOracle._captureNextResponse` directly with a hand-typed callback — it never actually rendered a reply through the real `addMsg`→`renderMarkdown`→DOM→`textContent` pipeline, which is exactly how both bugs slipped through verification and shipped anyway. Re-verified via a real headless-Chromium test that calls the real `addMsg()` and reads back what `lastMsg.textContent` actually contains — reproduces both bugs exactly as Alex saw them, then proves the fixed prompt+parser recovers all 5 real ideas through that same real pipeline (all 8 original assertions + 4 new real-capture-path assertions pass, zero console errors). `node --check` clean, re-minified, cache-bust bumped `20260913f`→`20260913i`. **Untested by Alex's own hand yet on this specific fix.** **Real follow-up, Sep 13 2026, same day, immediate — Alex confirmed the fix worked (a real "Tom Yum Cabbage Soup" recipe generated correctly) then raised 2 more real gaps verbatim: "no option to add or suggest ingredients (like meat, love meat, or fish) also don't see the option to add it to current planned cook for grocery list."** Real `/interrogation` (AskUserQuestion, both answered) confirmed scope before building: (1) **both** a manual add row and Oracle-suggested additions, explicitly **pantry-aware "so waste is reduced"** (Alex's own words); (2) the existing automatic "Save silently adds to the in-progress cook session" behavior gets a **real, separate, visible button** alongside it, not just a relabel. Built: the recipe card gained a plain manual "+ Add ingredient" row (name/amount/unit, no Oracle call, free) that also correctly resets the nutrition estimate to "not yet looked up" since the total is now stale; a "💡 Suggest additions (uses your pantry)" button firing a real, separate Oracle call — a new `INGREDIENT_SUGGESTIONS_JSON:` trailer, same compact-single-line-JSON convention `RECIPE_JSON:` already uses (never a numbered/bulleted list, per the standing landmine below) — reading the recipe's current ingredients plus real `pantry_stock` (a new `logic._loadPantryNamed` helper, factored out of `ui._showPantry`'s own inline query per rule 8, now shared by both) and asking Oracle to prefer a pantry item when it genuinely fits, to reduce food waste; each suggestion shows a real, **code-computed** "📦 in your pantry" tag (checked against the actual pantry rows already in hand, never trusted from the model's own claim, rule 7) and its own "+ Add" button. A new, explicit "📅 Add to Cook Session" button now sits next to "💾 Save to Journal" — Save's own existing automatic add-to-session behavior is unchanged (no regression); the new button is a 2nd real, visible path to the same H6 session builder for someone who hasn't clicked Save yet, saving the recipe first only if it hasn't been saved already (a `savedRecipeId` closure guard prevents a 2nd duplicate `recipes` row on a later click of either button). Verified via a real headless-Chromium test exercising the actual capture pipeline again (`addMsg`→`renderMarkdown`→DOM→`textContent`, not a mocked callback) — 6/6 real assertions pass, zero console/page errors from the app's own code (the only console errors were pre-blocked external network calls the test sandbox itself blocks, e.g. Google Fonts/Supabase boot reads — confirmed unrelated by direct inspection). `node --check` clean, re-minified, cache-bust bumped `20260913i`→`20260913j`. **Untested by Alex's own hand yet.** **Real, superseding hand-test follow-up, same day, immediate — Alex actually clicked both buttons and got both a journal save AND a session add from one recipe, then said "always do dedup" and asked to make the two buttons mean genuinely different things: "make so that save to cook just saves to cook, but not to journal, so that I can then rate the dishes and save liked ones."** Real root cause: `_save` always wrote a journal row internally regardless of which button called it, so "Add to Cook Session" was silently creating one too — and a fast click of both buttons in quick succession (or a same-button double-tap) could fire 2 concurrent saves, since each button only checked a flag (`savedRecipeId`), never a real in-flight guard. **Fixed with one shared `ensureRecipeSaved` helper both buttons now call** — it queues a second caller instead of starting a second real save whenever one is already in flight, so the `recipes` row is only ever inserted once no matter the click pattern (real dedup, per rule 8). `_save` gained a real `withJournal` flag: "Add to Cook Session" never writes a journal entry now; "Save to Journal" always does, via a new `_insertJournalEntry` (reused whether the recipe was already saved by the other button first, or not). **Same session, a third real Alex ask**: "clicking Dom module should also give way to a pop up for buttons to access either planned cook, grocery list planned or recipe generator." The dashDeck Cooking card's `go:` now opens a real 3-button hub (`ui._showModuleHub` — Planned Cook / Grocery List / Recipe Generator) instead of jumping straight into resume-session-or-generate; Grocery List checks the real `shopping_lists` table directly (never claims "none exists" just because the in-memory session hasn't rebuilt one yet on this page load). Verified via a real headless-Chromium test exercising the actual dedup path — a genuine concurrent double-click across both buttons, and a same-button rapid double-click, plus the new hub's 3 entry points and its honest empty-state fallback — 6/6 new assertions pass, no regression on the prior 6. `node --check` clean, re-minified, cache-bust bumped `20260913j`→`20260913k`. **Untested by Alex's own hand yet.** **Real hand-test follow-up, Sep 14 2026, next day — Alex actually generated a recipe and reported 2 real problems, verbatim: "the method is poor, why prep seafood before anything when it is added at the end? Prep should be for everything that is part of active cook without beforehand dead time, so why do this? I want oracle float button to be able to take criticisms while I'm on cooking module so I can discuss what I want changed, would be huge."** (1) **Prompt fix**: the real root cause was the generation prompt letting the model dump every ingredient's prep into one opening block regardless of when it's actually used — raw seafood was being prepped 20+ minutes before it's added, a real food-safety/freshness issue, not just an awkward flow. Fixed at the prompt level, not patched around: `_generate`'s prompt now explicitly tells Oracle to sequence prep like an experienced cook — only front-load ingredients that genuinely hold up sitting prepped (vegetables/aromatics/dry goods/marinating meat), and give anything quick-to-prep and used near the END of cooking (raw seafood/fish especially, delicate garnishes) its own separate step positioned shortly before it's actually used, never bundled into the opening block. (2) **Context-aware Oracle overlay, real new architecture**: the floating 🔮 overlay button (`oracleControl`, already reachable from any page since G41) had zero awareness of what was on screen. Built a real, generic `registerContextProvider(key, fn)` registry on `oracleControl` (rule 8 — a shared mechanism any module can plug into, not a Cooking-specific hack) — `cookingOracle` registers a provider returning the currently-open recipe's real title/ingredients/method, gated on a real `_recipeCardOpen` flag (set true when the card opens, cleared via the popup's own `onClose` — never a stale reference lingering after the card closes). The overlay now silently prepends this real context to the text actually sent to Oracle, while the visible chat bubble still shows Alex's own exact typed words unchanged — so "the method is poor, why prep seafood before anything" lands already knowing which specific recipe/method it refers to, without him having to restate it. Verified via a real headless-Chromium test: the generation prompt carries the new sequencing rules; no recipe open → zero context prefix; a recipe open → the real sent text carries title/ingredients/method while the visible bubble stays exactly what was typed; closing the card correctly clears the context. 5/5 assertions pass, zero console errors. `node --check` clean, re-minified, cache-bust bumped `20260913k`→`20260914a`. **Untested by Alex's own hand yet.** **Real "close the loop" follow-up, same day, immediate — Alex asked directly whether a critique typed into the overlay could actually change the open recipe card, not just be discussed: it could not, so this was built next.** Confirmed spec, Alex's own exact words: "Yes let me confirm changes first, but make sure only changes are shown, not the whole thing, so I can make a quick informed decision." Real design fork resolved per rule 5: Oracle returns the FULL revised recipe (reusing generation's own `RECIPE_JSON:` shape under a new `RECIPE_UPDATE_JSON:` trailer) and the client code-COMPUTES the diff against the real current recipe object (`cookingOracle.logic._diffRecipe`, ingredients matched by normalized name, steps by normalized description so a pure reorder never reads as delete-then-add) — never trusting Oracle's own self-reported account of what changed. A new, real PERSISTENT listener (`logic._listenForRecipeUpdates`, on the same shared `oracle:response-scanned` hook `DIRECTOR_CHOSEN:`/`EDL_JSON:`/`ORACLE_ACTION:` already use, rule 8 — deliberately not a one-shot capture like generation's own, since a critique can land as any later overlay message) only opens a real, distinct confirm popup (`ui._showRecipeUpdateConfirm`) when `diff.hasChanges` is genuinely true, showing ONLY the added/removed/changed ingredients and steps, never the whole recipe — matching Alex's literal words. Accept mutates the SAME live recipe object the open card's own closures already reference (a real `self._applyRecipeUpdate` function-to-module bridge, set while the card is open, cleared to `null` on close) and refreshes the ingredient list/step list/title IN PLACE — no popup close-reopen, so scroll position and any running step timer survive. `oracleControl._mirrorReplyToOverlay` also strips the raw `RECIPE_UPDATE_JSON:` trailer so the JSON blob never dumps into the visible overlay chat bubble. Verified via a real headless-Chromium test exercising the actual `RPGACE.hooks.fire` pipeline: 7/7 real assertions pass (context instruction present, diff-only popup on a real change, zero unchanged content shown, Accept refreshes the live card in place, raw trailer never leaks into the overlay bubble, a no-op update opens no popup, closing the card clears the apply-bridge so a late reply can't touch a closed card), zero real console/page errors. `node --check` clean, re-minified, cache-bust bumped `20260914a`→`20260914b`. **Untested by Alex's own hand yet.**

- **Asian ingredients/nutrition expansion — real sourcing pass + a generalized regional-food importer built Sep 14 2026, zero data imported yet.** Alex's own ask: "I would like to add Asian products to the list of products and ingredients we have made, where is best to get a dataset for you?" Real, web-search-checked findings per region (South/Southeast/East Asia, all three confirmed in scope via AskUserQuestion): **India (IFCT 2017)** — official National Institute of Nutrition table, 528 foods, already re-published as a structured Kaggle CSV — the easiest source of the five. **Japan (MEXT)** — official Standard Tables of Food Composition, ~2,478 foods, real government Excel download with English food names — CoFID-shaped, no scraping needed. **Southeast Asia (ASEANFOODS/INFOODS)** — official, ~1,700 raw ingredients, PDF-only, no structured export found — a real future PDF-extraction job, not started. **Korea (RDA)** — a real official English search UI exists, no bulk download/API confirmed. **China (CFCT)** — no public download found at all; likely needs Alex to source it directly, same as CoFID's own real XLSX files arriving as chat attachments rather than a URL. **Real, confirmed blocker**: this session's own egress proxy blocks kaggle.com/*.github.io/mext.go.jp directly (same restriction class `import_cofid.py`'s own docstring already names for gov.uk/supabase.co) — not worked around, per standing policy. **Built anyway, real rule-8 dedup**: `scripts/import_food_composition.py`, a generalized sibling to `import_cofid.py` reusing its exact proven mechanism (fuzzy header detection, OR-hint column matching, fail-loud, batched SQL) for any regional source (.csv or .xlsx), writing into the SAME `cofid_foods` table under a new `source` tag — confirmed directly that `source` is a plain unconstrained `text` column, zero migration needed; `_lookupCofid` already scans the whole table regardless of source, so zero client code change is needed either once real rows land. Verified via 2 real synthetic fixtures (an IFCT-shaped CSV, a MEXT-shaped XLSX) before shipping — one real cosmetic bug (a wrong summary-path string) caught and fixed via the same test. `py_compile` clean. **Real, honest scope at the time this was written**: tooling only, ready to run the moment Alex provides a real IFCT/MEXT file — zero Asian ingredients actually in RPGACE yet. **This scope line is now stale — see the real, superseding update immediately below, same day.**

- **Real, superseding update, Sep 14 2026, same day, later pass — "Do korea and china too, dig deeper, and give me every website i need to visit for downloads." India and China are now genuinely imported, not just tooled-up.** Real, previously-unknown environment fact discovered while trying to satisfy the "give me every website" ask: `raw.githubusercontent.com` is reachable from this session's sandboxed egress even though `github.com`/`api.github.com`/`kaggle.com`/`unpkg.com`/`cdn.jsdelivr.net`/`data.go.kr` are all confirmed blocked (`403`/`connect_rejected`, direct `curl` tests) — a materially better outcome than the original ask, since it let real structured datasets be fetched and imported directly rather than only cataloged as links. **India (IFCT 2017)**: fetched `ifct2017/ifct2017`'s own real `compositions/index.csv` (542 real foods, 151 nutrient columns) via `raw.githubusercontent.com`. **Real IFCT unit bug caught before any insert, not shipped blind**: the file's own `enerc` column is genuinely in kilojoules, not kcal (real INFOODS convention — bare ENERC = kJ, only ENERC_KCAL = kcal), caught by a plausibility check (amaranth seed showing 1490 "kcal" — implausible for a raw cereal) and confirmed via a known-value cross-check (raw milled rice: 1491/4.184 = 356.4 kcal, matching its real ~356 kcal/100g) — fixed with a new `--energy-is-kj` flag (`/4.184` Atwater conversion) on the import script rather than shipped with the wrong unit. **A real, deliberate manual correction, disclosed here plainly per rule 7**: 14 real "Edible Oils and Fats" IFCT rows (coconut oil, ghee, corn oil, etc.) showed `enerc=0` in the raw source despite being ~100% fat — a genuine gap in the source data, not a parsing bug. Rather than import a technically-accurate-but-actively-misleading `kcal_per_100g=0` for pure oils (which would silently zero out any recipe's oil-calorie contribution, since `_lookupCofid` only excludes NULL kcal, never zero), these 14 rows were manually overridden to `884.0` kcal (the standard Atwater fat-energy constant, 100g × 8.84 kcal/g) before insert — a high-confidence correction grounded in a universal physical constant, not a guess, but a real deviation from the raw imported file, named here so it's checkable. **China (CFCT)**: fetched `Sanotsu/china-food-composition-data`'s real, community-compiled CSV (1677 rows, 1241 with real usable English names) via the same `raw.githubusercontent.com` path — needed `--name-col englishName` (the shared hint list would have wrongly matched the Chinese-language `foodName` column) and `--carbs-col CHO` (an explicit override built specifically to avoid a bare "cho" hint risking a false match against "cholesterol"). `parse_number()` needed zero changes — its existing regex fallback already correctly handles China's real missing-value tokens (`'Tr'`/`'—'`/`'un'`) and footnote-marked values (`'899*'` → 899.0). **Real, live-verified final counts** (`select source, count(*) from cofid_foods group by source`): `cofid`=2887, `cofid_legacy`=393, `ifct_india`=542, `cfcd_china`=1241 — **5063 total real foods now in `cofid_foods`**, zero client code changes needed (`_lookupCofid` already scans the whole table regardless of `source`). **One real, small, honestly-flagged discrepancy**: the China source had 1242 usable rows at extraction time but only 1241 landed — a genuine off-by-one not tracked down further this pass (proportionate to its size, rule 11), not silently smoothed over. **Korea and Japan stay real website-only gaps, dug deeper as asked but not imported** — Korea: no GitHub-hosted structured mirror exists; the real official path is MFDS's Open API (`data.go.kr`, item id 15127578, base `apis.data.go.kr/1471000/FoodNtrCpntDbInfo02`, needs free registration) or the RDA's English search UI (`koreanfood.rda.go.kr/eng/fctFoodSrchEng/engMain`), both blocked from direct fetch by this session's own egress. Japan: a real GitHub JSON mirror exists (`katoharu432/standards-tables-of-food-composition-in-japan`) but is Japanese-language-only with no English food names, unusable for RPGACE's English-word `_lookupCofid` matching without a real translation step not attempted this pass — the real recommended path is MEXT's own official English-paged 2015 Excel table (mext.go.jp, confirmed blocked). Southeast Asia stays PDF-only (ASEANFOODS, `inmu2.mahidol.ac.th/aseanfoods/composition_data.html`), same as originally found. `scripts/import_food_composition.py` gained real, disclosed extensions this pass — `--name-col`/`--group-col`/`--kcal-col`/`--protein-col`/`--carbs-col`/`--fat-col` explicit column overrides (for when the shared fuzzy-hint list is unsafe) and `--energy-is-kj` (the real kJ→kcal fix above) — `find_header_row()` also extended to search on an override column, not just the shared hint list, so header-ROW detection still succeeds when the real name column is too generic for the hints (IFCT's bare `name`). **Untested by Alex's own hand** — same standing flag as every prior CoFID-family import.

- **Real, superseding update, Sep 14 2026 (continuation session) — Japan (MEXT) and Southeast Asia (ASEAN FCD) both genuinely imported now, both real Alex file attachments (chat uploads, not fetched links).** Alex provided the real official MEXT 2015 Standard Tables of Food Composition (Seventh Revised Edition) xlsx and the real official ASEANFOODS FCD V1 2014 PDF directly. **Japan (MEXT)**: the xlsx needed a real preprocessing step before `import_food_composition.py` could touch it (`scripts/prep_mext.py`, one-time, not a generalized script) — mapped 18 real, web-search-verified official MEXT food-group codes to English names, and stripped MEXT's own documented parenthesis-estimated-value convention (`"(12.5)"` = a real calculated/estimated value, not missing). **Real data-quality issue found and excluded, not imported blind**: one row had a placeholder food name (`'－'`, a bare full-width dash) — confirmed via a full-dataset scan as the only such row across 2191, excluded rather than guessed at. Final verified count: `select count(*) from cofid_foods where source='mext_japan'` = **2190**. **Southeast Asia (ASEAN FCD)**: real, harder extraction — plain-text PDF extraction (`pypdf`/`pdfminer`) was confirmed via direct spot-check to GARBLE adjacent numeric table cells with no space between glyphs (e.g. a real "557 2.2 23.4" rendered as "5 5 72 . 2 2 3 . 4"), a genuine silent-corruption risk if used blind — switched to `pdfplumber`'s character-position-based table extraction instead, which correctly isolated the same cells cleanly, verified by direct comparison on the same row. `scripts/extract_asean.py` (new, ASEAN-specific, not a generalized script — the PDF's own 26-column table with 3 space-joined 7-value blob cells is a one-off shape) parses "The Concise ASEAN Food Composition Tables" (PDF pages 34-66 of the "Electronic version 1, February 2014" edition), resolves the real documented `<INFOODS>` tagname convention (parens = estimated value, `T`/`Tr` = trace, `-` = no analysis, and a real, initially-mishandled `Np` suffix meaning "presumed zero" — confirmed from the PDF's own page-30 "Signs, symbols and abbreviations" key, fixed before shipping since it was silently dropping ~62 real, legitimate zero-carb values as missing). **A real, honest scope finding, disclosed not smoothed over**: the source text (page 21) describes the original 2000 print edition as having "1,746 food items" — this specific 2014 electronic PDF genuinely contains far fewer, cross-validated by two independent methods (table-cell extraction and a raw-text food-ID regex scan, both converging on ~610-620 real distinct items) — this is a real property of the specific PDF Alex provided, not an extraction bug, and is stated here plainly rather than the smaller real count being quietly presented as if it were the full 1,746. **A real, small hand-recovery pass, disclosed**: 8 rows were genuinely unrecoverable by the automated table extraction (a rare pdfplumber column-merge artifact on those specific rows) — the raw text for each was inspected directly; 5 had fully clean, legible values and were hand-entered (Duck blood/Grouper/Sheat-fish/Jelly w: coconut cream/Toddy palm in syrup), 1 more (Amaranth) and 2 more (Chicken thigh/wing) were recovered from a separate blob-token-count anomaly the same way; 3 rows (2 with garbled/unrecoverable food names, 1 with only ambiguous partial values and nothing of real substance) were excluded rather than guessed. Final verified count: `select count(*) from cofid_foods where source='asean_fcd'` = **612**. **Real, live-verified grand total across all 6 sources** (`select source, count(*) from cofid_foods group by source`): `cofid`=2887, `cofid_legacy`=393, `ifct_india`=542, `cfcd_china`=1241, `mext_japan`=2190, `asean_fcd`=612 — **7865 total real foods now in `cofid_foods`**, zero client code changes needed (`_lookupCofid` already scans the whole table regardless of `source`). **Korea stays the one remaining real gap** — same finding as before this pass, no structured source found, MFDS's Open API / RDA's English search UI both blocked from this session's egress; would need Alex's own hand the same way MEXT/ASEAN's raw files did. `node --check` clean on both new scripts (`py_compile`). **Untested by Alex's own hand** — same standing flag as every prior CoFID-family import; this pass adds a genuinely new source-quality risk class (PDF table-cell extraction) worth a real spot-check in the live recipe UI before fully trusting it at scale.

- **Real, superseding update, Sep 14 2026 (same continuation session, immediate follow-up) — real cross-source deduplication run against all 6 `cofid_foods` sources, Alex's own direct ask: "I would like all these rows to be deduplicated so all ingredients have one row with all columns in tact."** Real interrogation first (2 forks, both AskUserQuestion-confirmed on the recommended option) rather than guessing at a genuinely consequential, destructive-if-wrong request against a table with **zero Supabase backup/PITR**: (1) match rule = exact name match only (case/whitespace-insensitive), never a fuzzy/AI-judged "conceptually the same food" match — different regional cultivars/prep states of, say, "rice" genuinely have different real nutrition, and a wrong fuzzy merge would silently corrupt data with no way to recover it; (2) conflict rule = keep ONE authoritative row per duplicate name, but preserve every other region's real values rather than discard or average them. **Real evidence gathered before touching anything**: exact-name grouping (case/whitespace-insensitive) found **109 real duplicate groups, 231 rows involved, 122 rows to remove** (~3% of the then-7865-row table) — mostly genuine same-name overlaps across regions (e.g. "Honey" in both `cofid` and `mext_japan`: 288 vs 294 kcal — real, both correct, genuinely different honeys). **Built**: a new, purely additive `cofid_foods.regional_variants` jsonb column (zero destructive schema change) holds a real array of every non-winning region's full row (`source`/`food_name`/`food_group`/`kcal_per_100g`/`protein_g_per_100g`/`carbs_g_per_100g`/`fat_g_per_100g`) — literally "all columns in tact," per Alex's own words, just consolidated onto one canonical row instead of scattered across several. Winner selection uses a real, disclosed source-priority order (`cofid` > `cofid_legacy` > `ifct_india` > `mext_japan` > `cfcd_china` > `asean_fcd` — UK-official first since Alex is UK-based, then other official-government tables, community-compiled and older regional-compilation sources last) — a genuine judgment call, but a low-stakes one, since nothing is actually lost either way: the "wrong" pick still keeps every real value via `regional_variants`. **Verified at every step, not assumed**: the UPDATE (winner ← losers as `regional_variants`) confirmed exactly 109 rows tagged, matching the 109 real groups; a direct spot-check on "Honey" confirmed the real MEXT values (294 kcal, 0.2g protein, 79.7g carbs, 0g fat) landed correctly on the `cofid` winner row; the DELETE removed exactly 122 rows (7865→**7743**, matching the pre-computed count exactly); a fresh post-delete duplicate scan confirmed **zero remaining exact-name duplicate groups**. Real final per-source counts: `cofid`=2885 (-2), `cofid_legacy`=393 (unchanged), `ifct_india`=529 (-13), `cfcd_china`=1185 (-56), `mext_japan`=2174 (-16), `asean_fcd`=577 (-35) — losses sum to exactly 122. **Zero client code changes needed** — `_lookupCofid` reads `kcal_per_100g`/`protein_g_per_100g`/`carbs_g_per_100g`/`fat_g_per_100g` from whichever row it matches; the new column is purely additive and inert to every existing read path. **Real, honest scope note**: this catches only EXACT-name duplicates — a genuinely different-but-conceptually-same food (e.g. "Rice, white, polished, raw" vs "Milled rice, raw") is deliberately left as two separate real rows, per the confirmed match-rule fork above, since collapsing those would risk exactly the silent-nutrition-corruption Alex's own request was trying to avoid, not cause. **Untested by Alex's own hand** — same standing flag as the rest of this import effort; `regional_variants` itself is not yet surfaced anywhere in the Cooking UI (a real future nice-to-have, not built this pass — Alex didn't ask for it to be shown, only for the data not to be lost).

- **Real, superseding update, Sep 14 2026 (same continuation session, immediate follow-up again) — real sub-grouping columns added, Alex's own direct ask: "Now deduplicate food names by how they are cooked, brand etc as sub groups to make values in one area for ease of navigation."** A genuinely different operation from the dedup bullet immediately above — this is non-destructive categorization, never another merge; zero rows removed, zero rows changed shape. Real 2-round `/interrogation` (both AskUserQuestion) resolved scope before touching data: (1) data-layer grouping columns only, not a new browsable ingredient-picker UI (Alex didn't ask for new UI, only "ease of navigation" of the data itself); (2) brand — real evidence checked first: none of the 6 government/regional food-composition sources (CoFID/IFCT/MEXT/CFCD/ASEAN FCD/cofid_legacy) carry real brand data at all, only a separate, unrelated Open Food Facts fallback table does — Alex confirmed "1 for now, then brands if info is there," i.e. skip brand population now, revisit only once a real brand-bearing source exists. **Built**: 2 new purely additive columns, `base_ingredient` (mechanical, `trim(split_part(replace(food_name, '，', ','), ',', 1))` — handles both ASCII and the real full-width Chinese comma seen in `cfcd_china` rows) and `prep_state` (mechanical, a 30-branch priority-ordered `CASE`/Postgres word-boundary regex, compound/specific cooking terms — stir-fried/deep-fried/pan-fried/freeze-dried/sun-dried/oven-dried — checked before their generic counterparts, per this project's own standing "hyphens are boundaries, keyword lists need compound phrases" landmine discipline) plus a 3rd column, `brand` (schema added, deliberately left NULL project-wide — no real source data exists to populate it yet, not a bug). Zero AI/Oracle calls (rule 11 — purely mechanical SQL text extraction, no judgment call needed for either field). **Verified at every step**: a dry-run count matched the real post-UPDATE count exactly (7743/7743 rows tagged with `base_ingredient`, 4391/7743 = 56.7% tagged with a real `prep_state`); a live "Chicken" spot-check confirmed correct grouping and full-width-comma handling across sources (e.g. `cfcd_china`'s `Chicken，braised，whole` correctly extracted `base_ingredient='Chicken'`/`prep_state='braised'`). **Real, honest scope note, not hidden**: `prep_state` is deliberately left NULL for the other 43.3% of rows rather than defaulted to `'raw'` — many of those rows are processed-but-unstated items (flour, oil, spices, whole nuts) where guessing "raw" would be actively wrong; a food_group breakdown confirmed this spans every group, a real property of these sources' own naming conventions, not a coverage bug. `idx_cofid_foods_base_ingredient`/`idx_cofid_foods_prep_state` indexes added alongside. Zero client code changes needed — `_lookupCofid` is untouched, these columns are purely additive and inert to every existing read path. **Untested by Alex's own hand** — same standing flag as the rest of this import effort; no UI currently surfaces `base_ingredient`/`prep_state` browsing (real future nice-to-have, not built this pass, per the confirmed data-layer-only scope).


### The Galaxy Development Framework — full G1–G109 build narrative, Aug 13–26 2026 (verbatim, includes the (c)/(d)/(e)/(f) layer definitions AS THEY STOOD when archived)

### The 4 layers (c/d/e/f), built out Aug 11 — (e) added and (e)→(f) renumbered same day, later pass
- **(c) Oracle self-awareness** — `oracleAppGrounding.SELF_KNOWLEDGE` (rpgace_core.js), already a standing rule-6 obligation ("updated in the same session as this Current State section"), now formally named as A17's own (c) layer — this session ("Orchestrator CC" per the Total-systems extension above) keeps it current as the real rules Oracle answers from.
- **(d) Smoke-test + Error Log — 2 real docs, added Aug 11/Aug 12.** `smoke_test.html` (8th oversight artifact): current confirmed/unconfirmed state of every real function, grown from real completed work, not built empty. `error_log.html` (10th, added Aug 12, real new): every real error any AI/code in RPGACE Total systems has reported, deduplicated by normalized signature (repeats increment a counter on the same row, never mint a new one — same discipline as `intelDedup`), backtracked to what likely broke upstream, every real fix attempt tracked until one works, resolved history kept permanently. The two mirror each other live — an active `error_log` row also renders in a real "🔴 Just Failed" top group on `smoke_test.html`; a `smoke_test_items` row can carry `needs_confirm_highlight=true` (a real, distinct yellow/black-border/💡 visual) once a fix lands and awaits Alex's hand-test, clearing automatically (and resolving the linked error) the moment he confirms it. See both docs' own entries below and the new session-start check. **`session_lessons.html` (11th oversight artifact, added Aug 15) — a real, distinct doc, not a 3rd copy of the same idea**: `error_log.html` tracks runtime/code errors; `patch_notes.html` tracks what shipped; `session_lessons.html` tracks the PROCESS — real `/misunderstanding`s, `/drift` findings, and genuine build-time obstacles, each card recording the obstacle, how the solution was actually reached (the reasoning, not just the outcome), the solution, and any resulting standing rule. Hand-authored per session like `patch_notes.html` (not Supabase-live), appended via `/Bedtime` per Alex's own explicit ask — real material for his own and Orchestrator CC's post-session reflection, not just in-chat narrative that evaporates. Wired into `.claude/skills/update-logging-system/SKILL.md`'s dependency map as artifact 17. **Same day, real follow-up ask**: every card now opens with the real prompt scope that was active when the episode happened, before the obstacle/reasoning/solution blocks — new CEO SKILL.md Loop 1 Step 6, so a later reader can see what was actually asked versus what real evidence narrowed it down to.
- **(e) Future Integrations (future tense) + Achiever (past tense) — both halves real and built as of Aug 20/21 2026.** Real, new Aug 11 layer, added the same day `/colourgradient` gained its 4th color. `future_integrations.html` is the FUTURE-tense half: 🔵 blue (idea)/🔴 red (planned)/🟡 yellow (mid-build) content that would otherwise sit in Tier (a)-(d) and blur "actually true" with "not yet real" — see its own bullet in the artifact list above. **The PAST-tense half shipped Aug 20/21 2026 as `achiever.html` (G54)** — Alex's own words: "log a new oversight doc called achiever, which will establish what in a planned or built update will turn a green into a new colour brown... which will archieve anything that used to be true that is no longer true." A real, new `/colourgradient` 6th color (🟤 brown) — genuinely distinct from purple: purple tracks BROKEN CODE, brown tracks a STALE CLAIM (a count/rule/description that was true and a later real change made false, while the underlying feature may still work fine). Backed by a new `achiever_archive` Supabase table, real-seeded with the worked example that proved the need for it (`TOTAL_ZONES` sitting stale for 2 days after the Aug 18 River XVII split, found by G57's own file-count sanity check). Structurally mirrors `future_integrations.html` exactly (same sidebar/section/group/card conventions, live-rendered from Supabase) per Alex's own direct ask — both are the same real oversight-doc tier, opposite directions in time. **Real, mandatory removal step, Alex's own direct correction**: a brown finding isn't just corrected in place — it's actively removed from every Tier (a)-(d) doc asserting it, including a `smoke_test_items` row that exists solely to test a now-false claim (deleted outright, not just re-flagged, since there's nothing real left to verify). Full procedure: `.claude/skills/colourgradient/SKILL.md`'s own brown entry; wired into `update-logging-system` as artifact 18.
- **Real, ratified `/CEO` plan, Aug 13 — "RPGACE Total Systems Galaxy Map."** A new drill-down, Assassin's Creed-Valhalla-styled graphify/Obsidian visualization of RPGACE Total Systems, promoted per Alex's own real ask into a SECOND real truth source sitting alongside Tier (b) — read by Alex with Tier (a), by this session with Tier (b), specifically so a real `/misunderstanding`/`/drift` gap has less surface to occur on before it happens rather than being caught after. Also establishes a planned "trickle-down/up" system: a real upstream/downstream change gets cross-referenced against the galaxy map + Tier (b) for integration friction, generating a compiled report of suggestions + errors for Alex's own review — wired into `/CEO` Loop 2 itself (colourgradient → this cross-check → `/drift`, every plan item, once built). Real Supabase datasheet: `ceo_plans` (`name='RPGACE Total Systems Galaxy Map'`, `basis='ratified'`) + `ceo_plan_items` (G1-G14). **G1-G5, G10, G12, G14 (facet 1) are real and shipped**: G1's edge-interaction taxonomy (live in `graph.html`/Obsidian); G2 Level 0 (`graphify-out/galaxy_map.html`); G3 Level 1 river drill-down; G4 Level 2 module+dashboard-card drill-down; G5 Oversight-connection badges + river-to-river ring; G10 `/perspective`; G12 `/cartographer` Baseline Reconciliation; G14 facet 1 — a real Level 3 (`graphify-out/galaxy_map_level3.html`), all 44 real Level-2 modules now have their own function-call-chain drill-down (426 real functions, 422 real call edges), reached by clicking a module's 🔽-badged node. G6 (this bullet's own "truth-source promotion") is itself now real, current fact, not a future item — see below. G7 (trickle-down/up system) is real and being worked (the `/cartographer` skill + `system_map_flags` table exist; a retroactive scan across current Total-systems state is the remaining real piece). G8 (`/CEO` Loop 2 wiring) is real but needs a live hand-test before it's called done. G9 (Consumer/Developer visibility tiering) stays deliberately deferred, per Alex's own explicit call. G14 facets 2-4 (smoke_test.html group/level columns, manual.html Button/Chain fusion) are real, scoped, NOT built yet. Full record: `records/2026-08/galaxy_map_ceo_loop1_spec_paranoia_2026-08-13.txt`. **G6 corrected to green same day** — the truth-source-promotion doc updates this row itself asks for were already shipped in the commit that logged this bullet (interconnection_map.md/ai_tooling_and_rules_map.md/minotaur_map.html/manual.html/this file's own layer (f) all declare the Galaxy Map a real second truth source); the Supabase row had drifted stale (still `red`), fixed via direct `ceo_plan_items` UPDATE, not silently left.
- **Real `/misunderstanding`-diagnosed connectivity + crossing-reduction + navigation pass, Aug 13 (same day, real G14 continuation, Alex's own direct ask — "why is RPGACE not connecting," full `/CEO`+`/paranoia`+`/restructure`+`/drift`+`/Engineer` discipline).** Real GODMODE evidence, not a guess: River III's ~10-module "dead-end spoke" appearance (Alex's own screenshot) traced to `compute_intra_river_flow()` having exactly ONE detection signal (`RPGACE.modules.X()` direct calls) — River III's real architecture instead leans on 3 OTHER documented RPGACE idioms (a `window.callOracle`/`sendChat` wrap chain; a shared `RPGACE.utils.sendToOracle`/`fillGaps` convergence; direct `#chat-input`/`#send-btn` DOM triggering) the detector had never been built to see. **Fixed**: `compute_intra_river_flow()` now checks all 4 signals, each edge tagged `direct`/`wrap`/`utility`/`dom` so the map draws a literal call differently (solid) from an inferred one (dashed+labeled) — River III's isolated count dropped 10→3, the 3 real remainders flagged on-diagram (`⚠️ no link within River N`), not hidden. **Real, honest full re-audit, all 16 rivers**: 44 real modules total, 13 genuinely isolated after all 5 detection methods (4 intra-river + the new Level-3 cross-module backdoor check) — most likely a main.js-side relationship (hooks fired from main.js, DOM wiring outside rpgace_core.js) this pass's evidence can't reach, flagged as real follow-on, not fixed blind. Every Level-2 terminal now shows an explicit destination (`🖥️ Output: RPGACE UI` / `→ River N` / `🔮 external AI provider` / `⚠️ No declared downstream`), per Alex's own "if it doesn't [flow somewhere], it's not the true end of the map" rule. **Crossing reduction**: standard Sugiyama/Eades barycenter heuristic (Level 2/3 rank columns) + a real 2-opt local search (Level 1's ring order) — real, MEASURED result: 15→6 crossings on Level 1 (60% reduction), after a DFS-adjacency heuristic was tried first and found to measure WORSE (17) and was discarded per rule 4 rather than shipped as a false fix. One real remaining structural crossing (River XI) needs Alex's own call on the rank-to-X-position layout he originally directed, not changed blind. **Navigation**: breadcrumb bar on all 4 levels, a real "← Back to River" button per Level-3 module, a river-grouped module-switcher panel (was one flat 44-tab wall), and 38 real cross-module "backdoor" links (`compute_cross_module_function_calls()`, a new global function-level call detector) jumping Level 3 directly between modules — even across rivers — without climbing back up. **A real, unrelated bug found via idempotency verification**: `compute_function_rank()`'s DFS iterated a hash-randomized `set()`, meaning a fresh process re-run of `galaxy_map_level3.py` against the identical source could silently produce a different layout — a genuine correctness gap in a script that claims to be deterministic, predating this pass. Fixed with `sorted()`; re-verified reproducible across 3 fresh process runs. Full record + the discarded-DFS finding kept in-code, not erased: `records/2026-08/galaxy_map_connectivity_crossing_nav_2026-08-13.txt`. **Untested by hand/headless-Chromium this pass** — every claim above is verified by direct code/data evidence and exact math (div-balance, `count_crossings()`, idempotency), not by actually looking at the rendered page.
- **Real, permanent "Alex" UI-actor bubble, Aug 13 (same day, 3rd pass, real G14 continuation, Alex's own direct ask, "where it makes sense" across all 4 levels).** Two real, shared mechanical signals (`UI_OUTPUT_PATTERN`/`UI_INPUT_PATTERN`, `graphify_river_group.py`): real DOM/popup-rendering evidence and real button/input-wiring evidence — 132 of 426 real functions (31%) carry output evidence, 101 (24%) carry input evidence. **Level 3**: a real, permanent per-module `🧑 Alex` bubble, fixed position on every section, dashed edges to/from every function carrying either signal — "what combines with my input to go to another function" is satisfied by the already-drawn same-module call edges (no duplicate edges needed). **Level 2**: same bubble rolled up to module granularity (34/44 modules output, 30/44 input), one per river-with-modules section. **Level 1**: a real, deliberately lighter aggregate — a `🧑` badge on 9 of 16 rivers (a full bubble would be real noise at 16-node granularity, same judgment call already governing Level 1's externals/skills rings). **Level 0**: NOT rebuilt — the existing "Human Gate — Alex" node (built earlier this session, already using the exact same `#E25454` accent) is the real coarse-granularity version of the same recurring actor (Tier-3 confirmation vs. this pass's fine-grained DOM/button mapping); cross-referenced in its own note text rather than duplicated. **Real rule-8 catch made building this**: the per-function body-splitting logic was about to be pasted a 3rd time — factored into one shared `_function_bodies()` helper, existing callers refactored to use it, verified behavior-preserving (same 81 cross-module-call / 19 beatLog-edge counts before and after). All 4 regenerated files re-verified div/svg-balanced; Levels 2/3 re-confirmed idempotent. Full record: `records/2026-08/galaxy_map_alex_ui_actor_bubble_2026-08-13.txt`. **Untested by hand/headless-Chromium** — same standing gap as every Aug 13 galaxy-map shipment this session.
- **River-boundary "next function" stubs, `init` removed at Level 3, rank-band sub-pages, Aug 13 (same day, 4th pass, real G14 continuation).** A real clarifying question was asked and answered (rank-band sub-pages vs. a new Level 4 — Alex confirmed rank-band). **River stubs**: new `attribute_river_connection_function()`/`find_wrap_installer_function()` (`graphify_river_group.py`) resolve a river-to-river connection to the real specific function it lands on (a traced cross-module call, or a note-keyword-gated wrap-installer lookup — e.g. River III→IV correctly resolves to `bookworm._patchChatTrigger`, matching its own real "special prefix diverts the message" note) — a real over-attribution bug (River II→IV would have wrongly reused the same function without the note-gate) was caught and fixed before shipping. Honest coverage: 5 of 25 real connections attributed, 20 correctly show none. Level 2 draws a small dashed preview stub at both ends of a connection, deliberately unwired from the main flow graph. **`init` removed**: Level 3 no longer renders `init` as a node (43 of 44 modules affected) — its own real callees become the true depth-0 entries, the legend explains what `init` actually is (bootstrap plumbing, never called by real code inside the module), and a real "⬅ from River N" badge stands in its place wherever the same river-stub evidence lands on that specific function. **Rank-band sub-pages** (Alex-confirmed option): 8 of 44 modules with >15 real functions split into up to 3 depth-balanced bands (`_split_into_bands()`/`_render_band()`), real cross-band edges shown as dangling stubs (never dropped) — verified against real depth-distribution data before building, not assumed even. A real class-collision bug (band-tabs sharing `.tab` with the top-level module switcher, which would have broken every band-tab click) was caught and fixed before shipping. All 4 files re-verified div/svg-balanced; Levels 2/3 re-confirmed idempotent. Full record: `records/2026-08/galaxy_map_river_stubs_init_bands_2026-08-13.txt`. **Untested by hand/headless-Chromium** — same standing gap.
- **Real `/misunderstanding` resolved (authGate/pathRouter) + Alex-bubble precision fix + Level 4 scoped, Aug 13 (same day, 5th pass).** Alex's screenshot showed `authGate.verify()` (real password-check + external `/api/auth` call, genuinely leads to the dashboard on success) reading "0 shown to me · 0 buttons I press" on the Alex bubble — confirmed root cause: the whole Galaxy Map pipeline has only ever read `rpgace_core.js`; the real trigger (`window.RPGACE_verifyPassword`, the login form's own callback) lives in `main.js`, invisible to it. Same root cause independently confirmed for `pathRouter` (River II, "a complete mystery") — real `RPGACE.hooks`-driven URL-sync plumbing calling `main.js`'s own `showPage()`, zero DOM/button code of its own. **Confirmed as the literal, evidenced version of "galaxy map is only mapping out backend."** **Level 4 scoped** (a real clarifying question, given genuine ambiguity): click a dashboard card → shows the real frontend flow (page/popup, DOM evidence, buttons, where it leads), with real links back to Level 3's backend functions — directly motivated by the authGate/pathRouter gap. **NOT built this pass** — a real, separate, large deliverable (needs the main.js/hooks detection extension as a prerequisite), logged as `ceo_plan_items` G15, explicitly flagged as the next real work, not silently deferred. **Alex-bubble output-edge precision fix**: real critique comparing 3 screenshots (`phylumPath`→Alex "makes sense," several River V modules "shouldn't"). Two real hypotheses tried and DISPROVEN against his own confirmed `phylumPath` example (isolated-flag gate — `phylumPath` is also flagged isolated; strict `-> X module` regex gate — `phylumPath`'s real card citation is "...else phylumPath page browse," doesn't match) — both reverted, not shipped wrong (rule 4), his real rule asked for directly. New `dashboard_card_primary_module()`: a module's OUTPUT edge now requires it be a real card's own PRIMARY/fallback destination (not a shared-sibling citation, not a via-text-demoted one, e.g. "QoL layer only... not the core") — verified against all 6 real card examples, matches his confirmed cases exactly. Real effect: 34→12 modules keep a precision-confirmed output edge. `galaxy_map_module.html` re-verified div-balanced and idempotent. Full record: `records/2026-08/galaxy_map_alex_ui_actor_bubble_precision_2026-08-13.txt`.
- **Real `/Routine`-locked main.js/`RPGACE.hooks`-aware detection extension, Aug 14 — G15's own stated blocking prerequisite, now built.** Two real, additive mechanisms in `graphify_river_group.py`: `compute_mainjs_window_bridge()` (a real 2-hop chain resolving RPGACE's own documented override convention — main.js defines an original/stub, a module's `init()`/`_intercept()` reassigns `window.X` to the real implementation — confirmed for `authGate`: `index.html:61` onclick="checkPassword()" → main.js `checkPassword()` → `window.RPGACE_verifyPassword` → `authGate`) and `compute_hook_signal_edges()` (real `RPGACE.hooks.fire()`/`hooks.on()` module pairing, including rpgace_core.js's own real "FUNCTION WRAPPERS" section — its own comment: "Do NOT patch main.js — wrap here instead" — which bridges 6 real main.js UI functions, e.g. `checkPassword`→`'rpgace:login'`, `showPage`→`'page:show'`, to named hooks `pathRouter` listens on directly). A third fix (`_module_def_line_match()`) taught the Level-3 function-inventory parser to recognize a real bare, zero-indent `window.<Name> = function` module-scope statement — `authGate`'s own `window.RPGACE_verifyPassword` bridge was invisible as a Level-3 NODE entirely (not just missing UI evidence), the ONE such case in the whole file. **Measured, verified result** (real before/after HTML diff, not assumed): `authGate` Alex-bubble 0/0→0/1, `pathRouter` 0/0→3/1 — both Aug 13 "mystery" modules resolved with real evidence. Modules with a fully 0/0 Alex-bubble: 10→6 (4 resolved: authGate/pathRouter/ciAutoPropose/encSync; the remaining 6 are real, evidence-checked pure-logic/Oracle-grounding-wrap modules with no UI touch — not a remaining gap). Module-to-module isolation (no intra-river edge + no cross-module backdoor + no hook evidence, freshly measured this pass): 11→3 (agentsIntoOracle/authGate/intelBatchList remain genuinely isolated at that specific relationship grain — real, honest, not silently claimed fixed). **Real, honest negative finding**: wiring hook-signal edges into Level 2's own same-river check changed nothing (20 "no link within River N" flags before and after) — zero same-river hook pairs exist anywhere in the 44-module codebase; `RPGACE.hooks` is confirmed a purely cross-river mechanism here, a real structural fact, not a detector bug. `galaxy_map_level3.py` also gained a real, evidence-consistent fix attributing `init`'s own UI signal (e.g. `pathRouter`'s real `popstate` listener, which lives inside `init`'s own body) onto exactly ONE real anchor function (never fanned across every depth-0 entry, which would have inflated the bubble count into implying several independent real touches). All 4 levels regenerated, idempotency re-verified (2 fresh runs, identical md5), div/svg-balance clean. `ceo_plan_items` G4/G15 evidence updated to match. Full record: `records/2026-08/galaxy_map_mainjs_hooks_detection_extension_2026-08-14.txt`.
- **Real G15 (Level 4) shipped, same session, same day — Alex confirmed the "no same-river hook pairs" finding above ("the code doesn't actually have any rivers, it was a concept i came up with for minotaur map") and said "yes go g15."** New `graphify-out/galaxy_map_level4.html` (`scripts/galaxy_map_level4.py`): one section per real dashboard card, tracing `dashDeck.MODULES`' own real `go:` trigger (never DASHBOARD_CARDS' own hand-written `via` label) into what it actually opens — a real popup call or a real page navigation. New `compute_dashboard_card_flow()`/`parse_dashboard_card_go()`/`compute_module_calls_with_aliases()` in `graphify_river_group.py`. **Real bug caught before shipping**: a bare `RPGACE.modules.X.Y(` regex silently missed taxonomy's own real conditional popup branch (`var rq = RPGACE.modules.taxonomyReviewQueue; ... rq._openCard();`) since `rq` is a local alias, this codebase's own established style — fixed with a real, generalized alias resolver, reused everywhere a cross-module call is detected in this file (rule 8), not a taxonomy-specific patch. **Real, confirmed pattern found by direct read**: 5 of dashDeck's own `_openX()` popup-openers (`_openBookworm`/`_openMorningBrief`/`_openGaps`/`_openPipeline`, plus `_openResearch`'s own different real shape) hand off to a real target module's own `_inject*()` function BEFORE opening the popup — that target module (`bookworm`/`morningBrief`/`knowledgeGap`/`contentProductionLive`/`researchTabs`) is the real content owner, not `dashDeck` itself, which is confirmed NOT one of the 44 real `RIVER_MODULES`-tracked modules. Level 2's dashboard-card diamonds are now real clickable links into their own Level-4 section; a real, separate gap found and fixed in the same pass — the SVG diamond only draws inside the flow-layout branch, so `oversight` (the one card whose sole river, XIV, has zero real registered modules) had no diamond and would have had zero real Level-4 link anywhere on Level 2 — fixed via the always-rendered text legend row instead. **Measured result**: 12 of 12 real dashboard cards built, 7 of 9 real popup-kind targets resolve to a real linked Level-3 page, all 12 cards have at least one real working Level-2→Level-4 link. Idempotency re-verified (2 fresh 5-file pipeline runs, identical md5), div/a/section-balance clean. `rpgace_core.js`'s Oversight popup gained a real Level-4 entry and `oracleAppGrounding.SELF_KNOWLEDGE` a real Aug 14 paragraph (same edit corrected the stale "426 real functions" Level-3 count to 427); `rpgace_core.min.js` re-minified, `index.html`'s cache-bust bumped. **Explicitly still open, not silently dropped**: G7 (`/cartographer` retroactive scan), G8 (hand-test `/CEO` Loop 2 live), the 3 remaining genuinely-isolated modules, and no real hand-test/headless-Chromium check on Level 4 itself yet — same standing gap as every Galaxy Map shipment this multi-day effort. Full record (same file, same-day continuation): `records/2026-08/galaxy_map_mainjs_hooks_detection_extension_2026-08-14.txt`.
- **G7/G8/G14 closed, same day, later pass — real 2nd retroactive scan + Loop 2 wiring reframe + smoke_test.html/manual.html facets.** Alex: "yep do them, want to see final product with all g steps that arent developer/consumer based" — G9 correctly stayed deferred. **G7**: a real, 2nd retroactive trickle-down/up scan found and fixed 4 real stale spots (a stale-open `system_map_flags` row whose real answer already existed; 3 real "426 functions"/"4 levels" mentions across `minotaur_map.html`/`ai_tooling_and_rules_map.md`/`interconnection_map.md`, corrected to 427/5; a real documentation gap in `system_map_spec.md` — Alex was never named as a real tier-4 actor even though the code already treats him as one, fixed with a clarifying addition, not a hierarchy renumber). **G8**: reframed honestly — no literal browser UI exists to hand-test (deliberately, per the Aug 12 fix removing all RPGACE-facing CEO surface); the real test is the pipeline itself firing correctly on real work, demonstrated repeatedly this session. **G14**: `smoke_test_items` gained real `galaxy_river`/`galaxy_level` columns (additive migration, backfilled for all 15 real rows, rendered as tags sourced from the Galaxy Map hierarchy); `manual.html` gained a durable button-catalog fusion note (a button's own Chain cell already names its real module — cross-reference against Level 3/4 directly, never a per-row link retrofit that would go stale) plus a real stale-content fix found along the way (the dashboard-deck row said "13 cards" and named a dead function, `_openVideoPipeline`, deleted Aug 6). Full record: `records/2026-08/galaxy_map_mainjs_hooks_detection_extension_2026-08-14.txt`.
- **Real full MODULE-scope G11 sweep + Rules-and-Workflows section, same day, later pass — Alex: "yes g11 and g13, and every obstacle and bug and solution/workaround and mapping/finding files and functions we found to map out galaxy map should now be included as rules and workflows of galaxy development framework. i need g-11 to also be part of galaxy development framework."** All 44 of 44 real `RIVER_MODULES`-tracked modules now have a real `/perspective` report (2 hand-written — `authGate`/`pathRouter` — + 42 generated via new `scripts/perspective_generate_modules.py`, pulling directly from the same already-computed real detection data the Galaxy Map itself uses, never fabricated). `perspective_reports`: 65 rows total (4 galaxy + 17 node + 44 module). Real, honest quality note, not hidden: the 42 generated reports are more mechanical/templated than the 2 hand-written ones — real and evidence-grounded throughout, but a documented tradeoff (exhaustive hand-writing would have cost enormously more for marginal quality gain). `.claude/skills/CEO/SKILL.md` gained a real "Rules and workflows" section — 11 concrete, reusable rules (R1-R11) extracted from every real obstacle/bug/workaround this whole Aug 13-14 build actually hit (cross-file idiom blindness, rivers-aren't-real-code, signal-attribution fan-out, the 2-hypotheses-then-ask discipline, idempotency verification, measured-not-assumed improvement claims, methodology-doc drift, stale-flag close-the-loop, the "hand-test" reframe, generator-script-at-scale, opportunistic nearby-fix discipline) — plus a real, explicit statement that G11 is now a formal, standing sub-effort of the Framework itself, not a sibling item. `ceo_plan_items` G11 stays honestly blue (module scope done; FEATURE scope — main.js's ~240 functions, index.html's 93 onclick handlers — real, explicitly not started); G13 stays blue too, gated on that same feature-scope work, with a real, deliberate decision NOT to auto-populate `smoke_test.html` with 44 new module-level rows (would work against its own "hand-tickable, grown from real completed work" design). `/drift`+`/restructure` check run inline: on the ratified baseline (G11 always part of the approved plan, no scope creep), no skeleton change (one new script following the exact existing generator-script pattern, one skill-file section extension). **Real, superseding correction, Sep 8/9 2026 (Cross-Doc Drift Check, `/Bedtime`): G11's own "FEATURE scope... not started" claim above is stale from this point forward.** Onclick-feature scope closed (47/47) the same Aug 20 `/Routine` continuation this file already logs elsewhere; the last real slice — main.js's legacy-block functions — closed Sep 8 2026 (176/176 real `perspective_reports` rows, `scope_level='mainjs_function'`, see that date's own patch_notes.html card). All 3 of G11's real coverage scopes (module/onclick-feature/legacy-function) are now complete; `ceo_plan_items` G11 moved blue→yellow (built and real, genuinely untested by Alex's own hand — the same standing flag as every prior `/perspective` batch, not a gap unique to this item).
- **First official smoke test populated + real, evidence-driven Oracle bubble shipped at Level 2/3, same session, same day — Alex: "lets do g11 checks, then we implement each line/edge that has been mapped out as one passage in smoke test that i can confirm working or not working... also i want the galaxy map to also include permanent bubble of oracle ai wrapper and other externals on each level... /CEO and /paranoia and /drift and /restructure with /Engineer as advisor."** G11 checks: spot-verified all 44 generated `/perspective` rows (real jsonb types, no truncation/corruption, no duplicates) — clean. **Smoke test**: `smoke_test_items` gained 56 real new rows (44 modules + 12 dashboard cards — confirmed granularity via AskUserQuestion, never one row per function, which would have made the list impractical to hand-click), generated by new `scripts/smoke_test_generate_v1.py`, each citing its own full real baseline (`perspective_reports`/`galaxy_map_level4.html`) rather than duplicating it. 71 total rows now, all `status='unverified'`, ready for Alex's own hand-confirm pass "when Oracle AI comes back." **Oracle bubble**: Alex's own real correction on the ask — "other externals don't have to be permanent, oracle too tbh... what we are doing will also help map those out" — confirmed via AskUserQuestion (Oracle first, evidence-driven not forced). New `compute_oracle_call_counts()`/`compute_module_oracle_call_count()` in `graphify_river_group.py` (real, grep-confirmed patterns: `sendToOracle(`/`callOracle(`/`fillGaps(`, 26 real call sites across 12 of 44 modules) feed a real `#9B59B6`/🔮 bubble at Level 3 (function grain) and Level 2 (module grain), same identity Level 0's own Oracle harness node already uses, each edge carrying a real action-count number. **Real bug caught by the standing balance-check discipline**: the first Level-3 implementation had a stray duplicate `</g>` tag, found immediately, fixed before commit. Idempotency re-verified across the full 5-level pipeline. **Explicitly NOT built this pass, honestly scoped and logged as new `ceo_plan_items` G16 (Oracle bubble, partial)/G17 (Level 5 Logic)/G18 (Level 6 Detailed Decision, deferred)/G19 (left/right zoom navigation)**: Oracle bubble at Level 0/1/4; other externals; Level 5's curated named-decision-point page; Level 6's exhaustive sweep; and the new real navigation principle Alex asked for (entry objects zoom UP to level N-1, terminal objects drill FORWARD into Level 5/6) — designed and written up as real rule R13 in `.claude/skills/CEO/SKILL.md`, not yet implemented in any renderer. `.claude/skills/CEO/SKILL.md` also gained R12 (evidence-gated bubbles, never forced-permanent).
- **G17 (Level 5 Logic), G18 (Level 6 Detailed Decision), G19 (left/right zoom navigation) all shipped, same session, immediate follow-up — Alex: "do g17 and g18, then g19."** **G17**: `scripts/galaxy_map_level5.py` — 7 real, curated "core logic" decision points (Oracle Mode 3-way toggle, taxonomyReviewQueue's pending-count branch, phylumPath's scored placement decision + its dedup-extend branch, oracleAppGrounding's grounding gate, contentProductionLive's primary-action lookup, beatLog's artist-phylum routing), each with a verbatim rpgace_core.js excerpt whose anchor is checked against the LIVE file at build time — a moved/changed anchor fails the build loudly, never shows stale code silently. **G18**: new `compute_function_branches()` in `graphify_river_group.py` — real balanced-paren extraction (not a naive regex that truncates nested parens) of every real if/else-if/else/switch branch a function's own body contains. 1088 real branch points across 44 modules, confirmed by direct count before the render script was written; `scripts/galaxy_map_level6.py` renders them module-grouped, exhaustive and mechanical (not narrated) — the honest counterpart to G17's curated subset. **G19**: every real Level-3 entry-point (🚪) function now carries a real "🔭 zoom out: Level 2" link to its own specific module node (never a generic back button); every real terminal (🏁) function that also has a G17 write-up gets a real "🧠 into the logic: Level 5" forward link, cross-referenced directly from Level 5's own DECISION_POINTS (rule 8) — honestly, only 1 of 5 real func-tagged candidates (`beatLog._addNewArtistsToTaxonomy`) turned out to be a genuine terminal; the other 4 call further internal functions and correctly get no forced link. All 7 Galaxy Map levels (0-6) now built and live; full pipeline (all 7 files) re-verified div/g/svg/a/section-balanced and idempotent across 2 fresh runs. `oracleAppGrounding.SELF_KNOWLEDGE` and the Oversight popup's Galaxy Map group both updated same pass; `rpgace_core.min.js` re-minified, cache-bust bumped. **Real, honestly open**: Level 0/1/2 don't have the G19 zoom-nav yet (a real, explicitly scoped next extension, not silently assumed done); the Level-6 exhaustive list is intentionally unnarrated (condition text only, no prose explanation) per its own stated scope.
- **G20 (Level 1.5 — Meanders), G21 (Level 6's real n-1 fallback link) shipped, same session, immediate follow-up — Alex flagged River V's Level-2 rendering as "very messy" (real screenshot: 10 modules, all isolated, crossing Alex/Oracle bubble edges) and asked "we need to decide what to migrate to lower level, or possibly devide rivers into meanders and insert a level."** Real evidence gathered first: most rivers have almost no intra-river module-to-module topology at all (River V 10/10 isolated, River VII 5/5, most single-module rivers trivially so) — the mess is a rendering-density problem, not a data problem. 3 AskUserQuestion answers resolved the real architecture forks: (1) a "meander" = a real dashboard card (`CARDS_BY_RIVER`, zero new curation for the grouping boundary, "each dashboard would be a meander"); (2) a new Level 1.5, built ONLY where actually needed — `rivers_needing_meanders()` (2+ real dashboard cards to split by) — checked against live data: exactly River V qualifies; also shows real per-function external-connector integration ("how externals integrate into dashboard too with each function"); (3) isolated modules render as compact cards, not crossing graph nodes, full detail still at Level 3. **Built**: `scripts/galaxy_map_meanders.py` (new `graphify-out/galaxy_map_meanders.html`) — River V's 10 modules real-evidence-assigned to 4 meanders (research/agenda/morningBrief/journal), each module's own DOMAIN marker/registration comment checked directly (not guessed from name), matching Level 5's own curation discipline. New `compute_external_call_sites()`/`rivers_needing_meanders()` in `graphify_river_group.py` (real `RPGACE.api()` Composio-call grep, 8 real call sites project-wide). River V's Level-2 canvas (`galaxy_map_module.py`) swapped to a real compact notice + link to Level 1.5 — every other river's rendering completely untouched (early-return-style branch, zero risk to the other 15, re-verified). **G21**: per Alex's own real correction on isolated logic steps — "most logic steps wont have another logic step to transfer to at level 6, so it just connects to n-1 next step function" — every Level-6 function block now carries a real "n-1: module.func()" link back to its own Level-3 module page (honestly module-granular, not function-precise — Level 3's hash-router has no per-function anchor yet, stated plainly rather than overclaimed). Full 8-file pipeline (0/1/1.5/2/3/4/5/6) re-verified div/g/svg/a/section-balanced and idempotent. `oracleAppGrounding.SELF_KNOWLEDGE` and the Oversight popup updated; `rpgace_core.min.js` re-minified, cache-bust bumped. **New `ceo_plan_items` G22, blue, explicitly deferred**: Alex's own real, separate point — some of River V's "isolated" modules may be a genuine detection gap (same class of bug the Aug 14 main.js/hooks extension fixed for authGate/pathRouter), not a true fact — needs its own real module-by-module evidence pass, not fixed blind this session.
- **Real `/misunderstanding` pass on the Galaxy Map's own rivers → G23/G24/G25, same session, later pass — Alex's own direct questions: "why some rivers at level 2 only connect to other rivers"; "why do some lines not have functions or lower level detail, just a line... a line with no mediating body on it makes no sense at all"; "there are so many loops."** Real evidence gathered before answering (not guessed): (1) Rivers XII-XVI genuinely have 0 real modules each, by design — confirmed by direct `RIVER_MODULES` query. (2) Real cycle-detection (a first naive DFS produced 4 overlapping/redundant results, discarded per rule 4; Tarjan's SCC gave the real canonical answer) found exactly 2 real cycle groups — a 7-river group around River II carrying 4 genuinely different real interaction types (a real hub, not runtime circular causality) and a 2-river group (XI/XII) carrying exactly 1 type both directions (a real OpenMontage round trip, not a hub — correctly given a DIFFERENT explanation, not the same "hub" story forced onto both). (3) Only 5 of 25 real `RIVER_FLOWS` edges resolved to a specific function — the other 20 rendered IDENTICALLY to attributed ones (confirmed by direct code read: the render code's `if attr:` block had no `else`), which is the real root of "a line with no mediating body makes no sense." **3 G-items built, same session**: **G23** (River-Level Evidence Integrity) — unattributed connections now get an explicit "❓ no known function" marker; `attribute_river_connection_function()` gained a real signal 3 (nav_route connections attribute to main.js's own `showPage()`, confirmed via `CORE_WRAPPER_HOOKS`), raising real coverage 5/25→11/25 (measured, not assumed); Rivers XII-XVI gained an explicit "0 modules, by design" badge at both Level 1 and Level 2. **G24** (Hub/Loop Legibility) — `compute_river_flow_cycles()`/`describe_river_cycle()` (new, `graphify_river_group.py`) bake real cycle-detection into the build; `galaxy_map_river.py` gained a real 🔄 badge on every cycle-member river plus a new "Why some rivers loop back" section with evidence-grounded prose per group. **G25** (Isolated-Module Detection Pass, promotes G22) — real evidence found `compute_intra_river_flow()`'s own window-wrap-chain signal was hardcoded to only check `callOracle`/`sendChat`, never generalized; `ciAutoPropose`/`intelDedup` (both River V) both chain-wrap `window.syncIntelData`, the exact same idiom, just undetected. Generalized `WRAP_TARGETS` to compute dynamically from every real multi-wrapped `window.X` function in the codebase. Measured result: River V 10/10→7/10 isolated, River VII 5/5→4/5 — real, honest, and explicitly NOT claimed as fully resolved (7 of 10 River V modules still show no detected connection). Full 8-file pipeline re-verified div/g/svg/a/section-balanced and idempotent across 2 fresh runs each step. `oracleAppGrounding.SELF_KNOWLEDGE` updated; `rpgace_core.min.js` re-minified, cache-bust bumped.
- **G26 Phase 1 shipped, same session, later pass — a real `/interrogation` first (Alex: "lets build it out, /interrogation me to make sure no /drift or /misunderstanding if not fully planned out, if planned - build").** 4 real forks resolved via AskUserQuestion, all recommended options confirmed: RPGACE-app-code-only scope; gate detection broadened past the 2 sparse `human_confirm_gate` tags to real UI confirm-patterns (arm/confirm deletes, `_showXConfirm()`/`_acceptX()` functions, bare `confirm()` calls) — human gates only, not Oracle's own AI judgment calls; a real new parallel hierarchy, not an overlay; Phase 1 = core hierarchy only. Built: `scripts/galaxy_map_decisions.py` (`graphify-out/galaxy_map_decisions.html`) — 10 real decision points found by direct grep, grouped into 3 evidence-derived categories (Destructive Delete Confirms, Taxonomy Placement/Review Confirms, Content Pipeline Progress Confirms), each anchor-verified against the live file at build time. Cross-linked into Level 3 (a real "🚦 decision gate" badge) and Level 5 where a decision already has a curated write-up there (rule 8). `ceo_plan_items` G26 moved blue→yellow (Phase 1 real, Phase 2 — `smoke_test.html` grouping, `/colourgradient` purple-integration, Total-systems scope — still deferred). **Two more real dimensions logged mid-build, both blue, not designed yet**: **G27** — Alex's own words, "another smaller parallel universe... when external ai and repos interact with both ui and backend" (OpenMontage CC/Graphify CC/Composio/Kimi-Luna touching both UI and backend). **G28** — "another dimension where skills also interact with external ai, ui and backend" (RPGACE's own Claude Code skills as real actors). Both real Total-systems-wide extensions for a later pass, explicitly not built. Full pipeline re-verified balanced and idempotent; `oracleAppGrounding.SELF_KNOWLEDGE` and Oversight popup updated; `rpgace_core.min.js` re-minified, cache-bust bumped.
- **Real CEO Loop 1 report delivered, same session, later pass — "Level 0 should be dimensions" and 7 more real items (A-H), Alex confirmed "CEO Loop 1 first."** Full compiled spec: `records/2026-08/galaxy_map_dimensions_ceo_loop1_spec_2026-08-14.txt`. Real `/interrogation` resolved the shape (replace Level 0 with dimensions, dimension set open-ended, Item D's cartographer-integration verdict decided via `/restructure`-shaped reasoning since literal `/drift` had no baseline to check against). Item F (Omnitrix in the Oracle wrapper) fully resolved across 3 real clarification rounds: fires at the Oracle API call stage only — Level 1 (Y/N) local-Claude vs external-provider (Moonshot/Luna, dormant); Level 2 (2 independent Y/N, 4 combos) Fable×Opus. A real, load-bearing constraint surfaced and accepted: "external super team" reaching OpenMontage CC/Graphify CC live mid-Oracle-call isn't buildable — no live session-to-session link exists. Per Alex's own explicit instruction ("just blue highlight this"), all 8 items logged as `ceo_plan_items` G29-G36, all blue, none built — **Step 6 (Alex reads/approves) is still open, nothing here is authorized to build yet.**
- **Real Total-systems influence-boundary safeguard added to `oracleAppGrounding.SELF_KNOWLEDGE`, same session, per Alex's direct ask ("self-awareness hook on to my influences to safeguard everything").** Real evidence checked first (not assumed): Orchestrator CC/OpenMontage CC/Graphify CC were already mentioned in SELF_KNOWLEDGE (1/4/7 occurrences respectively) but scattered, with no coherent influence-boundary statement and no mention of the actual registry (`total_system_members`). Fixed with a concise real paragraph: Alex has real influence over RPGACE app/Supabase/Orchestrator CC/OpenMontage CC/Graphify CC; he has none over G27's external providers/APIs — G27 IS that real, evidenced boundary. **Honest, separate finding surfaced in the same pass, not fixed**: `SELF_KNOWLEDGE` itself has grown past 22,000 characters with zero pruning of fully-resolved entries — the exact bloat pattern this file's own July 31 prune fixed for CLAUDE.md, never applied to Oracle's self-knowledge. Flagged as a real future cleanup, deliberately not attempted mid-close-out (real risk of losing detail rushed).
- **Alex's own real assessment, worth preserving as a durable fact, not just chat**: this multi-day Galaxy Map + oversight-logging effort (Aug 13-14) is, in his own words, "the most influential update to date" — grounding RPGACE Total-systems development context directly to Orchestrator CC has measurably improved development quality, analysis depth, and token efficiency. Kept here verbatim rather than paraphrased, since it's a real judgment about what this whole multi-session effort was actually for.
- **`/Routine` continuation, same day, later pass — G26 finished, G11/G22/G29/G31 shipped, G38 (Level 2.5) built, real anchor-drift fixed repeatedly along the way.** Alex: "/routine, lets do g26 to finish mid build, then g11, g22, g29, g30, g31" (+ "and g11 somewhere in the mix too"). **G26 Phase 2**: 10 real `smoke_test.html` rows for the Decisions page's own decision points, each with a real HOW TO TEST instruction written directly into the doc (Alex's own direct ask — the doc must be self-sufficient, not dependent on chat history); 10 matching `perspective_reports` rows (new `scope_level='decision'`) give `/colourgradient`'s purple-regression check a real baseline to read. **G11**: the 47 real onclick-triggered functions now all have `perspective_reports` rows; real finding — CLAUDE.md's own "2 dead onclick buttons" landmine undercounted by one, `quickPrompt` looks dead by the same static-grep test but isn't (`quickActions._setup()` runtime-patches it live), the 2 genuinely dead ones reconfirmed exactly as stated. **G22**: real module-by-module investigation of the last 2 isolated modules — `intelBatchList` was a genuine detection gap (a real `WRAP_TARGETS` member wrapped by 3 modules across 2 different rivers, so the same-river chain logic found no peer; fixed with a new Signal 2b connecting to a `core-wrapper[mainjs:X]` pseudo-node), `agentsIntoOracle` confirmed a real single-instance DOM-relocation case not worth generalizing (rule 11). Measured: 2/44 → 1/44 isolated. **G29**: new `graphify-out/galaxy_map_orchestrator_openmontage.html` — the real dispatch history between Orchestrator CC and OpenMontage CC (8 real `openmontage_jobs` rows, summarized honestly); real drive-by fix, `total_system_members` still said "RPGACE CC," never updated to match the Aug 13 rename. **G31**: extended the evidence-gated bubble system to a 2nd real connector, Last.fm (`fetch('/api/lastfm')`, `beatLog`) — 2 of 13 eligible connectors now covered, the rest live in server-side `api/*.js` files this detector doesn't reach. **G38 (Level 2.5)**: real, generalized successor to Meanders — all 16 rivers, each pointing to its own real dashboard card(s), each card resolved to its real primary module (`dashboard_card_primary_module()`, reused) with a real UI-accessibility badge, linking into Level 3. Alex's own framing: "regrouping rivers by what is accessible by ui and alex, this will help connect dimensions later on." Real mid-build refinement (his own words: "each bubble of dashboard should have logic bubbles that lead to level 3 units, level 2 units of backend, and ui and alex bubble that sends off into the alex/ui conjoined dimension") — 2 of 3 real link types shipped (forward to Level 3, back to Level 2); the 3rd (a UI/Alex bubble into a future dimension) honestly depends on unbuilt G30/G37, not faked. **Two more real ideas logged blue, not built**: **G37** (a real Alex decision-tree bubble system — login → dashboard → buttons → DOM → text/decision forks, sourced from minotaur_map.html) and the deferred 3rd link on G38. **G30 (Level 0 → dimensions) stays blocked** on a real, still-open engineering question (single-home vs multi-home dimension membership for a river/module/function) — not guessed at. Real anchor-drift caught and fixed 4 separate times this pass by the standing anchor-verification system (Level 5's `artist-phylum-routing`/`placement-scored`, Decisions' `placement-confirm`/`article-confirm`) — every one of this session's own `SELF_KNOWLEDGE`/Oversight-popup edits kept shifting line numbers underneath hand-cited anchors; caught loudly every time, never shipped stale. One real JS-string syntax bug caught by the standing `node --check` habit — an unescaped apostrophe (`Alex's` instead of `Alex\'s`), rule 12's own documented mistake class, fixed immediately. Full 13-file pipeline re-verified balanced and idempotent across the whole pass; `oracleAppGrounding.SELF_KNOWLEDGE` updated; `rpgace_core.min.js` re-minified, cache-bust bumped.
- **Real Level 1/1.5/2/2.5/3 restructure, same session, immediate follow-up — Alex rejected Level 1.5 outright and reframed Meanders as the real convergence point.** Alex: "so i think we should rethink how 1,1.5,2,2.5, 3 is all structured, i dont want 1.5 it doesnt make sense... meanders should become the central point where all ui/alex/backend/externals all eventually meet." Given the size, ran a real compiled spec (`records/2026-08/galaxy_map_level1_25_3_restructure_ceo_loop1_2026-08-14.txt`) + `/paranoia`-shaped reasoning (literal `/drift` didn't apply — no baseline existed) + a real 3-question `/interrogation`, all answered as recommended: (1) Meanders' River-V declutter role folds into Level 2.5, no regression; (2) stays Level 2.5, same position, not a new hub-level; (3) dimension-links are a real, honest deferred stub pending G30, never faked ahead of it. **Built**: Level 1.5 (Meanders) retired from every breadcrumb, the Oversight popup, and River V's own Level-2 redirect (now points to Level 2.5) — old page kept on disk, nothing 404s. Level 2.5 rebuilt to ONLY cover the 10 of 16 rivers with a real dashboard card (I/II/XII/XIII/XV/XVI silently excluded, no placeholder noise, per Alex's own "only take into account rivers that actually have them"); each river section gained a real externals-attachment block (reused `EXTERNAL_RIVER_LINKS`) and an honest "pending G30" dimension stub. Real, separate verification: Level 2/3's own UI-accessibility badges (the Alex bubble) were checked directly and confirmed already real and permanent since Aug 13 — his own "show UI at level 2, 3" point was already satisfied, not rebuilt. **A real, already-logged item re-raised in this same context**: Alex — "i want to dissmantle research lab so bad into components and ship where it should be to make river 5 a lot more managable" — confirmed as `ceo_plan_items` A5 (Aug 10 massive-expansion spec, still red/not built), real evidence tying it directly to this session's own finding that `researchTabs`/`intelBatchList` show "no single primary module" on the new Level 2.5 — deliberately NOT folded into this Galaxy-Map-visualization pass (genuine Tier-2/CEO-scale app-code work), logged as the real next candidate instead. `ceo_plan_items` G38 updated in place (title now reflects the convergence-point role). Full 12-file pipeline re-verified balanced and idempotent; `oracleAppGrounding.SELF_KNOWLEDGE` updated; `rpgace_core.min.js` re-minified, cache-bust bumped.
- **G16 fully closed, same session, later pass — Alex: "move on with next phase or step of g-series that are planned."** Real check found Level 0 was already correctly covered (a permanent Oracle harness node, `galaxy_map.py`'s `HARNESS_NODES` — no gap, no build needed). Built the remaining real, evidence-gated pieces: Level 1 gained a real, lightweight Oracle badge (same treatment as the existing Alex-presence badge) on the 4 rivers that genuinely have Oracle calls (III/V/IX/XI); Level 4 gained a new `compute_card_oracle_call_count()` reusing `compute_dashboard_card_flow()`'s already-resolved target modules — 7 of 12 real cards show it; "other externals" closed via a real Composio bubble at Level 3 (`COMPOSIO_COLOR`, same pattern as Oracle, gated on `compute_external_call_sites()` — 2 real modules, `contentRepurpose`/`morningBrief`). **Real anchor-drift caught and fixed during regeneration**: Level 5's and the new Decisions page's hand-cited line numbers had drifted from earlier same-session `rpgace_core.js` edits — caught by the standing anchor-verification system (a build-time crash, not silently-stale code), recomputed and re-verified before shipping. `ceo_plan_items` G16 moved yellow→green. Full 9-file pipeline re-verified balanced and idempotent; `oracleAppGrounding.SELF_KNOWLEDGE` updated; `rpgace_core.min.js` re-minified, cache-bust bumped.
- **G27 and G28 shipped, same session, later pass — Alex: "yes lets do it," confirming a real `/interrogation` first (4 AskUserQuestion answers, all recommended options).** **G27** (`scripts/galaxy_map_externals.py`, `graphify-out/galaxy_map_externals.html`): all 13 real `EXTERNAL_CONNECTORS` classified into "touches both UI and backend" / "one side only" / "not yet active," a connector counting as UI-touching on either a real trigger OR a real displayed output. Real, function-level evidence for the 4 connectors with existing detection (Anthropic, Composio, Jina AI, Last.fm), connector-level evidence sourced from `EXTERNAL_CONNECTORS`'s own already-sourced fields for the rest. Measured: 5 of 13 (Anthropic, OpenMontage, Composio, Jina AI, Last.fm) genuinely touch both. **G28** (`scripts/galaxy_map_skills.py`, `graphify-out/galaxy_map_skills.html`): all 24 real RPGACE-authored skills classified on 3 real axes — external AI, UI, backend — reusing `ai_tooling_and_rules_map.md`'s own skill catalog as the source list rather than re-deriving it. Real, deliberately narrow "external AI" definition stated plainly: a skill's own procedure reaching a genuinely DIFFERENT AI system (Oracle, or a Total-system member), never just "Claude Code is running this skill" — which would trivially make every skill qualify and mean nothing. Only `/CEO` touches all 3 axes. Both pages cross-linked from the Decisions page breadcrumb and added to the Oversight popup. `ceo_plan_items` G27/G28 moved blue→green. Full pipeline re-verified balanced and idempotent; `oracleAppGrounding.SELF_KNOWLEDGE` updated; `rpgace_core.min.js` re-minified, cache-bust bumped.
- **G39 (Load Dimension) shipped Aug 15 — Alex: "we should also make a load dimension (what ui, backend or alex trigger certain backend and ui to load, in what steps etc) this could help tie everything together for diagnosing."** Real 2-round `/interrogation` confirmed: 3 real, separately-tracked load-trigger categories (never merged), serving both diagnostic-correctness and boot-performance visibility equally. Real, confirmed mechanism: `registerBootTask(fn)` runs `fn()` synchronously the instant called, queued into a single `Promise.all(...).then(_hideBootOnce)` gate (20s hard ceiling) — real source order is real fire order. Built: 3 new detection functions in `graphify_river_group.py` + `scripts/galaxy_map_load.py` → `graphify-out/galaxy_map_load.html` (27 real boot-task registrations, 29 real page-nav trigger pairs, 5 real click-load triggers), cross-linked into Level 2/3, added to Oversight. Real R5 idempotency bug caught and fixed before shipping (`LEVEL3_MODULES` is a Python `set()`, hash-randomized order — fixed with `sorted()`). **Explicitly deferred, not hidden**: a real Level 2/3 hub "⏳ Load" bubble with bubble-conjoining (Alex's own ask) is a separate follow-on, `ceo_plan_items` G39 stays yellow. **Same pass, separate real ask**: `session_lessons.html` retroactively seeded from `patch_notes.html` via a new `scripts/session_lessons_retroactive_seed.py` (R10 generator, not hand-curated) — 39 real title+date entries mechanically matched on genuine misunderstanding/correction/obstacle keywords, marked blue per Alex's own "will plan it later."
- **G30 (Dimensions Matrix) and G37 (Alex's Decision Path) shipped Aug 15, same session — real multi-home confirmed, both scoped as synthesis views given a real token/time constraint.** Alex: "lets do all the g steps that bring in other dimensions... im running out of weekly tokens, so i have to bedtime soon." Multi-home resolved (a module can belong to more than one dimension at once). **G30**: `scripts/galaxy_map_dimensions.py` → `galaxy_map_dimensions.html` — 44 real modules × 5 real shipped dimensions (Externals/UI-Alex-Accessibility/Load/Decision-Human-Gate/Orchestrator↔OpenMontage), each tag reusing that dimension's own already-computed detector, 8 real hub modules (3+ tags). **G37**: `scripts/galaxy_map_alex_path.py` → `galaxy_map_alex_path.html` — for each of 12 real dashboard cards, the real Level-4 flow to its target module(s), then the real Y/N fork (Decisions/G26) Alex actually hits on that path, if any (3 of 12 do). Both real synthesis views over already-shipped data (rule 8), not a Level-0 skeleton replacement — the 4 galaxies stay as-is. Real R5 idempotency bug caught in the Dimensions Matrix (same `LEVEL3_MODULES` set()-order class as G39's), fixed with `sorted()`. `ceo_plan_items` G30/G37 both blue→green.
- **Real /deduplication+/paranoia on Level 4 vs Level 2.5, Logic Dimension shipped, 9 stale anchors fixed — Aug 15, genuinely final pass.** Verdict: NOT duplicates — Level 4 is per-card DOM/button-level flow detail, Level 2.5 is per-river card-to-module summary, same non-instance precedent as `taxonomy_nodes`/`taxonomy_tree`. Real, cheap fix: Level 4's river chip now links to both Level 2 and Level 2.5 (previously only Level 2). New `scripts/galaxy_map_logic_dimension.py` → `galaxy_map_logic_dimension.html`, a real additive Level 5 companion (existing 7 curated decision points untouched) — 96 real river-to-river/external/skill connections as clickable `<details>` passages, river-grouped, reusing `RIVER_FLOWS`/`FLOWS_IN`/`LINKS_BY_RIVER`/`ALL_SKILLS`/`SKILL_SECONDARY_RIVER`/`attribute_river_connection_function()` as-is (rule 8) — the same real data Level 2's own river legend already renders. 9 real stale anchor-verified code citations (5 in Level 5, all 10 in Decisions) found and fixed — real line-drift from this session's own many `rpgace_core.js` edits, each real current line found by direct grep and re-verified by reading the live code.
- **G32-G36 shipped Aug 15 (same session, later pass), G34 Level 1 (Oracle provider toggle) shipped, Level 2 open pending Alex's own spec.** G32: `.claude/skills/scope/SKILL.md` + `omnitrix/SKILL.md` (GODMODE) gained the conditional `/cartographer` trigger (mapping-hierarchy work only; `commit-archaeologist` explicitly excluded). G33: `.claude/skills/colourgradient/SKILL.md` gained Step 2b — real G27/G28 axis evidence grounds the `/drift` verdict when an item maps to a real connector/skill, honest partial-coverage scope stated. G35/G36: `galaxy_map_skills.html`'s axis markers became real adjacent bubbles; new `scripts/galaxy_map_skill_network.py` → `galaxy_map_skill_network.html`, 117 real skill-to-skill invocation edges (a real `/otherSkillName` mention), clickable passages. G34 Level 1: new `oracleProviderMode` module (`rpgace_core.js`) + `main.js` `callOracle()` wiring — Local Claude vs external provider (Kimi/Luna), dormant until a real key exists; real landmine avoided (server's provider branch always returns plain JSON, client always sends `stream:false` for it, forwards final text to `onChunk` once). G34 Level 2 stays open — genuinely ambiguous, Alex writing his own spec. **Real future idea logged** (`ceo_plan_items` G41, not built): Oracle eventually taking real ACTION on pipelines via its own self-awareness layer, plus a possible Fish Audio voice integration.
- **Real L0/Dimension/River/Module/Current redefinition, Aug 18 — a real `/CEO` Loop 1 pass (compile→interrogate→paranoia-advised draft→drift-check→report), ratified as `ceo_plan_items` G42 (umbrella) + G43-G50, all `red` (specified, not built).** Full record: `records/2026-08/galaxy_map_l0_dimension_river_redefinition_spec_2026-08-18.txt` + `..._plan_2026-08-18.txt`. Real shape: Level 0 becomes 7 peer units (External AI, RPGACE Architecture, Skills, Orchestrator CC, Alex, Supabase, Oversight Docs — Oversight Docs promoted from a Tier layer to a real L0 unit), with **no privileged gateway unit** — a real bug in the first draft, Alex's own direct catch, now R16 in `.claude/skills/CEO/SKILL.md`. Every unit's own bubbles gate into its own real dimensions, each a genuinely different evidence-grounded lens over the same L0-4 hierarchy (Alex via G26 Decisions, Supabase via table read/write evidence, External AI via the existing Oracle-bubble data, Skills via `SKILL_SECONDARY_RIVER`, Orchestrator CC via commit history, Oversight Docs via doc-to-level mapping). Skills/Supabase additionally render as a distinct "💉 injection tool" bubble kind (attached to a step's handling — confirmed real, 113 of 502 functions/22% touch a Supabase table across 25 tables, before committing to build it). Levels 4/5/6 as standalone pages are confirmed genuinely redundant once the new model ships — folded, not deleted: Level 3 is replaced by an ordered per-module Current-series list, Level 4's freed slot becomes the zoomed per-Current walkthrough. River redefinition stays emergent (real evidence per river, via the already-locked III/V/VII closer look) — explicitly not forced to 1-module-per-river (real check: that would jump 16 rivers to 50 groupings for no gain). **New standing rule (R17)**: oversight docs now explicitly stem from the Galaxy Map — each G-step's own `/Engineer` execution scopes and executes its own relevant oversight-doc updates as part of building that step, not a separate trailing pass. **Nothing built yet** — Step 6 (Alex's read/approve) closed on the plan itself; Loop 2 execution starts with G43 (the L0 map, since everything else hangs off it).
- **Real River-v2 split, Aug 18 (G49) — River V is now 2 real rivers, not 1 holding 2 streams.** Real evidence (a fresh global module-interaction-graph run) confirmed River V's own name — "Two Independent Streams" — was already telling the truth: the 5 Content-Intelligence/research modules (researchTabs/intelBatchList/intelDelete/intelDedup/ciAutoPropose) share zero real interaction AND zero real shared goal with the 5 agenda/schedule/journal modules. Moved to a new **River XVII — The Research & Intel Stream** (real, sourced color: minotaur_map.html's own `--blue` `#4A90E2`, confirmed distinct from style.css's `--blue` `#4a8ccc` already used by River II). River V renamed **"Daily Ops: Agenda, Schedule & Journal"** (the old name no longer applies). River III (Oracle Current) and River VII (Library Current) were also real-evidence-checked and confirmed to hold up as-is — River III's apparent internal split is mostly a detection artifact (real shared-goal coherence across its persona-panel/grounding/mode-toggle modules), with one real exception flagged: `scheduleOracle` genuinely straddles River III and the new River V, a real candidate for a future cross-river Dimension rather than a full move. Full pipeline (all 22 real Galaxy Map pages) regenerated and reverified: idempotent, div/svg/a-balanced, zero console errors. `RIVER_MEANDERS`'s own hardcoded 'research' meander removed (its modules no longer live in River V); `CARDS_BY_RIVER`'s "research" dashboard card moved 5→17; `EXTERNAL_RIVER_LINKS`' Whisper citation moved 5→17; 2 stale `galaxy_map_decisions.py` anchors (drifted from this same session's earlier `rpgace_core.js` edits) caught and fixed by the standing anchor-verification build-crash discipline. `ceo_plan_items` G49 red→yellow.
- **G42-G50 all shipped same day (Aug 18) — the whole ratified L0/Dimension/River/Module/Current redefinition executed in one continuous Loop 2 pass, per Alex's own explicit "dont stop, just put all of it in yellow to then go through."** Real summary: G43 (L0 map, 7 units, 17 hand-curated edges, no privileged gateway), G44 (Dimension Matrix), G45 (Supabase page, 25 tables), G46 (Skills River-usage extension), G47 (Level 3 replaced by a 436-Current ordered list, absorbing Level 5/6), G48 (Module-level Supabase bubble, Level 4 marked superseded), G49 (real River-v2 split — see its own bullet above), G50 (Oversight Docs bubble trio — already delivered by G43, verified not rebuilt, rule 8). All 9 items sit at `yellow` (built, real, untested by Alex's own hand) in `ceo_plan_items`; a real new idea (G51 — a smoke_test.html↔yellow↔green confirm loop for every G-step, Alex's own words: "i think this is a banging idea you started off") logged `blue`, not built. Real bugs caught and fixed DURING this pass, not silently shipped broken: a per-function-instead-of-per-module recompute timeout in the zoom-walkthrough script, an `UnboundLocalError` when a river had Supabase touches but no Oracle calls, and the 2 stale decision-page anchors above. `node --check` clean throughout, re-minified, cache-bust bumped each JS-touching commit.
- **Aug 20 2026 — real main.js merge (G52, see the Architecture landmine above for the full technical record) + G22 continuation + a real error_log/G39 lag entry + G51 shipped.** Alex hand-tested login post-merge ("i logged in fine") but flagged a real ~12,000ms load lag — logged as a real `error_log` row (`boot_load_lag_client_interactive_12000ms`), linked to G39, citing the still-open Aug 11 `/decompress` forks (minification's real remaining contribution never re-measured, the boot-stagger pattern never revisited). **G22**: re-verified live post-merge rather than trusting the stored "1/44" figure, since the merge touched the same detection machinery — `authGate`/`pathRouter` still resolved correctly, but a real NEW gap turned up (`oracleProviderMode`, G34, postdates G22's original pass): its entire connection to the app is `main.js`'s own `callOracle()` calling it directly, a signal type (legacy-calls-module) no detector covered. Fixed: `compute_hook_signal_edges()` gained this 2nd real signal type. Measured: 2/45→1/45 isolated — only `agentsIntoOracle` remains (the same already-confirmed architectural fact). **G51 built** (was blue — "i think this is a banging idea you started off"): real `/interrogation` resolved 2 forks (generator script for the backfill, G42 auto-derives from its 8 children rather than its own row). `smoke_test_items` gained a `linked_plan_item_id` column; `smoke_test.html`'s existing confirm-click handler extended to PATCH the linked `ceo_plan_items` row to green on confirm (same real pattern as its existing `error_log` auto-resolve cross-write), plus a new `checkUmbrellaAutoFlip()` for G42. Functionally verified via headless Chromium with mocked Supabase routes — confirm → smoke_test row updates → linked plan item flips green → umbrella check fires correctly. 11 real rows backfilled (`scripts/smoke_test_generate_g_items.py`) for the then-yellow G34/G38/G39/G43-G50, each with a real per-item HOW TO TEST instruction — G42 excluded per its own auto-derive design. Real, honest finding surfaced not hidden: G43/G44/G50 substantially overlap post-merge (same physical `galaxy_map_l0.html` file) — each kept its own row for 1:1 linkage (rule 8: separate real decisions), but G44/G50 carry an honest `cross_ref_note` instead of duplicated prose.
- **Aug 22 2026 — real `/Routine` follow-up on the `boot_load_lag_client_interactive_12000ms` `error_log` row: 2 of its own named hypotheses ruled out, 1 real fix shipped, root cause still unconfirmed.** Direct measurement found `rpgace_core.min.js` ships 245KB gzipped — the row's own "1.38MB blocking JS" estimate predates minification going live and is no longer the real bottleneck. Re-reading all 33 real `registerBootTask` sites found the suspected "1300-1700ms boot-stagger" is almost entirely `page:show`-triggered re-injection, not boot-blocking — a real correction to that hypothesis. **Fixed**: `careerStatCard.init()` called `self._render()` twice every boot (once directly, once via `registerBootTask`) — the July 23 in-flight-fetch guard already deduped the network cost, but the redundant 2nd render pass was real dead weight, removed. **Honestly unresolved**: the leading remaining hypothesis is real network round-trip volume across ~33 concurrent boot-time Supabase requests (the same per-origin connection-queuing mechanism the July 23 careerStatCard fix addressed for one module, potentially recurring project-wide) — but this session cannot reach a live DevTools trace to confirm it. `error_log`'s row gained a real new attempt entry with the full diagnostic trail. **Real, concrete ask for Alex (rule 10)**: open the live app in Chrome DevTools' Network tab, hard-reload, and report (a) total request count before the boot overlay hides, (b) when the last one resolves, (c) whether any single request is noticeably slower than the rest — that's the evidence the next real fix needs.
- **Aug 20/21 2026 — G54 (Achiever/Archive + brown), G55 (Oversight Sync Dimension), G56 (real pipeline-logic docs) all shipped.** Alex: "log a new oversight doc called achiever, which will establish what in a planned or built update will turn a green into a new colour brown... which will archieve anything that used to be true that is no longer true." **G54**: `achiever.html` (12th real oversight artifact, layer (e)'s past-tense half) built to structurally mirror `future_integrations.html` exactly, per Alex's own direct correction ("both are same letter level oversight document"). A real, new `/colourgradient` color (🟤 brown) — genuinely distinct from purple: purple tracks broken CODE, brown tracks a stale CLAIM (a count/rule that was true, a later real change made false, the feature may still work fine). Real, mandatory removal step per Alex's own correction: "what gets brown is removed from smoke test, along with all other oversight docs that are reporting truth" — a `smoke_test_items` row testing solely a dead claim gets deleted outright, never re-flagged. Seeded with 10 real rows total: the 2 live worked examples (`TOTAL_ZONES`, CLAUDE.md's own stale "SEVEN docs" header, caught DURING this build) plus a real retroactive batch of 8 more genuine stale claims mined from this project's own history (the Aug 11 phylum renumber, the Aug 13 "Engineer CC"→"OpenMontage CC" rename, `style_profiles`'/`taxonomy_proposals`' stale counts, 3 function/module-count corrections, "12 of 21 phyla"→"all 21" — the one entry archived for completeness, honestly flagged as never actually caught wrong). Each row carries real `rationale`/`backtrack_note`/timestamps matching `error_log`'s own depth, per Alex's own direct ask for equal breakdown. **G55**: `scripts/galaxy_map_oversight_sync.py` — a new Galaxy Map page (real process-time counterpart to the existing static Oversight-Docs L0 unit) showing which oversight doc gets touched, in what order, during push/build (update-logging-system's 18-item map, hand-mirrored), Bedtime, Routine, Summary, and CEO Loop 2 (R17) — each ritual's real steps transcribed from its own skill file, never invented. **G56**: 3 real hand-written pipeline-logic Mermaid sections added to `system_flow_map.md` (§12 Oracle Mode/Fallback Scout, §13 Achiever/Brown, §14 Total-Systems Dispatch) — added as NEW SECTIONS in the existing canonical pipeline-flow doc (rule 8), not fragmented into separate new files, per real confirmed scope. Cross-linked into `achiever.html`, `galaxy_map_orchestrator_openmontage.html`, and the new Oversight Sync page. A real, in-passing fix made along the way: `system_flow_map.md`'s own stale `style_profiles` "0 rows" claim corrected to the real 3 rows, cross-referenced into the existing `achiever_archive` row rather than duplicated. All new/touched pages balanced, idempotent, headless-verified zero console errors.
- **Aug 21 2026 — Galaxy Map Unified Gateway, G59 shipped (G58 umbrella, G60 deferred).** Alex: "now I want all galaxy html maps to be all combined into one central html with all needed files having gateways to each other, and the top list to be restructured to show how one flows into another... map/table view being toggleable... sort by level objects shown and rivers/dimensions they belong to. some dimensions are inter or infra." Real `/interrogation` first (4 forks, all recommended options confirmed, `records/2026-08/galaxy_map_unified_gateway_spec_2026-08-21.txt`): single shell with lazy-loaded content (never a monolithic file — a real, direct regression risk given the just-logged ~12,000ms boot-lag `error_log` row); a grouped-list-plus-flow-arrows top nav (not a full graph-canvas landing page, mobile small-target risk); the inter/infra rule confirmed as "inter" (river-flow/hook-signal/cross-module-call-style connections) renders as an edge, "infra" (Supabase touch/Oracle call/external-connector touch) renders as a bubble on the one node that needs it — UNLESS shared across 2+ inter-edges, then a real per-edge selector instead; and a phased `/CEO`-tracked build (G58/G59/G60) over one giant pass. **G59 built**: `graphify-out/galaxy_map_hub.html` — all 23 real `galaxy_map_*.html` files catalogued (verified against the live directory at build time, fails loud on drift), each tagged with a real Level and a real 🌌 core/🔗 inter/💉 infra/🧭 meta kind. 66 real edges computed FRESH at build time from every page's own actual `<a href>` targets (never hand-typed) — `galaxy_map.html` excluded as a target since it's a universal home breadcrumb, not real flow. Table view groups by Level with a real "→ flows into" line per row; map view renders the same data as a column-by-level SVG node-graph; both toggle over the identical dataset. Clicking any row/node lazy-loads that page's real content into a shared iframe pane — the 21 existing pages stay untouched on disk. **Honest, deliberate scope limit**: no page-grain 🏁 terminal badge — real out-degree never hits zero across the 23-page set (every page cross-links for reference as well as drill-down), so a terminal claim would be false; only a real 🚪 root badge (`galaxy_map.html`) and a real 🧭 "hub-only" badge (3 genuine orphans with zero real inbound links) ship. A real bug (the build's own fail-loud self-check flagging its own output file as an uncatalogued real file on a 2nd run) was caught and fixed before shipping; re-verified idempotent. Wired into the Oversight popup as the new primary Galaxy Map entry point — the existing 5 sub-groups of individual links stay exactly as they were, never removed. Verified via headless Chromium: toggle, 23 nodes/23 rows, click-to-load, zero console errors; div/svg/g/a-balanced. `ceo_plan_items` G59 green, G58 (umbrella) yellow pending G60. `node --check` clean, re-minified, cache-bust bumped 20260821b→20260821c. **G60** (the deeper infra/inter edge-selector retrofit INSIDE each of the 21 existing pages' own rendering — converting a genuinely-shared infra bubble into a real per-edge selector) is logged `red`, explicitly NOT built this pass, per Alex's own confirmed phased-build choice.
- **Real correction, same day — Alex rejected the Unified Gateway shell outright; the REAL Level 0 is `graphify-out/galaxy_map_l0_fusion.html`.** His own direct words: "everything should fold into one galaxy html. not unified gateway, that should be used as reference of how to structure the map... everything should start at l0. put all infra into its bubble at level 0 (i press openmontage, it will give me a choice of infra for externals... or i can choose inter where i can choose a dimension open montage is present. or press alex... choose infra to see what decisions alex can make)." **Honest diagnosis**: the hub was a directory of the 23 still-separate pages loaded via iframe — a gateway ON the fragmented structure, not a fusion of real content INTO one page. **Compressed `/paranoia`** (its own guardrail: re-asking the already-answered core concept would repeat the exact failure Alex was angry about) found the real, previously-unreconciled overlap between `galaxy_map.py`'s 4 galaxies and `galaxy_map_l0.py`'s 7 units — merged into one real, deduplicated 9-unit L0 (rule 8). **Built**: click any unit, then a real CHOICE (explicitly NOT a toggle switch — Alex's own further correction, "that only for map view switching to table view") between 💉 Infra and 🔗 Inter, each showing real counts before picking; choosing expands real facet detail grouped by real dimension and cross-highlights every other unit sharing that resource/dimension via a shared `share_key`. Two explicit, justified overrides: anything touching External AI is always Infra (his own confirmed example); Alex↔RPGACE_Architecture is Infra specifically (his own literal "infra = decisions I can make" example), expanded to the full real 10-point Decisions list. **Verified against his own worked example directly**, not assumed: headless Chromium confirmed OpenMontage CC→Infra→"Externals (2)" (OpenMontage/FFmpeg), Alex→Infra→the real 10-point Decisions list, Alex→Inter→real dimension participation; cross-highlight glow fires only on correct units. 44/44 div-balanced, idempotent, zero console errors. Wired as the real primary Oversight popup entry; the old hub demoted to "superseded, UI-pattern reference only" with an on-page banner (its toggle/badge mechanics were reused correctly, per Alex's own instruction); `galaxy_map.html`/`galaxy_map_l0.html` both got pointer banners. 25 real Galaxy Map HTML files total now. **Real follow-ups handled same session**: a full list of all 25 files sorted by Level, framed as one decomposition/composition chain (L0 the whole system → L1 rivers, big components → L2 modules, sub-components → L2.5/Current/Zoom, individual functions → L5/L6, decision logic, the finest grain → Dimension pages, cross-cutting facts feeding the L0 Fusion facets themselves) — delivered in chat, never a separate artifact needed. A real `/perspective` pass on the 9 L0 units' own interactions found 4 real gaps (External AI/Skills/Alex/Oversight Docs had no unit-grain report; the other 5 already had galaxy/node-level ones) — filled with real, evidence-grounded `perspective_reports` rows at a new `scope_level='l0_unit'`, each citing real facet counts sourced directly from the fusion page's own computed data (rule 8). `ceo_plan_items` G59 updated in place with the real correction. **Lesson kept honestly, not smoothed over**: the first build technically matched a literal reading of "unified gateway" but missed the actual architectural shift Alex wanted — his own worked example (OpenMontage/Alex) should have been implemented literally from the very first pass, not abstracted into a generic per-page classification scheme.
- **Real 2nd correction, same day, more precise — the fused L0 now lives directly INSIDE `galaxy_map.html` (RPGACE Total Systems' own architecture map), not a separate 3rd file.** Alex: "the l0 7 units should exist in the bubbles in on rpgace total systems own architecture map." The first correction had fixed the interaction model correctly but still created a brand-new third file (`galaxy_map_l0_fusion.html`) — still fragmentation, one file smaller. **Fixed**: `build_facets()`/the unit-choice/cross-highlight mechanic moved out of the now-deleted `galaxy_map_l0_fusion.py` directly into `scripts/galaxy_map.py` (no circular import). The existing hand-tuned SVG diagram (crossing-reduction, curved edges, real Aug 13 crowding fixes) is untouched — 4 of its real nodes (the 3 satellites + the Human Gate-Alex harness node) gained a real `data-unit` trigger into the shared panel; 5 more real units (External AI/Skills/Alex/Supabase/Oversight Docs) render as a new bubble row directly below the SVG, same mechanic. The central RPGACE Architecture node keeps its own separate, pre-existing river drill-down link untouched (diverting its primary click would have broken real working navigation). **Naming**: title/eyebrow/h1 now explicitly say "RPGACE Total Systems," per Alex's own direct ask ("name l0 rpgace total systems too so it has a right reference") — previously only the eyebrow said it. `galaxy_map_l0_fusion.py`/`.html` deleted outright (rule 8, content moved not duplicated); `galaxy_map_l0.html`/`galaxy_map_hub.html` banners repointed at `galaxy_map.html`; `galaxy_map_hub.py`'s PAGES catalogue corrected to 23 entries (24 real Galaxy Map files total, was 25). A real, pre-existing stale claim found in passing and fixed: the closing note still said "G4... not yet built" — G4 (`galaxy_map_module.html`) has been live since Aug 13. Re-verified against Alex's own worked examples after the move (not assumed still-correct): OpenMontage CC via its SVG node → Infra → Externals; Alex via the bubble row → Infra → the real Decisions list; the river drill-down link still works. Zero console errors, balanced, idempotent. `ceo_plan_items` G59 updated in place a 2nd time; the 4 `perspective_reports` rows from the earlier `/perspective` pass had their evidence citation corrected to the new file location.
- **Real `/paranoia`+`/interrogation`+`/CEO` pass, same day, later — map/table toggle generalized, "nothing superseded" honored, Oversight cut to 2 links, recursive matrix idea logged.** Alex clarified "bubble system"/"bubble map" means the real radial structure galaxy_map.html/galaxy_map_river.html already use, then asked for the map/table toggle "everywhere that has 2 views," nothing left "superseded" since it folds into one html, Oversight links "reduced to a minimal number," and explicit real `/paranoia`+`/interrogation` before building. Real evidence found 2 pages with zero bubble view (skill_network, skills/G28) plus 3 MORE real graph+list pairs never merged (Level3/Current, Level4/Zoom, l0.html/galaxy_map.html). Real `/interrogation` (4 questions, all recommended): build just the 2 named pages this pass, defer the 3 folds to a scoped next phase (delete-outright once verified, matching this session's own l0_fusion precedent), keep `galaxy_map_hub.html` as a real standalone index (not "superseded"), log the Dimensions-Matrix reframing as a future recursive interaction-matrix phase — Alex's own generalization: "the same should be for l1 rivers and l0, it should all map out in this principle to connect rpgace app to all other candidates." **Built**: `galaxy_map_skill_network.html` gained a real circular bubble map (24 nodes, 118 real edges, hand-computed polar layout, no force-directed library — correcting that stale reasoning in its own docstring) toggled against its tab/passage view; `galaxy_map_skills.html` gained a real column-clustered bubble map toggled against its table view; `galaxy_map_hub.html` corrected from "superseded"/"Unified Gateway" to "Page Index," a real kept utility; Oversight popup's Galaxy Map section cut from 23 individual links to 2 (`galaxy_map.html` to start, `galaxy_map_hub.html` to browse everything else via its own index). **Logged, not built** (`ceo_plan_items` G65-G68): folding Level3→Current/Level4→Zoom/l0.html→galaxy_map.html as scoped next-phase work, and the recursive L0↔river/module/function interaction-matrix idea as a real, new analytical direction needing its own evidence pass. All touched pages balanced, idempotent, zero console errors. Full spec: `records/2026-08/galaxy_map_bubble_toggle_fold_ceo_plan_2026-08-21.txt`.
- **Real `/misunderstanding` catch on the Skill Network/Skills merge, then G65 (Level3→Current fold) and G69 (shared level-rail nav), same day, immediate follow-up.** Alex, mid-build: "okay so the skills composition network is the map view, whilst g28 is the table view, again you are duplicating and not integrating everything exactly as i said. why are you doing this? /misunderstanding" — a direct catch of the SAME duplicate-instead-of-merge mistake the earlier same-day L0-fusion pass had already been corrected for once (a first attempt gave `galaxy_map_skill_network.html` and `galaxy_map_skills.html`/G28 each their OWN independent bubble/table toggle instead of becoming one real merged page). **Corrected**: `galaxy_map_skill_network.html` is now the ONE real page — map view is its own real circular skill-composition-network diagram (24 nodes, 118 real invocation edges) with a new click-to-reveal per-skill detail panel (the real per-skill passage content is preserved this way, not dropped), table view is G28's own real classification grid, imported directly (`from galaxy_map_skills import SKILLS, GROUPS, build_group_section`) rather than re-derived. `galaxy_map_skills.py` is now a pure data/render module with no standalone page; `graphify-out/galaxy_map_skills.html` no longer exists. ~6 real cross-references repointed. Verified via headless Chromium: map default, 1 detail panel per click, table↔map jump correct, zero console errors, balanced, idempotent. **G65 shipped**: `galaxy_map_current.py`'s per-module section gained a real map/table toggle — map view is Level3's own real function call-chain/band diagram, moved verbatim (not retyped) to preserve carefully-debugged layout logic (`compute_function_rank()`'s cycle-guarded DFS, `_split_into_bands()`, the Alex/Oracle/Composio/Last.fm evidence-gated bubbles); one real rule-8 catch made moving it — the band-renderer referenced `LEVEL5_BY_FUNC`, undefined in Current's namespace, fixed by reusing Current's own already-identical `NOTABLE` dict instead of duplicating it. `galaxy_map_level3.py`/`.html` deleted; ~22 real cross-references across 10 files repointed at `galaxy_map_current.html#mod-X` (both href AND display label, e.g. "🔽 Level 3" → "🧬 Current Series"). One real stale anchor (`videoSummary._delete`, drifted from earlier same-session edits) caught by the standing build-time anchor-verification discipline and fixed. Verified: 45 modules, 436 real Currents, idempotent, balanced, headless-Chromium confirmed module/band/map-table toggling and hash-nav all working. **G69, a real separate ask, not part of the G65-G68 fold sequence**: "🌌 Level 0 → 🏛️ Level 1 → 🌊 Level 2 → 🔽 Level 3 → 🖱️ Level 4 → 🧠 Level 5; i want every possible version of these to all be present in each file so i can see it better too." One shared `inject_level_rail()` helper added to `graphify_river_group.py` (rule 8 — every one of the 22 real page scripts shares the identical `OUT.write_text(html, encoding='utf-8')` convention, so a single mechanical patcher script wired it into all 22 rather than hand-copying a nav block) — every page now shows the full 8-stop ladder (🌌L0→🏛️L1→🌊L2→🖇️L2.5→🔽Current(L3)→🖱️Zoom(L4)→🧠L5→🔬L6, current stop highlighted). **Real regression caught same pass**: the rail's own links inflated `galaxy_map_hub.py`'s "flows into" edge count 56→164 (its edge computation reads every real `<a href>` as content flow — the rail is navigation chrome, same class as the existing `galaxy_map.html`-as-home exclusion); fixed by stripping the `<nav class="level-rail">` block before scanning, count correctly returned to 56. All 22 pages re-verified balanced/idempotent. `ceo_plan_items`: G61/G62 titles corrected to describe the real merge (G62 cross-references G61 rather than duplicating evidence, rule 8), G65 green, new G69 green. G66-G68 planned and shipped same session — see the next bullet.
- **G66/G67/G68 shipped, same day, immediate follow-up — real `/interrogation` (3 AskUserQuestion forks) resolved before building, per Alex's own instruction ("planning out g66 to g68 planning with /interrogation and /paranoia").** Real evidence gathered first: Level4's own docstring already said its role was retired into Module by G48, and its 12-card grain never matched Zoom's 436-function grain the way Level3/Current shared one dataset; `galaxy_map.html` had no map/table toggle at all (an Infra/Inter facet CHOICE instead, a genuinely different mechanic per Alex's own established rule). **G66 (retire Level4, confirmed)**: `galaxy_map_level4.py`/`.html` deleted outright. `galaxy_map_module.py`'s 2 real dead links (SVG diamond + legend row) fixed to show the same info inline instead of linking to a retired page. Breadcrumb "Level 4" links in `galaxy_map_level5.py`/`galaxy_map_level6.py` repointed at `galaxy_map_zoom.html`, the real current Level-4 slot. **G67 (fold L0's matrix into galaxy_map.html, confirmed, Alex's own words: "use what we have, dont make new shit im tired of it, just use what we have or ask what fits")**: `galaxy_map.py` gained a real map/table toggle — map is the existing SVG+bubbles+facet view, unchanged; table is `galaxy_map_l0.py`'s own leftover 17-edge matrix, imported directly (`build_matrix()` plus a new `build_table_details()` factored out of old inline code for reuse, rule 8), exact CSS/JS toggle mechanic reused verbatim rather than reinvented. `galaxy_map_l0.py` is now a pure data/render module (dead `build_unit_panel`/`build_drop_panel`/`other_unit` removed, zero remaining callers); `graphify-out/galaxy_map_l0.html` deleted; ~4 cross-references repointed. **G68 (satisfied by naming the real chain, confirmed, Alex's own words: "these would be levels of matrixes... it all falls into map/html idea... no extra made stuff we got all of it from what i understand")**: G67 itself IS the real L0↔L1 matrix; the next real layer down, `galaxy_map_dimensions.html` (44 L2 modules × 5 dimensions), already existed — only a real cross-link was added, naming the recursive chain rather than inventing a new page. 20 real Galaxy Map pages now (down from 22), all balanced, idempotent, headless-Chromium confirmed zero console errors and zero dead level4/l0 links. `galaxy_map_hub.py` recatalogued (19 pages, 42 real edges, down from 56). `ceo_plan_items` G66/G67/G68 all green.
- **G70/G71/G72 shipped, same day, immediate follow-up — 4 more rapid-fire real asks.** **G70**: Alex flagged External AI's own facet panel as "too ambiguous" (3 vague partner edges, no visible live/dormant breakdown), asking to break Externals into its AI components with real `/paranoia`+`/drift`. Real evidence: the Anthropic(live)/Kimi(dormant)/Luna(dormant) breakdown was already computed (`ORACLE_PROVIDERS`) and attached to `rpgace_architecture`/`alex` (the units that USE it) but never reciprocally to `external_ai` itself (the unit that IS it) — a real asymmetry, fixed with 3 reciprocal facet rows sharing the same `share_key` so cross-highlighting works automatically. 2 edge descriptions tightened to name Anthropic specifically rather than vaguely "External AI." **Not done**: splitting External AI into 3 separate L0 units — would break the deliberate uniform L0 grain (R16) and duplicate `galaxy_map_externals.html`'s (G27) already-built breakdown. **G71**: "get rid of [the old breadcrumb chains]... i want only [the level rail] present" — the old `<div class="breadcrumb">` chains, redundant with the rail, removed entirely from 11 pages; 7 Dimension pages kept their own genuine non-ladder cross-links, stripped of the redundant ladder prefix. "2.5 is a table view of 2, so fix that too please" — `galaxy_map_module.py`'s per-river section gained a real map/table toggle (map = existing SVG flow diagram, table = `galaxy_map_level2_5.py`'s own `build_river_section()`, imported directly, its own `<section id>` wrapper and dead "zoom out" link removed to avoid an id collision with Level 2's own section). `galaxy_map_level2_5.py` is now a pure data/render module; `graphify-out/galaxy_map_level2_5.html` deleted. The rail's own L2.5 stop now points at `galaxy_map_module.html` (both L2/L2.5 co-highlight there). 19 real Galaxy Map pages now (was 22 at session peak). **G72**: "decisions should also be on whole, with information on deletes, choices and text inputs... too many docs that could be merged for alex infra" — of Level 5's 7 core-logic points, only `oracle-mode` is genuinely Alex's own choice (`decider: "Alex (manual toggle)"`); added to Alex's Decisions facet, cross-linked to `galaxy_map_level5.html` rather than literally merging `galaxy_map_decisions.py`/`galaxy_map_level5.py` (real evidence: they cover genuinely different things — human-confirm gates vs. curated core logic, same non-merge precedent as `taxonomy_tree`/`taxonomy_nodes`). **"Text inputs" as a decision category is explicitly NOT built** — genuinely new, no existing evidenced page to reuse, flagged for Alex's scope confirmation before a real sweep. All touched pages re-verified balanced/idempotent, headless-Chromium confirmed zero console errors. `ceo_plan_items` G70/G71 green, G72 yellow (partial) — completed same day, see the next bullet.
- **G72 completed, same day, immediate follow-up — the Decision Matrix, real /interrogation (2 forks) + a new permanent standing rule R22.** Alex: "let's do g72 flagged and unify all decisions types, then split by level at which decision is made and what rivers, this will be the decision matrix/table, then we map out bubble system. I want this rule, whenever we update galaxy, it must update the matrix first or create a new one for table reference to bubble system, bubble systems always follow and showcase what on table to keep everything coherent." Real evidence resolved 2 forks before building: "level" = documentation depth (every decision reaches Current(L3)+L6; +L5 only if curated there — not a literal L0-L6 spread, since no decision point is anchored above function-grain), text-input scope = small curated set, expandable later. **Built**: new `scripts/galaxy_map_decision_matrix.py` unifies `galaxy_map_decisions.py`'s 10 gates + `galaxy_map_level5.py`'s 7 logic points (both imported directly) with 4 new anchor-verified text-input points (Oracle chat prompt, Beat Log form, Director Blend inspiration, Taxonomy Placement Editor) — 21 real decisions across 6 rivers (module→river resolved via `RIVER_MODULES`). Real table view (river × kind cross-tab) built FIRST; bubble view is a pure rendering layer over the identical `build_unified()` data — no bubble without a real table row. **New standing rule, R22 (`.claude/skills/CEO/SKILL.md`), Alex's own verbatim words**: "bubble systems always follow and showcase what on table" — extracted as a permanent, checkable project rule from this exact build, generalizing a pattern several pages already followed implicitly. Cross-linked from `galaxy_map_decisions.html`/`galaxy_map_level5.html`. 20 real Galaxy Map pages now. Verified via headless Chromium: table default, cell→bubble jump, bubble detail reveal, zero console errors. A real rule-12 apostrophe-escaping mistake (double-backslash instead of single) was caught by the standing `node --check` habit and fixed before shipping. `ceo_plan_items` G72 moved yellow→green.
- **Real /paranoia+/drift+/free-for-all-debate pass, Aug 22 — "how has /decompress affected the layout of architecture against what's documented in Galaxy Map."** Alex's own direct ask, after the day's real hand-test-driven fixes. **Real GODMODE evidence, not asserted**: live `rpgace_core.js` has 55 real `RPGACE.register(...)` modules (confirmed by direct grep, matching Alex's own console log "Modules: Array(55)"), against `RIVER_MODULES`' 45 tracked + its own documented 9-module cross-cutting-exclusion comment + `dashDeck` (documented separately) = 55 — every live module accounted for **except `errorLog`** (added Aug 12, never reconciled into either list) — a real, MINOR, standalone classification gap, unrelated to today's own code changes, fixed inline (added to the exclusion comment). **Real, CONFIRMED, MATERIAL finding**: `scripts/galaxy_map_level5.py`'s hand-cited anchor for `mockOracle.setMode` still reads lines `(24104, 24132)` — the anchor text ("MODES: [...]") now lives at line **24239**, a ~135-line drift directly caused by this session's own real edits (A5 Phase 2, the Decision Matrix build, and today's hand-test-fix pass, all landing before `mockOracle` in file order). Confirmed via direct `sed`/`grep`: if the Galaxy Map pipeline ran right now, this script's own build-time anchor-verification would fail loud (its documented, correct fail-safe design — never silently wrong, just currently unbuildable). Other Level 5/Decisions anchors (contentProductionLive, conidPot, beatLog, phylumPath, taxonomyReviewQueue) sit at file positions almost certainly also shifted by the same cumulative edits, not individually re-verified this pass (scope-proportionate — the general finding is already established). **Real free-for-all-debate, 4 angles, not manufactured**: (1) module-classification gap — real but MINOR, fixed cheaply; (2) confirmed anchor drift — real, MATERIAL, the day's edits are the direct cause; (3) standing regeneration debt — already logged once this session (the `/Bedtime` pass), not a new discovery; (4) the case for NOT regenerating mid-session — Alex is actively hand-testing and requesting more UI iteration in the same session, so running the multi-step regeneration pipeline (`update --code-only` → `export html` → `recolor` → `river-group` → `to_obsidian`) now risks being immediately invalidated by the next edit, wasting the cycle — better run once, at session close. **Real /drift verdict**: VERDICT **drifted** (not on track) — confirmed, not speculative. BASIS **ratified** (the Galaxy Development Framework's own R17: oversight docs stem from the Galaxy Map, code changes get reconciled). GRADE **MATERIAL** — real, un-shipped documentation debt with a confirmed concrete consequence (a build that would fail if run), but not CAPTURED (nothing is lost — the anchors can be re-verified and fixed, the underlying code is intact, just at different line numbers). **Decision, not deferred silently**: fix the cheap module-classification gap now (done); do NOT run the full regeneration pipeline mid-session while more hand-test iteration is actively happening — that's the next real `/Bedtime` task, now backed by concrete evidence of why it matters rather than a general "it's stale" flag. Real, stale "54 modules" claim in the same comment logged to `achiever_archive` (brown) and corrected alongside the exclusion-list fix.
- **Aug 22 2026 — the real `boot_load_lag_client_interactive_12000ms` root cause found and fixed, via Alex's own successfully-captured DevTools Performance flame chart, closing the "do this that you sent me" ask directly.** Alex hand-captured a full 33.44s Performance recording (after several false starts — DevTools' "Record and reload" auto-stops on an idle heuristic well before RPGACE's delayed, login-triggered freeze occurs; the working method was: plain Record button → manual address-bar reload → log in → watch console for the `perfWatch` warning → manual stop). The flame chart's own call stack, read directly off his screenshot, showed the real 12-13 second red "Task" block under `refreshEncyclopediaDisplay → renderEncEntries → sortEntries` — a genuinely different root cause than either of the two hypotheses this project had been chasing (network-request volume, boot-task staggering), both honestly ruled out earlier the same day by offline reproduction. Reading `sortEntries()` (line 3103) confirmed the real bug: its `'unique'` sort mode recomputed a full O(n) cross-entry text-overlap score from scratch **inside the sort comparator**, meaning the same expensive scan reran on every single comparison during the sort (`O(n log n)` full recomputations, not one) — and `let ENC_SORT = 'unique'` (line 3057) is the actual DEFAULT value, so this ran unconditionally on the Encyclopedia's very first render, not on some rare mode a user has to opt into. **Fixed, 100% behavior-preserving**: each entry's overlap score is now precomputed exactly once into a `Map` before the sort runs, and the comparator just reads from it — same output ordering, same content, just computed once instead of ~n times. A separate, pre-existing correctness bug in the same original algorithm (a case-sensitivity inconsistency — `allText` was never lowercased while the per-entry comparison text was, silently breaking both the self-exclusion filter and the substring match) was found in passing and **deliberately left unfixed** to keep this a pure performance fix with zero behavior change — flagged here as a real, separate, open item for Alex's own call, not silently bundled in. **Quantified before shipping, not just asserted (rule 1/4 discipline)**: a standalone Node.js benchmark (old vs. new, both algorithms extracted verbatim) confirmed a consistent real **2.0x speedup** at every tested size (n=300: 2051ms→1003ms; n=500: 5426ms→2741ms), and at content length matching the real ~3000-char Oracle-prompt slice, the OLD algorithm's true severity became visible: n=300 took 9.5s, n=500 took 27s, n=800 took **68.8s** (all fixed at exactly 2.0x, so NEW = ~4.8s/13.4s/33.6s respectively) — a clean `O(n²)` scaling curve (each size-ratio squared predicts the measured time almost exactly), confirming this wasn't just "slow," it was genuinely capable of freezing the tab for over a minute at a large-enough real Encyclopedia, far past the ~12s Alex actually hit. **Honest caveats, not smoothed over**: the benchmark uses synthetic entries — Alex's own real-world re-test after this deploys is the true confirmation of how much better it feels; and the `'unique'` mode remains inherently `O(n²)`-in-entry-count even after this fix (just with the constant factor cut in half) — whether `ENC_SORT`'s default should move off `'unique'` entirely as a further safety margin is a real, undecided, unforced question for Alex, not acted on here. `node --check` clean, re-minified (`rpgace_core.min.js` regenerated), cache-bust bumped `20260822e`→`20260822f`. `error_log` row `aebddd95-3a2b-4e20-9a07-8693f6fdbee8` updated with this final root cause.
- **Aug 22 2026 (same day, immediate follow-up) — real /paranoia+Council-of-5+/interrogation on the sortEntries fix's own 2 flagged loose ends, closed with real evidence, plus a real-world confirmation of the fix itself.** Alex's own ask: paranoia the two open items (the case-sensitivity bug, whether to move `ENC_SORT`'s default off `'unique'`), Council of 5 the options, interrogate him, then `/drift`+`/CEO` the outcome. **Real, honest finding surfaced mid-pass, not smoothed over**: a fresh direct Supabase query (not assumed) found the live `encyclopedia` table holds only 70 real rows (median content 2435 chars, max 3871 — no outlier hiding a bigger real workload), and re-benchmarking the OLD pre-fix algorithm at that real scale predicted only ~470ms of blocking, not 12+ seconds — meaning the earlier "confirmed root cause" framing was ahead of the evidence at the time. `error_log` row `aebddd95` was reopened (`status='active'`) with this honest correction rather than left standing as prematurely "resolved." **Council of 5, real options laid out for both items** (fix-now vs. leave-as-documented for the case bug; keep-default vs. safety-valve vs. change-default vs. full-algorithm-rewrite for `ENC_SORT`) — full reasoning: the algorithm-rewrite option was rejected as disproportionate to RPGACE's real single-user personal-tool scale, no evidence of near-term need. **Alex's real answers** (`/interrogation`, AskUserQuestion): fix the case-sensitivity bug now; change `ENC_SORT`'s default to `'recent'` now (not a safety-valve, not status quo); yes, re-test live and report back. **Built**: `sortEntries()`'s `'unique'` branch now lowercases `allText` too (previously only the per-entry comparison text was lowercased) and excludes self by real array index instead of a string-equality check that only worked by case-matching coincidence — fixes both symptoms of the one root cause (self-inflation from comparing an entry against its own raw-case text, and missed cross-entry matches on any word capitalized in its original occurrence) with a single, real, Alex-approved change to the actual ranking output (not a perf-only tweak this time). `ENC_SORT`'s module-scope default changed `'unique'`→`'recent'`; `index.html`'s `enc-sort-recent`/`enc-sort-unique` buttons swapped which one carries the `active` class to match — `'unique'` stays fully available as a one-click mode, only the default changed. **Real-world confirmation, not just a benchmark**: Alex reloaded the live deployed fix and captured a fresh Performance recording — the reported freeze dropped from a repeatedly-measured ~12,150-13,329ms down to a single real `perfWatch` warning of **1,404ms** (~89% real reduction, a full order-of-magnitude change), confirming `sortEntries` really was the dominant cause despite the isolated Node benchmark under-predicting the real browser-measured time (real DOM rendering + network round trips + other interleaved boot work all ride along in the same measured Task, which a synthetic benchmark of the sort function alone can't capture). `error_log` row `aebddd95` marked `resolved` for real this time, backed by a live before/after measurement. **A small residual ~1.4s block + a collapsed group of 5 "setTimeout handler took Nms" console violations were flagged, not investigated this pass** — real, much smaller, lower-priority items logged for Alex's own future call, not silently dropped. **Real `/drift` check**: Baseline RATIFIED (Alex's own 3 AskUserQuestion answers, same turn) — Work map FULL (3 sanctioned items: case-sensitivity fix, default change, re-test) — Verdict ON TRACK, all 3 shipped exactly as approved, zero gap, no MINOR/MATERIAL/CAPTURED finding. **Real, deliberate `/CEO` scoping decision**: this did NOT get a new `ceo_plans`/`ceo_plan_items` row — the only 2 real rows in that table (Galaxy Map, Massive Expansion) are both multi-session frameworks, and forcing a small, single-session, already-fully-resolved bug fix + UX default change into that same ceremony would be disproportionate (rule 11) — the `/drift` check above and this oversight-doc entry are the right-sized real record of the outcome instead. `node --check` clean, re-minified, cache-bust bumped `20260822f`→`20260822g`.
- **Aug 22 2026 — real 3rd `/CEO` plan ratified and Phase 1 shipped: "Worm Family & Encyclopedia Wrapper."** Alex: "i want to next work on bookworm and encyclopedia, i think that whole system needs a revamp" → a real `/interrogation` (3 rounds, 11 real forks, every one resolved with the recommended option) → approved → built via Omnitrix (Opus 4.8 builder, Sonnet review, no Fable) → `/drift`+Council of 5 on the outcome. Full compiled spec: `records/2026-08/worm_family_encyclopedia_wrapper_ceo_spec_2026-08-22.txt`. **Phase 1 (W1-W5) shipped, real `ceo_plans` row ratified** (3rd real plan alongside Galaxy Map/Massive Expansion). **W1**: Corpus is now reachable directly from Bookworm's own card popup (a new "🎼 Open MusicWorm (Corpus)" button calling `dashDeck._openCorpus()` directly — Corpus is already its own full popup, not a raw static panel, so it correctly bypasses `_jumpToResearchPanel`) — a real UI-entry-point move only, matching the proven Aug 15 A5 Phase 1 pattern; `refCorpus`'s own module/data and its standalone dashboard card are both untouched. **W2/W3**: File Analyzer → "Videoworm" (real evidence basis: its own on-page copy analyses video content only), Video Finder → "Videoworm: Find Videos" (a sub-action, since it just feeds URLs into the same panel), Corpus → "MusicWorm" with an honest subtitle ("Reference-track library. Insight extraction not yet built.") so the UI never implies parity with Bookworm/Videoworm — UI labels/copy only, zero module/function renames, panel ids unchanged. **W4**: the existing jump-button list IS the real "which worm?" picker A8 already envisioned — reused with a new "Choose a worm:" heading rather than building a duplicate UI (rule 8); Bibliography sits under its own "Reference shelf:" heading since it isn't a worm. **Articleworm explicitly NOT built** — no real existing Bookworm-reachable panel maps to it (the closest generic-ingestion feature, `scheduleOracle._ingest`/F11, is a chat-triggered feature, not a panel) — stated plainly rather than inventing a button pointing at nothing. **W5, the real new architecture piece**: `phylumPath._insertNewSteps` (confirmed via direct grep to be THE one real choke point every "insight accepted into the taxonomy tree" moment converges on, from every source — 6 real call sites project-wide) gained an optional 6th parameter, `sourceMeta` (`{source, title}`, default `null` — every existing caller not passing it is byte-identical to before). When a real leaf gets committed AND `sourceMeta` is present, one new, purely additive, fire-and-forget write lands a real `encyclopedia` companion entry — content is a **direct excerpt of the insight text** (Alex's own explicit choice, zero new Oracle call, zero added token cost), reusing the *already-existing* `encyclopedia.source`/`encyclopedia.taxonomy_node_id` columns (no schema change) — cross-linked in the reverse direction from F7's existing Encyclopedia→Taxonomy back-reference. All 6 real call sites wired with real source attribution: `taxonomyReviewQueue._acceptPhylumPathProposal`/`_editPhylumPathProposal` (via a new shared `_sourceMetaForProposal(p)` helper, rule 8 — maps the real `taxonomy_proposals.source_type` column: `'content_intelligence'`→`videoworm`; `'encyclopedia'`-sourced and any unrecognized type → `null`, correctly skipped since an encyclopedia-sourced proposal already has its own real row via F7, a 2nd entry would be a real duplicate), `taxonomyTree._proposeLineageViaPhylumPath` (its own real `sourceType` param, defaulting to `'oracle'`), `phylumPath._placeInsight` (the manual Phylum Path panel, labelled `'oracle'`), and both of `bookworm`'s own real accept paths (`'bookworm'`, using the real book title) — **plus a 3rd, previously-unresearched real caller the build phase itself found and fixed** (`_renderChapterSummary`'s "🔁 Retry Leaf Creation" button), a genuine coverage improvement beyond the original compiled spec, not scope creep (same real goal, more completely served). **Real, honestly-flagged gap, not silently decided**: the dedup-extend branch of `_insertNewSteps` (extending an existing leaf instead of creating a new one) does NOT get a companion entry yet — a real MINOR completeness gap for a future small follow-up, not a violation of what was approved. **Real `/drift` check**: Baseline RATIFIED, Work map FULL (W1-W5), Verdict ON TRACK, one MINOR finding (the dedup-extend gap above). Council of 5: technical risk low (additive-only, zero schema change, taxonomy_tree's own human-gated write untouched), scope/cost proportionate (Phase 1 only, zero new Oracle calls), maintainability good (shared helper not duplicated logic), UX reasonable (reused existing UI, honest MusicWorm disclosure) — real why-not-ship case: genuinely untested by hand, same standing gap as most of this project. **Phase 2 (W6-W10 — code-reduction check, the A4 revisit, Bookworm's card-list UX, EPUB support, Encyclopedia's own source-worm filter/category revisit) is real, ratified, and explicitly deferred** — `ceo_plan_items` rows exist (red) but nothing built, per Alex's own confirmed phasing answer. `node --check` clean on both source and minified, re-minified, cache-bust bumped `20260822g`→`20260822h`. **Untested by hand.**
- **Aug 22 2026 (same day, immediate follow-up) — the dedup-extend gap debated and deferred with real evidence; W11 (Articleworm)/W12 (Debate Worm) /paranoia'd and logged blue.** Alex asked for a real debate (`/paranoia`+`/drift`+`/misunderstanding`+`/scope`) on the W5 dedup-extend gap before deciding whether to fix it now. **Real evidence, not assumed**: a direct `taxonomy_decision_log` query found **zero** real dedup-extend events have ever happened (34 real normal placements logged since the July 22 audit-log build, 0 extends) against 243 total real tree leaves — the gap is currently theoretical, not a real observed loss. **Recommendation, evidence-backed**: defer, don't fix now — folded into Phase 2 (W6's code-reduction check / W10's Encyclopedia revisit), since a real design question (should repeated dedup-extends against the same leaf mint multiple companion entries, or one) is better answered once real usage data exists than guessed at now. **Real, separate ask, same message**: Alex wants two new worms — Articleworm (an external GitHub-repo analyzer for real scientific papers, JSTOR/Google Scholar) and Debate Worm (cross-worm contradiction detection + adversarial debate feeding taxonomy updates) — `/paranoia`'d and logged as blue, not built. **Real, load-bearing honest pushback delivered, per `/interrogation`'s own discipline**: live web evidence (checked this session, not assumed) found neither JSTOR nor Google Scholar is a safe scrape target — JSTOR's real API is free but capped at 1000 docs/request and gated behind an official Text & Data Mining program; Google Scholar has no official API and its own terms prohibit large-scale automated access. Real, legitimate alternative identified instead: the Semantic Scholar API (Allen Institute for AI, non-profit, free, no key, 200M+ papers) plus arXiv/PubMed/CORE for open-access full text — and the real architecture fit is RPGACE's own already-proven Total-systems pattern (a separate Claude Code session on its own external repo, dispatched via a Supabase queue, same shape as OpenMontage CC/Graphify CC), not a new invention. **Debate Worm's own real, load-bearing findings**: the existing `/debate` skill is a Claude-Code-side, human-in-the-loop protocol — a live in-app autonomous version needs genuinely new Oracle-API infrastructure (a real, recurring per-insight token cost, rule 11, needing a free prefilter before any expensive debate call) and must non-negotiably still route through the same human-checkpoint taxonomy gate (rule 4) rather than auto-updating the tree on its own verdict. Both logged as real `ceo_plan_items` W11/W12 (`blue`, `basis='provisional'`) under the same Worm Family plan (rule 8 — not a 4th `ceo_plans` row for what's really 2 more ideas on one plan's own list) and in `future_integrations.html`'s Blue section. Nothing built — genuinely idea-stage, pending a future formal `/interrogation` pass (W12 additionally depends on W11 existing first).
- **Aug 23 2026 — Phase 2 continued: W6 resolved green, W7/W10 re-confirmed correctly gated, W8/W9 shipped.** New day (session-start checks re-run: 0 new oracle suggestions, 0 active errors, 0 flagged map issues — clean). Alex: "we will continue from same point, build the phase 2 you can, then we /interrogation the rest." **Real `/interrogation` on W6/W7/W10**, all landing on the recommended option: W6 (code-reduction check) marked **green** — the real duplication it was meant to catch (`taxonomyReviewQueue`'s 2 accept paths needing the same `source_type`→worm mapping) had already been fixed in passing during W5's own build via the shared `_sourceMetaForProposal` helper; no further consolidation opportunity has surfaced, so a dedicated pass would be looking for a problem rather than responding to one. W7 (A4 revisit) and W10 (Encyclopedia filter) both stay red — real confirmation the original usage-data gating still holds, no separate urgency; Alex will hand-test Phase 1 himself soon, the real forcing function that unblocks both. **W8 shipped**: real evidence found the actual gap narrower than the original July 17 backlog text implied — Bookworm's top-level book list was already a clean card list; the real problem was one level deeper, 4 of 5 chapter-level views (`_renderChapterRead`/`_renderAddChapterText`/`_renderInsightReview`/`_renderWaitingForNextInsight`) had no way back to the chapter list except a full exit to the dashboard, only `_renderChapterSummary` had one. All 4 now carry the same "← Back to chapter list" button, `_renderWaitingForNextInsight`'s own background poll-loop `stopped` flag correctly set on back too (matching its existing Exit-to-Dashboard behavior). **W9 shipped**: EPUB upload, mirroring the existing PDF-upload pattern exactly — `_ensureEpubJs()` dynamically loads JSZip (never a static script tag), `_startBookFromEPUB` parses the real `META-INF/container.xml`→OPF manifest→spine reading order (namespace-agnostic tag lookups, since real EPUBs vary between default/prefixed namespaces), extracts each content file to plain text via `DOMParser` with block-element/`<br>`-to-newline conversion (a real text-quality bug in the first draft — raw `.textContent` ran headings and paragraphs together — caught by testing before shipping, not after), sequential not parallel (guarantees reading order, bounds memory for a large book), non-fatal fallbacks for a missing spine file or a broken/empty spine. Feeds the same `/api/bookworm-fetch` pipeline as PDF/URL, tagged `'uploaded-epub'`. **Real opportunistic fix, found and applied to both the PDF and new EPUB upload rows**: the upload label's `.textContent` reset was silently removing its own nested `<input type="file">` on every restore, making it a real one-shot control — worked once, then dead until the widget re-injected. Fixed on both rows rather than shipping a fixed EPUB clone next to a broken original (rule 8). **Verification, honestly scoped**: both W8 and W9 verified via a headless-Chromium harness testing the real shipped source against realistic fixtures (a synthetic EPUB with prefixed namespaces, nested OPF dir, relative paths, percent-encoding, a missing spine file, a dangling idref) — real, but not the same as Alex's own hand-test with a real purchased ebook, which hasn't happened yet. `node --check` clean, re-minified, cache-bust bumped `20260822h`→`20260823a`.
- **Aug 23 2026 (same day, later pass) — real /Aintergration on `scrollcraft` (a third-party scroll-driven-page Claude Code skill Alex brought), verdict Reference only; DESIGN.md gained a real refuse list + audit.** Alex wanted to use the repo for "UI redesigns of rpgace app." Real GODMODE evidence: scrollcraft's whole mechanism (video-scrubbing, pinned sections, parallax) directly conflicts with DESIGN.md's own already-standing, explicit rule (`"no GSAP/Lenis/scroll-jacking — this is a working tool, not a landing page"`, "L1 is a hard cap") and needs a real local build toolchain (ffmpeg/playwright/Node scripts) against RPGACE's zero-build-step architecture — same conflict class as the July 20 Framer Motion rejection. **Real, genuine value found anyway**: scrollcraft's own design-craft principles (typography floor, color roles, depth-via-5-tools) turned out to already be independently present in DESIGN.md — but its named "refuse list" concept (identical card grids, gradient text, fake stats, AI-purple gradients) was a real, useful addition RPGACE didn't have. Alex confirmed: add the list, audit pages against it. **Built**: DESIGN.md §8 gained 6 new refuse-list Don'ts, adapted for RPGACE's real context (not copied verbatim — items like a "cream-and-brass artisan palette" don't apply here). **Real audit (§11, new)**: grep-checked the live app — clean on gradient text, decorative counters, scroll cues, invented stats. **A real near-miss caught before reporting**: `.dd-card`'s uniform border/gold-CTA styling first read as "identical undifferentiated cards," but re-checking against DESIGN.md's own existing §8 Do #2 ("one accent color, gold for every go action") confirmed this is the correct, deliberate execution of an already-standing rule, not a gap — `.quest-card`'s independently-built colored-border-bar accent is a second real example of the same correct restraint. The one real gap surfaced is pre-existing, already-tracked debt (the ~72 unreconciled hardcoded hex colors, CLAUDE.md's own live count), not new. Doc-only change, no code touched.
- **Aug 23 2026 (same day, later pass) — real /misunderstanding + a whole-app UI-messiness audit, "UI Consistency & Worm Family Cohesion — Batch 1" shipped (5 items) — Research Lab genuinely retired, not just retitled.** Alex asked for an audit against 3 named complaints (Bookworm/worm-picker asymmetry, Research Lab references not fully removed, Corpus not fully absorbed) via `/impeccable`+`/paranoia`+`/drift`, then `/free-for-all-debate` for more, compiled to a `/CEO` plan. **Real, caught mistake**: the first pass scoped itself to Bookworm alone; Alex called it out directly ("why are you disregarding some asks i make, /misunderstanding?") — his own wording ("go through all buttons possible," "so many pages look messy") named the 3 items as examples, not the boundary. Ran the real 3-step repair, then swept the whole app (Oversight/Knowledge Gaps/Content Pipeline/Morning Brief/Taxonomy & Review/Chronicles), surfacing 2 more real findings. Full record: `records/2026-08/ui_consistency_worm_cohesion_audit_ceo_spec_2026-08-23.txt`. **Alex approved 5 items, expanding one**: "research tab should be completely gone, with needed part going to either the worms or content pipeline, i did ask for that exactly to keep its module parts in tact." Real `ceo_plans`/`ceo_plan_items` rows created (3rd item stayed within the same batch as the other 4, not spun into a 4th plan), a condensed `/paranoia` pass run (real risk enumeration + a genuine dissenting case, deliberately scaled down from the full multi-round ceremony — named honestly rather than silently downscoped), built via Omnitrix (Opus 4.8, Sonnet review). **Shipped**: (1) "Video Workshop"→"Upload Workshop" name leak fixed in 6 strings, 3 of which were writing new Encyclopedia saves under the wrong title going forward. (2) `#page-learning` (the Research Lab page) converted to a plain non-routable staging div (`#legacy-worm-panels`) — new shared `RPGACE.utils.panelHome()` replaces 5 hand-rolled fallback chains; new `dashDeck._openPanelPopup()`/`_openPanelById()`/`PANEL_POPUPS` replace the deleted `_jumpToResearchPanel` (0 remaining callers) — Videoworm/Videoworm-Find-Videos/Bibliography now relocate into popups from Bookworm's own worm-picker, Upload Workshop from Content Pipeline's, matching the same relocate-into-popup pattern Corpus/Knowledge Gaps/Morning Brief already used (never a page-navigate). 5 real `page:show` hook listeners rewired to a new `worm-panel:open` hook (config's `applyIntelUI`, `feynman`, `intelDelete`, `taxonomyTree`'s `taxtree-manual-btn` sibling-carry, plus a real live crash-on-click bug the original audit missed — `beatLog`'s "Return to Beat Log" retro path — found by a fresh grep during the build itself). (3) The dead `page-research` fallback clause removed everywhere. (8) Oversight popup's doc-count label — hardcoded to imply 8 items while the real array had grown to 12 (Error Log/Future Integrations/Achiever/Session Lessons added Aug 12-21, label never revisited) — now derived from `primary.length`. (9) Corpus's duplicated false "feeds the taxonomy tree" claim (dashboard card AND popup footer) corrected to honestly describe current scope; the real write path stays a named, unbuilt future item. **Independently re-verified before shipping, not just trusted**: full diff read line-by-line against the build report; div-balance re-confirmed against a fresh `git show HEAD:index.html` baseline (232/231→231/230, same pre-existing +1 delta, not new); min.js re-checked for the new real identifiers; both remaining "Video Workshop" string matches confirmed intentional historical comments. Deployed, commit `c7e4887`, Vercel deployment confirmed `READY` with a matching `githubCommitSha`. **Real, honestly flagged residual, left alone per Alex's own stated scope**: `researchTabs` (already-empty `TABS`) still has 3 `page-learning` lookups — 2 guard cleanly, the 3rd (`show()`) has zero reachable callers so it's a latent, currently-unreachable throw, not a live bug; `researchTabs`/`dashDeck._openResearch()` were both explicitly out of bounds for this batch. `ceo_plan_items` UI1/UI2/UI3/UI8/UI9 moved red→yellow. `oracleAppGrounding.SELF_KNOWLEDGE` updated same pass.
- **Real G74-G76 restructure, Aug 25 2026 — the level-rail is now FOUR levels, not eight; L2.5/Zoom(L4)/L5/L6 retired as rail stops.** Alex's own direct critique, via `/paranoia`+`/free-for-all-debate`+`/interrogation`: L0→L1→L2→Current(L3) is a real containment hierarchy (each level genuinely nests the one below); L2.5/L4/L5/L6 never were — they were cross-cutting LENSES over the same content, the same shape as a Dimension. Real /interrogation answers, all confirmed: Dimensions get equal visual/structural standing with Rivers WITHOUT becoming numbered rivers (preserves Rivers' one-module-one-home partition property); L5's 7 curated core-logic points merge into the Decision Matrix at the same `#d-<id>` anchors, `galaxy_map_level5.py`/`.html` deleted outright (real evidence found Decision Matrix already imported the 7 points by reference — genuinely redundant, not a smaller zombie page, same discipline as the Aug 21 L0-fusion precedent); L6 (1180 real branch points, 45 modules — grew from the historical "1088/44" figure, a live re-count, not a discrepancy) keeps its own page but loses ladder/bubble status, link-out-only from Decision Matrix/Current; L2.5's content stays exactly as-is (Level 2's own table view), only its rail chip is gone. Zoom/L4 folded into Current as a real per-function "Expand walkthrough detail" toggle (`galaxy_map_zoom.py`/`.html` deleted). A real, separately-confirmed evidence-gated-bubble mechanism (hand-built 5x across Alex/Oracle/Composio/Last.fm/Supabase bubbles) was generalized into one shared `render_evidence_bubble()` — proven byte-identical output on `galaxy_map_module.html` and `galaxy_map_current.html`'s 261 legend rows before/after; the Alex bubble deliberately stayed hand-rolled (bidirectional, no count label, drawn permanently — folding it in would have needed 3 mutually-exclusive flags). A real consolidated River×Dimension cross-tab shipped as `galaxy_map_dimensions.html`'s default view (table first, bubbles second per R22) — explicitly did NOT retire the module-grain Dimensions Matrix or the L0 unit matrix, since both answer a genuinely finer-grain question the roll-up destroys by construction. Every table view (L0 matrix, Decision Matrix, the new River×Dimension cross-tab, Current) now links its rows down into the same bubble/detail destination the map view already uses — except two real, honestly-flagged non-links: the module×dimension grain (no module-grain bubble exists on the page it would need to point at) and L2.5's card blocks (not bubbles at all — repointed to real Level 2 + Current + dimension links instead of the stale "pending G30" stub they used to carry). 18 real Galaxy Map pages now (was 20). A real pre-existing blocker found and fixed in passing: the pipeline itself was unbuildable going into this pass — 21 real stale code-anchor citations across `galaxy_map_level5.py`/`galaxy_map_decisions.py`/`galaxy_map_decision_matrix.py`, the Aug 22 `/paranoia` drift finding, never actually fixed until now; all 21 re-derived against live `rpgace_core.js`. `ceo_plan_items` G74/G75/G76 not yet flipped in Supabase as of this bullet — do that in the same pass that commits this. **Untested by hand** — headless-Chromium-verified only (21/21 real functional assertions).
- **G102 shipped, Aug 26 2026 — Rivers XII-XVI marked deprecated/merged, not deleted (Alex's own confirmed answer, "all 5 and yes too"), plus a real left-nav sidebar built and corrected twice.** Real evidence for exactly these 5: all always had zero real `rpgace_core.js` modules (`RIVER_MODULES[12..16] == []`) — a role-description of a Total-systems category, never a river of real app code, now superseded by the L0 Infra/Inter bubble system's own per-actor breakdown (Supabase/Oracle/Composio/Jina AI/Last.fm/librosa/n8n/Whisper for XII, Skills for XIII, Oversight Docs for XIV, Orchestrator CC as an honestly-partial successor for XV/XVI). New `RIVER_RETIRED` dict + `river_retirement_note_html()` helper (`graphify_river_group.py`, single shared source, rule 8) renders a real "⚠️ Retired — see instead" banner wherever a retired river's identity is shown — Level 1's ring (badge + legend), Level 2's per-river section, the Logic Dimension page, and each retired river's own Obsidian vault note. A real, unrelated bug fixed in passing while touching the same frontmatter field: `graphify_to_obsidian.py`'s `carries_data_flow = num <= 11` predated the Aug 18 River-v2 split and silently mis-flagged River XVII (a real app-code river) — fixed by deriving the flag from `RIVER_RETIRED` itself. `minotaur_map.html`'s own "Rivers XII-XVI" wing and `interconnection_map.md`'s Total-systems paragraph both carry a matching real note (rule 16 cross-doc drift); `system_flow_map.md` gained a new §15. **Also this pass**: a real, evidence-grounded left-nav sidebar for the whole Galaxy Map (`LEFT_NAV_*` in `graphify_river_group.py`, injected into all pages via the existing `inject_level_rail()`), corrected twice from Alex's own direct feedback before landing on the real structure — Levels as the main category, Rivers/Modules/Dimensions nested inside each level (never sibling categories), each dimension appearing exactly once at its highest applicable level. Along the way: a stale "Research Lab" entry in the Galaxy Map's own hand-curated `DASHBOARD_CARDS` mirror was found (the card was retired from `rpgace_core.js` Aug 23 but never removed here) and fixed, and a real migration-bubble visual bug on `taxonomyReviewQueue`'s own Supabase chip was root-caused (not a caching issue, genuinely tested and ruled out — a real visual-consistency gap, fixed by reusing the established `.idd-mig` component instead of a bespoke pill style). 9 real `/perspective` reports written for Oracle + 6 connector units (Alex/Skills refreshed in place). Full pipeline (18 page scripts + Obsidian export) regenerated, idempotency/link-integrity/tag-balance/headless-Chromium re-verified.
- **G103 shipped, Aug 26 2026, immediate follow-up — real rechronologize, Alex's own direct correction: "why didn't you rename river 17 - come on that an obvious fix, rechronologise the rivers please."** G102's own retirement left the one real, live river after River XI (old River XVII, Research & Intel Stream) stranded with an awkward numbering gap where XII-XVI used to sit — the real, obvious fix is closing that gap so the LIVE river sequence stays contiguous. **Real permutation applied everywhere a river number appears**: old 17 → new 12 (Research & Intel Stream, now the live sequence's own next river); old 12 → 13 (API/Auth Layer); old 13 → 14 (Skills); old 14 → 15 (Oversight Docs); old 15 → 16 (Session Records/Backlog); old 16 → 17 (Dev Tooling) — the 5 retired categories shift one slot each to sit AFTER every real river (now I-XII), in their same original relative order. Applied to `graphify_river_group.py`'s `RIVER_COLOR`/`RIVER_NAME`/`RIVER_MODULES`/`RIVER_ROLE_NOTE`/`RIVER_RETIRED`/`RIVER_FLOWS`/`DASHBOARD_CARDS`/`EXTERNAL_RIVER_LINKS`/`SKILL_SECONDARY_RIVER`, `SKILL_RIVER`/`OVERSIGHT_RIVER` constants (`galaxy_map_module.py`/`galaxy_map_river.py`/`galaxy_map_logic_dimension.py`), every live-copy text reference across `galaxy_map_module.py`/`galaxy_map_river.py`/`galaxy_map_logic_dimension.py`/`galaxy_map_l0.py`/`galaxy_map_skills.py`/`galaxy_map_skill_network.py` (including a real raw-literal bug: `galaxy_map_skills.py`'s `_river_chip(13, ...)` was pointing every skill's own river chip at the WRONG new river — the old Skills slot, now API/Auth — until fixed to `14`, exactly the class of bug rule 13's own "two separate greps" discipline exists to catch), and the live `ceo_plan_items`/`smoke_test_items` Supabase `galaxy_river` columns (structured classifier fields, not narrative — corrected directly; narrative `evidence`/`description` text describing the OLD numbering at the time it was written is left as historical record, same precedent as the Aug 11 phylum renumber). **Purely historical/narrative in-code comments citing an old river number are deliberately left as-said** (per this file's own doc-discipline) — `graphify_river_group.py`'s `RIVER_COLOR` header carries the one authoritative mapping note to read old comments against. Full pipeline regenerated, data-integrity self-check confirmed every `RIVER_FLOWS` target label resolves to the correct new river number, idempotency/link-integrity/tag-balance/headless-Chromium re-verified before shipping.
- **G104 shipped, Aug 26 2026, same day — real `/paranoia`+`/CEO`+`/drift` pass on an in-chat loop-analysis, catching a real undercount and a real rule-8 miss before anything shipped.** Alex asked "identify loop between all levels and objects of levels, infra and inter, river and modules" — a first ad hoc chat pass found only 2 small cycles using direct function calls alone. Alex pushed back directly ("surely there are more than just those as loops") — Step 5's own fact-check (per `/paranoia`'s design, "this is the step that keeps it honest") found the real cause: hook fire/listen pairs had been wrongly dismissed as "one-to-many broadcast, never a loop" — true for a SINGLE hook, false once two modules fire *different* hooks at each other. Adding that signal plus a THIRD, previously-unchecked one (shared Supabase table writes/reads) revealed a much bigger real picture: one 15-module/7-river mega-loop (direct calls + hooks) and two 6-module data-mediated loops (the beat→video pipeline's shared state; the whole taxonomy placement/review ecosystem). **Real second catch, same session**: Alex then asked "wouldn't these hooks calls and shared tables be present in galaxy map too? like in load, supabase..." — checked directly, confirmed TRUE for both: `compute_hook_signal_edges()` already computes the full project-wide fire/listen graph (Load Dimension only ever rendered its `page:show` slice of it), and `compute_all_supabase_table_touches()` already returns op-tagged `(module, function, operation)` per table (the Supabase page renders it, never cross-referenced into "does this close a loop"). **Built, per real `/interrogation` confirmation (both forks answered)**: Load Dimension gained a real 4th tab, "📡 Cross-Module Event Signals" (`galaxy_map_load.py`, reusing `compute_hook_signal_edges()` directly, `page:show` excluded since it already has its own tab) — a real, correctly-placed fix, not a duplicate. A new page, `galaxy_map_loops.py` (G104's own real deliverable), does the actual cycle math (Tarjan SCC, same method as `compute_river_flow_cycles()`) over two SEPARATE graphs — direct-calls+hooks vs. shared-table writes/reads, kept apart since they're genuinely different real mechanisms — reusing all three underlying detector functions directly (rule 8, nothing re-derived) plus `compute_module_ui_signal()`/`galaxy_map_decision_matrix.build_unified()` for a real Alex-touchpoint cross-reference per loop (per Alex's own confirmed "yes, cross-reference it"). Registered in `DIMENSION_PAGES` and `galaxy_map_hub.py`'s catalog; a real stale "3 categories"/"27 boot tasks" count on Load Dimension's own hub entry fixed in passing. Full pipeline regenerated, re-verified (idempotency/link-integrity/tag-balance/headless-Chromium).
- **G105 shipped, Aug 26 2026, same day — every module's Oracle/Composio/Last.fm/Supabase/Jina AI evidence bubble is now a real clickable link, at Level 2 AND Current(L3), Alex's own repeated direct complaint ("for the 10th time") finally traced to its real root cause.** Alex sent an annotated screenshot of `visualOracle`'s own Current-series page circling the static top-of-page "🔗 INTER (2 UNITS COMPOSE HERE)" Oracle/Supabase box ("Get Real") with arrows to the real Alex/Oracle bubble nodes already inside the diagram ("Put Here"). **Real root cause, direct code read**: `render_evidence_bubble()` — the ONE shared function both Level 2 and Level 3 diagrams use to draw every Alex/Oracle/Composio/Last.fm bubble — never had a `link_href` param; every bubble at every level has always been inert decoration despite looking clickable. Separately, Level 3's own bubble loop only covered 3 of the 5 real module-grain units `_unit_module_evidence()` defines (missing Supabase, Jina AI) — the real reason the static box kept reappearing: for most modules it was the ONLY place Supabase ever showed. **Fixed**: `render_evidence_bubble()` gained an optional `link_href` — the hub node now renders as a real `<a href>` with a "🔽 jump to this unit's own Infra bubble system ↗" cue; every real caller (2 at Level 2, 5 at Level 3) passes `UNIT_BUBBLE_SYSTEM.get(unit_id)`, the same destination map the L0 Infra/Inter system already uses (rule 8, never a 2nd hand-typed URL). Level 3 gained real Supabase (`compute_supabase_table_touches`) and Jina AI (`compute_outbound_api_call_sites`, filtered to `/api/scout`/`/api/bookworm-fetch`) bubble rows from data already computed elsewhere in the same file. `ALEX_MARGIN` raised 420→660 for the 2 new bubble rows' real vertical clearance, caught by computing the geometry before running the script. The static box (`build_module_infra_inter_row()`) is now a real evidence-gated safety net, not deleted — filters out the 5 bubble-covered units (`_BUBBLE_COVERED_UNITS`), currently always empty (confirmed by `_unit_module_evidence()`'s own closed 5-unit set), kept future-proofed against a hypothetical 6th unit rather than risking the same "silently vanishes" failure in reverse. **Independently verified**: both scripts regenerated, byte-identical on a 2nd run (idempotent); link-integrity + tag-balance clean across all 20 pages; headless Chromium confirmed on `visualOracle` itself (the exact screenshotted module) — real clickable Oracle/Supabase bubbles with correct hrefs and evidence text, Jina AI correctly absent, static box gone, zero console errors; River 11's Level-2 section confirmed the same fix; an actual screenshot was captured and visually reviewed before shipping. `ceo_plan_items` G105 logged green.
- **G106 shipped, Aug 26 2026, same day — every real Infra bubble system (Oracle/Supabase/Externals connectors/Orchestrator CC/Oversight Sync) now renders as a real SVG hub-and-spoke bubble diagram, matching Level 2/Current, not a flat card grid.** Alex, on a screenshot of `galaxy_map_connectors.html`: "these are well done, but it should be the same bubble system as in level 2 and 3, i think inters should have bubble systems too." **Built**: `render_bubble_row()` (`graphify_river_group.py`) — a real radial hub-and-spoke SVG panel reusing `render_evidence_bubble()`'s exact dashed-glow-edge visual language, via the SAME `_curved_edge`/`_build_markers` helpers (moved from `galaxy_map.py` into `graphify_river_group.py` to avoid a circular import — `galaxy_map.py` imports FROM this file — re-exported so every existing importer keeps working unchanged, rule 8). `render_infra_drilldown()` (Oracle/Supabase/the 6-connectors page/Orchestrator CC/Oversight Sync's own Infra pages) now renders all 3 real levels this way — same click-to-reveal interaction and real destination hrefs, only the visual language changed. **Real, pre-existing bug found and fixed while wiring this in**: `INFRA_DRILLDOWN_JS` used global `document.querySelector('.idd')`/`getElementById('idd-c1'/'idd-l2'/'idd-l3')` lookups that always resolve to the FIRST such element on the page — invisible on a single-drilldown page, but on `galaxy_map_connectors.html` (3 real drilldowns on one page) only the FIRST connector's own bubbles ever had a working click-through; Jina AI's and Last.fm's river/module bubbles were silently unwired the whole time, predating this session. Fixed by scoping every lookup to `document.currentScript.previousElementSibling` and switching id-based refs to per-instance class lookups. **A second, smaller real issue fixed**: every panel embedded its own `<defs>` (harmless duplicate ids, since all byte-identical per color) — fixed with an `emit_defs` flag so only the top-level panel emits defs, since nested panels' colors are always a subset. Measured: Oracle's drilldown went from 19 duplicate `<defs>` blocks to 1. **Independently verified**: `py_compile` clean, all 5 affected pages + `galaxy_map.py`/`galaxy_map_module.py`/`galaxy_map_river.py` idempotent, the 2 unaffected pages confirmed byte-identical to pre-change output, full tag-balance + link-integrity clean across 20 pages, headless Chromium confirmed real click-through on all 3 connector tabs plus Oracle with zero console errors, plus an actual screenshot captured and reviewed. `ceo_plan_items` G106 logged green.
- **`/Routine`, Aug 26 2026, same day — every real red G-step cleared, plus G89 closed from the blues.** Alex: "go through all the red g-steps so it can be built out, then we look through blues." Real evidence pass found G60/G86/G92/G95 already effectively resolved by earlier same-day work (G60 superseded by G92-98's real multi-bubble-list design; G86 superseded by G105/G106; G92 umbrella closed since all sub-items green; G95 corrected to match its own recorded resolution) — status corrections only, no build needed. **G87+G88 shipped together** (real necessary companions, not optional polish): Current(L3) gains 3 more evidence-gated bubble types (Decision/Load/Logic — new `compute_boot_task_by_function()`/`compute_load_signal()`/`compute_decision_targets()`/`compute_logic_attribution_targets()`, all reusing already-computed data, zero new detection) rendered as a real, collapsed-by-default Tier 2 panel (up to 3 `render_bubble_row()` sub-panels inside a native `<details>`) so the existing 5-bubble Tier 1 diagram (Alex/Oracle/Composio/Last.fm/Supabase/Jina AI) stays completely unchanged. **G73 shipped**: real evidence-gathering found exactly one confirmed dead-end — `contentProductionLive._buildVideoPipelinePayload` has zero outgoing edges yet its own payload is inserted into `openmontage_jobs`, the real hand-off to OpenMontage CC — a new, deliberately narrow `EXTERNAL_HANDOFF_TARGETS` dict wires a real "🐙 hands off to calesthio/OpenMontage ↗" link into both the compact row and expanded walkthrough detail for this one function. **G89 closed** (documentation-only): CEO SKILL.md's new R23 (renumbered R26 Sep 9 2026 — collided with the pre-existing R23, see CEO SKILL.md's own correction note). All verified: `py_compile` clean, idempotent (2 runs byte-identical), tag-balance + link-integrity clean across 20 pages, headless Chromium confirmed real click-through/expand behavior with zero console errors, actual screenshots captured and reviewed. `ceo_plan_items` G60/G73/G86/G87/G88/G89/G92/G95 all green.
- **G53 real pilot begins, Aug 26 2026, same day — real `/interrogation` locked the split's shape before any code was touched.** Alex's own confirmed ask (Aug 20): every one of the 60 real `RPGACE.register()` modules splits into its DOM/rendering half and its business-logic half. Real 2-question interrogation confirmed: **split shape** = one module, two internal namespaces (`module.ui.*`/`module.logic.*`), thin top-level pass-throughs preserving the exact existing public API (zero caller/onclick breakage) — over 2 riskier alternatives (fully separate modules per split; a cosmetic-only reorg with no real separation). **Pilot set** = `videoPipeline` (tiny, 3 fn), `beatLog` (medium, 18), `bookworm` (largest, 41, DOM-heavy), `phylumPath` (40, logic-heavy) — a real spread chosen from actual function-count data. **Module 1/4 shipped**: `videoPipeline` restructured — shared constants (`STAGES`/`STAGE_LABELS`/`EXPORT_TARGETS`) stay module-scope; `logic.updateEntry`/`logic._computeAutoAdvancedStatus` (pure Supabase write + computation); `ui._showDetails` (the one popup-building function) delegates every non-display decision to `mod.logic.*`. Thin top-level pass-throughs preserve `contentProductionLive`'s own real external call site (confirmed via grep as the ONLY external caller) byte-identically. **The one real load-bearing risk, found and handled correctly**: a function moved into `ui`/`logic` is invoked with `this` bound to THAT sub-object, not the module — every bare `self.X`/`this.X` inside a moved function was rewritten to a fully-qualified `RPGACE.modules.videoPipeline.X`. Verified with a real isolated Node test (a minimal RPGACE stub + the exact extracted module source) confirming the top-level passthrough and a direct `.logic`/`.ui` call produce byte-identical results. A real, safe in-passing cleanup: a byte-identical duplicate `STAGES`/`STAGE_LABELS`/`EXPORT_TARGETS` declaration (rule 8) merged into one. `node --check` clean, re-minified, cache-bust bumped `20260826a`→`20260826b`. `ceo_plan_items` G53 set to **yellow** (genuinely mid-pilot, 1/4 done) — `beatLog`/`bookworm`/`phylumPath` real, unstarted, larger, riskier work still ahead. **Untested by hand** — verified via an isolated Node harness only. **G41 stays blue**: Alex confirmed deferral — "we will do it once the others are done... leave both blue" — no CEO Loop 1 started for either piece (Oracle pipeline orchestration, Fish Audio voice).
- **Real, superseding update — G53 is now 100% COMPLETE, Sep 2-3 2026, 19 batches after the pilot above, `ceo_plan_items` G53 moved yellow→green.** The bullet above is accurate as historical narrative of the pilot (kept as-said, same doc-discipline as the Aug 11 phylum renumber) but its own status line ("yellow, 1/4 done") is stale from the moment this line was written. All 60 of 60 real `RPGACE.register()` modules now have the real `ui`/`logic` split: 56 real splits across 19 batches (self-handled and Opus-dispatched, every single one independently re-verified line-by-line before shipping — `node --check`, marker/register-balance (59/59/57), a self/this-requalification sweep, a `self.X=` field-risk grep, a 3-form external-touchpoint re-grep, and a dedicated headless-Chromium test per batch, never trusted from an agent's own report alone) plus 4 genuine no-op housekeeping notes for single-function modules (`oracleFetchGuard`/`intelBatchList`/`perfWatch`/`config`). `taxonomyTree` (33 external touchpoints) and `dashDeck` (the single most-depended-on shared hub in the app — `_popup()` alone has 38+ real direct callers) were deliberately saved for last, given the extra-care self-handled boundary-asserted-extraction treatment `visualOracle` (batch 17) had already proven, rather than the pilot's originally-planned order. **Real methodology born mid-series, not planned from the start**: the three modules whose functions sat interleaved in original source order (`visualOracle`/`taxonomyTree`/`dashDeck`) needed a boundary-asserted Python extraction (assert exact boundary text before touching anything, extract via precise line-range slicing, a reconstruction self-check proving the slices exactly tile the original body before any write) rather than Edit-tool surgery — born directly from a real batch-14 file-corruption incident (an unscoped string search for a common `logic: {` literal matched the WRONG occurrence elsewhere in the 20,000+-line file; caught immediately by `node --check`, reverted via `git checkout` with zero real loss since batch 13 was already safely committed). A real, recurring bug class — a function's own leading comment landing on the tail of the wrong function once reordering broke its original source-order adjacency — was found once in batch 17 and then systematically caught before shipping in batches 18 (6 instances) and 19 (9 instances) via a predecessor-comparison table built specifically because the first instance proved it would recur. One genuine pre-existing bug (`taxonomyTree.extractNamedTopics`'s undeclared `self`, always resolving to `window.self`) was deliberately left untouched per rule 4, with a real headless assertion proving its exact behavior survived the split unchanged. Full obstacle/reasoning/solution write-ups for the corruption incident and the comment-misattribution bug class: `session_lessons.html`. **Untested by Alex's own hand** — same standing flag as this whole multi-week restructure; the whole app has not yet had a real hands-on pass since it began.
- **G107 (unnumbered UI fix, Aug 26 2026, same day, immediate follow-up) — top level-rail bar + in-page Dimensions index block removed site-wide (left-nav sidebar only), sidebar's L2 Rivers+Modules duplicate list merged into one nested hierarchy, Alex bubble made clickable at Level 2/Current.** Alex's own direct ask: "get rid of these, i only want left nav now" (the top L0→L1→L2→Current chip bar + a separate "🌌 Dimensions" index card beneath it — both a second, always-visible copy of what the G101 left-nav sidebar already covers) — `level_rail_html()`/`LEVEL_RAIL_CSS` deleted outright, `dimension_index_html()` reduced to a documented no-op (function names kept since ~19 page scripts call them as their one shared post-process hook). Separately: "get rid of the duplicate scroll down list - divide modules by river as category, so river 1 - then under its module, then sub head river 2 - smaller sub head module etc." — the sidebar's L2 section previously rendered 2 flat lists back to back (all 17 rivers, then all 45 modules alphabetically); replaced with one real nested list, `_river_module_nested_rows()` — each river is its own sub-heading, its real `RIVER_MODULES` members nest directly beneath it, in river-number order; a river with zero tracked modules (5 of 17) still gets its own sub-heading with an honest "— no modules —" note. Separately: Alex's own direct bug report ("but alex is still not clickable in video pipeline") — the Alex hub node was deliberately excluded from G105's `link_href` treatment by its own original design; fixed in both `galaxy_map_current.py` and `galaxy_map_module.py`, the Alex hub now renders as a real `<a href>` to `UNIT_BUBBLE_SYSTEM['alex']` (Decision Matrix, same destination the L0 Infra system already uses, rule 8). A real, pre-existing, unrelated bug found in passing and NOT fixed (flagged, out of scope): `galaxy_map_loops.py` has 10 real `href="#d-..."` fragment links missing their target filename (should read `galaxy_map_decision_matrix.html#d-...`), confirmed via git history to predate this session. All 19 real page scripts regenerated; idempotency re-verified (byte-identical on a 2nd run), div/a tag-balance clean, full link-integrity scan clean except the pre-existing loops.py issue, headless Chromium confirmed zero console errors and every fix working as described.
- **G108 (Aug 26 2026, same day, immediate follow-up) — real Full/Choice sub-toggle on every Infra/Inter page and on Level 2/Current(L3)'s own evidence-bubble diagrams, plus the Dimensions Matrix reworked into 3 real levels (L0/River/Module).** Alex's own direct ask: "all infra inter should have map view with full/choice with full open at default when navigated to, and the table toggle able too, same with level 2 and 3." New shared `render_fc_bar()`/`FULL_CHOICE_CSS`/`FULL_CHOICE_JS` (`graphify_river_group.py`, injected globally via `inject_level_rail()`) — **Full** (always default) = everything expanded at once; **Choice** = narrowed to one real object's own chain. **Infra-drilldown pages** (Oracle/Supabase/Connectors/Orchestrator CC/Oversight Sync, all via the one shared `render_infra_drilldown()`): Choice is the existing river→module→function pane click-through, unchanged; Full force-opens every pane via CSS override. Orchestrator CC/Oversight Sync's "Shared Infrastructure" sections had no Table view at all — built real ones matching Oracle/Supabase's own row format, so all 5 Infra pages now uniformly offer Table (default) + Map (Full/Choice). **Level 2/Current(L3)'s evidence bubbles** (Alex/Oracle/Composio/Last.fm/Supabase/Jina AI/Decision/Load/Logic — flat siblings, no drill structure) get a real picker instead: one button per evidence type actually present on that specific river/module, evidence-gated. Alex's own bubble is excluded from the picker (fixed anchor, always visible both modes). Level 3's Tier 2 (Decision/Load/Logic) lost its old collapsed-`<details>` wrapper, folded into the same picker as Tier 1 (rule 8). **Real bug caught by the standing regen-and-check discipline**: Level 2's per-river `river_ev_present` list was first initialized inside an `if mods:` block, crashing `UnboundLocalError` on a module-less river; fixed by moving it to the top of the function. **Dimensions Matrix rework, same pass** — Alex: "the dimension matrix bubble map should be reworked and the 2 tables are actually 2 different levels of the same thing. and the l0 objects should be there, I don't want the vertical row currently there (retire that categorization)" (confirmed via a follow-up AskUserQuestion — he pasted the River×Dimension table's own header row to point at exactly what he meant). The old flat 3-button toggle ("River table"/"River bubbles"/"Module table") is retired, replaced with 3 real LEVEL TABS (🌌 L0 units/🏛️ L1 rivers/🌊 L2 modules) matching this pipeline's own established vocabulary, each with its own real Table (default) + Map. `compute_l0_dim_tags()` is a real new addition reusing `build_facets()`'s already-computed per-unit facet data (rule 8) — an L0 unit "touches" a dimension when at least one of its real facets links to that dimension's own registered page. A shared `_bubble_ring()` helper replaced 3 near-identical hand-written bubble layouts. All 19 pages regenerated, idempotency re-verified (2 full re-runs, byte-identical), div/svg/a tag-balance clean, link-integrity clean except `galaxy_map_loops.py`'s own 10 pre-existing same-file fragment links (confirmed via git history to predate this session, flagged not fixed). Headless Chromium confirmed every real interaction (toggle defaults, picker hide/show, Tier 2 folded in, all 3 Dimensions Matrix levels) working, zero console errors.
- **G41 (Oracle Control half) real CEO Loop 1 planning pass, Aug 26 2026, same day — moved BLUE → RED, per Alex's own direct ask ("lets do g41 planning in the meantime to get it red, lets start with oracle control").** Full compiled spec + real `/interrogation` Q&A + Orchestrator CC's own asked-for opinion: `records/2026-08/g41_oracle_control_ceo_spec_2026-08-26.txt`. Real evidence gathered first: the closest already-shipped precedent is `visualOracle`'s `DIRECTOR_CHOSEN:`/`EDL_JSON:` trailer pattern (Oracle's reply ends with a structured marker, the client parses it via the `'oracle:response-scanned'` hook and writes to Supabase) — real, narrow, proven in production. Real, load-bearing constraint confirmed directly: `api/oracle.js` is a stateless serverless function with zero access to `.claude/skills/*.md` — Oracle cannot literally "invoke `/CEO`," so any "Oracle control" mechanism must extend the trailer-marker pattern, not simulate a skill call. **Alex's confirmed scope** (3-question interrogation): target pipeline = Content Pipeline (beat→video→captions); mechanism = Alex types a natural-language cue ("log the beat") that Oracle recognizes against a pre-agreed vocabulary and triggers the matching real action; safety = every action shows a real structured confirm (data touched/path/significance/benefit) before executing, never a silent auto-run. **Orchestrator CC's own opinion, asked for directly**: the FEEL Alex wants is fully buildable; the literal mechanism ("`/paranoia`+`/interrogation` running per action") isn't, for a real cost reason (rule 11) — running live reasoning per action would be a recurring extra API call for near-identical output each time. Real fix: the confirm popup's explanation text is written ONCE per action type (getting real `/paranoia`-level scrutiny at PLANNING time, not per use), not live-reasoned by a 2nd Oracle call every time. **Real correction from Alex, same pass**: "why pre-written, just use supabase where skills are stored, so whenever i update a skill it can give oracle an updated pre-written" — right catch: the vocabulary/explanation belongs in a real Supabase table (new `oracle_actions`, same live-editable-content pattern `oracle_module_anatomy` already proves in production), never hardcoded in JS, so a future session or Alex can update/add/retire an action with a plain UPDATE/INSERT and Oracle picks it up on its next fetch, zero redeploy. `.claude/skills/*.md` files themselves still stay Claude-Code-only (Oracle's deployed runtime has no access to this repo), so the real fix is a Supabase table paralleling the "living, curated content" pattern skills represent, not a literal skill-file sync. **Real Phase 1 plan**: `oracle_actions` (one row per recognized command: trigger phrases → target module.method → pre-written explanation fields → `confirm_required: true`, deliberately conservative default), starting with exactly one action — `log_beat` (Alex's own example), mapping to `beatLog._submit` unchanged — proving the mechanism end-to-end before any vocabulary expansion. Explicitly NOT built this pass (planning only) and explicitly NOT in Phase 1: no auto-execute-without-confirm path, no open-ended NL parsing beyond the curated list. 2 small non-blocking items flagged for a quick confirm before `/Engineer` executes (confirm-popup surface, whether a Deny gets logged) — neither blocks red status, per the plan's own §5.
- **Fish Audio (G41's 2nd half) real `/interrogation`, same day, immediate continuation — also moved BLUE → RED, per Alex's own direct ask ("lets /interrogation on fish audio too if you need to so g41 is fully red").** Same spec file, §6. Real evidence gathered first (live web search — Fish Audio has zero code presence in this repo, a genuinely new connector): Bearer-token auth (same shape as every other paid connector); real-time WebSocket streaming for BOTH TTS (`wss://api.fish.audio/v1/tts/live`) AND ASR/speech-to-text (confirmed via a 2nd real search — not TTS-only as first assumed), plus a simpler batch ASR endpoint; voice cloning from a ~15-60s reference sample; free tier (S2.1 Pro, Fair Use) confirmed only through Aug 31 2026 — 5 days out at spec time — after which TTS is $15/million chars and ASR $0.36/audio-hour, real ongoing cost. Real, load-bearing architecture constraint: this project sits at 11 of 12 Vercel Hobby serverless functions — a Fish Audio proxy must fold into an EXISTING `api/*.js` file (closest fit: a new action branch inside `api/oracle.js`), never a 14th physical file, same landmine Phase H already hit once. **Real interrogation answers**: (1) commit to the paid cost or hold off — Alex: "ill hold off until i start saving money for running rpgace app once ready to be used" — build now, real API-key activation deferred entirely to a later moment of his own choosing, confirmed again directly ("activate the free tier for fish audio when i say so, not on build"). (2) scope — a direct follow-up question surfaced Alex's real, blunt reason: "it should replace the mic we currently have, it is shit" — BOTH directions confirmed: TTS (Oracle speaks) AND ASR (Fish Audio's real speech-to-text becomes the primary path, replacing `voiceInput`'s current unreliable free browser `SpeechRecognition`). (3) voice identity — **a real, honest pushback delivered before this resolved**: Alex's first answer named a real, identifiable public figure (a UK TV/radio presenter) to clone — flagged directly as a genuine legal/ethical issue (right of publicity, likely a real platform ToS violation), not just a style preference; his real, considered answer instead: a similarly-styled (energetic/warm/UK-accented) library voice, or a properly licensed voice actor's cloned sample — never a named real person's identity. (4) auto-speak — off by default, same discipline as `mockOracle`'s own toggle. **Real Phase 1 plan**: a dormant scaffold (same proven pattern as `api/oracle.js`'s own Kimi/Luna scaffold) — real code wired end-to-end but gated behind "is a Fish Audio key actually configured," false until Alex activates one; `voiceInput`'s existing free `SpeechRecognition` path stays as the real, honest FALLBACK (never deleted) with Fish Audio ASR as the primary once a key exists — never a silent forced replacement that breaks voice input if a key lapses. Explicitly NOT built/done this pass: no real API key added anywhere without Alex's own explicit in-the-moment instruction, no voice-identity selection yet (real Phase-1 build work), no removal of the existing mic fallback, no cloning of any real person's voice ever.
- **The overlay + voice-condense flow — real correction, same day, immediate continuation — this IS "Oracle Control," not a separate feature.** Alex's own direct words: "lets make oracle be able to control anything in the ui... make oracle as an overlay... it should input what i say into a text field, then that text field gets condensed from my mess into a precise text rephrase... that i can then accept/deny... i want to be able to talk to oracle the same way i can type to him." A 2nd real `/interrogation` confirmed 2 things directly: (1) "control anything in the UI" is the real, named DIRECTION `oracle_actions` grows toward — Content Pipeline first (already-confirmed Phase 1 scope), expand later, never an open-ended "improvise any action" system; (2) the overlay/voice-condense flow **is** what Alex meant by Oracle Control from the start, not a bolt-on. Real, evidence-grounded precedent found: `voiceInput._injectFloatingButton()` already ships a real, persistent, fixed-position (`z-index:999999`) mic button on every page — built specifically for Alex working one-handed — the exact "reach it from anywhere" pattern this ask needs already exists; this extends it rather than inventing a 2nd competing overlay. **Real, unified flow, Phase 1 scope**: Alex reaches Oracle from any page via the extended overlay, types OR speaks (voice needs Fish Audio ASR active, §6) into the same real chat pipeline (`sendChat`/`callOracle`, never a 2nd mechanism) — for voice, the raw transcript shows visibly first, then Oracle's own reply (a real, zero-extra-cost prompt addition, not a 2nd API call, per rule 11) opens by restating the request as one precise sentence, which the client parses out (same trailer-scanning mechanism as `ORACLE_ACTION`/`DIRECTOR_CHOSEN`) and shows as its own real confirm ("did I understand you right") — separate from and prior to the existing action-confirm ("should I do this") if the now-confirmed request matches a registered action. Two real, separate confirms for two real, separate questions, never collapsed into one. **Explicitly NOT built this pass**: the overlay's own exact UI (size/position/animation) and the trailer-scanning code are real Phase-1 build work; the voice path doesn't ship until Fish Audio ASR is genuinely active — the text path (typed input through the same condense-confirm/action-confirm flow) proves the mechanism first. Full spec: `records/2026-08/g41_oracle_control_ceo_spec_2026-08-26.txt` §7.
- **G108 continuation (Aug 26 2026, same day) — 2 real follow-up fixes from Alex's own direct screenshots.** (1) "why are they still not 2 separate infra?" — `graphify_jobs`/`total_system_members` (zero code touches) were sitting in the same flat table list as `openmontage_jobs` (a real code touch), with only a prose note above distinguishing them. Fixed on `galaxy_map_orchestrator_openmontage.py`/`galaxy_map_oversight_sync.py`: the Table view now splits into 2 real, separately-headed groups (code-verified vs. non-code), matching `galaxy_map_supabase.py`'s own `build_oversight_note()` precedent (rule 8). (2) "no point in choice and full map since they are the same and not cluttered... cluttered ones need the choice one on top of full one" — the Full/Choice toggle now only renders when there's real evidence of clutter: `render_infra_drilldown()` gates on `rivers > 1 or functions > 12`; Level 2/3's evidence picker gates on 2+ real evidence types present. A small unit (Orchestrator CC: 1 river, 4 functions) now renders in Full mode permanently with no toggle UI. Measured post-fix: Level 2 shows the picker on 2 of 16 rivers, Level 3 on 20 of 69 bands — exactly those with genuine clutter.
- **G41 Phase 1 shipped, Aug 26 2026, real "go" — Oracle Control's curated action-execution mechanism is now real and live code.** New `oracle_actions` Supabase table (`anon_read_only`/`authenticated_all`, matching `oracle_module_anatomy`'s precedent), seeded with `log_beat` (target `beatLog._submit`, unchanged). New `oracleControl` module (`rpgace_core.js`): extends `oracleAppGrounding`'s existing `window.callOracle` wrap with a real 3rd gate (never a 4th wrapper, per the standing landmine) so Oracle can end a reply with `ORACLE_ACTION: <id>`; a real `dashDeck._popup()` confirm (what happens/data touched/why this path/benefit, Accept/Deny — never auto-execute) shows before anything runs; Accept opens the Beat Log panel and calls `_submit` unchanged, matching `_openRetroactive`'s own established open-then-act pattern. A new floating `🔮` overlay button (beside the mic, zero overlap) reaches Oracle from any page, reusing the EXACT real `sendChatWithImage()`/`sendChat()` pipeline via the persistent `#chat-input`/`#chat-msgs` elements — zero forked Oracle-calling mechanism. **A real, pre-existing bug found and fixed along the way**: `RPGACE.utils._runPhylaScan` fired the shared `oracle:response-scanned` hook (the same one `DIRECTOR_CHOSEN:`/`EDL_JSON:` already use) AFTER a `text.length<60` early return — silently breaking trailer detection for exactly the short-reply shape a real action-trigger produces, though never an issue for the long-document trailers this hook was originally built for. Fixed to fire unconditionally (`matches=[]` on short text — accurate, not fabricated); only the phyla-badge-specific rendering stays length-gated. `api/oracle.js` also gained a dormant Fish Audio TTS/ASR scaffold (`action:'fish-tts'`/`'fish-asr'`), same honest gated-until-a-real-key pattern as the existing Kimi/Luna scaffold — a real architecture finding along the way: Fish Audio's WebSocket-live variant can't be relayed by a Vercel serverless function (no persistent process), so this uses Fish Audio's real synchronous REST endpoints instead, which genuinely fits `voiceInput`'s speak-then-transcript usage shape. No real API key added anywhere. Verified: `node --check` clean, re-minified, headless-Chromium functional tests (both floating buttons render with zero overlap, overlay open/close, trigger matching, confirm popup Accept/Deny, the real non-mocked hook→popup path, the real `_execute` path's fail-loud validation toast) all pass with zero console errors. Committed `d5aa6a4`, Vercel production confirmed `READY` matching the SHA. `ceo_plan_items` G41 moved blue→yellow. **Untested by Alex's own hand.**
- **G41 Phase 2 shipped same day, immediate follow-up — real compressed `/paranoia`+`/CEO`-Drift pass, Alex's own direct ask right after Phase 1 shipped: "oracle control should also use self-awareness to know how to execute tasks itself using modules, that is an added bonus."** Real `/CEO`-Drift finding: read literally, this would reverse Phase 1's own just-ratified §4d boundary ("no open-ended NL parsing... a different, much bigger, riskier build"), MATERIAL if built on assumption — genuinely traces back to G41's own original Aug 15 framing, but Phase 1's interrogation deliberately simplified away from it for real safety/cost reasons. Real GODMODE evidence: `oracle_module_anatomy` is human-written prose with no verified function-signature contract and can drift stale; RPGACE has zero Supabase backup/PITR to recover from a wrong-target misfire. Three real, distinct readings scoped and put to Alex directly (full detail: `records/2026-08/g41_oracle_control_ceo_spec_2026-08-26.txt` §9) — he confirmed the recommended middle path, **self-aware vocabulary growth**: `oracleAppGrounding` gained `buildSuggestBlock()`, gated on the SAME already-tuned `TRIGGER_KEYWORDS` gate (rule 8, no new heuristic), telling Oracle it may end a reply with `ORACLE_SUGGEST_ACTION: {target, trigger_phrase, description}` when it recognizes a real action request with no matching registered row and can confidently name a real module/function from its own self-awareness. A real, DISTINCT green confirm popup ("Add as new action"/"No thanks," never the same UI as the execute-confirm) shows before anything is added to the vocabulary; approving writes a real `oracle_actions` row via the server-side write proxy (table added to `api/data-write.js`'s `ALLOWED_TABLES`, matching `oracle_module_anatomy`'s own RLS posture), `action_id` defensively slugified from the trigger phrase, never taken as free text from the model (rule 5). **Real, deliberate 3rd safety gate, not incidental**: even an approved suggested row still hits `_execute()`'s own honest "not wired up yet" toast until a human actually codes a real dispatch branch for it — Oracle can grow the VOCABULARY but genuinely cannot cause any new real effect by itself. Verified: `node --check` clean, re-minified, headless-Chromium functional tests (block output, the real hook→distinct-popup path on an `ORACLE_SUGGEST_ACTION` trailer, Deny writes nothing, Accept calls the real write-proxy path with a correctly-shaped row) all pass, zero console errors. Committed `e59c7f4`, Vercel production confirmed `READY` matching the SHA (11 of 12 functions still — no new physical `api/*.js` file). `ceo_plan_items` G41 evidence updated to match. **Untested by Alex's own hand.**
- **G109 shipped same day, immediate follow-up — a real "error → /perspective baseline → smoke_test broken → ceo_plan_items purple" loop, Alex's own direct ask.** Started from a real correction: Alex claimed Oracle Control would let Oracle "monitor toast pop-ups" — checked directly and found false (`RPGACE.utils.toast()` is purely visual, writes/fires/signals nothing; Oracle Control is entirely reactive, no live DOM watcher exists). The real underlying gap WAS genuine though: `errorLog` only ever caught `window.onerror`/`unhandledrejection`, so a handled validation toast (e.g. "Add a beat title first") was invisible to `error_log` entirely. **Step 1 (commit `408e2f4`)**: `toast()` now writes error-colored (`#CC4A4A`, the codebase's own established error convention — confirmed via grep, 33 of ~80 real call sites) toasts into `error_log` via `errorLog._capture`, and fires a new `rpgace:error-toast` hook; `oracleControl` buffers the last 5 and surfaces them to Oracle via a new `buildRecentErrorsBlock()` on the existing `TRIGGER_KEYWORDS` gate. **Step 2, the bigger ask, resolved via 3 real `AskUserQuestion` forks rather than guessed**: Alex confirmed (1) extend `/perspective`'s existing 44-row per-module `expected_behavior` baseline, not a new learning mechanism; (2) `smoke_test_items` (which already has real `broken`/`linked_error_id`/`linked_plan_item_id` columns) is the real trigger surface — flips to `broken` automatically, cascades to `error_log` and, only from a genuine prior-`green` state, to `ceo_plan_items.status='purple'`, matching `/colourgradient`'s own existing regression definition exactly; (3) `error_log` itself is the real async queue, tied into a new CLAUDE.md session-start-check addendum + `/Routine`. **Real technical finding along the way**: a stack-trace-based module attribution does NOT work as first assumed — `RPGACE.register()` modules are plain object literals, so V8 reports a real error generically as `Object._submit`, with no module name at all (confirmed via a real headless Chromium test against the live minified app). Fixed with a new build-time generator, `scripts/generate_method_module_map.py` (real brace-depth parsing of every `RPGACE.register('name', {...})` block) — `mangle:false` (this project's permanent minify setting) preserves every real method name, so 432 of 456 real distinct method names resolve to exactly one owning module; the 24 genuinely ambiguous ones (`_inject`/`init`/`_render`/etc, shared by 2-8 modules) are correctly excluded, never guessed. `errorLog._capture` now accepts an optional stack, resolves it via `METHOD_MODULE_MAP`, attaches the matched module's real `perspective_reports` baseline (`linked_perspective_id`/`expected_baseline`) to a new `error_log` row (raw `fetch` + `Prefer:return=representation`, avoiding the standing `RPGACE.sb.insert()`-never-returns-the-id landmine, rule 8), and cascades to `smoke_test_items`/`ceo_plan_items` exactly as confirmed above. `future_integrations.html`'s own `#purple-live` section already live-queries `ceo_plan_items` for `status='purple'` — confirmed via direct read, zero changes needed there. Verified via headless Chromium with mocked Supabase network calls (real network is sandboxed here): the full cascade fired with correct request shapes at every step (dedup-check → perspective lookup → `error_log` insert → `smoke_test_items` broken-flip → `ceo_plan_items` purple-flip, correctly gated on the mocked prior `green` status), zero console errors; `_extractModule` separately confirmed `_submit`→`beatLog` and `_inject`→`null` (ambiguous, correctly rejected) against a real stack shape. `ceo_plan_items` G109 logged yellow. Full record: `records/2026-08/g109_error_perspective_smoketest_purple_loop_2026-08-26.txt`. **Untested against a real live error yet — mocked-network headless verification only.**
- **The "Galaxy Development Framework," named Aug 13 (Alex's own words).** Everything under the "RPGACE Total Systems Galaxy Map" `/CEO` plan (this whole block of bullets, `ceo_plan_items` G1-G15) is now formally named this — full definition, the sibling skills that compose it (`/CEO`/`/cartographer`/`/drift`/`/perspective`/`/misunderstanding`/`/paranoia`/`/restructure`), and the real design goal for its own docs (stable, precise, cache-friendly reference material for Oracle + Orchestrator CC, minimal token cost to re-read — rule 11) all live in `.claude/skills/CEO/SKILL.md`'s own new section, not duplicated here. **Real, stated future convergence, not built, flagged plainly**: Alex's own words, "the galaxy development framework will also be a blueprint that combines with taxonomy development later on when i get to it so it becomes a more coherent system" — the Taxonomy section's own multi-session restructure effort and this framework are currently independent; unifying them is his own real, named future intent for a later planning pass.
- **(f) Graphify (Obsidian) + the Galaxy Map** — renumbered from (e) the same day layer (e) above was redefined around tense (this whole layer is neither past nor future — it's the real, present-tense code-structure TRUTH SOURCE feeding (b)/(c), a structurally different job from a tense-organized layer). The real truth SOURCE feeding (b)/(c): `graphify-out/graph.html`/`GRAPH_REPORT.md` (the code-structure truth) and `obsidian-vault/` (the human-browsable knowledge layer, built Aug 11 via `scripts/graphify_to_obsidian.py`) — Graphify does the actual AST analysis Obsidian structurally can't do; Obsidian is the real browsable presentation layer over graphify's own output, not a second copy of it. **Real gap found and fixed same day**: `obsidian-vault/`'s raw `[[wikilink]]` markdown only renders as real navigation inside the Obsidian app itself — there was no URL Alex could actually open from Oversight. `scripts/obsidian_vault_to_html.py` (a small self-contained converter, not `obsidianhtml`/Quartz — both checked and real-overkill/blocked for a 16-file vault, `obsidianhtml` needs a `pandoc` binary not present here) renders it into one self-contained `graphify-out/obsidian_vault.html` with real working links (resolved wikilinks jump between pages; unresolved module-level links — v1 scope has no per-node note yet — render as honest dashed-underline text, not dead brackets), wired into Oversight's "Auto-generated" group next to Graphify Map/Tree. Verified via headless Chromium: sidebar nav, hash deep-linking, and wikilink navigation all confirmed working, zero console errors. `/update-logging-system` artifact type 10 gained a 4th pipeline step (export → recolor → river-group → obsidian-vault-html) so this never goes stale next to graph.html. **Real G6 promotion, Aug 13 (same day, real /CEO Loop 2 execution)**: the Galaxy Map (`graphify-out/galaxy_map.html`/`galaxy_map_river.html`/`galaxy_map_module.html`/`galaxy_map_level3.html`) is now formally a SECOND real truth source in this layer, alongside graph.html/the Obsidian vault — same "auto-generated, code-derived, never hand-authored" discipline, sourced entirely from `graphify_river_group.py`'s real data tables and live regex/grep evidence against `rpgace_core.js`, never guessed. It answers a genuinely different question than graph.html (real code-structure relationships at every level of granularity) does (raw AST-derived import/reference graph) — both real, both auto-generated, neither a copy of the other (rule 8). `interconnection_map.md`/`ai_tooling_and_rules_map.md` updated same pass to cite this promotion directly.

**Real trickle-down attempt, Aug 11/12 (later pass), genuinely STOPPED, not completed** — Alex's own direct ask, since (f) is sourced from (a)-(d)'s content and today's real changes should flow down. Real attempt made: `graphifyy` (the npm package backing `/graphify`) reinstalled fresh (v0.10.1, a materially newer version than whatever produced the currently-committed graph), `graphify update . --scope tracked` run to capture today's real code growth. **Real, unexpected finding that stopped the pipeline (rule 4 — one failed fix, get real evidence, don't force a second blind pass)**: the fresh rebuild produced **744 nodes vs. the committed 967** — a large, unexplained discrepancy, not a rounding difference, and the new CLI version also uses a different state directory (`.graphify/` vs. `graphify-out/`) for its `update`/`watch` commands specifically. Did NOT proceed to overwrite the currently-tuned, already-linked-from-Oversight `graphify-out/graph.html`/`obsidian_vault.html` against a graph whose shape-change isn't understood yet — cleaned up the untracked `.graphify/` dir and the global npm install rather than leave stray state. **(f) is honestly NOT updated this pass** — a real, separate future investigation (diff the two `graph.json` file lists, decide on the `.graphify` state migration and its wider blast radius on every hardcoded `graphify-out/` reference) is needed before a safe re-export. Full record: `records/2026-08/oversight_a_to_f_trickledown_audit_paranoia_2026-08-12.txt`.

- **smoke_test.html** — the real (d) layer, new Aug 11: what's actually confirmed working right now, ticked by Alex's own hand after he verifies it live — never auto-ticked. Grown from real completions, starting with this session's own shipped work (see the file itself). When something ticked later breaks, it gets un-ticked with a real note of what broke, and `oracleAppGrounding.SELF_KNOWLEDGE` should reflect that state too (per (c) above) so Oracle never confidently claims something works that's currently known-broken.
- **error_log.html** — the real (d) layer's 2nd doc, new Aug 12: `error_log` Supabase table (real, deduplicated by normalized signature — a client-side `window.onerror`/`unhandledrejection` capture in `rpgace_core.js`'s new `errorLog` module is the one real reporting source built so far; every other Total-system AI/code path sending its own error codes here is real, scoped future work, not built blind this pass). Tracks `rationale` (real diagnosis), `backtrack_note` (what likely broke upstream in the real pipeline/river), a real `attempts` array (what was tried, why it failed, per real attempt) until one actually works, then `resolved_at`/`lessons_learned` kept permanently as the real past-error record Alex asked for ("vital for future development and coherence"). **Real, honest scope of what's automated vs. not**: the mechanical parts (dedup-on-insert, the smoke_test.html "Just Failed" mirror, clearing `needs_confirm_highlight` + auto-resolving the linked error on Alex's real confirm click) are real and live. The JUDGMENT parts (writing a real rationale/backtrack note, planning and executing an actual fix, deciding whether a resolved error's lesson is worth promoting into a numbered CLAUDE.md rule) are NOT automated — they're this session's own real work each time, matching the `oracle_dev_suggestions`/`graphify_jobs` session-start-check pattern, never a background job pretending to do that judgment on its own.

**`future_integrations.html` — a real, new 9th artifact and layer (e)'s future-tense half (Aug 11, same day, later pass), the deliberate MIRROR IMAGE of the tier docs above.** Per Alex's own explicit rule, added the same day `/colourgradient` gained its 4th color: **only 🟢 green (verified built and live) items are ever written into the seven tier docs above** — 🔵 blue (an idea, quickly jotted, not yet a real plan), 🔴 red (a real, specified plan, not built), and 🟡 yellow (genuinely mid-build) all live in `future_integrations.html` instead, so a real shipped feature and a not-yet-built idea are never sitting side by side making the tier docs harder to trust at a glance. **Grouped PLAN-first since Aug 23 2026 (DD2), not by color** — Alex's own words, "i want plans to be first category - main one, then it breaks down by what river and modules are touched based on galaxy map": one section per real `ceo_plans` row, sub-grouped by the real Galaxy Map river each item touches (`ceo_plan_items.galaxy_river`), color demoted to a per-item badge. Every card is now live-fetched (the old page hand-typed the Massive Expansion plan's cards and live-fetched only the Galaxy Map plan, which is how A5's card came to claim "Research Lab page confirmed fully intact, untouched" long after that page was retired), and the old single hardcoded "🌌 Galaxy Map" group label — which had been silently mislabeling four other ratified plans' items as Galaxy Map items — is structurally gone. Backed by the real `ceo_plan_items.status` column (shared source of truth with `/colourgradient` itself — never derived twice, rule 8); updated whenever a `/colourgradient`, `/paranoia`, or `/drift` pass generates a real finding and pushes, per Alex's own explicit ask. When an item reaches green, it's removed from here and its real status lands in the appropriate tier doc(s) instead — never both places at once. Wired into the live Oversight popup (`dashDeck._openOversight`). See "The 4 layers (c/d/e/f)" below for the full tense-based reasoning and the still-unbuilt past-tense Archive half of layer (e).

**Two archive files sit alongside these, and neither is an oversight doc**: `patch_notes_archive.html` (pre-July-12 patch notes) and `CLAUDE_archive.md` (this file's own dated Current-state/Prior-state sections, moved out July 31). Both exist so the live docs stay readable. Never read either for current state — they are explicitly historical. (`ai_tooling_and_rules_map.md`'s own Part 2 catalogs several more historical `.md`/`.txt` files beyond these two archives — read that doc, not this list, for the full historical/live split.)

**Before any nontrivial task read the relevant sections of these first** — and check real current state (git log, live Supabase, actual code) before trusting a doc's claim. This project has repeatedly had docs describe something as "done" when the code disagreed.

**Another place this now lives, real-time: The Chronicles** (`#page-chronicles` in the app itself). Every real Claude Code change to RPGACE also gets a short row in the `system_updates` Supabase table (title/summary/category) in the same session it ships, alongside `patch_notes.html` — Chronicles is the app-side, always-on view of the same real work, not a separate record to keep in sync by hand. For a comprehensive multi-item sweep across all the file-based docs, use the **`/scope` skill** (`.claude/skills/scope/SKILL.md`) first — it's the GODMODE evidence-gathering pass for exactly this, organizing git/Supabase/doc state into digestible grouped categories so nothing gets missed or buried.

